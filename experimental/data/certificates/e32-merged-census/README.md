# E32-MERGED profile census certificate

This directory pins the Wave 5 E32-MERGED profile census.

Run:

```bash
python3 experimental/scripts/verify_e32_merged_census.py
```

The verifier enumerates Venn profiles, evaluates the eliminant normal-form
rank on canonical realizations, and records the exact coordinate-embedding
counts represented by the profiles.
