# Audit

Hand review checked the collinear-triple component's three dominant pair
projections. The d-2 bound is a residual SCHEME-length bound, so it controls
inseparable degree as well as distinct points. Separability justifies both
derivatives; tangent concurrency would force projection degree at least p.
The p>d guard excludes that possibility.

For each finite collection of bad centres, general geometric projections
preserve distinct marked points and their normalization branches. The
planar genus budget then bounds the whole finite collection, proving
finiteness and the branch-weighted sum. Integer branch-ratio minimization
was checked algebraically before finite controls.

Normal/O controls pass in0.03/0.10seconds and11648/16128KiB RSS:
1617 branch prices, bounded exact ratio minima, the10-coordinate singular
control and the characteristic3 countercontrol. No large computation.
The universal geometry remains a written proof; external review is due.
