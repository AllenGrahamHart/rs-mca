# Dependency Sub-DAG

Required, both PROVED:

- pair_space_regular_projection_anchor: actual shared hyperplane kernel.
- pair_space_operator_branch_genus_energy: normalization/fixed-divisor
  degree facts; the proof explicitly extends the line-bundle argument
  from plane images to any projective target.

No dependency on the new finite consumer or a generic-selection premise.
