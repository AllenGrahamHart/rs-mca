# Excess Requires An Actual Normalization-Degree Jump

Keep the original KoalaBear MCA rank12 source on J9965..21499, actual P2
rank19, dimension11 shared carrier and the existing eight regular anchors.
All original assignments, raw1/2 weights, full gcds and degree slack remain.
For a profile ending at J1, put H=floor((J1-1)/10).

Every pencil-free regular3 terminal with plane-image degree eta2..5 and
normalization degree nu<=H fits its original L_t allowance, both cutoffs
and EVERY degree slack v>=0. There are416 exact spectral/profile/eta prices.
The earlier branch-energy theorem still pays eta>=6.

Thus any excessive pencil-free terminal has nu>H, although its original
dimension11 carrier has nu_0<=floor((J-1)/10)<=H. The exact actual-anchor
ledger forces at least one inner projection of degree mu_j>=2. This is
a realized nonbirational anchor, not a claim about a formal hull or a
generic parameter. In the conic branch, kernel chi3/4 is still required
by the preceding double-conic payment.

At every actual prefix, the geometric branch-weighted Segre budget applies
because characteristic2130706433 exceeds all actual carrier/image degrees.
It bounds coordinates at high-degree projection centres, including singular
ones. Its guarded weighted-anchor use is written in anchor_guard.md, but
uniform affordability of the resulting anchor schedule is NOT yet proved.

Whole constant3 on J14965..21499 remains separate. The source available-weight
maximum is unchanged, not an unrestricted rank19 bound. No original whole
degree, row endpoint, active upstream atom, LIST row or Prize problem closes.
