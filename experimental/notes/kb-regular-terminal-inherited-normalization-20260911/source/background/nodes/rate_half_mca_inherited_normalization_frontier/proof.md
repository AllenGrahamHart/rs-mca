# Pay The Small-Normalization Class Before Selecting New Anchors

Use R=1048576,d=67472 and inherited profile allowances L_t. The original
one-pair raw weight is C_t=R-d+t. An excessive terminal supplies exactly
M=floor(L_t/C_t)+1 actual pairs. Put kappa=eta*nu+1 and keep
N_v=R+kappa+v, A_v=d+kappa-t+v with the previously proved v>=0.

The inherited source eigenline/plane capacities bound removed eigen-root
incidence by B=(kappa-1)*c_e, with
c_e=(0,qbar,qbar+q1,3*qbar/2) for e=0,1,2,3. Here qbar uses the OLD full
profile degree ceiling J1-8, so no smaller-degree capacity is assumed.
The actual pair budget is P=binom(M,2), and g=binom(eta-1,2).

The new certificate proves, for every eta2..5 and nu in1..H,

    Z=2*(M*A_v-B)-N_v>0,
    Z^2>(N_v+2*nu*g)*(N_v+8*nu*P).

At v=0 the difference is quadratic in nu. Exact endpoints and an interior
vertex when needed bound its real minimum. Its v derivative at zero is
affine in nu and nonnegative at both endpoints; the v^2 coefficient is
4*M*(M-1)>0. Thus ALL actual nu and v are covered. The first condition
is checked independently before squaring. All these degrees also satisfy
eta*H+1<=J1-8, as the certificate checks.

The branch-energy supplier gives the reverse weak inequality, a contradiction.
So original weight<=(M-1)*C_t<=L_t. There is no claim that H is optimal.
Higher eta uses the existing proved frontier; low eta with nu>H remains.

Along any actual regular anchor path, the shared dimensions are11 through3.
The degree-ledger supplier gives nu_0<=floor((J-1)/10) and
nu_8=nu_0*product mu_j. An excessive pencil-free terminal must have
nu_8>H>=nu_0. At least one ACTUAL mu_j is therefore greater than1.
Its geometric image is rational by Luroth's theorem: its function field
is an intermediate field of the original rational function field. Its
actual image degree is at most J-1<2130706433, so that prefix image
satisfies the characteristic guard of the Segre theorem.
This proves a source-bound location of the remaining obstruction, not an
affordable census of its coordinates or permission to use generic anchors.
