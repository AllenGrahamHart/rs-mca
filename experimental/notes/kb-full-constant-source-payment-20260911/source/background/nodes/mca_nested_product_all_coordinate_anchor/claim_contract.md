# Claim Contract

Object: actual subfamilies of one original finite-label MCA assignment.
Field: original F, distinct finite evaluation coordinates; no characteristic
or field-extension assumption. Costs must satisfy the printed scalar
same-field padding conditions.

Input: a nested product pair-direction enclosure W=(V,0)+(0,B), B subset V,
with primitive shared V, nonnegative ORIGINAL weights and A joint agreements.
Here omega_t(f) sums original raw values of that pair's assigned raw<=t labels.
The pair-component basis may change only as coordinates, not original labels.

Output: an all-coordinate weighted incidence inequality and a degree-band
recurrence with two exact child types. Root anchors drop pair rank by ONE
and codimension by one, rather than silently dropping pair rank by two.
All children reduce shared dimension; full gcds may lower degree excess.

Whole-constant terminals use one global preferred-slope charge and scalar
LIST on the original core union, with gcd roots counted on that union.
The single-pair terminal retains its original same-pair raw-weight cap.

No affordable universal terminal, original source budget, maximal safe
endpoint or Prize resolution is claimed by this generic node.
