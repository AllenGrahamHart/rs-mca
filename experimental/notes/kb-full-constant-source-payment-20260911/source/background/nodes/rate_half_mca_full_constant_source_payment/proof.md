# The Full Constant Carrier Makes All Coordinates Usable

First apply the previously proved whole-source pencil alternatives.
Their caps are at most270000000000000000. In the remaining branch
the original constant-pencil core-union gate applies to all hereditary
constant-direction subfamilies. This is not an avoidance conjecture.

The full constant11 subspace puts the original pair-direction hull,
in a fixed F-basis of pair components, in the form

    W=(V,0)+(0,B), dim V11, dim B8, B subset V.

Subtract only actual pair offsets. This change of basis does not change
the finite labels being counted. Both P1 and P2 use this same enclosure.

The new all-coordinate supplier now applies at every shared stage.
B-root coordinates are NOT discarded: they give a rank-one evaluation,
shared dimension one lower and codimension one lower. All other
coordinates give the usual rank-two evaluation with codimension unchanged.
Full shared gcds and unused degree are retained; degree excess cannot
increase. No actual subset is assumed to span its enclosure.

For each original J profile, the certificates cover every integer
kappa in[0,J1-11] by width1000 bands. Scalar dimensions1,2,3 are priced
using complete union-complement boxes and independently checked LIST
traces. The single-pair base has original weight at most981104+t.
The42-state nested-product table supplies all branch prices, including
codimensions0,1,2 needed after rank-one anchors.

The inherited W(J_low) bounds sum min(raw,3); the two table bounds
are original raw-weight sums, not counts of pairs or auxiliary labels.
For t1,2 the final state max_i T[11,3,i] bounds ORIGINAL low-raw weight.
The original weighted identity gives

    floor(W(J_low)/3 + max T[11,3,.,t1]/2
                         + max T[11,3,.,t2]/6)+134944
      <=239161133377346211

over all13 whole degree profiles. These weights ALREADY contain every
anchor incidence factor. Higher raw and near are included once.
Maximizing with the paid original alternatives proves270000000000000000.

A rank-one F-subspace has dimension at most11; if it has dimension11,
its direction is constant by polynomial-product growth. Combining this
case with the preceding maximum-dimension10 source theorem gives the
printed bound for maximum pencil dimension at least10.
Rank-nullity supplies the lower bound8 in an original19-to11 projection.

Every projective scalar-projection kernel is a constant-direction pencil.
After maximum dimensions10/11 are paid, its dimension is8 or9; hence
the projection rank is11 or10. The earlier full-generic projection
result permits at most three finite rank-drop directions.
No coefficient extension or auxiliary infinite-label charge is introduced.

Unrestricted original rank19, ranks20..22, whole degrees and the two
Prize problems remain open. The old terminal tail is not silently paid
individually; the all-coordinate original-source composition is the theorem.
