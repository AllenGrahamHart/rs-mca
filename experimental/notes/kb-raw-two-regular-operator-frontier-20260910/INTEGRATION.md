# Integration And Review Map

**K3 / DIRECT; KoalaBear MCA; agreement1116048; target2^-128;
B*=274980728111395087.** Scope, quantifier, projection and falsifier are
frozen in [the row contract](README.md). Status: PROVED LOCAL at those
scopes, not an active-ledger atom. Independent mathematical review is due.

## Relation To The Current Upstream Program

Upstream main was checked at
`93fba1be3f3299b0ba4708d88715377bbb656e45`, including agents.md, the
newest main agents-log entry and the live four-row completion note.
The direct KoalaBear numerator remains open. We use its current K3
source-bound/projection terminology, not older names for different
quotient or asymptotic atoms.

Scott Hughes's #1179 and #1180 introduced the weighted-anchor,
Pluecker and compression interfaces reconstructed in our preceding
six-node packet. Their checked heads are
`c4e4e7918764a590c4b8cfeac11004d297e2480b` and
`478bfc41278c7669543388107a2fabff4b9fbd1b`.
This companion extends that locally paid rank18 frontier. No unmerged
claim is treated as an independently verified theorem merely because
it appears in a PR.

The two new payments eliminate deficient GENERIC projection already at
the raw cutoff TWO, rather than the previously paid raw cutoff SEVEN.
The other two nodes describe the remaining actual-pair source in a
regular operator enclosure. This is a normal-form reduction, not the
missing census or an assertion that all points of the enclosure occur.

The anchor argument uses successive weighted incidences, not a proposed
formal-history denominator from #1183. It imposes no unique-history
premise. This packet makes no claim to audit or refute #1183.

## Four New Nodes

| Source node | Mathematical role | Upstream use |
| --- | --- | --- |
| rate_half_mca_raw_two_quadratic_normal_payment | Exact original-source graph and contained-line payments | K3: pay the remaining short-normal source class |
| rate_half_mca_raw_two_projection_regular_frontier | Rank/codimension exhaustion plus existing source payments | K3: all surviving actual P2 hulls project generically onto the shared carrier |
| pair_space_regular_projection_anchor | Generic-kernel exclusion preserving projection under joint anchors | Reusable source-bound descent with a necessary extra guard |
| rate_half_mca_regular_operator_terminal_reduction | Original-weight transport to square regular enclosures | K3: focus the surviving ranks19..22 on operator dimensions3..0 |

The [manifest](SOURCE_MANIFEST.json) records the exact incoming required
edges of all 107 nodes. All inherited mathematical sources are separately
hashed and match the pinned published versions. The new source files
remain node-sized; no monolithic proof or speculative conditional leaf
is introduced.

## Main Proof Checks

1. The rank-ten basis lemma is UNIFORM on m-7 support points.
   Using it for actual raw<=2 labels neither changes their raw values
   nor assumes G7 containment. Five-owner mass has floor
   M5=222676884802638507; the conservative two-owner bound is
   floor(2*(M5+1)/5)=89070753921055403.
2. The actual raw-two pair bound is12763910835039. Noncontained lines
   have at most two inside labels apiece, and moving-kernel zeros cost
   at most21490 labels. Every such exception, higher raw and near is paid.
   One common tuple resource is used, not a resource per kernel.
3. For pair codimension c=22-r<=3, a deficient full-shared-carrier
   projection has minimal normal degree d>=1 and d+2<=c.
   Only r=19,d=1 remains. Empty P2, low ranks and proper carriers are
   routed to their already proved whole-source bounds.
4. In the regular anchor, E consists of CONSTANT functionals annihilating
   the generic projection kernel U_z. Independence persists under scalar
   extension, giving dim E<=2s-r and |B|<=K+s-r. For x outside B,
   a generic-kernel vector adjusts both component evaluations while
   preserving their projection. Joint-rank-two goodness alone is weaker.
5. Stop after r-11 anchors, exactly when pair and shared dimensions agree.
   An actual child need not span its enclosure. Earlier anchors are common
   zeros and cannot be selected again. Original owner weights survive.
6. The finite chart uses a nonzero degree-at-most-c determinant and
   |F|>c. The zero-dimensional terminal, nilpotent operators and nonsplit
   characteristic polynomials remain included. No diagonalization or
   field extension is required.

The small regularity fixture is V=span(1,X,X^2) over F101 and
W=span((1,0),(X,1),(X^2,0),(0,X^2)). Generic projection is onto V and
joint evaluation has rank two everywhere. At x=0 the section projects
only onto span(X^2), whereas V_x=span(X,X^2). The repaired excluded set
contains0. This disproves an unguarded inheritance claim, not an MCA
source inequality or the Prize conjecture.

## Adoption Boundary

After independent review, import these as proved source-class payments
and structural reductions. To bank an active-v4 atom, a separate bridge
must still bind exact source scope, owners, partition, chronology and
exhaustive coverage to that atom. Alternative whole-source bounds combine
by maximum, not as additive payments in unrelated partitions.

Our local router remains TARGET with no incoming logical requirements;
these consumers supply evidence. The new generic lemma is a required
dependency of the operator reduction, not a new unresolved premise.
Neither the critical grand roots nor any deployed safe row is promoted.

## Compute Requests

None. The unfinished dimension-three weighted census is a mathematical
obligation, not a request for an unbounded operator or field enumeration.
