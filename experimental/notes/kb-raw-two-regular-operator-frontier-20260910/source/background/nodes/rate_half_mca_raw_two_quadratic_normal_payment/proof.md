# Reuse The Uniform Basis Lemma, Not A Raw-Seven Containment Premise

Write R=1048576,d=67472,E=21499,W=578501226347492453. The required
minimum-envelope resource gives sum min(tau,3)<=sum min(tau,9)<=W
on the unchanged original source. Fix the declared quadratic Q.

## Retained Contained-Line Mass

Apply the moving-normal tuple theorem to the SUBFAMILY of original
raw<=2 labels whose entire assigned pair line is contained in Q.
Use its parameter T=7 and e=2. Every original tau is still <=2;
T merely allows choosing m-7 points from each joint core of size>=m-2.
No original raw value or source is reset to seven. Original evaluation
on V is nonzero, so at most J-9<=21490 labels have a moving-kernel zero.

Section1 of the required contracted-basis payment proves UNIFORMLY
that every retained rank-ten kernel on these m-7 points has at least
B_*(J) ordered bases. This lemma does not assume G7 containment.
Its decreasing quotient certificate gives

    floor[5*(R+J)_11/(11*B_*(J))]<=M5=222676884802638507.

The stronger-basis interface of the moving-normal theorem counts every
common ordered tuple at most e=2 times, in one fixed incidence space.
The retained ORIGINAL raw mass is therefore at most

    M2=floor[2*(M5+1)/5]=89070753921055403.

Indeed 5*(R+J)_11/(11*B_*(J))<M5+1. Taking the floor of the
strictly larger scaled bound is conservative. We do NOT infer G7
containment from G2 containment or apply the old graph-payment conclusion.

## Noncontained Lines And Moving Zeros

The entire actual P2 pair family fits in the original carrier V x V.
At shared dimension s=11,...,1 the ambient pair enclosure has rank2s,
so the guarded joint-anchor theorem gives

    |P2|<=P=floor product_(i=1)^11 (R+i)/(d-2+i)
           =12763910835039.

This counts actual represented pairs, not all points of their affine hull.
Each noncontained pair line restricts Q to a nonzero polynomial of
degree<=2, hence owns at most two inside labels. Together with moving
zeros there are at most Z=21490+2*P=25527821691568 inside exceptions.
Each exception has original deficit3-tau<=2. An outside low label also
costs at most two. On retained contained labels, 3-tau<=2*tau.

## One Original Resource And One Add-Back

The exact original-label identity gives

    3*|Gamma|=sum min(tau,3)+sum_(tau<=2)(3-tau)
             <=W+2*(M2+Z+x)=756693789832986395+2*x.

Floor once and add near134944 to prove PRICE and GRAPH. If every pair
line is contained, x=0 and omit2*P from Z, giving LINES. Empty classes
are included. All higher raw labels remain funded by the same W.

For |Z_bad|>=B*+1, the relation |Z_bad|<=|Gamma|+near forces
2*x>=3*(B*-near+1)-756693789832986395. Its least integer solution is
34124197250397019. Adjacent sufficient envelopes are B* and B*+1;
this arithmetic is not an unsafe witness or true-numerator optimality.
