# Audit

The hand check separates basis support parameterT=7 from actual raw<=2.
Every selected joint core has enough points; the basis theorem is uniform
over the moving kernels and does not assume the old G7 graph conclusion.
Degree-two tuple ownership scales the unrounded quotient, with a safe
integer upper bound derived from M5+1 rather than scaling floor(M5).

The pair cap uses raw cutoff2, not7. Formal contained lines and merely
pointwise inside labels are separated. Two roots per noncontained line,
moving zeros and every higher raw value are included before near once.
The affine hull is an enclosure, not a source of additional explanations.

The primary verifier replays the inherited exact basis coefficients.
The independent audit imports no local code and derives a conservative
integer cap from the separately verified M5 floor; it reconstructs every
pair-anchor guard and the final floor using integer products. Mutation
tests target source scope, raw/basis distinction, exceptions and prices.
The inherited basis primary and independent audit are separate required
replays. Finite arithmetic does not establish universal geometric inputs.
No field enumeration or external mathematical review is claimed.
