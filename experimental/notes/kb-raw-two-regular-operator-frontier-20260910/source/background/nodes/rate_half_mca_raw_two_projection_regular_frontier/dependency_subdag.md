# Required Dependency DAG

Four PROVED suppliers are required: polynomial-projection dichotomy,
raw-two quadratic-normal payment, actual P2-rank18 payment, and the
proper shared-low-carrier theorem. The normal is a consequence of these
rank bounds, not a new TARGET leaf. The new quadratic consumer in turn
uses the existing universal basis and original-resource proofs.

This node owns evidence to the open original router. No older node or
critical threshold requires it, so the refinement introduces no cycle or
red promotion. Exact dependency IDs are in node.json.
