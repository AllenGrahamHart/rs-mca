# The Remaining Raw-Two Ranks Reduce To Operators Of Dimension At Most Three

Status: PROVED locally; independent mathematical review remains due.

Use every original KoalaBear rank12 source J9965..21499 and any valid
assignment. An over-budget source has actual P2 rank r in19..22 and
full generic projection, by the required regular-frontier payment.
Put c=22-r and a=r-11. For t=1,2 preserve the ORIGINAL owner weights
omega_t(f)=sum of raw values of assigned labels with raw<=t.

There is a good-anchor descent with unchanged worst-case weight factor

    A_(r,t)=product_(j=1)^(r-11) (1048576+j)/(67472-t+j).

Every populated terminal has a pair/shared ENCLOSURE of dimensions c/c
with full generic projection, after a common anchors:

    r    anchors a    terminal c
    19      8             3
    20      9             2
    21     10             1
    22     11             0.

After subtracting one actual terminal pair and dividing only differences
by its anchor locator, the terminal can be written over the ORIGINAL
field using an operator T on a c-dimensional polynomial space U:

    (a_pair,b_pair)=f_*+H_a*((I-alpha*T)y,T*y), y in U,
    h_gamma=a_*+gamma*b_*+H_a*(I+(gamma-alpha)*T)y.

Only ACTUAL represented y and their original labels/weights are retained.
If L_t uniformly bounds those terminal weights, then M_t<=A_(r,t)*L_t.
This is the exact incidence transfer, NOT an asserted affordable L_t.
The c=0 terminal has one pair and weight<=981104+t.

Nilpotent and nonsplit operators are not excluded. No original label,
source degree or raw value is reset. No rank19 payment, whole J,
active-v4 atom or Prize closure follows just from this reduction.
