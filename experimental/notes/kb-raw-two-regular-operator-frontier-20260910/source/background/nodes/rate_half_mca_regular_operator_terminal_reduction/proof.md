# Carry Regularity Through The Same Weighted Shared-Anchors

The required raw-two frontier has already paid every nonregular P2
source, every pair rank<=18 and proper shared carrier. In an over-budget
source the original P2 hull direction W is regular in V x V, dim V=11,
dim W=r in19..22. Use this same enclosure for its actual P1 subset.

At stage i the enclosure dimensions are r_i=r-2i,s_i=11-i.
Its codimension2s_i-r_i=c=22-r is constant. For i<a=r-11 we have
r_i>s_i. Apply the regularity-preserving anchor, with original degree
J and A_t=67472+J-t. Its bad set has size at most J+s_i-r_i,
and its weighted factor is

    (1048576+r_i-s_i)/(67472-t+r_i-s_i).

All denominators are positive. Every retained child enclosure remains
regular; its actual subfamily need not span it. Iterating for exactly
a steps gives the displayed product with j=a,a-1,...,1 and terminal
pair/shared dimensions c/c. Each populated path adds a distinct common
joint-core coordinate. Earlier anchors are common zeros and lie in the
next bad set, so they cannot be selected again.

Original weights survive every incidence. Every represented pair in a
terminal retains its original complete core, with all a anchors included.
The selected supports may omit anchors; none of their defects can be an
anchor. No full original source is replaced by a smaller MCA source.

Choose one actual terminal pair f_*. Differences lie in V_i x V_i and
are divisible by the locator H_a. Define U=V_i/H_a, dim U=c, with
degree bound J-a. Division is injective and commutes with pi_z, so the
divided pair direction is square and regular. The field q=2130706433^6
has more than c<=3 elements. The generic anchor supplier's determinant
argument gives a finite alpha and an F-linear T with the printed form.
Substitution at the SAME original gamma proves the h_gamma identity.

Only pair differences were divided. Neither f_* nor the received values
are asserted divisible by H_a; auxiliary received values can be defined
by pointwise division away from the anchors. The representation does
not create pairs for unrepresented y, nor change original owner weights.

If every populated terminal has weight<=L_t, the successive weighted
incidences give M_t<=A_(r,t)*L_t. At c=0 there is at most one original
pair, whose raw-owner mass is at most n-(m-t)=981104+t by the required
same-pair ownership proof. That bound alone does not pay the whole source.

This is a proved normal form and transfer, not a guessed terminal census.
It stops exactly at r_i=s_i, where the strict anchor theorem cannot be
applied again. Scalar, nilpotent, split and nonsplit operator cases all
remain available to the actual terminal family.
