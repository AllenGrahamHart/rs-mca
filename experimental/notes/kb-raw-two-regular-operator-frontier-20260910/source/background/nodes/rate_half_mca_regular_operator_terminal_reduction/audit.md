# Audit

The initial actual P2 hull is regular, but ordinary joint-good anchors
do not necessarily preserve that property. This proof uses the newly
guarded set at every step, with the unchanged worst-case size bound.
Codimension stays c, and strict r_i>s_i holds precisely until the printed
terminal. Eight anchors concern rank19, not rank18.

The proof separates the actual represented subset from its regular
enclosure; otherwise a false full-actual-span claim could enter. Original
raw weights and base pairs are never divided. The finite chart rewrites
gamma algebraically and does not alter original slope ownership.

The verifier checks all eight rank/cutoff chains and their exact products,
not a family of actual Prize sources. It assigns no terminal bound for
c=1,2,3. The c=0 bound remains only a supplier for subsequent composition.
External independent mathematical review remains due.
