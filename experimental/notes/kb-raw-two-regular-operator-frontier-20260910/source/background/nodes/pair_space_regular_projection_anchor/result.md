# Result

Full generic projection can be preserved through every strict shared
anchor at unchanged worst-case incidence cost. The larger bad set is
necessary, as shown by a joint-good coordinate where naive preservation
fails. Square regular terminals admit an original-field operator graph
when the field has more elements than their dimension. Their weights
are not bounded merely by obtaining this normal form.
