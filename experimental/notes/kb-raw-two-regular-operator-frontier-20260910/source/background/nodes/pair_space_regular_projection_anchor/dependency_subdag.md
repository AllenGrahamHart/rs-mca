# Required DAG

The existing shared-carrier rank-two anchor supplies the root-flat
dimension argument and the usual anchor interface. This node adds the
generic-kernel excluded set and proves regularity preservation directly.
The determinant argument proves the finite operator chart locally.

There is one PROVED required supplier and no new open premise node.
The finite original-source consumer owns any critical-router evidence;
this generic lemma does not promote the router.
