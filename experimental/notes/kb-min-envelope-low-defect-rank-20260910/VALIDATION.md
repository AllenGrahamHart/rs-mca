# Validation And Limits

Status: PROVED LOCAL at the printed scopes; external mathematical review
remains due. Hash checks establish custody, not mathematical truth.

## Frozen Sources

[SOURCE_MANIFEST.json](SOURCE_MANIFEST.json) has SHA256

    36395ad03690b56a49e052c564cb7422b446799c4e1dc9f09eb67cb2332dfa34.

It freezes 30 new source files (67020 bytes), an 86-node acyclic locally
PROVED required closure and 259 inherited mathematical documents.
The exporter compared every inherited proof document with the dirty
research worktree. No clean-research-HEAD claim is made.

The replay checks all 1006 listed source hashes in ten pinned supplier
packets and all 30 new hashes. It validates exact root scopes and graph
adjacency and rejects 26 corrupted manifests (identity, scope, source
inventory, dependency and proof-pin mutations). Large integers in the
publication scope object use decimal strings to avoid consumer rounding.

The new source certificate is 10262 bytes, SHA256

    bbacb3699165acb3dbf018fc21116d0f19b3ee1e650ea9994100adc038f1973e.

The mathematical sources are split by node and proof step. The longest
new mathematical document has 80 lines; this packet adds no long monograph.

## Replay

From any checkout containing this packet and its pinned sibling packets:

```sh
python3 -B experimental/notes/kb-min-envelope-low-defect-rank-20260910/replay.py
python3 -B -O experimental/notes/kb-min-envelope-low-defect-rank-20260910/replay.py
```

The replay is offline, standard-library only, serial and bounded. It stages
only hash-checked runtime files in a temporary directory, removed on exit;
each child has a 20-second timeout. On the RAM-limited development laptop,
both commands ran under RAMguard tiny (256 MiB memory / 60-second limit).

| Replay | Elapsed | Measured peak RSS |
| --- | ---: | ---: |
| Normal Python | 2.34 s | 23288 KiB |
| Python -O | 2.92 s | 24396 KiB |

Five new focused checks run in both modes:

- Exact mixed-branch derivative identity, a nonconvex-envelope control and
  six actual rank-four F17 basis counts. These fixtures are NOT the
  universal proof.
- Shifted-polynomial resource reconstruction: three full 256-branch trees,
  765 branch-shape checks and 512 coefficientwise transfers.
- Independent unshifted-degree reconstruction, importing neither primary
  checker nor polynomial helper; all endpoint floors and 24 wrong-floor
  rejection checks.
- Eleven height bands, 121 LIST transitions, all anchor guards, both
  equality terminals and the exact original-source price.
- Independent chosen-step and integer-ratio audit, importing no local
  checker/compiler; 10 corrupted scope/coverage/step/price certificates
  rejected.

Four inherited resource/pencil checks are also rerun, including the old
HIGH44 pencil primary and independent audits (582 LIST transitions and
10 additional corrupted certificates). The old filtration and completed
HIGH scripts print OLD resource values, intentionally: those checks
validate inherited inputs, not the improved values proved by the new node.

## What Is Not Certified By Replay

The universal contraction, tuple ownership, actual-label disjointness,
hereditary section theorem and guarded anchor proof remain hand-proof
obligations for the independent reviewer. No computer algebra system or
proof assistant has formally verified these arguments.

Other inherited arithmetic, the global DAG software/crosswalk, current
PRs and unrelated source classes were not freshly audited. There was no
field/source/J enumeration, numerical search, floating-point bound,
Modal job or spend. No compute request is outstanding.

The unfinished coupled rank-seventeen/eighteen work is excluded. Both
Prize problems, unrestricted endpoints, active null atoms and every whole
degree in the remaining gap stay open. Publication changes no DAG status.
