# K3 / DIRECT Integration Map

This is a source-class theorem packet, not a new Grande Finale v4 ledger
atom. Its receiver quantifier, original assignment and distinct-slope
projection are fixed before any auxiliary restriction. Stable papers,
earlier packets and the PR author's branch are unchanged.

## Required Sub-DAG

The new acyclic chain is

    mca_fiber_contraction_core_basis_resource [inherited]
      -> mca_min_envelope_fiber_contraction
      -> rate_half_mca_min_envelope_raw_mass
      -> rate_half_mca_raw_two_pair_rank_sixteen_payment.

The raw-mass node also requires the original raw-margin basis insertion
and completed-basis resource proofs. The payment also requires the old
HIGH44 complete-pencil-union theorem, guarded shared-carrier rank-two
anchors and hereditary pencil-section incidence. The complete adjacency
map is the `requirements` object in [SOURCE_MANIFEST.json](SOURCE_MANIFEST.json).

All 86 required nodes are locally PROVED, and the exporter checked their
acyclic closure. This is not an external-review certificate. The original
critical router is an evidence consumer, not a proved required leaf:
publication does not promote that TARGET or either Prize root.

For each inherited node, `inherited_sources` gives its packet, exact
relative path and SHA256. These files remain in the earlier sibling
packets; they are not silently copied from a live worktree. The replay
reconstructs its runtime from pinned sources and has no research-directory
or network dependency. Local DAG reference paths are logical source paths;
use the manifest to resolve inherited documents across packets.

## Recommended Mathematical Review

1. Check the full-fiber contraction supplier and the new mixed-branch
   second derivative. The two child arguments may select DIFFERENT
   branches. The minimum of concave weighted branches is concave, whereas
   convexity of the unweighted envelope is neither needed nor asserted.
2. Check every-branch propagation, not a handpicked branch. For each
   rank/cutoff, the four SHAPE inequalities hold on the full real interval.
   Monotonicity of the resource follows branchwise before taking extrema.
3. Check same-original-label tuple ownership in all three raw ranges.
   The intermediate transfer and completed HIGH151 are alternatives for
   the same tuple set. No separately normalized source is substituted.
4. Check full P43 core unions for EVERY pencil child, the original
   off-pencil deficit bound 981147, and the hereditary 35/4 ratio.
   The smaller on-pencil union complement cannot price off-pencil pairs.
5. Check all five rank guards and both equality terminals. A pair is
   counted only when actually represented; enlarging its auxiliary hull
   does not declare other hull points to be bad explanations.
6. Check the final rational floor and add near=134944 exactly once.
   Large-pencil and rank-based whole-source bounds combine by maximum.

## Relation To The Active Program

This improves an unconditional input for every original rank-twelve
source, including high-rank regular families, and pays a larger actual
low-defect pair class. It is directly reusable in further source-bound
rank/projection arguments with the SAME units and normalization.

It does not supply an exact Q, exhaustive BC, owner bridge or active
U_paid. Banking such an atom still needs that architecture's exhaustive
partition and ownership proof. A direct route must still handle the
remaining high-rank sources and the other original error ranks.

Upstream main was checked at 93fba1be3f3299b0ba4708d88715377bbb656e45;
#1175 is OPEN at 6c59f9aa75b897c9274e94c7aa8864acd26a85ea.
The live agents workboard, newest upstream agents-log entry and four-row
compiler were read; none reports an adjacent safe-row closure. This packet
starts after companion commit ff68347e6ea08d4c4dc20d14949a577e338b85a7.
No wider current-PR audit, rank-seventeen payment or full Prize closure is
claimed here. Independent mathematical review remains due.
