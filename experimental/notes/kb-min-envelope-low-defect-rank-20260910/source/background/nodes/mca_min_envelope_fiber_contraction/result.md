# Result

The spike/equal-fiber contraction extends from a single quadratic to the
minimum of finitely many smooth branches with explicit differential gates.
This retains complete polynomial branches across successive ranks instead
of replacing each level by a new quadratic minorant. No row is closed.
