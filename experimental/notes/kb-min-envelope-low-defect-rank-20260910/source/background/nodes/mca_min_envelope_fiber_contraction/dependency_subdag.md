# Required Sub-DAG

    mca_fiber_contraction_core_basis_resource
        -> mca_min_envelope_fiber_contraction
        -> rate_half_mca_min_envelope_raw_mass

The first edge is logical. The final displayed edge is consumer-owned.
No return edge, conjectural envelope gate or source normalization is added.
