# Audit

PROVED by the source-bound pencil/anchor proofs and exact finite gates.
Independent external mathematical review remains due. Original error
rank12 and auxiliary P2 affine rank16 are explicitly distinguished.

The new10262-byte certificate has11 height bands and121 LIST transitions.
verify.py reconstructs the new W9 basis endpoint and compiles every band.
verify_audit.py imports no local module and checks each chosen LIST step,
every exact ceiling, guarded anchor, BOTH terminal types and final price
using a separate integer-ratio implementation. It rejects10 corrupted
scope/coverage/step/price certificates.

Certificate SHA256:
`bbacb3699165acb3dbf018fc21116d0f19b3ee1e650ea9994100adc038f1973e`.

The constant complete-P43-union gate is inherited unchanged; it is not
recomputed in this node. Off-pair owners retain their own complete-core
bound981147; only on-pencil nonpreferred owners use the union complement.
The raw-two consumer's own same-pair bound is981106. These differ by design.

The new source uses truncation3 funded by truncation9, not a mixture of
old and new refunded budgets. Prior anchor zeros remain in bad evaluations.
The constant equality terminal dominates here, unlike the older P7 case.
No hull point is relabeled as an actual explanation; no unsupported full
generic projection, whole-degree movement or Prize closure is asserted.
