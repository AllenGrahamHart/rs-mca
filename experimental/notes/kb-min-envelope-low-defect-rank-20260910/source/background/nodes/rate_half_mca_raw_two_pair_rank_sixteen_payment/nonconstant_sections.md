# Strengthen The Hereditary Raw-Two Pencil Ratio To 35/4

Keep R=1048576,d=67472,E=21499. For each primitive polynomial pencil
A0*a+A1*b=Q, let U be the union of COMPLETE joint cores of ALL its
actual P43 pairs and e=n-|U|. The required original HIGH44 pencil
theorem already pays constant directions with e<=567500 by
261996525491320703. Its proof also supplies the following ON/OFF
bound for nonconstant primitive height h; no constant refund is used here.

Each nonpreferred ON pair has all assigned defects outside U, hence
deficit at most(43/44)*e. The scalar parameter dimension is at most10,
and at most R+E finite preferred labels are charged in total. Maximizing
e*(R+1-e)^10 gives the already proved envelope

    BASE=W_old/44+R+E+134944
         +(43/44)*(R+1)^11*10^10/[11^11*(d-42)^10],
    W_old=624373932788019251,
    ceil BASE=43891123512334720.

Every OFF pair has at least d-42-h=67430-h joint agreements outside
U, by the nonzero primitive-row remainder degree<J+h. The sum of
deficits of its assigned labels is at most(43/44)*(R-d+43), using
its OWN complete core, not the possibly smaller complement e.

For ell in{1,2001,...,20001} put

    u=min(ell+1999,21498),
    ebar=ceil(R+ell-(35/4)*(d-1+ell))
        =ceil((4R-35(d-1)-31ell)/4).

For ell<=h<=u and e<=ebar, auxiliary complement padding and degree
relaxation give the shared-dimension-eleven joint LIST instance

    (r,w,K_max,s)=(ebar-E,67430-u-E,E,11).

All field, positive-denominator, agreement and dimension gates hold.
The eleven traces, with121 transitions, bound the whole original source by
BASE+(43/44)*981147*M_OFF. Their maximum ceiling is220845070943887497,
below the existing constant-union cap. The original labels, high raw
values and near are retained once in each alternative, even for singleton
on-pencil families. An empty complete core union cannot meet these gates.

Now suppose neither the constant gate nor any new nonconstant gate occurs.
Fix ANY affine pencil subspace of dimension v and count its actual P2
pairs. Empty/singleton intersections satisfy all scalar bounds below.
Otherwise two distinct bounded-degree pairs imply a primitive height h<J.
Use the complete P43 union of the FULL pencil, independent of the child.

Constant directions have e>=567501 and scalar degree<J. Since every P2
pair has at least m-2 joint agreements, scalar incidence bounds their count
by C^v, C=481076/67471. For nonconstant h in its declared band, e>=ebar+1
and degree<J-h give scalar ratio at most

    (R-ebar+h)/(d-1+h)<=(R-ebar+ell)/(d-1+ell)<=35/4=P.

The first inequality follows from R-ebar>d-1 and h>=ell, the second
from the exact ceiling definition. Since1<=C<=P, EVERY affine pencil
section is bounded by P^v, with the sharper C^v for constant directions.
This hereditary statement changes no receiver, original field, complete
core, raw value or finite label in subsequent sections.
