# Keep Low Normalization And Pay The Exceptional Exits

## Original-Source Preparation

The preceding all-coordinate theorem already pays every actual P2-rank<=19
source on J13965..21499. On J9965..13964 it also provides valid, although
not uniformly affordable, U/G prices for every hereditary coordinate state.
Earlier source theorems pay rank<=18, deficient generic projection and proper
shared carriers by274462040894062110.

Apply the proved whole-source pencil alternatives. Their cap is at most
270000000000000000. Only on the residual branch use their hereditary local
gates and terminal allowances. A gate failure pays the entire source;
it is not assumed impossible without that alternative payment.

It remains to bound actual rank19, shared dimension11 and full generic
projection on the lower five original J profiles. Write H=floor((J1-1)/10).
The primitive shared carrier starts with degree D_0<=J-1 and image
degree eta_0>=10, so its normalization degree satisfies nu_0<=H.

## A Third Table With The Same Original Owners

Retain the existing U and G tables without changing their terminal prices.
Define L[s,i], s3..11, to bound regular rank2s-3/shared-s enclosures whose
actual normalization degree is at most H, on degree-excess band i.

For s3 use the maximum of the whole-constant scalar price and the minimum
of the existing function-field-rank-two price with L_t.
The latter inherited allowance applies: every L path from s11 to s3 has
eight actual rank-two regular anchors and nu<=H. Rank-two terminals with
pencil planes are already paid; pencil-free ones are paid by the inherited
normalization theorem and its eta>=6 supplier. No physical degree cap
D<2*(H+1) is needed now because the ACTUAL normalization ceiling is retained.
Whole constant terminals keep their separate scalar price.

For s>=4 let C be the lower-band prefix maximum of L[s-1].
Let F be the maximum of the old G[s-1,2] and U[s-1,3] prefix prices.
These pay generic rank-one and nonregular rank-two exits.
Let T be the old G[s-1,3] prefix price, allowing arbitrary normalization.

The new geometric supplier bounds high-normalization coordinates.
On a band with upper primitive degree D_hi=s-1+hi, take its exact
maximum over1<=nu<=min(H,floor(D_hi/(s-1))).
Replacing image degree by floor(D_hi/nu) is justified by monotonicity.
The primary partitions nu by both floor quotients; the independent audit
checks EVERY integer nu, including values not attained by a real source.

The all-coordinate paid-exit inequality gives

    L <= [N*C+(kappa+3)*(F-C)_+ + B_geo*(T-C)_+]/A.

The two exceptional sets need not be disjoint. No coordinate is discarded
and there is no new (A-B_geo) denominator. Use the two degree-band endpoints
at v=0 and the limit C for every v>=0, then take the minimum with the
existing G[s,3] prefix price. Lower-degree prefix maxima retain all extra
full-gcd drops. Paid exits do not re-enter L under an unsupported ceiling.

All weights are the ORIGINAL raw-t owner sums. Both cutoffs use the same
P2 enclosure. No actual occupation, label reassignment, field extension,
extra regular-anchor factor or additional source resource is introduced.

## Exact Source Composition

The certificates cover254 low degree bands,1016 geometric rows,
900184 integer normalization candidates and160107 quotient intervals.
They verify2032 new paid-ceiling recurrences and19304 inherited U/G identities.
The five original residual-source bounds are:

    J9965..10964:   249604027270088959
    J10965..11964:  243591486993244111
    J11965..12964:  245976215664433629
    J12965..13364:  241134325029679811
    J13365..13964:  249682145677014171.

Each follows from the SAME original resource

    floor(W(J_low)/3 + max L[11,.,t1]/2
                         + max L[11,.,t2]/6) + 134944.

Higher raw and near are counted once. The numerical proof is an exhaustive
finite recurrence audit, not a census of the huge field.

Taking MAXIMUM with the earlier whole-source alternatives gives
274462040894062110 on the prefix. Taking MAXIMUM with the preceding tail
payment gives274839785069298661 throughout J9965..21499.
Original-source rank19 is paid at every normalization and pencil class.

The raw-two regular frontier now forces every over-budget assignment to
have actual pair rank20..22. Its old generic-minor bound gives at most2/1/0
finite rank-drop directions there. This theorem does not transplant the
rank19 pencil bounds to higher pair ranks or pay the full row.
