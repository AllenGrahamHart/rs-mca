# Audit

Hand review checked the proved initial nu<=H, the lower-ceiling terminal
history, full shared gcds and unused degree, all generic and geometric exit
types, overlap, original owners and final maximum/floor/near accounting.
There is no discarded-coordinate denominator or implicit free jump.

Primary normal/O0.76/0.83seconds,29156/30876KiB.
Independent normal/O2.65/2.83seconds,32304/32796KiB.
The primary uses quotient endpoints and stage-first recursion.
The audit scans all900184 integer nu values and computes band-first.
It imports the pinned PRECEDING U/G audit and legal-trace checker, not
the new primary or geometric bounds.py. Older U/G arithmetic is inherited,
not advertised as a newly independent implementation.

All254 bands,1016 geometry rows,160107 quotient intervals,
2032 paid-ceiling and19304 inherited identities agree.
The checker rejects58 semantic mutations with outer hashes bypassed,
including a fully repriced zero-cost normalization-crossing ledger.

Certificate index:
c8adce1a573f9cb167705dce1234a75be2ca5b41f59f6e4479bdcc5cbc54d6bc.
Five JSONL shards total423598bytes. Full finite coverage is explicit.

Fresh inherited tail audit:2.93seconds,41716KiB;844 bands,190088 LIST
transitions,64144 identities and61 mutations. Branch-budget controls:
0.04seconds,11776KiB,1617 branch prices, the actual singular10-versus8
coordinate catch and the characteristic3 countercontrol.
Degree-ledger controls:0.01seconds,10624KiB, including an actual
normalization jump1->12 and full fixed-divisor transport.
Other inherited arithmetic was not freshly replayed in full.
The inherited normalization audit was also replayed:0.35seconds,20864KiB,
416 exact prices and49 mutations plus a fully recomputed raised ceiling.

All checks are serial under RAMguard tiny,256MiB/60seconds.
No Modal, spending, large field census or new compute request.
Written proofs still need external review; no global-suite pass is claimed.
