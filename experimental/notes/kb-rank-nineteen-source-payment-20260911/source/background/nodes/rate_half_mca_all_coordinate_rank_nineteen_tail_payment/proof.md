# Two Enclosure Tables And The Original Source Resource

## Original-Source Alternatives

Use the already proved raw-two regular frontier. It pays every original
source with pair rank<=18 or a deficient generic projection by
274462040894062110. Empty P2 and proper shared carriers are included.
Only actual pair rank19 with shared dimension11 and full generic projection
is new here.

Next apply the inherited whole-source pencil-union gates, including the
constant and all-height gates. Every source excluded this way pays at most
270000000000000000. In the remaining source, these gates hold for every
actual hereditary pencil subfamily. Such a subfamily's complete union is
contained in the original P43 pencil union, so a violation would already
pay the source. Formal enclosures need not contain new represented pairs.

The local gates are therefore legitimate, but only on this residual branch.
Their whole-source alternatives are included by MAXIMUM at the end.

## Exhaustive Coordinate Descent

Start at shared dimension11, pair dimension19, codimension3, and generic
projection onto the shared carrier. Preserve the same original P2 enclosure
for both raw cutoffs even if P1 has smaller actual affine span.
The all-coordinate supplier applies with nonnegative original raw weights.

U[s,c] bounds arbitrary enclosures of pair dimension2s-c in a primitive
shared s-space. G[s,c] bounds full-generic enclosures reached along paths
that have remained in G. A rank-zero U child lowers c by2; a rank-one
child lowers c by1; a rank-two child preserves c. Every branch lowers s.

Generic sources have no rank-zero coordinates. Rank-one G children remain
generic. Ordinary rank-two G children remain generic, but at a kernel-root
coordinate a rank-two child can become nonregular. Charge it using U, not
G. Both exceptional types lie in ONE set of size at most kappa+c.
Codimensions0/1 cannot produce nonregular rank-two children.

There are42 states per table, with four terminals s=c and38 recurrences.
Full shared gcd division gives kappa_child<=kappa. For each fixed original
J profile, width500 bands cover every integer kappa in[0,J1-11].
Preceding-stage PREFIX maxima retain all smaller degrees. Positive affine
ratio denominators allow the two band endpoints at v=0 and the v-infinity
limit to bound all degrees and every unused v>=0.

## Terminal Prices And The History Guard

A terminal has pair/shared dimensions c/c, c<=3. Whole function-field-rank-one
spaces are constant by full-carrier rigidity; their scalar LIST price uses
the original core-union complement, OWN union gcd-root count, and a single
preferred finite-label raw charge. Zero-dimensional terminals cost981104+t.

For function-field rank two, c2/3, take the minimum of:
the determinant anchor price using the integer original-weight line bound;
and a SAME-FIELD JOINT LIST count times981104+t. The joint LIST induction
uses shared polynomial dimension c, not a projection or a field extension.
All padding fits the original field.

In G[3,3] only, a further inherited terminal allowance is available when
D<2*(H+1), H=floor((J1-1)/10). To reach this state from G[11,3],
all eight steps must preserve codimension3. A rank-one step decreases it
irreversibly, and a nonregular step leaves G. Thus all eight ancestors
were actual rank-two regular anchors, exactly as the old supplier requires.

If this terminal contains a rank-one plane and has function-field rank two,
the inherited rational-plane theorem pays L_t at every degree.
Otherwise the primitive three-dimensional carrier has a nondegenerate
plane image of degree eta>=2 and D=nu*eta. Hence
nu<=floor(D/2)<=H. The inherited normalization theorem pays all such
pencil-free terminals, including the already-paid eta>=6 range.
Characteristic2130706433 exceeds all relevant degrees, as required there.
Whole constant terminals retain their separate scalar price.
No L_t allowance is used in U[3,3].

## Finite Certificate And Composition

For each height atom intersect the proved source-gate intervals and use the
larger applicable gate. Integer populations m on a pencil have the exact
upper complement from the small-terminal supplier; this covers all
heights and all original union complements. Nonconstant inside defects
are charged once per coordinate globally. Scalar complement boxes have
width10000 and use legally checked uniform-in-degree LIST traces.

The primary evaluates stage-first. The independent checker rebuilds the
height partition, integer populations and terminal traces, then reconstructs
all states band-first; it does not import the primary or selector.
All13 source profiles are exact reconstructions of frozen certificates.

Let M_t be the maximum G[11,3] price over all degree bands for cutoff t.
The inherited original mass bound W(J_low) bounds sum min(raw,3).
The original raw identity gives the whole residual-source upper bound

    floor(W(J_low)/3 + M_1/2 + M_2/6) + 134944.

No extra regular-anchor factor belongs here: all incidences are already
in the tables. Across J13965..21499 the maximum is274839785069298661.
Taking MAXIMUM with274462040894062110 and270000000000000000 leaves
the same bound and reserve140943042096426.

The first five J profiles remain above budget. These are upper recipes,
not actual witnesses. The earlier full-constant/maximum-pencil10 theorem
narrows their rank19 excess to maximum pencil dimension8/9.
Higher actual pair ranks, full degree closure and both Prizes remain open.
