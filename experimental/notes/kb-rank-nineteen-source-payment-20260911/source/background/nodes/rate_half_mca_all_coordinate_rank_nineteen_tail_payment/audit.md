# Audit

Analytic review checked original source alternatives, hereditary gate
transport, all three joint ranks, nonregular rank-two children, decreasing
shared dimension, degree/slack transport and the G33 eight-regular-anchor
history. The L_t shortcut is never used for unrestricted terminals.
Joint LIST is applied in the same field with shared dimension c.
Original owners, global line inside charge, near and higher raw stay once.

Primary normal/O:3.72/3.29seconds,37256/37840KiB.
Independent normal/O:2.96/3.02seconds,41660/43252KiB.
The primary is stage-first; the audit reconstructs every state band-first
and intersects height intervals independently. It imports only the inherited
LIST-trace checker, not the new primary or scalar selector.
All844 bands,190088 LIST transitions,64144 recurrence identities,
1068 height atoms and13 profiles agree.

The independent checker rejects61 semantic corruptions with outer hashes
bypassed, including a fully repriced omission of BOTH exceptional child
charges. Omitting only the nonregular charge changes no prices in the chosen
official mutation profile, so that was NOT counted as a rejected mutation.
A separate synthetic-price control checks its implementation when binding.
The geometry control supplies12 real rank-two regularity losses.

Certificate index SHA256:
37e1f226deb4ba670373090103ebe7d666200d3186542870c46595bf05797aae.
Thirteen frozen JSONL shards total7648432bytes. Coverage is exact, not a
large-field census. Checks use RAMguard tiny,256MiB/60seconds, serial.
No Modal, spending or new compute request. External review remains due.

Inherited original source/gate/allowance audit:1.92seconds,20480KiB,
184084 LIST transitions,544 gates and24 mutations. Inherited normalization
audit:0.30seconds,20992KiB,416 exact prices and49 mutations plus a fully
recomputed raised-ceiling rejection. Other inherited arithmetic was not
freshly replayed in full; dependency proofs are not replaced by hashes.
