# Validation And Its Limits

## Source Custody

- Parent companion commit: ae6fbacd96826d9a273cdd2fbbfb90cb1a411ced.
- Manifest SHA256: 7b53163ad4e60003b3c1cb4d44d95743b52dd0def950f33e7b29ca6760600cc9.
- 56 new source files, 1,122,842 bytes; 111 acyclic locally PROVED nodes.
- 325 inherited mathematical documents compared byte-for-byte with research.
- 1,138 published source hashes plus 56 new hashes checked during replay.
- Thirteen finite certificate shards avoid a monolithic proof/data file.

The research origin is dirty. Per-file hashes, not its HEAD alone, identify
the contribution. The manifest includes complete required dependencies and
supplier pins. Later proof status in the original worktree cannot silently
alter this frozen copy.

## Focused Checks

Replay runs six new checks and one inherited source/gate/compression audit,
serially in an isolated temporary source tree, with no network access needed.
Normal and optimized Python modes must both pass. It rejects44 malformed
manifests independently of the arithmetic mutation checks below.

Measured full replay: normal4.84seconds /28,668KiB peak RSS;
optimized5.47seconds /29,380KiB peak RSS. Both ran under RAMguard tiny.
No Modal or paid computation was used.

The finite rational-plane verifier covers13 complete J profiles,544 height
gates and92,042 mass boxes. The independent audit validates184,084 proposed
two-step LIST transitions, plus frozen ON/OFF gates and original resource
arithmetic. It imports no new primary proof module; the old selector may
propose a trace only because a separately written legality checker checks
every step. Selector optimality is never assumed. There are24 mutations.

The actual-span audit checks26 profile/cutoff combinations and388 complete
constant-prefix boxes, with1,164 frozen scalar LIST transitions. Its20
mutations cover scope, coverage, weights and traces. The inherited audit
also checks2,448 LIST transitions and rejects22 corruptions.

The kernel fixture checks101 coordinates over F101, including the exact
triple fibre and98 rational-inverse controls. The universal characteristic
statement, pencil-freeness and exact geometric fibre are hand algebra, not
consequences of finite sampling. The rational-compression fixture likewise
tests guard examples rather than enumerating original MCA sources.

## Boundaries

Hash/schema passes establish custody and structural consistency, not
mathematical truth. Universal geometry, original-owner uniqueness, source
gate scope and the existential excess argument need independent review.
The remaining inherited arithmetic is pinned but not all freshly replayed.
No formal proof assistant, full-row enumeration, exhaustive source census,
active-v4 atom or new safe/unsafe endpoint is claimed.

Audit verdict: NO ISSUE within the stated local claims and focused checks.
The missing source-bound census is a declared research gap, not covered
by the verdict. Both Prize problems remain open.
