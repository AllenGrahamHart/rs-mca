# Price Actual Rational-Plane Terminals Without Claiming A Cover

The required regular descent supplies eight common joint anchors and
the original factors A_(19,t)=prod_(j=1)^8(R+j)/(d-t+j), with R=1048576
and d=67472. It preserves every original raw owner weight; actual
children may occupy only a subset of the regular3/3 enclosure.

Let H be a function-field-rank-one plane in its rank-two W.
The required rational-compression theorem parameterizes H with scalar
dimension2 and primitive height h. Its normal form gives h=0 or
1<=h<=floor((J-9)/2). In both cases quotienting W/H gives ACTUAL
fibres with masses x=|C|-J-h+1, shared budget S_h=1048577-h,
scalar degree J-8-h and agreement d+J-t-8.

The [pencil gate proof](pencil_gates.md) pays a whole original source
whenever one full P43 pencil union is large enough. Every fibre here
is contained in such an original pencil of the SAME primitive height;
its complete union C is a subset of the full union. Outside the paid
alternatives, every fibre complement is therefore at least g+1.
No large-union hypothesis is guessed or required as a new leaf.

The [terminal envelope proof](terminal_envelopes.md) uses the
height-corrected shared mass, scalar LIST dimension2, one global
inside-defect charge and at most two large integer fibres.
Every integer J, primitive height and shifted mass is covered by
the13 J profiles,544 height rows and92042 small scalar boxes.

For a fixed J profile, maximize its proved terminal weight over its
height rows separately for t=1,2. This remains an upper bound when the
actual P1 subfamily occupies fewer fibres; it uses the SAME original
P2 enclosure, not a presumed P1 rank or fullness.

The original truncated-three identity is
1=min(raw,3)/3+raw*1_(raw<=1)/2+raw*1_(raw<=2)/6.
The inherited truncation9 resource also bounds truncation3, decreases
with J, and gives the printed W(J_low). Substitution of the terminal
numbers into the original eight-anchor template, followed by one floor
and near once, gives the displayed available-weight test.

Crucially this last substitution is a test of sufficient terminal
constants, NOT a proof that the constants hold for every child.
Whole rank-one W has no nonzero quotient annihilator; W without a
rank-one plane cannot be put into these fibres. Their actual weights
remain unpaid. No exhaustive cover, reassignment, affine-hull realization
or new required census conjecture is being asserted.

If every terminal had both weights<=L_t, the existing weighted descent
and source identity would give the certified below-budget bound. Its
contrapositive yields a terminal with a weight above its L_t, necessarily
outside the class just priced. Whole rank-one W has dimension equal to
the shared carrier, so the inherited full-carrier rigidity makes its
direction constant; in any invertible regular chart T is scalar.
Otherwise W has rank two and contains no rank-one plane. This is a
proved survivor restriction, not an assumed exhaustive rational cover.
