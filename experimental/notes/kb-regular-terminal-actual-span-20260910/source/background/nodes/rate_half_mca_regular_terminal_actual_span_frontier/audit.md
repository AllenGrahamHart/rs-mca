# Audit

The proper-span case split covers dimensions0,1,2, including nonconstant
rank-one planes. A nonconstant plane is handled by the previous
rational-plane theorem, NOT the constant-plane inequality. A whole
rank-one3/3 enclosure has constant direction by full-carrier rigidity.

The determinant step uses its own rank-two hypothesis and the original
eight-anchor bound2J-10, not a shared-rank step with r=s. Constant2
monotonicity holds because every g+1>(R+1)/3. All scalar padding
preserves r,w and the actual degree J-8 in the same source field.

For the constant prefix, the preferred charge is t once and the outside
raw charge is b times a proved scalar LIST bound. Original raw defects
are not recomputed after division. All legal complements are covered.

The independent audit imports no new primary module or selector. It
checks every frozen LIST trace against the inherited legality checker,
reconstructs inherited pencil bounds and recomputes all new inequalities.
Mutation tests distinguish a changed dimension/anchor/scope from a valid
proof. External mathematical review remains due.

No failed tail envelope is called a counterexample or an impossibility
theorem. The two remaining full-span geometries stay explicitly open.
