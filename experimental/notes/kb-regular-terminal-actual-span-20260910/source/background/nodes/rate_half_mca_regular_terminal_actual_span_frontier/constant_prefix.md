# Direct Scalar Dimension3 Count On The Whole Constant Pencil

Use the six parent profiles from9965..10964 through13965..14964.
Their constant source gates g are

    684191,673270,662350,657878,651329,640409.

For a whole function-field-rank-one3/3 terminal the direction is constant
by full-carrier rigidity. Its actual P_t pairs inject into an affine
scalar polynomial space of dimension<=3. If C is their COMPLETE core
union and e=n-|C|, the full P43 pencil union contains C. Thus absence
of the paid original source class forces

    g+1<=e<=R-d+t.

All eight old anchors belong to C and to every pair's complete core.
Subtract an actual pair and divide only scalar differences by their
locator. On C minus those anchors the scalar LIST has

    length=R+J-e-8, degree=J-8, agreement=d+J-t-8.

The receiver is defined by pointwise division; it is not asserted to
be a divisible polynomial. All original labels and weights survive.

For an integer complement box[a,b] in a J profile[J0,J1], the same-field
padded scalar instance is

    (r,w,Kmax,s)=(R-a,d-t,J1-8,3).

Padding replaces e by the smaller a. The uniform-in-degree theorem
covers every J in the profile with actual degree J-8. All corridor,
dimension and field-size gates are checked by the finite certificate.
Let C3(a) be its selected three-step cap. The original weighted-pencil
ownership proof then gives

    raw weight<=t+e*C3(a)<=t+b*C3(a).

There is only one preferred finite slope for this whole pencil. Its
original raw charge is at most t, not t per scalar word. Other original
defects lie outside C and remain disjoint for each assigned pair.

The boxes have width10000, with their final endpoint clipped to R-d+t.
They cover the entire legal complement interval in each profile.
Their388 three-step traces total1164 exact LIST transitions.
The six maximum pairs of bounds are printed in statement.md and all
are below the existing L_1,L_2. No fractional refund or new source
resource is needed for this prefix.

The analogous coarse envelopes for J>=14965 were above the allowance.
Those are insufficient upper estimates, not counterexamples or a proof
that the whole-constant tail is false. This theorem does not extend
the prefix or claim its endpoint is optimal.
