# Generic Kernel Degree Does Not Bound Every Coordinate Fibre

Status: PROVED by an explicit algebraic construction. Independent
mathematical review remains due.

Over any field F of characteristic zero or greater than7, put

    v=X^3-X, w=X^4-X^2=X*v, U=span_F(1,v,w),
    T(1)=v, T(v)=w, T(w)=1,
    W={(f,Tf):f in U} subset U x U.

Then W has pair/shared dimensions3/3 and full generic projection:
pi_0(f,Tf)=f is already invertible. Its function-field rank is two,
joint evaluation has rank two at every finite geometric coordinate,
and W contains NO two-dimensional function-field-rank-one plane.

In the coefficient basis(1,v,w), the joint kernel-direction map is

    d(X)=(v-w^2, v*w-1, w-v^2) in P(W).

Its coordinate polynomials have degrees8,7,6 with no common finite
zero. The map is generically one-to-one, with a rational inverse on
a nonempty open set. Nevertheless

    d^(-1)([0:1:0])={-1,0,1},

including geometric coordinates, exactly THREE distinct points.
The two candidate pairs(0,0) and(v,w) have those same three joint
agreement coordinates.

This refutes the proposed inference from generic fibre cardinality one
to a uniform one-coordinate kernel-fibre or joint-intersection cap,
even in the regular pencil-free3 class. It does NOT construct an
original large-agreement/full-code-bad Prize source, and does not refute
any existing source or terminal bound. No image-curve degree formula
or published theorem about singularities is needed for the construction.
