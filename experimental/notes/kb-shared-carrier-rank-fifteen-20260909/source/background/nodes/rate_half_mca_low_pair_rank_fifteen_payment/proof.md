# Use The Actual Low-Owner Deficit Before Four Joint Anchors

Put R=1048576,d=67472,E=21499, near=134944 and
T=274138707278280353. All source and selection conventions are those
of the required rank-fourteen theorem. In particular n-m=R-d=981104.

## 1. The Existing Owner Lemma Gives A Smaller Charge

For a represented low pair f, let H_f be its COMPLETE joint core and
c_f=max(1,m-|H_f|), which is either1 or2. Each assigned original
label has raw>=c_f and at least one selected defect. For distinct labels
assigned to the SAME pair, these defect sets are disjoint: outside H_f,
the nontrivial equation (u-a)+gamma*(v-b)=0 has at most one finite
solution gamma. Different pairs may reuse coordinates.

The required raw-margin theorem's (OWNER), with kappa=3, therefore gives

    G_f=sum_(owners of f)(1-raw/3)
       <= (1/c_f-1/3)*(n-|H_f|).

If c_f=1, then |H_f|>=m-1, so

    G_f <= 2*(n-m+1)/3=2*981105/3=654070.

If c_f=2, then |H_f|=m-2, and

    G_f <= (n-m+2)/6=981106/6<654070.

This is a deficit charge, not a claim that there are at most654070
labels per pair. Removing the possible finite kernel label only deletes
nonnegative deficits and leaves the bound valid on all retained records.
This argument is a direct specialization of the existing owner theorem;
no new generic hypothesis or assumed exception census is introduced.

## 2. Extend The Constant-Pencil Complement Gate

Let ell be a constant-direction pencil, U its complete P_2 core union,
and e=n-|U|<=520000. The required dominant-pencil theorem already
pays e<=500000 by T. It remains to pay500001<=e<=520000.

As in that proof, ell is compatible with the receiver on U in either
the finite chart u+gamma_0*v=c with c in h_*+V, or the infinite
chart v=c with c in V. Charge the possible finite kernel label once.
The compatible-domain refund applies to ALL retained original records,
including off-pencil and higher-defect owners. No coordinate is deleted
from the original source, and no rank/core gauge is changed.

Every off-pencil low pair has at least67471 joint agreements on D\U.
Its components still share V of dimension11. Auxiliary padding to520000
points and degree relaxation to E give the joint-LIST input

    (r,w,K_max,s)=(498501,45972,21499,11).

All positive corridor and field-size gates hold. The eleven exact steps
in outside_list_cap.json give M_off<=85934633384. Applying section1
to these original owners, the SAME groupwise refunded envelope becomes

    |Gamma|+near <= E(a,b)+654070*85934633384,         (BOX)

where E(a,b) is the required all-pair-pencil certificate's original
envelope. Its tuple resource pays every retained raw value, its on-pencil
deficit is unchanged, and only the conservative OFF charge is sharpened.
The unrounded rational resource is kept inside the refund.

Use the old thousand-wide boxes intersecting500001..520000. There
are21 of them, indexed500..520. The first/last boxes are clipped only
for coverage: their original conservative endpoints remain in E(a,b).
Since e<981104, the earlier |U|>=m anchor gate holds throughout.
The exact maximum of (BOX) is attained at [500000,500999], with ceiling

    261389606553601974<T.                            (CONSTANT)

The existing982-row, eight-shard certificate is reused unchanged. Hence
the entire constant-pencil class e<=520000 is paid by T.

## 3. Hereditary Pencil Bounds Outside Paid Classes

Retain the required rank-fourteen theorem's eleven nonconstant height-band
payments and the preceding constant/Johnson source classes, each priced
below or at T. In their complement, every constant pencil has union
size<=n-520001. The previous height-resolved nonconstant restrictions
are unchanged. Scalar affine LIST on EVERY pencil subspace therefore gives

    C_t=528576/(67473-t) for constant directions,
    P_t=781095/(85474-t) for arbitrary directions, t=1,2.

Here 528576=R-520000. The argument uses the union of COMPLETE cores
of all P_2 pairs on a pencil, not only its intersection with the current
child. Two distinct counted degree-<J pairs guarantee primitive height h<J;
empty/singleton intersections are immediate. All hereditary scalar ratios
are at least one on nonempty lists. Exact comparison gives1<=C_t<=P_t.

## 4. Four Anchors, With An Explicit Final Child Split

Let S_t be the represented pairs with complete core size>=m-t. If P_2
is empty the unrefunded mass alone pays. Otherwise, enlarge its affine
direction hull inside V x V to dimension15, which is possible because
dim(V x V)=22. This is only an auxiliary upper-counting space; it leaves
the original source and actual V unchanged and includes every lower pair rank.

The required shared-carrier theorem successively applies at pair/shared
ranks15/11,13/10,11/9 and9/8. The bad-anchor counts are at most
J-4,J-3,J-2,J-1. Their positive original-domain incidence factors are

    Q_c=(R+c)/(d-t+c), c=4,3,2,1.

Good sections reduce pair rank by two and shared rank by one. Earlier
anchor coordinates become common zeros and remain explicitly counted.
No source coordinate, degree bound J or agreement threshold m-t changes.

The terminal child has pair/shared ranks7/7. A rank-one direction is
therefore constant, giving cap C_t^7. A rank-two direction is paid by
hereditary incidence, with cap H_t(E)*P_t^5, where

    H_t(E)=1027079/(45975-t).

The exact gates H_t(E)<=P_t^2 and H_t(E)*P_t^5<=C_t^7 pay BOTH
child types by C_t^7. No repeated-rank-drop assumption is used after
reaching equality. Thus

    M_t=|S_t|<=C_t^7*product_(c=1)^4 (R+c)/(d-t+c),
    pair floors105622838829 and105640059975.           (COUNT)

## 5. Finish In Original Slope Units

The unchanged source chain gives the UNREFUNDED integer mass
W3=613022127444579907 and same-pair defect bound
L_t=sum_(raw<=t)raw<=(R-d+t)*M_t. Exact telescoping gives

    |Gamma|+near
      <=floor(W3/3+sum_(t=1)^2 (R-d+t)*M_t/(t*(t+1)))+near
       =273428272906977732.                         (BRANCH)

The sum uses the rational caps (COUNT), not their separate floors.
The maximum of (BRANCH), (CONSTANT) and the previously retained
whole-source alternatives is T. This proves (RANK15), with all raw>=3
labels and near once. Lower ranks are counted again in the same auxiliary
space, not paid by summing other full-source bounds.

Contraposition holds under every valid assignment. It does not exclude
or pay pair rank>=16, original error rank>=13 or either full Prize problem.
