# Audit

The old off-pair charge was a label-count bound. The new654070 charge
is justified only for G=sum(1-raw/3), using the EXISTING weighted owner
theorem and complete-core threshold c_f. It does not bound label count.
For c_f=1, the outside capacity is<=981105; for c_f=2 the deficit
density is only1/6 and raw1 labels cannot occur. Raw0 or repeated
same-pair defect coordinates would invalidate the argument and are excluded
by the source proof, not by an empirical assumption.

The same compatible-domain refund funds ALL retained records. Kernel
label deletion does not change the original carrier or invalidate the
per-record source resource. Near and the possible kernel are charged once.
The new520000 complement count is auxiliary, over the original field.

The21 inherited boxes500..520 cover500001..520000. Their full old
endpoints are retained at clipped boundaries. The unrounded R0 enters
the fractional refund; no floored resource is used there.

Four anchor stages each satisfy pair rank>shared rank. At7/7, both
constant and rank-two children are paid. Smaller actual pair ranks are
included by auxiliary embedding, not by remeasuring the source.

verify.py reproduces the eleven new LIST steps, tests the owner arithmetic
on small capacities and checks the final bound with exact Fractions.
verify_audit.py imports no primary/compiler, independently checks the
transitions and old-box fractions, and rejects malformed certificates.
External independent mathematical review remains due. No numerical test
or source hash substitutes for the source, ownership or rank proofs.
