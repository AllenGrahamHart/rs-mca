# Track Pair Rank And Shared-Carrier Rank Separately

Write R=1048576,d=67472,E=21499 and near=134944. Every source and
selection below has the fixed original normalization in statement.md.
All original labels and higher raw values are retained.

## 1. Extend The Nonconstant-Pencil Complement Gate

For a nonconstant primitive pencil of height1<=h<J, let U be its
COMPLETE represented P_2 core union and e=n-|U|<=272000. U is nonempty.
The required dominant-pencil proof, sections1-2, gives the groupwise
whole-source inequality

    |Gamma|+near<=228260637755610995+981106*M_off.      (GROUP)

This is valid for an arbitrary pencil group: all off-pencil low-pair
deficits are retained with their proved original-label factor981106.
Its base resource still pays every original record. It does not apply
an all-pairs-on-pencil theorem to the original source.

The required escape theorem gives every off-pencil pair at least
67471-h>=45973 joint agreements on D\U. Its two polynomial components
share the SAME carrier V of dimension11. In this auxiliary count only,
relax degree to E and pad the complement to272000 distinct coordinates,
with arbitrary received values. Every original pair and its agreements
survive, and q is larger than the padded length. The shared-carrier LIST
compiler applies with

    (r,w,K_max,s)=(250501,24474,21499,11).

The eleven exact steps in outside_list_cap.json give
M_off<=45437954634. Substitution into (GROUP) proves

    |Gamma|+near<=272840087674756199.                  (EXT)

No claim that the compiler or this complement endpoint is optimal is used.
The already published dominant-pencil proof and artifact remain unchanged.

## 2. Retain All Previously Paid Alternatives

Assume affdim_F P_2<=12. If it is at most eleven, the required
rank-eleven theorem pays the whole source by T=274138707278280353.
If any earlier dominant-pencil gate applies, the same T pays. If (EXT)
applies, its smaller cap pays. These alternatives retain near once and
all higher-defect owners.

In their complement, Z_*=aff(P_2)=f_*+W has pair dimension12, with
W subset V x V and dim V=11. Every nonconstant height-h<J pencil's
complete P_2 core union has size<=n-272001, and every constant-direction one
has size<=n-500001. Also W has function-field rank two: a rank-one
direction in V x V has F-dimension at most11 by pencil normalization.

## 3. Pay Every Pencil Subspace Hereditarily

For t=1,2, let S_t be the pairs in P_2 whose COMPLETE joint core has
size at least A_t=m-t. For ANY affine polynomial-pair subspace Z of
dimension v with function-field direction rank at most one, the same
primitive normalization and scalar affine LIST proof as the required
rank-eleven supplier gives

    |S_t intersect Z| <= C_t^v for a constant direction,
    |S_t intersect Z| <= P_t^v in all cases,           (PENCIL)
    C_t=548576/(67473-t), P_t=776577/(67474-t).

If an intersection has at least two distinct bounded-degree pairs, its
primitive row automatically has h<J. For clarity, its scalar ratio is
(|U|-J+h+1)/(d+h+1-t), with U the complete union of ALL P_2 pairs
on the pencil, not just S_t intersect Z. At h=0 the constant union
bound gives C_t. For h>=1 the new union bound gives

    (R-272000+h)/(d+1-t+h).

This decreases with h, so h=1 gives P_t. The actual parameter hull
has dimension at most v; a nonempty list has scalar ratio at least one.
Zero/singleton intersections are immediate. Exact inequalities give
1<=C_t<=P_t. Thus (PENCIL) holds on EVERY affine pencil subspace,
including those inside anchored children, without a census conjecture.

## 4. The First Anchor Uses Shared Rank

Apply the new dual-flat theorem to (s,r,K)=(11,12,J). The number of
coordinates where joint evaluation on W has rank less than two is
at most J-1, not merely2J-2. Since A_t-(J-1)=d+1-t>0, incidence
costs at most

    Q_t=(n-J+1)/(A_t-J+1)=(R+1)/(d+1-t).             (FIRST)

Every nonempty good agreement section Z_x is an affine10-space with
directions in V_x x V_x, where dim V_x=10. Its common zeros are retained;
there is no further puncturing or degree/threshold change.

If this child's function-field rank is one, full-carrier pencil rigidity
makes its direction constant. Therefore (PENCIL) gives the child cap
C_t^10. If its function-field rank is two, the required hereditary
incidence theorem gives the child cap H_t(J)*P_t^8, where

    H_t(J)=(R-J+2)/(d-J+2-t) <= H_t(E).

The denominator is positive throughout the corridor, and H_t increases
with J. Exact endpoint inequalities give

    H_t(E)<=P_t^2,   C_t^10<=H_t(E)*P_t^8.           (CHILD)

Thus either type of child has cap H_t(E)*P_t^8. Direction rank zero
is impossible for a nonzero10-dimensional ambient space; an empty
intersection is harmless. No assumption that S_t spans a child is used.
Combining with (FIRST) gives

    M_t=|S_t| <= Q_t*H_t(E)*P_t^8,                   (COUNT)
    separate floors:106906553861 and106923140583.

Only the first anchor is required to have the improved bad-set bound.
The second stage explicitly pays both possible child direction ranks.

## 5. One All-Raw Ledger Pays The Original Slopes

The required rank-eleven/pencil chain supplies the unrefunded integer
resource and the same-pair defect ownership inequalities

    sum min(raw,3)<=W3=613022127444579907,
    L_t=sum_(raw<=t)raw <= (R-d+t)*M_t.

The core-sixteen/HIGH187 basis proof pays every raw value, independent
of the pencil or pair-rank hypotheses. Flooring its rational resource
is legitimate here because this mass is an integer and is not multiplied
by a fractional refund. Within each pair the assigned defects are disjoint
original coordinates; defects from different pairs need not be disjoint.

Exact telescoping gives the no-large-pencil branch bound

    |Gamma|+near
      <= floor(W3/3 + sum_(t=1)^2
          (R-d+t)/(t*(t+1))*Q_t*H_t(E)*P_t^8) + near
       =274267808872348771.                         (PRICE)

This uses the unfloored rational pair bounds in the sum. All raw>=3
records are already paid by their resource term, and near is added once.
The maximum of (PRICE), (EXT) and T is (PRICE), below B* by
712919239046316. This proves the whole source-class bound (RANK12).

Contraposition applies under every valid selection/minimizer assignment.
It does not assert that pair ranks>=13 are impossible or paid, nor that
original source error rank is at most twelve. Both Prize problems remain open.
