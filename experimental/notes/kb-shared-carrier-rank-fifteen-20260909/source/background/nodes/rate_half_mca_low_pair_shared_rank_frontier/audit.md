# Audit

The common direction carrier U is the SUM of both component images of
the actual pair direction hull. Bounding the images separately would
undercharge s. Changing the base pair does not change their spans.
The original carrier V remains dimension11 for the all-record resource.

Auxiliary offsets a_0,b_0 are retained; b_0 need not lie in U. This is why
the shared-carrier theorem's affine-offset formulation matters. The new
carrier can have many common zeros without invalidating any anchor.

At18/9 the final space after nine anchors has dimension zero, so no
constant-pencil cap is needed. At17/10, seven anchors leave3/3 and
both direction ranks are explicitly paid. S_t need not span any child.

The smaller238911770855075395 cap is valid on the entire s<=9 source
class. The s<=10,r<=17 branch273174666855895812 is not universal:
its maximum with older paid alternatives is274138707278280353.
All raw values, same-pair defect charges and near remain present once.

Two exact implementations check the rank paths, pair floors, child gates
and final ledger. Tiny linear controls test the component-sum definition,
base-point invariance and the danger of dropping an affine offset. These
are controls, not proofs of the universal source theorem. External independent
mathematical review remains due. Surviving high-rank sets are not claimed
dense in their affine hulls or paid merely from codimension bounds.
