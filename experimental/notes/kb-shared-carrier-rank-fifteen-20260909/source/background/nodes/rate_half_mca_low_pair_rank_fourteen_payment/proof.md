# Three Guarded Anchors With A Height-Resolved Pencil Cap

Put R=1048576,d=67472,E=21499 and T=274138707278280353. The
source, labels, complete shared core, field and actual scalar carrier are
exactly those of the required rank-twelve chain. Only auxiliary pair
counting spaces will be enlarged; no original source gauge is changed.

## 1. Price Eleven Height Bands, Not One Worst-Height Complement

Consider a nonconstant primitive pencil of height1<=h<J, and its
complete represented P_2 core union U. For ell<=h<=u in a stated band,
every off-pencil pair has at least67471-h>=67471-u joint agreements
on D\U. If e=n-|U|<=e_bar, the SAME eleven-dimensional shared-carrier
LIST proof applies after auxiliary padding to e_bar and degree relaxation
to E. The input is

    (r,w,K_max,s)=(e_bar-E,67471-u-E,E,11).

Every w is positive, r>=w, and q exceeds e_bar. The121 exact steps in
height_caps.json supply eleven pair caps C_off. The groupwise ledger
in the required dominant-pencil proof gives the WHOLE-source price

    228260637755610995+981106*C_off.

Their maximum is272840087674756199<T. All off-pencil low owners
and higher raw values are retained, and near is included once. The bands
cover every possible nonconstant height1..J-1, since J<=21499.

Outside these and the earlier dominant-pencil paid classes, every
height-h pencil in a band has union size<=n-e_bar-1; every constant
pencil has union size<=n-500001. These are consequences of whole-source
payments, not unproved census hypotheses.

## 2. Derive One Hereditary Bound Across All Heights

For t=1,2, let S_t be the pairs in P_2 with COMPLETE core size at
least A_t=m-t. The scalar affine pencil argument of the required
rank-twelve proof, section3, gives a cap on EVERY affine pencil subspace
of dimension v. Constant directions have cap C_t^v, where

    C_t=548576/(67473-t).

For a nonconstant height-h pencil in a band the scalar ratio is at most

    (R-e_bar+h)/(d+1-t+h).

It decreases with h because R-e_bar>d+1-t. Its maximum in that band
is attained at h=ell. Exact comparison of all eleven lower endpoints
gives the uniform maximum

    P_t=781095/(85474-t),                            (PENCIL)

attained in the band18001..20000. A nontrivial intersection contains
two distinct bounded-degree pairs, so its primitive height is <J.
Empty/singleton intersections are immediate. As before, the actual
parameter dimension is at most v, and common agreement coordinates
are handled by the scalar theorem. Exact inequalities give1<=C_t<=P_t.
Thus every pencil subspace has cap P_t^v, with the stronger C_t^v
available for constant direction, regardless of the rank of P_2.

## 3. Enlarge Only The Auxiliary Pair Space

If P_2 is empty, all raw values are at least three and the unrefunded
resource in section5 already pays. Otherwise choose f_* in P_2. Its
affine direction hull lies in V x V, of dimension22. Since the actual
pair dimension is at most14, extend that hull to an F-subspace W_0
of dimension14 inside V x V. Set Z_0=f_*+W_0.

This extension introduces only extra candidate pairs for an upper bound.
It leaves the original carrier V, actual source rank, selected records
and P_2 unchanged. It is not a claim that P_2 has actual dimension14.
The counted S_t may fail to span every ambient space below.

## 4. Three Rank-Two Anchors And Both Terminal Child Types

Successively apply the required shared-carrier anchor theorem at

    (pair rank,shared rank)=(14,11),(12,10),(10,9).

At these stages pair rank exceeds shared rank, so bad coordinates number
at most J-3,J-2,J-1 respectively. Every nonempty good section drops
pair dimension by two and shared-carrier dimension by one. The incidence
costs, in order, are

    Q_c=(R+c)/(d-t+c), c=3,2,1.                     (ANCHORS)

Their denominators are positive throughout the interval. All stages use
the ORIGINAL normalized domain, degree bound J and agreement A_t.
Common zeros at earlier anchors are retained and counted by the theorem.
The actual bad sets may differ from child to child; their uniform bounds
are sufficient for iterated incidence.

After three good anchors the ambient pair and shared ranks are both8.
If its function-field direction rank is one, full-carrier rigidity makes
the pencil constant, giving cap C_t^8. If its function-field rank is two,
hereditary incidence gives cap H_t(J)*P_t^6, with

    H_t(J)=(R-J+2)/(d-J+2-t)<=H_t(E)=1027079/(45975-t).

Exact inequalities show H_t(E)<=P_t^2 and H_t(E)*P_t^6<=C_t^8.
The former is the hereditary theorem's gate; the latter pays BOTH final
child types by the same constant cap. Rank zero is impossible for the
nonzero8-dimensional ambient direction; empty intersections cause no cost.

Working back up the three incidence levels therefore gives

    M_t=|S_t| <= C_t^8 * product_(c=1)^3 (R+c)/(d-t+c),
    separate floors71666785024 and71678469894.        (COUNT)

No repeated rank-two drop is assumed after reaching pair/shared equality.
Before equality every drop has its own proved r>s gate.

## 5. Finish In Original Finite Slope Units

The required source chain gives the UNREFUNDED integer mass
W3=613022127444579907 and same-pair defect ownership
L_t=sum_(raw<=t)raw<=(R-d+t)*M_t. These facts retain every original
raw value and are independent of pair affine dimension. Thus

    |Gamma|+near
      <= floor(W3/3 + sum_(t=1)^2 (R-d+t)*M_t/(t*(t+1)))+near
       =251217725856263117.                         (BRANCH)

Use the rational upper bounds from (COUNT), not their separate floors,
in this sum. Near is included once, and no fractional refund uses a
floored resource. The maximum with the height-band prices and the
earlier dominant-pencil cap T is T, proving (RANK14).

We do not defer smaller pair dimensions to their older theorem's cap:
section3 covers them in the same auxiliary space. This is why the stronger
universal cap T, rather than the previous rank-twelve cap, is justified.
Contraposition gives pair dimension>=15 under every valid assignment.
The theorem does not exclude such families or close either Prize problem.
