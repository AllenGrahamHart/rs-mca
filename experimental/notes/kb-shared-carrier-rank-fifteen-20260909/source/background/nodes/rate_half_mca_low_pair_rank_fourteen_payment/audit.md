# Audit

The height bands cover integer heights1..21498 without gaps or overlap.
For each, the outside agreement is lowered using the UPPER height endpoint;
the hereditary scalar ratio is bounded using the LOWER height endpoint.
Those endpoint roles are opposite and are not interchangeable.

The ambient pair extension is permitted because dim(V x V)=22. It only
enlarges an auxiliary set, leaving the original source fixed. Each section
then has its exact ambient dimension even when S_t does not span it.
The r>s hypotheses are checked separately at14/11,12/10 and10/9.
At8/8 the proof explicitly allows both constant and rank-two directions.

The exact profile supplies121 LIST transitions. The primary regenerates
them with the credited compiler; the independent audit uses integer
transition formulas, reconstructs the price with a common denominator and
rejects malformed bands/inputs/steps. Neither requires compiler optimality.
The old eight-shard resource certificate is unchanged.

All raw values, source labels and one near charge stay in the same ledger.
The branch251217725856263117 is not the universal bound: the earlier
whole-source alternative274138707278280353 is retained by maximum.
The weaker old rank-twelve cap is not retained because lower pair ranks
are directly re-proved by the auxiliary embedding in this theorem.

External mathematical review remains due. Arithmetic, source hashes and
small controls alone do not establish the universal proof. Pair ranks>=15
and original error ranks>=13 remain different unresolved obligations.
