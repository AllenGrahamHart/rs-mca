# Hand Audit

The graph's affine difference space, not only the polynomial explanation
span, must lose dimension. Distinct labels ensure the slope projection has
rank one unless at most one label is present. The gauge therefore removes
exactly one dimension from the at-most-eleven-dimensional graph flat.
No assumption that its kernel projects injectively to the original receiver
is needed; the polynomial map on the graph is explicit.

The shifted received pair has the same normalized degree/length/agreement
row and full-code-bad supports. The padded source cap applies to arbitrary
label subsets and affine explanation dimension at most ten. Different
hyperplane gauges are not added as independent whole-source payments.

Original integrality supplies the plus one in the spread margin. The
exception cap includes carrier-zero labels as well as fiber secants, and
the original near allowance is separate and included once. The final
rank-collapse bound is below the existing main paid-interval maximum.
This is a real source-class payment, but not a full-dimensional census.

Small exact linear-algebra controls check gauge rank loss and its kernel;
independent integer arithmetic checks the interval, near and exception
constants. These controls do not replace the source proof. External
independent mathematical review remains due.
