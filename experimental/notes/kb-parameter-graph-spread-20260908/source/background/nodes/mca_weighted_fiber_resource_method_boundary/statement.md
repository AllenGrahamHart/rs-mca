# A Gate-Respecting Boundary Of The Uniform Weighted Resource Method

Status: PROVED by the explicit construction and rational inequalities.
External independent mathematical review remains due.

Use the normalized KoalaBear row over the ORIGINAL field of size
q=2130706433^6, with n=1048576+J,m=67472+J,degree bound J and actual
polynomial carrier dimension eleven. Set B*=floor(q/2^128).

For EVERY integer9941<=J<=17000 there is a source and a selected
full-code-bad raw-one record forced by maximal-raw selection at its label,
with empty universal carrier core and no
fiber-secant or carrier-zero exception, such that the following recipe
cannot certify a bound<=B*:

- Fix weights w_x=a_i-1 from complete nonzero source fibers.
- Choose ANY0<=lambda<=11/(2(n-12)).
- Use as resource even the EXACT weighted sum over all ordered twelve-
  tuples meeting every source fiber at most once.
- Divide that resource by one lower bound required to hold for EVERY
  eligible record's weighted agreeing independent tuples.

The resulting quotient is>B* even before adding near or exceptions.
The coarser(n)_12 resource cannot repair this failure. Improving only
the universal per-record core count within this recipe is insufficient.

The carrier has one fiber of size a and otherwise singleton fibers, where

    a=J-10       on9941..13999,
    a=J-2001     on14000..17000.

It avoids the existing degree-2000 source-fiber payment wherever that
gate applies. The degree-6000 gate starts at21000 and the clustered-arc
gate at20481; neither is applicable in this interval. Older density gates
start at23000. This is NOT a counterexample to any of those theorems.

## Exact Obstruction

Put c=J-a,N=1048576+c,b=67471+c and

    X_lambda=a*(1+lambda*(a-1)).

The exact allowed source resource U and an UPPER bound H on this
actual record's entire weighted independent-tuple count satisfy

    U=(N)_11*(N-11+12*X_lambda),
    H=12*(b)_10*(b-10+11*X_lambda).                 (RATIO)

Hence every universal record lower bound beta obeys beta<=H, and
floor(U/beta)>=floor(U/H). Uniformly at the stated coefficient range:

    J interval       lower bound on floor(U/H)
    9941..13999      379243051432496463
    14000..17000     275795460861917515.

The second exceeds B*=274980728111395087 by814732750522428. No near
allowance is used in these numbers. Together the ranges contain7060
of the11559 currently unpaid normalized degree values.

## Exact Limits

The constructed received line has EXACTLY TWO bad slopes,0 and-1;
both bad labels have only the zero eligible polynomial explanation.
Its full selected error family therefore has affine rank one. At-1 the
only bad size-m support is the printed support and its maximum raw is one.
Thus canonical maximal-raw selection at that label does not remove the
cheap record, but a genuine full original-rank-twelve restriction does.

This is a METHOD boundary, not an unsafe line, an original error-rank-
twelve complete family or a Prize counterexample. It does not kill the
router or the desired row inequality. The source resource counts many
tuples that may not be agreeing, independent or owned by any original label.

Possible escapes include pricing a source-adaptive lambda ABOVE the
sharp uniform range with its actual larger resource; different weights;
actual incidence rather than distinct-fiber resource; nonuniform records;
receiver-fiber descent; or additional complete-family/source restrictions.
Those require their own proofs. The published intervals are unchanged.
