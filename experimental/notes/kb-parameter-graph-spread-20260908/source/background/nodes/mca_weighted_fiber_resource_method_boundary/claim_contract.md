# Contract

- Quantifier: for each integerJ9941..17000, an actual normalized source
  and eligible raw-one record defeat the specified universal-cost recipe.
- Original fieldq=2130706433^6; the evaluation domain remains any n-point
  set. No extension, coefficient-field normalization or denominator change.
- All weights come from full source fibers, not selected core multiplicities.
- The coefficient is restricted to0<=lambda<=11/(2(n-12)). Extrapolation
  to larger, separately funded coefficients is explicitly prohibited.
- Resource U is the exact weighted DISTINCT-FIBER tuple sum, not the actual
  agreeing/independent/owned source resource. Those can be much smaller.
- H is an UPPER bound on the entire actual selected record's independent
  tuple weight, hence any uniform lower record charge is at most H.
- The full-code-bad support has size m, complete core m-1 and raw one.
  Carrier rank eleven is actual. The selected explanations need not span it.
- The raw-one choice at gamma=-1 is forced even by canonical maximal-raw
  selection: all nonconstant explanations have fewer than m agreements,
  and the only other rich constant is full-pair-explained. The source has
  exactly two bad slopes, both with zero explanation, so full error rank
  is one. No original rank-twelve family is asserted.
- The explicit source has no carrier zeros and no fiber-secant exceptions.
- The minimum ratio already exceeds B* before near; do not double-add near.
- The no-go is a restriction on one resource/minimum-cost method, not on
  all isolated-core arguments, nonuniform source accounting or the Prize.

Falsifiers: a wrong full source fiber partition; an explaining degree-<J
polynomial pair on the constructed support; a missing independent tuple
in the record upper bound; a failure of the coefficient/degree monotonicity;
or either endpoint ratio at most the printed floor. Source-dependent
methods outside the exact recipe are not falsifiers of this theorem.
