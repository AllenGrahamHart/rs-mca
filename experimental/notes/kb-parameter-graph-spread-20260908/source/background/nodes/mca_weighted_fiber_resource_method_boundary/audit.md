# Hand Audit

The source polynomial degrees are a+9<J, including the gate-respecting
c=2001 branch. Root counts establish actual rank and full-code badness
without enumerating the large domain. XP_A/P_A separates all non-A
projective rows, so the weight formula really uses complete source fibers.
The normalized receiver color on A is constant and exceptions are empty.

Only x0 contributes a nonzero final incidence coordinate on the selected
support. This proves the upper bound concerns ALL agreeing independent
tuples, not merely the twelve inserted copies used by an earlier lower
count. Both zero/one-A classes and their weights are retained. Possible
dependencies among outside polynomial rows can only decrease actual cost.

The rational ratio is decreasing in the single scalar X_lambda, with a
strict sign checked at both fixed-c regimes. X_* increases with a by an
explicit positive derivative. Only two exact endpoint divisions are
load-bearing. The separate streaming audit is a control, not the proof of
the infinite coefficient interval. Small actual-field fixtures check
full-code badness, incidence ranks, weights and empty secant sets.

The scalar has two levels on large sets and one singleton exception.
Root counting bounds every nonconstant explanation by2J-1<m agreements.
Checking the constants proves that at-1 the bad support/explanation are
unique, so the raw-one record survives canonical maximal-raw selection.
The same calculation gives exactly two bad slopes with error affine rank
one. The primary checks the constant cases in both small actual fixtures.
Do not convert the resource quotient into an unsafe-row lower bound.
Its useful scope is to stop spending the whole closure effort on
better uniform costs under the already sharp coefficient. Additional
source information or separately funded larger weights remain open routes.

The primary uses two actual small-field sources, two endpoint divisions,
four wrong floors and six scope/coefficient guards. The separate binomial/
integer audit streams7060 degrees and rejects14120 wrong floors; it imports
neither primary nor Fraction. Final normal/-O timings are in the journal.
All computation is serial under RAMguard tiny; no Modal or large field
enumeration. External independent mathematical review remains due.
