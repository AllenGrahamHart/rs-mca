# KoalaBear MCA: Actual Parameter Graph And Weighted-Method Boundary

K3 / DIRECT companion to #1175. Two hand-proved local results, submitted
for independent mathematical review. This is an extension of the
[published weighted/switching packet](../low-core-flat-source-cumulative-20260908/SWITCHING_CREDIT.md),
not another finite interval scan or an active Grande Finale v4 atom.

## Row And Impact

Original field q=2130706433^6, n=2097152, k=1048576, agreement1116048,
target2^-128, finite challenge denominator q, budget274980728111395087.
Unit: distinct bad slopes of one received line, with full-code-bad supports.
The original rank-twelve normalization has(n,K,m)=(1048576+J,J,67472+J),
an actual11-dimensional polynomial carrier and one original near allowance134944.
All labels and the original field are retained, including nonuniversal zeros.

1. **Actual graph hyperplane cap.** Every proper affine hyperplane of
   the slope/explanation parameter space contains at most156765527508668296
   selected bad labels. On an over-budget original rank-twelve source,
   at least118215200602591848 labels lie outside every such hyperplane,
   uniformly over all valid reselections in the fixed normalized carrier.
2. **Paid post-exception rank collapse.** Throughout J9941..21499 the
   existing secant/zero exceptions cost at most1149710068. If the surviving
   graph lies in a proper hyperplane, the entire original line is paid by
   156765528658513308 including near once. Thus every over-budget source
   retains full graph dimension12 and explanation dimension11 after them.
3. **A precisely scoped method obstruction.** Throughout J9941..17000,
   an actual raw-one record makes even the exact weighted distinct-fiber
   resource divided by any uniform record lower cost exceed the budget,
   for every coefficient0<=lambda<=11/(2(n-12)). Its label forces the
   raw-one canonical choice, but the actual line has exactly two bad slopes
   and error affine rank ONE. It is not an unsafe rank-twelve example.

The results belong together: the local record obstruction cannot be treated
as a complete high-rank source. Actual family geometry and tuple ownership
are possible ways around the uniform-resource limitation.

## Review Order

- [Graph statement](source/background/nodes/rate_half_mca_parameter_graph_spread/statement.md)
  and [full gauge proof](source/background/nodes/rate_half_mca_parameter_graph_spread/proof.md).
- [Weighted boundary statement](source/background/nodes/mca_weighted_fiber_resource_method_boundary/statement.md)
  and [actual construction and two-endpoint proof](source/background/nodes/mca_weighted_fiber_resource_method_boundary/proof.md).
- The credited [uniform rank-ten explanation cap](../low-core-flat-source-cumulative-20260908/source/background/nodes/rate_half_mca_scan_free_error_rank_eleven_payment/statement.md),
  [original-source transport](../low-core-flat-source-cumulative-20260908/source/critical/nodes/rate_half_mca_rank_twelve_paid_interval_assembly/proof.md),
  and [source collision bound](../low-core-flat-source-cumulative-20260908/source/critical/nodes/mca_coordinate_weighted_collision_resource/statement.md).
- [Validation and provenance](VALIDATION.md), with the exact acyclic
  requirements in [SOURCE_MANIFEST.json](SOURCE_MANIFEST.json).

For the graph proof, restrict to any fixed hyperplane's actual labels.
If there are at least two, its difference space E has slope projection
rank1 and dim E<=11. Choose(1,b) in E. The polynomial gauge
v'=v-b,h'_gamma=h_gamma-gamma*b removes one dimension, preserves the
same-support full-code badness, and invokes the existing dimension10 cap.
Subtract that cap from B*-near+1; then subtract the single exception set.
Different hyperplane gauges are NOT summed without a priced cover.

## Nonclaims

The remaining degree interval J9941..21499 still contains11559 integers.
No new complete degree interval, higher-rank case, unrestricted endpoint,
ordinary LIST bound, v4 owner atom or Prize problem is closed. The previously
paid interval total274979661975561635 and its reserve are unchanged.
The graph theorem neither covers low-cost records by finitely many
hyperplanes nor bounds the remaining full-dimensional family. The weighted
boundary does not rule out separately funded larger coefficients, different
weights/resources, nonuniform charges, ownership or complete-family rank.

Both new local nodes are PROVED evidence for the open router, not added
requirements of the old72-node interval assembly or114-node threshold closure.
The74-node dependency union here includes that assembly as a supplier of
original-source transport, not an asserted exhaustive resolution.

## Replay And Compute Requests

From any working directory, use Python3.10+ and run this folder's replay:

```bash
python3 -B experimental/notes/kb-parameter-graph-spread-20260908/replay.py
python3 -B -O experimental/notes/kb-parameter-graph-spread-20260908/replay.py
```

The relative commands above assume the repository root. The runner itself
resolves its inputs independently of the working directory. Checks are serial,
small-memory and timeout-bounded. Hashes and controls do not certify the
universal hand proofs. No large computation is requested; no Modal run or
spending was needed. The next task is a proof about actual high-rank families,
not a proposed expensive enumeration.
