# Provenance and theorem-subtraction audit

Date: 2026-09-07. Agent: Codex acting for AllenGrahamHart.

## Pins and coordination

- Upstream main: `93fba1be3f3299b0ba4708d88715377bbb656e45`.
- PR #1175: `6c59f9aa75b897c9274e94c7aa8864acd26a85ea`, OPEN,
  Scott Hughes, `codex/kb-mca-rank12-multilevel-post-1174`.
- PR #1174: `1b613fc669158a690a52b64f0eeb440f10672f1e`, OPEN,
  Scott Hughes. Its nonuniform-resource theorem descends from #1168.
- PR #1160 near-reduction pin named by #1175:
  `c5f4ea7a0c78828c901ae5f3428894a8b2e2806b`, still an explicit
  integration dependency, not silently merged into main.
- Latest preceding public continuation:
  [canonical selector and seven-margin comment](https://github.com/przchojecki/rs-mca/pull/1175#issuecomment-5569924934).
- Local source tree: `prize-codex-resolution-v12-20260807`, dirty HEAD
  `3b51e86d2595f28a02e842c81effca1dfcf98e77`. New theorem text is
  identified by the file hashes, NOT that preexisting commit.
- Canonical Fable tree inspected read-only:
  `0dd5b324482194208be0289f76ed3f0817648a46`.

No source tree controlled by another agent was edited. This companion
commit is on AllenGrahamHart's fork; it does not change Hughes's PR head.

## Subtraction and scope

The current `agents.md`, newest `experimental/agents-log.md` entry,
four-row compiler note, and current open-PR metadata were inspected.
The current main ledger does not supply an adjacent safe KoalaBear row.
The focused main-paper search found no copy of the new moving-axis
equality argument, full-kernel strip proof or exact integers. This is a
targeted overlap check, not an exhaustive audit of every PR's mathematics.

The nearest live overlap is #1175 and its discussion. Its advertised
theorem pays gluing quotient rank zero and isolates ranks one/two; its
body explicitly does not close affine error rank twelve. We continue that
same source-based program with algebraic families, not another competing
gluing-rank-zero proof. The prior public comments supply provenance for
selection, transport and the old relation ledger; the present packet
exports the newer proof sources together rather than adding isolated
numerical comments.

The nonuniform support-margin resource is reconstructed and attributed
to Hughes's #1168/#1174 theorem. Basic Johnson incidence, same-field
padding, hypersurface Bezout and the curve/function-field facts are
standard ingredients, not novelty claims. The submitted additions are
their source-compatible algebraic-family applications, the two-tangent
and moving-direction equality bounds, the exact curve/cover accounting,
and the complete 4801..7116 full-kernel reduction.

No equivalence is asserted with the earlier equality-wall program's
405 labelled conics. Their owners, variables and projection must first be
matched; both programs using the word "conic" is not such a theorem.

## Compact proof dependency graph

```text
ordered-normal margin resource ------------------------------+
canonical maximal-raw selection -----------------------------+-> gain ledger
ordinary Johnson + padding -> scalar/shared-carrier lists ----+-> pencil prices

hypersurface degree cover + joint incidence -----------------+-> graph prices
two coprime tangent images -> graph dimension bounds ----------+
equality exclusion in shared output carrier -----------------+-> moving conic
fixed divisors on a projective curve -------------------------+-> rank-two conic

pencil prices + all conic cases + parallel-fiber prices -------+
full interpolation kernel -> gcd degree <=2 + <=100 exceptions+-> strip theorem
half-domain ordinary Johnson -> <=43 pairs on one line --------+
```

There is no reverse dependency from a geometric supplier to the strip
consumer. Historical local IDs remain only in the vendored source paths.
For DAG integration, the new node is
`rate_half_mca_low_core_kernel_quadratic_strip`, with req from
`rate_half_mca_cancelled_low_core_relation_payment` and ev to
`rate_half_mca_global_core_rank_support_distance_router`. Its source
subgraph is PROVED locally; the latter router remains TARGET.

## Upstream integration obligations

1. Independently review the geometric proofs and verify the original
   label/group resource accounting against SOURCE_CONTRACT.md.
2. For a direct restricted-source theorem, retain that contract literally.
   This route needs no claim that every source is already covered.
3. For a rank-twelve residual elimination, bind the actual normalization,
   support saturation and original near bound at the integration commit.
   The two cited unmerged upstream branches cannot be treated as merged.
4. For an active-v4 integer, prove exhaustive ownership/transport and retain
   all inherited charges. Do not insert the restricted-source total as
   `U_paid`, `U_BC`, `U_Q` or `U_new` by matching parameters.

No paper build or accepted upstream theorem status is asserted here.
