# Contract

Use the exact source and row in statement.md, not arbitrary polynomial
pairs or an arbitrary choice of supports. F is unchanged; all slopes are
the original finite slopes. The canonical selector, complete pair cores,
one common carrier, and empty universal core are load-bearing.

The new mathematics is the full interpolation kernel's common-factor
degree bound, the finite off-factor exception count, and the Johnson
argument for nonparallel constant lines. Inherited exact group prices are
from the required finite conic/interpolation supplier. Their full source
quantifiers and original-resource partition are retained.

Status is a proved restricted-source upper bound, NOT an unconditional
rank-twelve or prize-row bound and NOT an active-v4 bankable atom.
Independent external review and upstream source/owner transport remain
separate obligations. Arithmetic tests do not certify the hand proof.
