# Review record and falsifiers

Mathematical verdict: NO ISSUE at the explicit restricted-source scope.
This is an author's hand audit, not independent external acceptance.

The central proof checks the ENTIRE interpolation kernel, primitive
division over F[X], additive weighted degree, finite Bezout exceptions,
and all quadratic factor patterns. It does not jump from one polynomial
relation to a universal conic description. The line-intersection shift is
used for pair geometry only, never as a change of finite MCA labels.

The moving-conic proof counts in the original degree-<J pair coordinates
after using its rational parameter only for dimension. The shared carrier
and nonconstant quadratic direction are load-bearing. The affine-product
argument retains the total coefficient degree, even though it does not
enumerate divisor patterns. Graphs allow denominators in X, not in the
unknown component Y; exceptional characteristics stay excluded.

Exact source controls exhibit actual canonical LOW families on a square
graph, rational-X graph, hyperbola and moving parabola. They also falsify
omitted support-maximality, the boundary 2T=d, missing characteristic
guards, unrelated output carriers, and dropping the degree charge.
These finite controls are evidence about the proof's gates, not substitutes
for its uniform dimension arguments or deployed unsafe witnesses.

The primary and independent integer checkers verify the finite list
recurrences and directed-rounded envelopes, plus the new monomial counts,
the 43-pair cap and final reserve. Explicit hostile checks reject endpoint
7117, cap 42, one-unit underpayment, and an insufficient exception allowance.

The inherited verifiers use `assert`. The public replay wrapper checks
hashes with explicit exceptions and invokes fresh, nonoptimized children,
removing PYTHONOPTIMIZE. Therefore `python -O replay.py` does not silently
disable these mathematical checks. The optimized wrapper run is a runtime
guard check, not a second universal proof.

No exhaustive local test suite, Lean proof or TeX/PDF build is claimed.
No main status, null atom, higher-rank result or unrestricted threshold
changes. Broader J intervals, higher ranks, the active-owner bridge and
independent hand review remain open.

The fork commit and SOURCE_MANIFEST.json pin the review text. A source
hash pass establishes identity only; it does not prove the mathematics.

## Completed replay

- Normal wrapper: 68 frozen sources and 21 serial checks PASS,
  3.10 seconds wall time, maximum child/parent RSS 16512 KiB.
- Optimized wrapper with assertion-enabled children: same 21 checks PASS,
  3.29 seconds wall time, maximum RSS 20804 KiB.
- Local DAG compilation and manifest equality: PASS, 2729 nodes and
  8147 edges, with three manifest mutation controls rejected.
- The affected preexisting threshold closure remains 63 acyclic PROVED
  nodes. The new strip node is separate evidence to the still-open router;
  no claim of a clean global DAG/crosswalk audit is made.

All replay was serial under a 256 MiB RAMguard ceiling. No Modal job,
large computation, detached Git maintenance, or ongoing test remains.
