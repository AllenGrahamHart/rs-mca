# Audit

Hand review checked that scalar multipliers are polynomial by Bezout,
that nonconstant multiplication cannot preserve a finite-dimensional
space, and that maximal component span equals the ENTIRE shared carrier.
This makes the maximal-pencil section nonzero at every regular coordinate.

For a5/4 plane, every regular child intersects it nontrivially. A whole
rank-one child would force a maximal nonconstant pencil whose components
span V but all vanish at the regular anchor, a contradiction. This does
not rely on actual occupation of the formal plane.

The quadric inverse is the rational second intersection of a line through
a smooth quadric point. Nondegeneracy keeps the curve out of both the
chosen complement hyperplane and tangent plane, so the inverse exists on
a dense open set. Curve singularities are allowed. The cone has one
geometric vertex also in characteristic2; vertex birationality is NOT
asserted. Its actual child is paid only by the separate finite consumer.

Exact controls pass normal/O:36 maximal-pencil contractions, three covers
of a singular curve point on a smooth quadric, an actual cone-vertex mu2,
and counter-controls for constant pencils and single nonconstant directions.
Runs0.09/0.14seconds,12288/16348KiB peak RSS. They are not official MCA
sources or a substitute for the universal proof. External review remains due.
