# Pay Every Regular Child Without Assuming Its Plane Is Occupied

Work first outside the previously paid whole-source pencil alternatives.
This supplies all hereditary source capacities needed by the existing
terminal L_t bounds. Nothing asserts that every original input avoids
those alternatives.

Take a seven-anchor5/4 enclosure with a nonconstant pencil plane and
primitive normalization nu<=H. The new geometric supplier places its
projective carrier image on a smooth quadric or a quadric cone.
Every regular child has function-field rank TWO.

At a nonvertex inner centre the projection is birational, including
when the curve itself is singular. The actual degree ledger gives
nu_child=nu<=H. If the regular3/3 child is pencil-free, the inherited
normalization frontier pays it by L_t, for all image degrees and slack.
If it has a pencil plane, the existing rational-plane theorem pays it
by the SAME L_t. These are exhaustive alternatives because whole rank
one was excluded geometrically.

At a cone vertex, the child contains the entire original nonconstant
plane. The rational-plane theorem therefore pays it by L_t regardless
of its normalization increase. No exceptional coordinate is removed
and no allowance is enlarged.

Thus EVERY retained regular child has original weight<=L_t. The existing
original weighted-anchor factor at this stage is
1048577/(67473-t), proving the first assertion. Dividing the full gcd
or an anchor is only a change of polynomial coordinates. The full divisor
can be larger than the set of removed domain roots; the inherited
nonnegative slack accounts for that difference.

## Maximal-Pencil Prefixes

At a prefix with shared dimension s and pair dimension2s-3, a
nonconstant pencil of dimension s-1 survives every regular anchor
with its dimension reduced by one. After s-3 anchors every terminal
is rank-two3/3 with a nonconstant pencil plane. Hence all its original
weights fit L_t, independently of normalization.

The original weighted factors have indices ell=s-3,...,1. Multiplying
them proves the prefix inequality, without shortening the raw labels
or replacing the original source by a new lower-rank MCA source.

At j=0 the same full P2 enclosure contains the actual P1 subfamily.
Applying both cutoff bounds in the unchanged raw-resource identity gives

    floor(W(J_low)/3 + A_(19,1)*L_1/2 + A_(19,2)*L_2/6)+near
       <=272127061148955779.

This is the exact existing thirteen-profile arithmetic. If a previously
paid pencil alternative occurs instead, combine its smaller whole-source
bound by MAXIMUM, not addition.

Finally, any excessive source supplies an excessive cutoff/path by
contraposition of the weighted descent. Small-normalization pencil-free
parents are paid by the preceding node, and small-normalization parents
with a nonconstant plane are paid here. Thus a remaining small-normalization
parent has only constant-direction pencil planes. The large-normalization
degree3..9 alternative and earlier actual jump follow from the preceding
degree ledger; a nonconstant3 pencil there is paid by the maximal case.
No actual pencil occupation or exhaustive source closure is inferred.
