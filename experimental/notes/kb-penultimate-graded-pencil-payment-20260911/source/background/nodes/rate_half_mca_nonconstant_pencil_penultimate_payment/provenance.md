# Provenance

This local extension combines the new quadric/pencil geometry with
previously proved original-source terminal prices. It continues the
#1179/#1180 source-bound program but imports no new unmerged PR theorem.
The geometry removes a missing terminal-cover hypothesis for the
printed source classes, not for every original source.

The published normalization packet already scopes local allowances
after paid whole-source alternatives. Three local normalization files
now spell out that antecedent, while the frozen fork copies remain
unchanged. See source_scope.md before the next grouped export.
