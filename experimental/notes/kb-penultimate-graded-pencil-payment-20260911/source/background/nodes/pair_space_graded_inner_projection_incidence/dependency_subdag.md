# Dependency Sub-DAG

Required: rational_curve_inner_projection_multiplicity_budget and
pair_space_regular_projection_anchor. The first prices one common genus
budget; the second bounds the regular bad set and retains actual owners.

The finite consumer provides every child count with its own complete
proof. It depends on this generic theorem, never conversely. No old
supplier, source router or terminal cover is required to depend back here.
