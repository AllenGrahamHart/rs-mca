# Dependency Sub-DAG

Required: pair_space_graded_inner_projection_incidence and
rate_half_mca_inherited_normalization_frontier. The former includes the
new multiplicity budget and original regular anchor. The latter includes
all original source capacities, branch energy, interpolation, full divisor
transport, rational-plane allowances and exact raw-resource suppliers.

The127-node required closure is PROVED and acyclic. This node owns the
thirty-seventh structural evidence edge to
rate_half_mca_global_core_rank_support_distance_router, which stays TARGET
with requires[]. Generic child counts are proved here, not new leaves.

No old supplier depends back on this refinement. The original threshold's
logical requirements and the grand root statuses are unchanged.
