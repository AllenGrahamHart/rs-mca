# Dependency Sub-DAG

Required, all PROVED:

- polynomial_carrier_inner_projection_degree_ledger: nu_0 ceiling and
  exact multiplication along the actual eight-anchor path.
- rational_curve_inner_projection_branch_budget: guarded finite
  exceptional-coordinate count, including singular branches.
- rate_half_mca_double_conic_terminal_payment: the preceding source
  frontier and its complete branch-energy/original-allowance suppliers.

The124-node required closure is acyclic. This node owns the thirty-sixth
structural evidence edge to the rank/support router, not a logical
requirement or conditional affordable-anchor premise. The router stays
TARGET with requires[]; no existing supplier depends back on this node.
