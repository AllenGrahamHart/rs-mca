# Validation And Limits

## Publication Hand Review

The publication pass revisited the full-divisor/branch distinction,
marked generic projections, one-blowup genus calculation, graded
positive-part sum, singular birational children, quadric inversion,
cone-vertex retention, maximal-pencil descent and original source owners.

The residual-source antecedent was made explicit in the amended supplier
and both consumers. No numerical price or certificate changed.
Independent external mathematical review remains due.

Audit verdict: **FIXED** (the standalone source-scope clarification).

## Frozen Replay

The wrapper verifies1508 source hashes across the packet chain,394
inherited proof documents, a129-node acyclic declared required closure,
and exactly three declared scope amendments. It rejects61 malformed
manifests, including changes to scope, dependencies, hashes and amendments.

Six new focused checks cover:

- 48 exact tensor-grid identities for the price polynomial,960 price
controls and two F97 multiplicity examples.
- 84 graded-ledger controls, including G=0 and N>=A.
- 26 original profile prices and1208 child certificates in two independently
written arithmetic implementations.
- 55 rehashed corruptions, plus a fully recomputed ceiling5477 whose price
fails with gap-1717026; an off-eigen-mask F5 plane control.
- 36 regular maximal-pencil contractions, a singular curve on a smooth
quadric, a cone-vertex degree jump retaining the plane, and constant/single-
direction scope controls.
- 208 exact prefix-factor identities and all13 original whole-source
resource values, retaining higher raw and near once.

Five inherited focused checks replay the normalization primary/independent
arithmetic, the rational-plane allowance audit, the actual degree ledger
and the branch-budget controls. They reject49 normalization and24 allowance
mutations, plus a fully recomputed oversized normalization ceiling.
Other inherited arithmetic is not freshly replayed in full.

Normal Python:4.53seconds,35016KiB measured peak RSS.
Optimized Python:5.52seconds,35216KiB measured peak RSS.
RAMguard tiny limited address space to256MiB and wall time to60seconds.
No Modal, spending, large computation or live source dependency.

The first packaging replay exposed one omitted inherited certificate in
the temporary runtime. It was added to the pinned runtime inventory;
both complete replays then passed. This was a packaging failure, not a
mathematical correction.

Algebraic controls are NOT official large-agreement MCA witnesses.
Hash/schema checks do NOT prove the universal mathematics. The closure
statuses refer to complete local written proofs at their printed scopes,
not external acceptance or a proof assistant's verification.

## DAG And Custody

The local generated DAG has2847nodes8531edges and38 structural evidence
roots. Manifest compilation and the affected-subgraph checks pass.
The original router remains TARGET with no logical requirements; grand
roots remain CONDITIONAL.

The full legacy DAG validator again reports260 errors:124 partition and
136 root-reachability errors, with none naming the five new nodes.
No clean global-audit claim is made. This packet exports only the
declared proved dependency closure, not the dirty research tree wholesale.
The origin HEAD identifies provenance only; per-file hashes identify the
actual dirty snapshot. Canonical prize is read-only.

There is no whole J, unrestricted rank19, endpoint, active-v4 atom or
Prize closure, and no change to any upstream proof status.
