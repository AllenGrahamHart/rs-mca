# Source-Bound Integration And Review

Workboard item: K3 / DIRECT, KoalaBear MCA at agreement1116048 and
target2^-128. The normalized original error-rank12 source has
`(n,K,m)=(1048576+J,J,67472+J)`, carrier dimension11, and empty
universal carrier core. Quantifiers and full-code-badness are inherited
from the declared original source, not recreated after restriction.

## Credit And Increment

Scott Hughes's [#1179](https://github.com/przchojecki/rs-mca/pull/1179)
at `c4e4e7918764a590c4b8cfeac11004d297e2480b` and
[#1180](https://github.com/przchojecki/rs-mca/pull/1180)
at `478bfc41278c7669543388107a2fabff4b9fbd1b` supply the
weighted-anchor, shared-carrier exterior-product and compression methods.
They are credited explicitly in the frozen provenance files. Their
unmerged review badges are not treated as proved local dependencies:
the six nodes reconstruct the needed arguments using the printed DAG.

#1180 leaves two unpaid actual-span primitives: full-span-five compression
and a span-two rank-one sparse descendant. The coupled-frontier node pays
the sparse branch using our stronger all-original minimum-envelope
resource and height-resolved P43 gates. The final integer-compression
node pays the remaining full-span-five branch. Together these give an
entire-source payment of the intrinsic `(shared,pair)=(11,18)` class over
the full declared J interval, allowing arbitrary mixtures of terminal types.

No marked-Mobius incidence conjecture is asserted. This is another
source-bound way to pay that class, not a proof that #1180's coarser
upper bounds were wrong. The common-defect-anchor shortcut is NOT used.

## Dependency Boundaries

The six new roots are the four coupled-frontier nodes followed by the
integer counting lemma and its finite consumer. All 90 nodes in their
required union are locally PROVED; no open critical router is a required
supplier. The exact adjacency list is `requirements` in the manifest.

The inherited resource, original-label basis, shared-carrier anchor and
padded LIST suppliers remain in pinned sibling packets. The manifest
maps every inherited proof document to its exact packet, path and SHA256.
The exporter compared all262 inherited mathematical documents against
the research tree. The runtime is assembled from those pinned packets,
not a mutable local supplier directory. Logical DAG paths in source
documents are resolved through that manifest, not treated as network URLs.

The local critical router receives evidence from the finite results.
It remains TARGET and neither grand Prize root is promoted. These are
source-class payments, not additive charges for a Grande Finale partition.
An active-ledger use still needs its own exhaustive ownership bridge.

## Load-Bearing Review Points

1. Raw weights belong to ORIGINAL labels. Good anchors lie in complete
   cores and need not lie in the originally selected support. Restrict
   actual pairs and divide ONLY pair differences by the anchor locator.
2. Weighted pencil ownership bounds the nonpreferred mass per actual
   pair. The finite preferred slope is charged once per pencil; after
   quotient-fibre decomposition its direction is common, so it is charged
   once for the entire compression terminal, not once per fibre.
3. The Pluecker three-space alternative is excluded only for shared
   dimension at least three, with the printed root-count and rank guards.
   Rank18 uses shared dimension five; this packet does not extend the
   lemma blindly to later low-dimensional terminals.
4. Use the original annihilator polynomial `H_a*g`, not just `g`, for
   intersections of different complete-core unions. Its degree is at
   most J-1, yielding one shared shifted mass1048577. Fibre scalar
   dimension is the ACTUAL represented affine span, at most five.
5. The sparse recurrence retains the rank-two and zero-dimensional exits,
   both rational-pencil heights, and the exceptional constant children.
   There are six shared anchors, not an unproved eight-anchor denominator.
6. At most TWO integer masses exceed349525. On each full mass box, the
   small ratio and the large affine excess decrease in x. The LIST cap
   uses actual degree maxima12958/14158 after the six anchors. Nothing
   licenses replacing dimension five by four.
7. Prefix compression is dominated by the old sparse NUMERICAL cap;
   its geometry is not relabelled sparse. Middle terminal dominance is
   in the other direction. All J ranges and both original cutoffs are
   covered before the original source identity and one final floor.
8. Combine alternative entire-source branches by maximum. Include
   all higher original raw values and near134944 exactly once. This does
   not prove generic-full P2 or any unlisted source case.

## Current Upstream And Custody

Upstream main was checked at93fba1be3f3299b0ba4708d88715377bbb656e45;
#1175/#1179/#1180 remain open at their pinned heads. The live agents
workboard, newest main agents-log entry and four-row completion note were
read. They still report no completed adjacent safe row. No wide PR audit
or fresh upstream verifier run is claimed by this export.

The packet is an incremental companion after457bbc7361599dfa6ee0926b11e953b8cc356029.
Stable papers, earlier packets, canonical prize and Scott Hughes's branches
are untouched. A future integration should review the final payment and
its six-node closure as one contribution rather than bank the earlier
coarse rank18 boundary as the latest conclusion.

## Compute Requests

None. These certificates need no field enumeration or large numerical run.
