# Two Large Integer Core Masses Replace A Fractional Fibre Average

For each compression fibre let x=|C|-J+1, S=1048577 and
w_t(x)=(S-x)*L_t(x), where L_t bounds the ACTUAL five-dimensional
scalar LIST after six anchors. Its original nonpreferred raw weight is
at most w_t(x). All fibres share at most one preferred finite slope.

Put T=349525. Since every x is an integer and sum x<=S,
three fibres with x>=T+1 would require at least1048578>S.
Thus there are at most TWO large fibres. This is an unconditional
consequence of the proved actual-core packing, not a fitted census.

For degree range[J0,J1], set Kmax=J1-6. For t=1,2 the scalar LIST
instance has (r,w,Kmax,dimension)=(x-1,67472-t,J1-6,5).
It covers all J in that interval over the unchanged source field.
Use the two ranges[9965,12964] and[12965,14164].

Partition the SMALL mass interval[67473-t,349525] and LARGE interval
[349526,481076] into integer boxes[ell,u] of width10000. There are
29 small and14 large boxes per range/cutoff case, four cases in total.
A valid LIST cap L_t(u) also covers every x in the box by auxiliary
domain padding. The actual scalar degree is at most the declared Kmax.

Define, with all arithmetic exact,

    alpha=max_small ((S-ell)*L_t(u)/ell),
    beta=max(0,max_large ((S-ell)*L_t(u)-alpha*ell)).

The small ratio bound is valid because (S-x)/x decreases. The large
bound holds because (S-x)*L_t(u)-alpha*x decreases with x, with
alpha>=0. Therefore w_t(x)<=alpha*x on every small box and
w_t(x)<=alpha*x+beta on every large box. The integer-envelope supplier
gives the terminal cap

    T_int(t)=t+S*alpha+2*beta.

The SAME mass budget S is used once. The affine excess beta is charged
only to the at-most-two large fibres. No fictional fractional collection
of maximally expensive fibres is substituted for the actual integer one.

The four exact terminal floors are

    prefix t1:562398541, t2:563536655;
    middle t1:11369148900, t2:11370307018.

Only for display are these floored; all comparisons and the final source
payment retain the unrounded rational caps. There are172 mass boxes and
860 legal LIST transitions. No constant-pencil gate is enlarged, and no
hypothesis about actual fibre span beyond the proved maximum5 is added.
