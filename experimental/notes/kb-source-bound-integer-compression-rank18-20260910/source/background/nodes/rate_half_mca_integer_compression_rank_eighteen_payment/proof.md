# Keep The Degree Range And The Integer Number Of Large Fibres

Use the required coupled-frontier source frame without changing any original
field, pair, support, label, raw value or receiver. Rank<=17 and every
large-P43-pencil alternative already pay by270000000000000000. Assume
actual P2 rank18 and no such pencil. At six guarded shared-carrier anchors,
every populated terminal has pair/shared dimensions6/5 after dividing
ONLY pair differences by their anchor locator.

The required Pluecker classification is exhaustive: sparse or compression.
The sparse raw-weight cap T_sp(t,J) for t=1,2 retains the rank4,rank2
and zero-dimensional exits. It is increasing in J, with the exact formula
in the required source_recursion.md. Weighted descent multiplies a uniform
terminal cap by

    A_t=prod_(c=2)^7 (1048576+c)/(67472-t+c).

For compression, actual shifted fibre masses are integers x_c in
[67473-t,481076] and sum x_c<=1048577=S. The upper endpoint comes from
the unchanged full constant-P43-union gate. Their actual scalar span is
at most5, not assumed at most4. The
[integer-envelope calculation](integer_envelopes.md) proves caps T_int(t)
on two degree ranges, with the common preferred finite label charged once.

## Prefix J9965..12964

Here scalar degrees are at most12958 after the six anchors. The exact
certificate gives

    T_int(t)<=T_sp(t,9965)<=T_sp(t,J), t=1,2.

Thus BOTH geometric branches satisfy the same numerical terminal cap used
in the earlier sparse calculation. Repeating its weighted descent and
original source identity gives the previously certified prefix maximum
274462040894062110. This transfers a NUMERICAL CAP, not the false premise
that a compression terminal is sparse or has smaller scalar span.

## Middle J12965..14164

Here scalar degrees are at most14158. The integer envelope now dominates
every sparse cap on this interval:

    T_int(t)>=T_sp(t,14164)>=T_sp(t,J), t=1,2.

Consequently M_t=sum_(raw<=t) raw<=A_t*T_int(t) for the SAME original
assignment. The proved original resource decreases with J and gives
sum min(raw,3)<=sum min(raw,9)<=491840933524792929 at12965.
The original-label identity is

    1=min(raw,3)/3+(raw*1_(raw<=1))/2+(raw*1_(raw<=2))/6.

Substitution, one final rational floor and near134944 give
270702759681887570 for the entire middle interval. Every higher raw
record remains priced; the two low-weight bounds are not independent
tuple budgets.

## Upper Interval And Whole-Source Maximum

The required frontier already pays ALL rank<=18 on J14165..21499 by
273715780528023745. Combining the three exhaustive J intervals with the
rank<=17/large-pencil alternatives by MAXIMUM gives274462040894062110.
There is no degree gap or unpriced span5 compression case for pair rank18.

The resulting P2 rank>=19 obstruction holds for every over-budget
assignment, leaving actual pair ranks19..22. A generic-projection corollary
requires its separate supplier; this payment does not establish generic
fullness of P2. Higher pair and original error ranks remain open; this
is not a full Prize resolution.
