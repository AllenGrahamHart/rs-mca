# Price Compression Using One Shared Shifted-Core Mass

At pair rank q in{17,18}, let a=q-12 and s=23-q. After the first
a guarded good anchors, the compression direction is

    W=(delta tensor U) direct_sum F z,
    dim U=s, U subset F[X]_<J-a.

Apply the compression-fibre supplier to the ACTUAL P_t pairs in this
terminal. Its quotient fibres have complete-core unions C_c. Set
x_c=|C_c|-J+1. The original annihilator polynomial H_a*rho(z) is
nonzero of degree<=J-1, so distinct fibre unions intersect only in one
common root set. Consequently

    d+1-t<=x_c, sum_c x_c<=R+1=S.

Each fibre lies in a constant original pencil. Outside the paid
constant-P43 alternative its full union complement is>=567501, so the
fibre's own union also obeys x_c<=481076. This conclusion is hereditary
for terminal subfamilies; their unions need not equal the full union.

After dividing only pair differences and deleting the a anchors, each
fibre injects into the scalar LIST with

    (length,degree,agreement)=(x_c+J-1-a,J-a,d+J-t-a),
    (r,w,Kmax,dimension)=(x_c-1,d-t,E-a,v).

Here v=s for unrestricted compression, or v=4 for a PROVED actual
span<=4 rank18 fibre. That latter case still uses a=6, not a=7.
All actual P1 fibres are contained in their corresponding P2 fibres.
The arbitrary receiver is defined only off the anchors; it is not
asserted divisible by H_a.

For each(q,v,t)=(17,6,1/2),(18,5,1/2),(18,4,1/2), partition
[d+1-t,481076] into42 boxes [ell,u] of width10000. By padding the
auxiliary domain, the legal chosen-step LIST cap L(u) at r=u-1 also
bounds every x in that box. Since (S-x)/x decreases and is nonnegative,

    (S-x)*L(x)/x <= (S-ell)*L(u)/ell.

There are252 boxes and1260 exact dimension transitions. Their largest
ratio F_(q,v,t), multiplied by the SINGLE shared mass S, gives terminal

    T_comp(q,v,t)=t+S*F_(q,v,t).

The additive t is ONE preferred finite slope common to all fibres.
Nonpreferred original weights are at most (S-x_c) per pair. Summing
with the above ratios and sum x_c<=S proves the terminal bound without
independent maxima for fibre number and fibre size. No unproved
anchor-history denominator or owner-reassignment assertion is used.
