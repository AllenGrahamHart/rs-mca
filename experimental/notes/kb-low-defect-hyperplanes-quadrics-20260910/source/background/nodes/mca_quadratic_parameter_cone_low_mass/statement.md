# A Two-Label Tuple Resource For A Split Parameter Cone

Status: PROVED. External independent mathematical review remains due.

Fix n distinct field points, integers3<=s<=K,1<=T<=d,m=K+d<=n,
a degree-<K polynomial space V of dimension s, and fixed degree-<K
offsets a0,b0. Choose independent linear functionals ell1,ell2 on V.

For each DISTINCT finite label gamma choose a pair(a_gamma,b_gamma)
in(a0+V)x(b0+V) and a size-m scalar agreement support. Let tau_gamma
be its chosen second-component mismatch count, with1<=tau_gamma<=T.
Require evaluation on V to be nonzero on every selected joint core.
Write a'=a_gamma-a0,b'=b_gamma-b0 and assume

    ell1(a')=0, ell2(a')=ell1(b'), ell2(b')=0.        (CHAIN)

These conditions say that the ENTIRE affine parameter line
(z,a'+z*b') lies on the split quadric ell1(h)=z*ell2(h).
A single point on that quadric is not by itself(CHAIN).

There is an exceptional set of at most K-s+2 original labels such that

    sum_(nonexceptional gamma) tau_gamma
       <=floor(2*(n)_falling_s /
          (s*(m-T)*product_(j=1)^(s-2)(d-T+j))).     (TWO)

Consequently the full low-family deficit obeys

    sum_gamma(T+1-tau_gamma)
       <=T*(floor(2*(n)_s /
          (s*(m-T)*product_(j=1)^(s-2)(d-T+j)))+K-s+2).

No source coordinate or field is changed; the moving carriers are NOT
individually normalized or assigned separate tuple budgets. Each common
ordered s-tuple can be owned by at most TWO labels, not necessarily one.
Minimization is unnecessary for this generic statement. Full-source HIGH
funding, noncontained pair lines and near accounting belong to a consumer.
