# Audit

Hand checks: correct original-label projection; ell=0 vertical exception;
both affine offsets in a nonvertical hyperplane; one label per incompatible
represented pair; simultaneous compatible-family tuple count; auxiliary
core cancellation without assuming every support contains that core;
all eleven guarded pair/shared drops; positive falling-factorial ratio;
unrefunded integer W3; and integral exact outside-label threshold.

Primary and independent checks reconstruct both integer caps, monotonicity,
all guarded anchors, whole-source payments and adjacent exception counts.
Small exhaustive field controls check the hyperplane pair partition and
integer deficit identity. These arithmetic checks do not replace the
universal hand proof or external independent mathematical review.

The new s<=10 payment is unconditional on pair rank. The older stronger
s<=9 payment remains credited. Original higher ranks and the full graph
case remain open; no complete J or critical TARGET is promoted.

NO ISSUE
