# Contract

Original rank-twelve source J9965..21499, fixed field, complete original
core, dimension11 V, distinct labels and size-m full-code-bad supports.
Every valid original minimizing-pair assignment is allowed. The low
parameter graph is ACTUAL (gamma,h_gamma), not a coefficient variety.

One fixed proper hyperplane at a time. Its compatible pairs share the
dimension10 kernel with both affine offsets retained; incompatible pairs
own at most one inside label EACH. Auxiliary core cancellation only bounds
the compatible low family's weighted mass. The all-record W3 resource
is used only on the original unchanged source. Near is included once.

No original source gauge/rank is remeasured, no active-owner atoms are
summed, and no higher-rank or full-row coverage is claimed.
