# Low-Defect Labels Must Span The Full Parameter Space Quantitatively

Status: PROVED. External independent mathematical review remains due.

Original KoalaBear row:q=2130706433^6,n0=2097152,K0=1048576,
m0=1116048,B*=274980728111395087,near=134944. For EVERY complete
post-near original error-rank-twelve selection, normalize only its COMPLETE
original shared scalar core. On9965<=J<=21499 the unchanged source is

    (n,K,m)=(R+J,J,d+J), R=1048576,d=67472, dim V=11,
    h_gamma in h_*+V, |Z_bad|<=|Gamma|+near.

Keep any valid original size-m supports and minimizing pairs
b_gamma in V,a_gamma=h_gamma-gamma*b_gamma in h_*+V.
Write tau for their original raw defects, and
G_2={(gamma,h_gamma):tau<=2} in F x(h_*+V). All tau>=1.

For EVERY proper affine parameter hyperplane L,

    sum_(tau<=2, (gamma,h_gamma) in L)(3-tau)
        <=156627788100273718.                       (DEFICIT)

If e counts the original raw<=2 labels OUTSIDE L, then

    |Z_bad|<=floor((769649915544853625+2e)/3)+134944. (PRICE)

Thus affine dim G_2<=11 pays the ENTIRE original source by
256549971848419485, reserve18430756262975602. More generally any
proper hyperplane missing at most27646134394463403 low labels pays.
All higher-defect labels remain funded in the SAME original resource.

Consequently EVERY over-budget original source, under EVERY valid
assignment, has at least27646134394463404 raw<=2 labels outside EACH
proper parameter hyperplane. In particular G_2 has FULL affine dimension12.

There is also the sharper source-class payment

    intrinsic shared low-pair dimension s<=10
      => |Z_bad|<=256541462574529459,                (CARRIER)

with no restriction on pair affine dimension. Here s is the dimension of
span{a-a0,b-b0} for the represented low pairs, not the original error rank.
Combined with the required rank-fifteen theorem, an over-budget source
must have s=11 and pair affine dimension16..22. The former s=10/r18..20
branch is fully paid. No complete J value, original error rank>=13,
unrestricted row, active-v4 atom, endpoint or Prize problem is closed.
