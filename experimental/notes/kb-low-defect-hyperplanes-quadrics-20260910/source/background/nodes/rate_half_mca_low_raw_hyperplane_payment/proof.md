# Isolate Compatible Pairs In Each Hyperplane, Then Count Their Labels

Use exactly the original source and assignment from the required pencil
and rank-fifteen chain. It supplies the UNREFUNDED integer resource

    sum_Gamma min(tau,3)<=W3=613022127444579907.       (ALL)

Every original raw value is funded. No source deletion or rank/core
remeasurement will be used in(ALL).

## 1. Any Ten-Dimensional Affine Low-Pair Carrier Has Small Raw Mass

Suppose a subset of low labels has pairs in(a0+U)x(b0+U), where
U subset V has dimension10. Apply the required weighted low-family
theorem with s=10,T=2. It preserves original labels and defect counts,
even if a0 or b0 is outside U and the auxiliary universal core is nonempty.
Its core size z is at most J-10. With k=J-z,10<=k<=21499,

    sum_subset tau <= floor F(k),
    F(k)=(R+k)_falling_11 /
         (11*(d+k-2)*product_(j=1)^9(d-2+j)).        (LOW)

This is ONLY an auxiliary weighted count. It does not apply(ALL) or
the original rank-eleven beta resource to a remeasured source.

For consecutive integer k,

    F(k+1)/F(k)=(R+k+1)/(R+k-10)*(d+k-2)/(d+k-1).

This is at most1 if11*(d+k-2)<=R+k-10. The latter difference
is linear decreasing in k and still91406 at k=21499. Hence the
maximum is F(10), whose exact floor is

    M_low=78301130139301820.                         (MLOW)

For low labels3-tau<=2*tau. Thus the subset's integral deficit
sum(3-tau) is at most2*M_low. No minimizer of an auxiliary source is
introduced; the selected original b realizes its original defects.

If the entire intrinsic low-pair carrier has dimension<=10, enlarge
only that direction space to dimension10 INSIDE the original V. Choose
a0,b0 from one represented pair; both offsets are retained. All low
pairs then meet(LOW), irrespective of their affine pair rank. Empty
P_2 is paid by(ALL) alone. Since

    3*|Gamma|=sum min(tau,3)+sum_(tau<=2)(3-tau),

the whole source is paid by floor((W3+2*M_low)/3)+near
=256541462574529459. This proves(CARRIER), without a pencil assumption.

## 2. There Are Few Exceptional Pairs Per Parameter Hyperplane

First bound ALL represented low pairs, each with complete joint core
size>=m-2. Enclose them in(a0,b0)+(V x V), of pair/shared ranks22/11.
Apply the required shared-carrier anchor successively at22/11,20/10,
...,2/1. Every stage has pair rank strictly greater than shared rank;
after eleven good anchors the affine child has dimension zero. Therefore

    |P_2|<=floor product_(j=1)^11 (R+j)/(d-2+j)
          =12763910835039=:M_pair.                 (PAIR)

Common zeros created at earlier anchors remain included in the proved
bad-coordinate bound J-s. Degree, domain and original agreements do not
change. No assumption that actual pairs span every auxiliary child is used.

Write a proper parameter hyperplane in original coordinates as

    ell(h-h_*)+c*gamma=e0,

where ell is a linear functional on V and(ell,c) is not zero.
If ell=0, the hyperplane contains at most one distinct gamma; its low
deficit is at most2, already below(DEFICIT). Suppose ell is nonzero.

Choose a0 in h_*+V,b0 in V such that ell(a0-h_*)=e0 and
ell(b0)=-c. Put U=ker ell subset V, of dimension10. A low pair
f=(a,b) owning a label in this hyperplane must satisfy

    [ell(a-h_*)-e0]+gamma*[ell(b)+c]=0.             (LABEL)

If both brackets vanish, f belongs to(a0+U)x(b0+U). All labels
assigned to these compatible pairs INSIDE the hyperplane form ONE
family meeting section1, so their total deficit is at most2*M_low.
Otherwise(LABEL) has at most one finite solution gamma. There are
at most M_pair such represented pairs by(PAIR), so their assigned
distinct labels have total deficit at most2*M_pair. Original ownership
selects one pair per label; no pair-to-label multiplicity is discarded.

It follows, uniformly for every hyperplane, that

    sum_(low labels inside L)(3-tau)
        <=2*(M_low+M_pair)=156627788100273718.

This proves(DEFICIT). The pair count pays only exceptional labels,
not one full source per pair or a sum over hyperplanes.

## 3. The Same Original Resource Pays Everything Else

Let e be the number of low labels outside this ONE fixed hyperplane.
Their deficit is at most2e. Combining with(ALL) and the exact identity
in section1 gives

    3*|Gamma|<=W3+2*(M_low+M_pair)+2e
              =769649915544853625+2e.

Adding original near ONCE proves(PRICE). At e=0 the exact total is
256549971848419485. For e<=27646134394463403 it is still<=B*.
If the original source is over budget, |Gamma|>=B*-near+1; reversing
the last inequality yields

    e>=ceil((3*(B*-near+1)-W3-2*(M_low+M_pair))/2)
      =27646134394463404.

This holds for every proper hyperplane and every original valid assignment,
proving quantitative FULL affine rank12 of the actual low parameter graph.

Finally, if the intrinsic low-pair carrier has dimension s<=10 then its
graph lies in{(gamma,a0+gamma*b0+w):w in U}, of dimension at most s+1.
This also shows why the older s<=9 graph payment was already available;
the new proof pays the formerly open proper dimension11 graph case.
Thus an over-budget source has s=11, and the required rank-fifteen
theorem gives pair rank>=16, while r<=2s=22. This is a necessary
restriction, not a payment of the remaining full-dimensional graph.
