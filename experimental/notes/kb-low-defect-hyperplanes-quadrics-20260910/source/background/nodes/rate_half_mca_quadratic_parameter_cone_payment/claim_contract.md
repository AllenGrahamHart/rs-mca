# Contract

Original rank-twelve normalization, field, labels, supports, near and
dimension11 carrier are fixed. Cone normals ell1,ell2 are independent
F-linear coefficient functionals, with a0 in h_*+V,b0 in V.
The cone concerns(gamma,h_gamma), not a curve in(a,b) over F(X).

Each noncontained pair line owns at most TWO inside labels. Each
contained line satisfies the exact three-coefficient chain. The moving
carriers are not separately normalized; at most21490 labels are priced
for their zero evaluations. W0 or W1 funds every original raw value.

The raw<=2 conclusion is only on J>=14000; do not transplant it into
the lower interval or subtract W0/W1 upper bounds. Outside-cone counts
refer to one cone at a time. No source-class bounds are summed as atoms.
