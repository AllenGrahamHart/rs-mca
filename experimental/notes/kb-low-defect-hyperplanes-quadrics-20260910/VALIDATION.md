# Validation And Review Limits

2026-09-10. Four completed source-class results for independent review;
not a deployed atom, exhaustive source classification or row certificate.

## Frozen Custody

Parent:04ee9b6c5276828bc974634dc8b1a1f745d7e545.
Manifest SHA256:
`e36c4d33ba2357fe313a3788d6f46ea5da7b1b1d2438b796febd914d611e72d3`.

The exporter checked 899 listed hashes in seven earlier supplier packets
and compared 269 inherited mathematical documents with the research tree.
All relevant inherited proofs and required edges matched the latest pinned
snapshots. Earlier interval-assembly snapshots are superseded by their
later published versions, not treated as simultaneous current graphs.
The 34 new files total 62988 bytes. Their required union contains 92
acyclic PROVED nodes; each of the four new nodes has a complete proof.

The research tree was dirty at3b51e86d2595f28a02e842c81effca1dfcf98e77:
the file hashes, not that HEAD alone, identify the contributed mathematics.
Earlier published files, the PR author's branch, canonical prize and stable
papers are unchanged. The source manifest records local theorem status;
neither its hashes nor its dependency checks constitute proof verification.

## Bounded Replays

Python3.10 or later, standard library only, no network or research-tree
imports. Run serially from this companion checkout:

```bash
python3 -B experimental/notes/kb-low-defect-hyperplanes-quadrics-20260910/replay.py
python3 -B -O experimental/notes/kb-low-defect-hyperplanes-quadrics-20260910/replay.py
```

Both pass. The wrapper validates 933 listed source hashes, the 269
inherited mathematical documents, the 92-node graph and 23 malformed
manifests. It reconstructs relative imports from pinned source bytes in
an automatically removed temporary directory. Each of eight serial child
checks has a 20-second timeout. Use an outer memory/time limit on a
constrained machine; our replays used RAMguard tiny (256MiB/60 seconds).

The six new checks cover:

- Eight actual affine F11 selections, offsets outside the auxiliary
  carrier, twelve supports omitting its universal core, 216 owned
  independent tuples and both original defect multiplicities.
- Exact proper-carrier and hyperplane prices, 625 F5 pair/hyperplane
  controls, 625 deficit identities, independent integer-only reconstruction
  and the sharp adjacent sufficient exception-count envelope.
- Two retained F11 cone records with actual full polynomial minima,
  132 owned tuples but 126 distinct tuples, 21 checked two-root sections,
  a genuine two-owner tuple and a moving-zero exceptional label.
- Both quadratic prices, guarded pair-count products, interval-wide
  decreasing-ratio gates, 160 small-field quadratic restrictions, and an
  independent integer-only implementation of the finite arithmetic.

Two inherited checks also pass: the low-raw filtration's eight rational
certificates and original HIGH gate, plus the raw-margin multi-defect,
flag, owner and HIGH-funding controls. The new hyperplane check separately
reconstructs its inherited unrefunded W3 resource. Other inherited
arithmetic, including older rank-fifteen certificates, was not freshly
rerun during this publication; all its listed bytes were hash-checked.

Normal replay:0.96 seconds,22808 KiB peak RSS.
Optimized replay:1.20 seconds,24128 KiB peak RSS.
No Modal, large computation, compute spending or compute request.

## Publication Hand Review

Reread all four proofs. Checked that auxiliary cancellation preserves every
original defect even if a support omits some universal-core points, and
that no original all-record resource is reapplied after that cancellation.
Both fixed offsets are retained. Every exceptional pair is converted to
an explicitly bounded number of original labels before composition.

For the cone, checked the coefficient identities, independent moving
carrier, root-product bound on moving zeros, uniqueness of an exceptional
label at each such coordinate, and invertibility of the defect-inserted
Jacobian. An ambient incidence line cannot be contained in the cone:
its tangent direction would contradict that invertibility. Thus two
owners, rather than one, are the correct tuple allowance in all
characteristics. The explicit two-owner fixture tests this distinction.

Checked the original all-record deficit identity, field and normalization,
finite interval restrictions, integer floors and adding near only once.
Membership of one parameter point in a cone is not mistaken for containment
of its pair line: noncontained lines are separately paid. Full affine span
of a cone is not asserted to prove exhaustive coverage by such cones.

The local generated-DAG manifest check passes (2803 nodes,8401 edges).
The focused critical audit retains 21 threshold requirements with 116
PROVED ancestors and 14 structural evidence suppliers; the global router
stays TARGET and the grand roots remain CONDITIONAL. No clean global
software/crosswalk audit or external independent proof review is claimed.

NO ISSUE
