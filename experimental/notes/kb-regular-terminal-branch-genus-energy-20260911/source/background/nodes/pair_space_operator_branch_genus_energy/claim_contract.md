# Claim Contract

Object: actual polynomial operator pairs with shared dimension3, primitive
gcd1, actual degree D, at most one-dimensional original-field eigenspaces,
and the required original-field eigen-root mask. Pair ownership is inherited
from pair_space_operator_collision_interpolation.

The image degree is eta; the normalization-map degree is nu=D/eta.
The finite fibre bound is nu times the GEOMETRIC BRANCH count b_p, not nu
alone. The branch budget follows from planarity and the normalization
quotient; no conjectural singularity classification is required.

Z>0 is mandatory before squaring STRICT PRICE. The n parameter is an upper
bound for the remaining domain size; B is an upper bound for removed actual
core incidence. All numerical quantities are ordinary nonnegative real or
integer counts, even when the coefficient field has positive characteristic.

No code-rate change, reassignment, new source field, source census, row
optimality, whole-rank payment or Prize closure is asserted.
