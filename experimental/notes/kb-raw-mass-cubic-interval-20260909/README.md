# KoalaBear MCA: A Complete Raw-Mass Cubic Interval

```yaml
workboard_item: K3
row: KoalaBear, q=2130706433^6, n0=2097152, k0=1048576
object: MCA
target_epsilon: 2^-128
agreement: 1116048
B_star: 274980728111395087
architecture: DIRECT
atom_or_cell: original rank-twelve selections with J=k0-g in 9941..9964
quantifier: every complete post-near selection in the stated source class
projection_and_unit: distinct original finite bad slopes per received line
direct_statement: "|Gamma|+134944 <= 274861473951141154"
claimed_bound: 274861473951141154
status: PROVED locally; independent mathematical review remains due
impact: LOCAL_ONLY, 24 complete original core sizes removed from the residual
falsifier: a source satisfying the printed contract exceeding the claimed cap
replay: replay.py, SOURCE_MANIFEST.json and VALIDATION.md in this directory
```

Companion to #1175, following the
[low-defect rank-filtration contribution](../kb-low-defect-rank-filtration-20260909/README.md).
The earlier packet explicitly excluded the unfinished cubic experiment.
This packet supplies its completed hand proof, exhaustive coverage and
original-source transport. The earlier immutable packet is not rewritten.

## The New Theorem

For **every** normalized source on `9941<=J<=9964`,

```text
(n,K,m) = (1048576+J, J, 67472+J),
|Gamma| + 134944 <= 274861473951141154,
reserve = 119254160253933.
```

The actual polynomial carrier has dimension eleven and empty universal
carrier core. The child is obtained by gauging and cancelling only the
complete shared scalar core of a complete original rank-twelve selection.
The field, finite labels, full-code-bad saturated supports and nonuniversal
carrier zeros remain. Reselection occurs within that fixed carrier, without
remeasuring original rank or shared core. The original near cost is included
once. There is **no chosen-curve or fiber-density premise**.

The complete original core sizes `1038612<=g<=1038635` are therefore paid.
The assembled rank-twelve residual becomes

```text
J = 9965..21499, equivalently g = 1027077..1038611,
11,535 integers, down from 11,559.
```

The lower union `4801..9964` retains its larger existing cap
`274979661975561635`; the high union `21500..169999` is unchanged.
These are alternative whole-line estimates combined by maximum, not
charges summed over possible core sizes. Original ranks at least thirteen
and the remaining rank-twelve source classes still require a proof.

## What Changes Mathematically

**A reusable high-defect interface.** The generic raw-mass theorem can use
any separately proved high-record tuple bound on the same resource. Eight
fixed rational quadratic certificates, with core cutoff `500`, establish

```text
sum_gamma min(raw_gamma,51) <= 646273487661620022.
```

The previously proved `5500*m*P_d` high-record count funds this application;
the coarser `501*m*P_d` bound does not. Core cutoff `500`, mass truncation
`51` and curve cutoff `50` serve different roles. All raw values are retained.

**Exhaustive curve coverage and payment.** The full double-point kernel has
gcd of pair degree at most three, with at most `256` off-gcd pairs retained.
Its exact dimension gap is `3627124-364J`, positive throughout the interval.
Every irreducible cubic type, coefficient component, singular solution,
reducible line/conic pattern and exceptional pair is priced. The binding
branch uses all fifty nested defect sums, with explicit same-pair ownership
weights. The large-line branch retains its coupled resource and does not
add the generic base a second time.

At `J=9965` this fixed-cutoff gap is `-136`. That is a limit of this
certificate, not a counterexample to the MCA inequality. This packet makes
no higher-degree coverage or complete-pair census claim.

## Review And Integration

1. [Generic raw-mass statement](source/critical/nodes/mca_raw_margin_basis_mass/statement.md)
   and [proof](source/critical/nodes/mca_raw_margin_basis_mass/proof.md).
2. [Finite theorem](source/critical/nodes/rate_half_mca_raw_mass_cubic_interval/statement.md),
   [basis-mass proof](source/critical/nodes/rate_half_mca_raw_mass_cubic_interval/basis_mass.md),
   [cubic prices](source/critical/nodes/rate_half_mca_raw_mass_cubic_interval/cubic_payment.md)
   and [full-kernel/factor proof](source/critical/nodes/rate_half_mca_raw_mass_cubic_interval/proof.md).
3. [Original-source assembly](source/critical/nodes/rate_half_mca_rank_twelve_paid_interval_assembly/proof.md),
   especially section 18; sections 2--4 retain the normalization contract.
4. [Source manifest](SOURCE_MANIFEST.json) and [validation record](VALIDATION.md).

The 74-node dependency closure extends the published 72-node assembly by
the raw-mass supplier and finite cubic consumer. The low-defect filtration
and graph-spread nodes are not premises, so there is no backward dependency
through the assembly. The raw-mass node's ID is unchanged by its local move
from `background` to `critical`. Frozen earlier paths remain historical.

This is a **K3 / DIRECT source-class theorem**, not an active Grande Finale
v4 `U_paid`, `U_Q` or `U_BC` atom. It supplies no first-match owner bridge,
unrestricted endpoint, ordinary LIST theorem or full Prize resolution.
Credit Hughes's #1174/#1175 rank and source framework and the explicitly
pinned suppliers; the new contribution is the stronger raw accounting,
completed interval payment and its existing-bridge specialization.

## Bounded Replay

From the repository root, with Python 3.10+:

```bash
python3 -B experimental/notes/kb-raw-mass-cubic-interval-20260909/replay.py
python3 -B -O experimental/notes/kb-raw-mass-cubic-interval-20260909/replay.py
```

Paths are resolved independently of the working directory. Four focused
checks run serially with 20-second child timeouts. The independent finite
checker imports no primary implementation. Hashes, arithmetic and mutation
controls do not certify the universal hand proofs or replace independent
mathematical review. Inherited arithmetic suites are not freshly replayed.

## Compute Requests

None for this contribution. No field enumeration, Modal spend or large
computation is needed to review the exact certificate.
