# Claim Contract

- Object: selected MCA bad supports, not ordinary lists or arbitrary tuples.
- Unit: distinct original finite labels and their integer raw weights.
- Scope: one fixed actual carrier and an empty universal carrier core.
- Polynomial badness: the full degree<K pair code on the SAME support.
- Required inputs: a universal nonzero core-basis lower bound B and(HIGH).
- HIGH may instead be any separately proved H>=kappa*beta tuple count
  on EVERY raw>T record of the SAME source and incidence resource.
- Retained coordinates: every nonuniversal carrier zero remains in the source.
- Counting: exactly one chosen defect per tuple; recoverable insertion position.
- Output: truncated-raw mass and an inverse bound using a SEPARATELY proved
  low-raw label cap. No low-raw cap is asserted in this node.
- Scope boundary: a nonempty universal core invalidates the printed g=0
  baseline and nonzero-core argument; first normalize or use a corrected theorem.
- Status: hand-proved, with small controls only; external review remains due.
