# Geometric-progression singular cubics are paid

The [cutoff-125 extension](cutoff125.md) counts ALL d=10 cubic pairs
on 9822..9940 by 79311626937. The maximal-dimensional case has
<=3106 nonsingular pairs, with the full 21h loss and infinity retained.
The larger bound covers every smaller coefficient dimension and singular
case. Source resource and exceptions remain consumer-owned; no new req.
Earlier cutoff and interval statements below keep their historical scopes.

The [tail continuation](tail9821.md) pays the same d=10 source class
on 9527..9821 by N<=83344622367408351, including 256 exceptions.
It retains the actual height <=982 and the full 21h agreement loss.
Dimension four has <=13333 nonsingular pairs; smaller dimensions have
the separate weighted bound. No old interpolation-factor height is assumed.

Status: PROVED, at the source and characteristic contract below.
This pays the d=10 pattern left by the weighted cubic supplier. The
new [single-fiber argument](single_fiber_payment.md) also pays this source
class on 8656..9526, with N<=83243563858163463, including <=64
off-curve pairs. Dimension four has at most 760 nonsingular pairs there.
The generic degree-sixteen double-fiber identity gives a single-list
bound after paying an agreement loss of at most 21h. It includes infinity
and does not descend the receiver. The original sharper earlier-strip
constants below remain valid. Historical d=7 frontier statements below
precede its separate component-list payment; no quartic closure follows.

## Generic maximal-family theorem

Let k be algebraically closed of characteristic zero or >3. Let H be a
geometrically integral plane cubic over k(T), with top binary form a
nonzero multiple of `(T P+Q)^3`, smooth at its unique point at infinity
and singular in the affine plane. Suppose its FULL coefficient locus
of polynomial pairs `P,Q in k[T]`, `deg P,deg Q<=10`, has dimension four.

There is a birational polynomial parametrization of the curve

    P=c*z^3+p(T)*z^2+u(T)*z+v(T),
    Q=-c*T*z^3+b(T)*z^2+w(T)*z+j(T),                 (P3)
    c in k*, deg(p,b)<=4, deg(u,w)<=7, deg(v,j)<=10.

Every bounded nonsingular pair is represented by a UNIQUE
`z=eta(T) in k[T]` with `deg eta<=3`. Every such eta gives a bounded
pair. The possible singular pair is one additional pair, not silently
deleted. The polynomial `Delta(T)=T*p(T)+b(T)` is nonzero, of degree
at most five. The parametrization has fibers of size at most two at
each projective T-value except at most five values, where three suffice.
This includes T=infinity, using the degree-ten output and degree-three
parameter homogenizations.

## Pullback count

Let `rho=A(X)/B(X)` have degree h>=1, A,B coprime, and take n distinct
original evaluation coordinates. Keep an arbitrary received pair at each
coordinate; it need NOT descend to T. After the original affine offset
and common multiplier are handled as in the proof, suppose each represented
nonsingular pair has joint agreement at least a, with a>3h. Put

    S=2n+5h, c_col=3h.

If `a^2>S*c_col`, its number is at most

    floor(S*(a-c_col)/(a^2-S*c_col)).                 (LR)

Any larger slot and collision budgets and smaller fixed agreement a may
be substituted if the displayed denominator stays positive.

## KoalaBear finite consequence

Use the canonical normalized source
`(n,K,m)=(1048576+J,J,67472+J)`, s=11, T_low=500, empty universal
carrier core and original labels as in `claim_contract.md`.
Assume the earlier affine-singular cubic with projection kernel d=10
contains all but at most 64 represented LOW pairs. Then on
`7117<=J<=8655` the WHOLE source is paid:

    N=|Gamma|+134944 <=82951498428798039 < B=274980728111395087.

If its coefficient dimension is four, the sharper count is at most
1251 nonsingular on-cubic pairs, plus one singular pair and 64 off-curve
pairs, giving

    N<=4194118281875211, reserve=270786609829519876.

Dimensions at most three use the earlier weighted bound and give the
larger first constant. Its reserve is 192029229682597048.

Therefore the full-kernel lower-strip theorem now has just ONE unpaid
pattern: d=7, coefficient dimension three, 1301<=h<=4327. Its paid
alternative remains 274979661292365251. No unrestricted row, original
red, higher rank, ordinary LIST or prize is closed by this supplier.
