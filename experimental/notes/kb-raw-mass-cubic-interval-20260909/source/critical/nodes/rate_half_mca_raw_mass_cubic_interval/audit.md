# Hand And Exact-Arithmetic Audit

The source is fixed before any canonical choice. A rich joint-core point
has nonzero V-evaluation because the universal core is empty; this does
not remove the other carrier zeros. Each defect contributes a recoverable
tuple insertion. The separately proved5500 weight is essential for HIGH:
the baseline501*m*P_d does not fund51*beta. No extra tuple resource is used.

The eight rational certificates use D66972,E10000,X5000, not the
previous low-raw rank-filtration values D67347,E21499. Whole-interval
spike, squared-remainder, HIGH and logarithmic derivative gates establish
the bound; the integer-degree controls are not its source-uniform proof.

Cover cutoff50 is independent of core cutoff500. Full double-point
interpolation costs4n, gives factor weight<2A and has positive gap228
at9964. The conservative256 off-gcd allowance appears in every branch.
The fixed-cutoff gap at9965 is negative136, not an unsafe-line witness.

The binding e7 low-height price retains all50 nested raw sums. The e7
high-height proof couples height in slots and agreements, counts every
top component and the entire complement, and adds the singular point.
The e10 proof retains21h loss, infinity and all smaller coefficient loci.
The height51391 projection price is used only after verifying actual<2A
height<=41635. Every other projection-kernel dimension is charged.

Line/complement LIST counts do not descend the receiver or discard its
defects. The original501 coupled primary-line expression is used for ANY
assigned low subset. Raw>=51 already has weight>=561, so no medium range
is lost. Its resource and near must not receive another base addend.
Small lines, all conic types, intersections and constant gcds are included.

verify.py checks fixed rational gates and every factor price. The independent
verify_audit.py uses polynomial convolution and integer floors without
importing the primary, checks all24 degrees and501 raw coefficients,
and separates the harmonic exception sum from the cubic contribution.
Both preserve their guards under-O. Small controls do not formally verify
the algebraic geometry or replace independent external review.

This closes a complete24-degree normalized interval, not the full lower
gap. The generic cubic full-kernel boundary at9981 still applies. A broader
proof needs different coverage or source-owned pair counting; further
small endpoint tuning alone cannot close the remaining original node.
