# Full-Kernel Coverage And Every Factor Pattern

Fix the source in statement.md. Choose its full-code-bad maximal-raw
selection in the SAME carrier, using the required finite consumer's
canonical-selection supplier. It preserves every label. For raw<=50,
the minimizing pair has at least m-50=J+67422 complete joint agreements;
the canonical complete-core interpretation is valid since2*500<67472.
No assertion about the original selection rank is needed after reselection.

The hand proof and fixed certificates in basis_mass.md give MASS51,
CUM and DIRECT for every record, including raw51..500 and all larger raw.
In particular no LOW cover is assumed in that resource theorem.

## 1. Obtain The Entire Cover

Put A=J+67422,w=J-1,n=1048576+J. The required multiplicity theorem
uses the FULL double-point interpolation kernel with four conditions
per original received triple. Its gcd corollary gives

    Phi_w(2A)-Phi_w(2A-4w)-4n=3627124-364J,
    Phi_w(z)=sum_(i>=0)(i+1)*max(z-i*w,0).

The pair-degree cutoffs are15 and11 throughout9941..9964. Each
monomial-regime inequality is linear in J, so its two endpoint checks
prove the entire regime. The displayed gap decreases from8600 to228.
The full gcd has total pair degree<=3. Its primitive factors have
weighted degree<2A; every rich pair is on it except at most15^2=225
pairs. Retain the conservative allowance256. No selected-interpolant
or irreducibility assumption substitutes for the full-kernel statement.

If the gcd is irreducible cubic, cubic_payment.md exhausts every curve
type and pays the entire source by(C50). Otherwise its factors are
lines and conics of total degree<=3, handled below. Repeated factors do
not increase the represented pair set; assign intersections once.

## 2. Small Lines

A populated line containing two distinct bounded pairs has primitive
identity A0*a+A1*b=Q, gcd(A0,A1)=1, height0<=h<=J-1<=9963,
and deg Q<=J-1+h. Pair collisions on it have size<=J-1-h.
Let S be the compatibility coordinates A0*u+A1*v=Q.

If |S|<=590000+16h, every represented LOW_50 pair has at least
amin=77363 joint agreements on S. Relax the collision count to9963-h.
The Johnson denominator is at least

    77363^2-(590000+16h)*(9963-h)
      =106863769+430592h+16h^2>=106863769>0.

First relax the incidence fraction to agreement amin, then upper-bound
its numerator by(590000+16*9963)*77363. The ratio has floor542.
If the actual domain is too small it has no pairs. Empty and singleton
lines already satisfy the same cap. No original source coordinate is removed.

## 3. A Large Line Bounds Its Complement

Otherwise |S|>=590001+16h and its outside domain has at most
468539-16h coordinates. Any off-line polynomial pair can jointly agree
inside S at at most J-1+h coordinates: its compatibility polynomial is
nonzero and has that degree bound. Thus it has at least67423-h joint
agreements outside S. In this auxiliary LIST count use component degree
at most9963, not a fictitiously descended code. Its ratio is bounded by

    Q_out<=(458576-16h)/(57460-h)
          <=458576/57460<8.

The denominator stays positive for h<=9963, and the ratio decreases
because458576-16*57460<0. If the outside domain is too small the pair
set is empty. The other factors have total degree<=2. The credited
joint-incidence and conic-dimension arguments give at most2*8^11 pairs
on two lines, or2^16*8^6=2*8^11 on one conic, including all singular
conic types. These are pair counts; their original labels are priced below.

## 4. Keep The Primary Line's Coupled Budget

Let w_gamma be the completed-basis weight and F(J) its original resource.
The required supplier proves w_gamma>=11*min(raw_gamma,500) for EVERY
raw value, and sum w_gamma<=F(J). Consequently

    |Gamma|<=F(J)/501+sum_gamma(1-w_gamma/501)
            <=F(J)/501+sum_(raw<=50)(1-raw_gamma/501).

For every raw>=51 the discarded summand is nonpositive since11*51>501.
There is no unpaid medium-defect interval. On LOW_50, w_gamma>=raw_gamma.

The rational-pencil group proof in the required finite relation consumer
applies to ANY such subset assigned to the fixed primary line. It uses
the union of that group's complete cores; it does not assume every LOW
pair lies there. Its section3 bounds primary gain plus F(J)/501 and
original near by255637082864553899 on the larger proved J range.
The nonconstant-direction alternative is smaller: its gain is at most
17200000000000000 and the global F bound is23067643444721720934.
Singleton and small-core-union cases are included by the same group proof.

Charge all other LOW_50 pairs and256 exceptions at981154 labels per
pair. This gives

    |Gamma|+134944
      <=255637082864553899+981154*(2*8^11+256)
       =272493180485087659 <(C50).

This branch already has its resource and near. Adding DIRECT's base
again is invalid. The outside-domain LIST restriction only counted pairs;
all original defects and labels still appear in the source ledger.

## 5. No Large Populated Line

Every line now has at most542 represented pairs. The credited conic
classification gives the direct LOW_500 pair cap127031877504 for a
moving conic; the other conic types have smaller direct pair bounds.
It therefore also bounds the LOW_50 subset of any conic here.
The remaining complete patterns are three small lines, or one conic
and a small line. DIRECT bounds them by, respectively,

    12672029169970630+981154*(3*542+256)
      =12672031016502458,
    12672029169970630+981154*(127031877504+542+256)
      =137309864693491138.

Both are below(C50). A constant gcd or smaller factor degree only
reduces these counts. Singular conics, repeated factors and intersections
are included. Combining the exhaustive whole-source alternatives by
MAXIMUM proves the statement, with the binding case in cubic_payment.md.

## 6. Scope

The new interval adds24 complete normalized degrees beyond9940. Original
labels and one near allowance are explicit; original-source transport is
owned by the downstream assembly, not assumed in a supplier.
At9965 the fixed-cutoff dimension gap is-136. The prior general
cubic_recipe_boundary.md limits even varying positive cutoff and
multiplicity to at most9980 for this same criterion. Neither failure is
an unsafe-source construction or a proof that a different kernel cannot
work. Broader closure needs higher-degree coverage or a source-owned
pair census, not extrapolation of this finite extension.
