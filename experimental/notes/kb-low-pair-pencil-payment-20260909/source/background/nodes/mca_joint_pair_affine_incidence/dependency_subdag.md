# Dependency Sub-DAG

    mca_low_core_quadratic_graph_payment
       [algebraic_list_bound.md, joint affine specialization]
                         |
                         v
    mca_joint_pair_affine_incidence
                         |
                         v
    rate_half_mca_low_pair_affine_span_payment

The incoming requirement credits the existing joint LIST inequality.
The determinant-root count and first rank-two section are proved here.
The finite consumer owns every MCA source and label charge. No reverse
dependency on the original interval assembly is introduced by this node.
