# A Compatible Core Union Pays For Its Remaining Scalar Lists

Write R=1048576,d=67472,L=9965,E=21499 and near=134944. The
original-source assembly supplies exactly the fixed source in statement.md;
selection or minimizer choices do not remeasure its original rank or core.
The preceding pair-span theorem already pays nonconstant rational pencils
by228260637755610995. It remains to handle constant directions, including
the full parameter dimension11.

## 1. The Actual Constant Pencil And Its Complete Core Union

Let U be the union of COMPLETE joint cores H_f of P_2, and e=n-|U|.
For a populated constant pencil the required polynomial-relation theorem
gives either

    finite chart:   a+gamma_0*b=c, c in h_*+V;
    infinite chart: b=c, c in V.

These are polynomial identities for all represented pairs. Thus on U,
u+gamma_0*v=c or v=c, respectively. No arbitrary low-degree c is used.
Charge the possible finite label gamma_0 at most once and retain Gamma'.
The original source and all its other labels stay unchanged.

Every x in U has nonzero evaluation on V. Otherwise a represented pair
agreeing there would have (u,v)=(h_*,0), a point of the forbidden universal
carrier core. Nonuniversal zeros elsewhere are not removed.

## 2. All Raw Values Share The Refunded Resource

The separate basis_mass.md proves beta(J)*min(raw,3) independent tuples
per original record, for every raw value. The compatible-domain refund
therefore gives

    sum_(Gamma') min(raw,3)
      <= [(n)_12-(n-e)_12]/beta(J)
       =R(J)*(1-(n-e)_12/(n)_12).                     (MASS-U)

Here (z)_12 is the falling factorial. A low-pair core union defines the
compatibility domain, but the refund applies to ALL retained labels,
including raw>=3 whose pairs need not belong to the pencil. No high family
is assumed to lie on U, and no source coordinates are punctured.

In the finite chart, group the remaining low labels by b in V; their pair
is (c-gamma_0*b,b). On U, its core is exactly {v=b}. In the infinite
chart group by a in h_*+V; its pair is (a,c), with core {u=a} on U.
Every selected defect of a nonkernel low label lies outside U. For one
pair these defect sets are disjoint across its distinct slopes.

Let M_t count represented pairs with |H_f|>=m-t, t=1,2. The existing
pencil group proof, now with the SAME truncated-three resource (MASS-U),
gives

    |Gamma| <= 1+R(J)/3*(1-(n-e)_12/(n)_12)
                   +e*(M_1/2+M_2/6).                 (LEDGER)

The sum is the exact raw-deficit identity, not a transplant of the old
cutoff-500 resource. Each raw>=3 record has zero remaining deficit.

## 3. One Nonzero Anchor And Uniform Padded LIST Certificates

Assume |U|>=m for now. Evaluation on V is nonzero at every x in U,
so a scalar agreement anchor drops its dimension from11 to10. Subtract
one anchored polynomial and divide by X-x. The child has

    (N_child,K_child,A_child)=(|U|-1,J-1,m-t-1).

Counting its incidences gives M_t<=|U|*V_t/(m-t), where V_t is any
valid rank-ten ordinary LIST cap on the child. The required padded LIST
theorem is uniform over all affine translates and handles child common
zeros without losing words. The original field easily supplies its padding.

For an integer interval a<=e<=b, take its cap with corridor parameters

    r=R-a, w=d-t, K_max=21498, dimension10.

Adding distinct coordinates with arbitrary received values embeds the
actual smaller child domain into this larger one without losing its old
agreements. No MCA source is extended or restricted in this auxiliary count.
The exact field gate q>=r+K_max holds. Each cap is a sequence of ten
explicit Johnson/padding or dimension-descent integer inequalities, checked
independently in the frozen certificate.

## 4. Bound Every J Analytically, Every Core-Union Size Exactly

basis_mass.md proves R(J)<=R0=R(L) and monotonicity on the whole J
interval. At fixed e, the factor

    (n-e)_12/(n)_12=product_(i=0)^11(1-e/(n-i))

increases with n, so the nonnegative refund factor decreases with J.
For e<=R-d, each factor is positive and the list prefactor
(R+J-e)/(d+J-t) also decreases with J. Hence L is a simultaneous
worst endpoint for the entire envelope, without independently maximizing
different J-dependent source quantities.

Partition every integer0<=e<=981104 into intervals

    a=1000*i, b=min(a+999,981104), i=0,...,981.

Set n_L=1058541,m_L=77437, and write V_t(a) for the cap in section3.
On each interval, (LEDGER) is at most the EXACT rational envelope

    E(a,b)=1+near + R0/3*(1-(n_L-b)_12/(n_L)_12)
             + sum_(t=1)^2 b*(n_L-a)*V_t(a)
                                  /((m_L-t)*t*(t+1)).  (BOX)

The resource refund increases with e, so it uses b. The list is padded
at a, while b bounds its outside multiplicity. All quantities are
nonnegative. The intervals are exhaustive and adjacent; no optimization
claim about the compiler or actual source is needed.

The982 frozen rows contain19640 exact LIST transitions. Their largest
ceiling is

    max ceil E(a,b)=274136923022229951
    at [a,b]=[103000,103999].                          (MAX)

It is below B* by843805089165136. The unrefunded version of this same
recipe exceeds the budget; that observation alone is not a no-go theorem.

## 5. Small Or Empty Low Unions

If |U|<m, any two distinct P_2 pairs would have core intersection at least
2(m-2)-|U|>=m-3>=J. Their degree-<J difference would vanish there,
impossible. Hence P_2 has at most one pair. Its total low labels are at
most n-|H_f|<=R-d+2=981106 by same-pair defect ownership.

The UNREFUNDED basis resource on all original labels, before removing any
kernel slope, gives

    |Gamma|+near <= R0/3+981106+near < (MAX).

This also includes an empty P_2. Thus the |U|>=m gate in the rectangular
certificate leaves no unpriced small-union case.

## 6. Exhaustive Pencil Payment And Remaining Source

The constant-direction source, all its high defects and its original near
allowance are paid by (MAX). Nonconstant directions are paid by the
required previous pair-span theorem; take the maximum of whole-source
alternatives, not their sum. This proves (PENCIL) for every rational
pencil P_2, under every valid selection and minimizer assignment.

An over-budget rank-twelve source cannot admit any such pencil. The same
previous supplier forces its pair affine dimension>=11; now its direction
space must additionally have rank two over F(X). No iterative rank-two
anchor or census of that survivor is assumed. Original ranks>=13 and
both full Prize problems remain open.
