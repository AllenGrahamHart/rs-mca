# Claim Contract

- Original KoalaBear MCA q=2130706433^6, agreement1116048, budget
  274980728111395087, finite denominator q, distinct original bad slopes.
- EVERY complete post-near rank-twelve normalization on J9965..21499,
  fixed actual dim V=11 and empty universal core; no original rank retest.
- Only P_2, the chosen raw<=2 minimizing PAIR family, must lie on one
  rational pencil. Higher-defect pairs are arbitrary and still fully funded.
- All choices of valid supports, explanations and minimizers are covered.
- Constant-pencil compatibility uses c in h_*+V or c in V, not an arbitrary
  polynomial or a nonconstant rational row.
- The complete low-pair core union defines U. Internal incidence tuples
  are refunded only after the one possible kernel slope is charged.
- No coordinate, nonuniversal zero, field, code or raw margin is deleted.
- Core cutoff16, mass truncation3 and pair cutoff2 are distinct. HIGH uses
  the proved187*m*P_d count, not the insufficient17*m*P_d baseline.
- The EXACT rational source ratio R0 is coupled to its refund; its integer
  floor is not substituted before multiplying by a fractional factor.
- Auxiliary LIST padding changes only a counting domain, never the source.
- The 982 integer e intervals are exhaustive; small unions are separately
  paid by the complete-core overlap argument. All near and exceptions are
  included once. Branch estimates combine by maximum.
- No function-field-rank-two census, entire J interval, rank>=13 bound,
  active-v4 atom, unrestricted endpoint or full Prize closure is asserted.
