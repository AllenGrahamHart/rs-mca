# Small Low-Defect Pair Span Pays The Whole Original Source

Status: PROVED by proof.md and its credited suppliers; independent
external mathematical review remains due.

Use the original KoalaBear MCA row q=2130706433^6,
(n0,K0,m0)=(2097152,1048576,1116048), finite slope denominator q,
target 2^-128 and budget B*=274980728111395087. Normalize a complete
post-near rank-twelve selection by its complete shared scalar core g:

    J=K0-g, (n,K,m)=(1048576+J,J,67472+J),
    9965<=J<=21499.

The actual fixed carrier h_*+V has dim V=11 and empty universal
carrier core. Original field, finite labels, full-code-bad saturated
supports and nonuniversal zeros remain. Only g is cancelled.

For ANY valid selection in that same carrier, choose any minimizing
b_gamma in V and put a_gamma=h_gamma-gamma*b_gamma. Let P_2 be the
distinct represented polynomial pairs (a_gamma,b_gamma) at raw<=2,
and r=dim_F aff(P_2). Empty P_2 is included in the paid alternative.

If r<=10, then the ENTIRE original line is paid:

    |Gamma|+134944 <=257846243054097181 <B*,             (PAY)
    reserve=17134485057297906.

There is no rational-pencil premise. The direction space of aff(P_2)
has function-field rank zero, one or two, handled exhaustively. Rank two
has the displayed bound; the rational-pencil branch has the smaller cap
228260637755610995, including near once and all preferred slopes.

Consequently every over-budget original rank-twelve source in the remaining
interval has dim_F aff(P_2)>=11 for EVERY valid selection and EVERY
minimizer assignment in the fixed carrier. This is actual affine dimension
of polynomial PAIRS, not of the (gamma,h_gamma) parameter graph. It does
not follow by projecting the earlier graph-rank theorem through a linear map:
(gamma,a,b) -> (gamma,a+gamma*b) is bilinear.

Moreover, the credited common-carrier pencil dimension theorem makes r<=10
automatic for a NONCONSTANT rational direction. Every such low-pair pencil
is therefore paid. Any over-budget P_2 must either be a constant-direction
pencil with actual affine dimension exactly11, or have function-field
direction rank two and affine dimension at least11. This is an exhaustive
necessary alternative, not an asserted census of either survivor class.

The new theorem pays source classes on the entire remaining degree gap.
It does not pay every source in that gap. The residual J9965..21499 still
has 11535 integers; original ranks>=13, unrestricted MCA, ordinary LIST
and both Prize problems remain open. No new speculative premise or
active-v4 atom is installed.
