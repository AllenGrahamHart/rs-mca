# Dependency Sub-DAG

    mca_joint_pair_affine_incidence --------------------+
    [joint affine count; guarded two-dimensional anchor]|
                                                       |
    mca_low_core_polynomial_relation_payment -----------+
    [rational_line_cores.md: parameterization, preferred |
     labels and same-owner group gain]                  |
                                                       v
    rate_half_mca_low_raw_rank_filtration ---> rate_half_mca_low_pair_affine_span_payment
    [uniform truncated-four mass]                      ^
                                                       |
    rate_half_mca_rank_twelve_paid_interval_assembly ----+
    [fixed original-source normalization and near]

The finite node supplies evidence to the original open rank/support router.
It is NOT a requirement of the paid-interval assembly or threshold bracket.
In particular the mass supplier already requires the assembly: reversing
that edge would be a proof cycle. Both new nodes stay in background until
their scope is consumed by an actual critical requirement.

No conjectural pair census is a logical leaf. The original router remains
TARGET with no incoming logical requirements. The generic anchor supplier
requires only the existing joint affine LIST lemma, not this consumer.
