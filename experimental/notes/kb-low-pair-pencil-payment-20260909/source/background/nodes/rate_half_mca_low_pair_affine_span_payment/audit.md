# Hand Audit And Focused Controls

The proof separates the affine pair rank over F from direction rank over
F(X). Function-field rank two guarantees a nonzero determinant only before
the first section; every child uses the ordinary affine bound. All determinant
roots cost original agreement incidences. Nonuniversal zeros remain.

The raw-mass inequality holds uniformly under valid selection and minimizer
choice, so no canonical-reselection assumption is imported. Own-core defect
sets are disjoint for a fixed pair only. The rational-pencil branch first
establishes receiver compatibility on the full core union and charges
preferred slopes before using its outside-of-union factor. The parameter
space uses actual affine rank<=10, including constant-direction pencils.
For a nonconstant pencil the credited multiplier-invariance argument makes
rank<=10 automatic in the common dimension11 carrier. Constant directions
only imply rank<=11; the remaining full-rank case is not declared paid.

The scalar bound on U handles thresholds above |U| by zero counts, while
its numerator stays positive because |U|>=m-2. The degree interval is
handled analytically by a positive derivative and endpoint denominators.
The union optimization is the exact derivative identity for e*(Z-e)^10.
Whole-source branch estimates combine by maximum, with near once.

verify.py computes both prices with rational arithmetic and checks every
four-record raw pattern in1..7 plus same-owner gains. verify_audit.py imports
no primary implementation: it uses integer common denominators, reconstructs
the derivative coefficients and rechecks the actual F17 preferred-label
obstruction. Six constant v-values force a degree-<3 second interpolant
constant and the seventh contradicts it, certifying full-code badness in
that control. Carrier minimization is exhausted over only17^2 possibilities.
This is not a Prize-row finite-field search.

The generic lemma's separate F7 controls refute the two tempting omitted
hypotheses. The finite scripts also reject changing the printed exact floor,
forgetting the rank-ten limit, and adding whole-source alternatives.
Failure of the rank-eleven bound in this recipe is not an unsafe-source
construction or proof that rank eleven cannot be paid by another method.

No missing upper census is installed as a proved premise. Tests are controls;
the universal hand argument and credited suppliers need independent review.

Scoped audit verdict: NO ISSUE.
