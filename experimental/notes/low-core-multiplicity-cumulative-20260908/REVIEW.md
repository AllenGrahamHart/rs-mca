# Review Priorities

This packet is a contribution for independent review, not a report that
independent mathematical review has already occurred.

## Two Short Reusable Arguments

1. Read the multiplicity supplier's statement.md and proof.md.
   For every actual joint agreement, all three translated generators map
   into (X-x) after substitution. This proves multiplicity r without
   differentiating an arbitrary receiver. For r=2 four jet conditions are
   necessary. The dimension count uses an upper bound on conditions,
   not unproved independence. Strict weighted degree gives the root identity.
2. Check primitive division over F[X] and the strict dimension surplus.
   Multiplication by primitive G identifies the divisible subspace and
   weighted degree is additive. Coprime plane curves bound actual
   F(X)-points, including singular/isolated points, not just components.
3. In kernel_corollary.md the whole kernel is used. Two coprime residual
   combinations are chosen over the infinite field F(X), not by an
   unjustified generic choice over a small finite field. Factor height
   is <rA, and all off-gcd pairs stay in the ledger.
4. Read cumulative_low_weights.md in the completed-basis supplier.
   Eleven factors give 12*(67473-5500)-11*67473=1473>0.
   Monotonicity bridges raw 500..5500; the truncated weight handles larger
   raw, including raw>67472. The L_t and S_t identities use the same
   selected source for every t. Equal expressions are not an exact maximum.

## Finite Composition

The main finite entry is
[source/.../double_point_cubic_tail.md](source/background/nodes/rate_half_mca_low_core_kernel_quadratic_strip/double_point_cubic_tail.md).
Its [hand audit](source/background/nodes/rate_half_mca_low_core_kernel_quadratic_strip/cubic_tail_audit.md)
is a checklist for, not a replacement for, reviewing the component proofs.

- The exact dimension gap changes at 8930/8931 and 9568/9569. Its last
  positive value is 80 at 9821; the next value is -284. Earlier two-piece
  extrapolation was corrected before publication. Failure at the next row
  is only failure of this sufficient recipe.
- The double-point tail needs 256 off-gcd pairs and actual factor height
  <2A. Review the infinity-singular height bound and the entire d=7/d=10
  coefficient loci at this changed scope, not their former <A/64 proofs.
- The d=7 low-height calculation bounds the entire locus at every depth,
  with parameter degree <J+h. Convexity gives an exact endpoint sum.
  All singular and lower-dimensional components are retained.
- The d=7 top-component denominator is 80873689; the d=10 one is
  4201567. The height/degree dependence must stay coupled. The d=10
  original-coordinate proof retains infinity and the full 21h loss.
- A large factor line is paid using its bounded-degree complement.
  Outside its compatibility set, use the original pair degree <J and
  the actual remaining agreement. The other factors have total degree <=2.
  No unrestricted large-line theorem is extrapolated past its range.
- The primary-line expression already contains a resource and near.
  Add other factor/exception labels, not another HIGH/near base.
  Complete whole-source alternatives combine by maximum.

The preceding quartic source stage uses ONLY sections 1--5 of
quartic_obstruction.md plus general multiplicity escape. Later special
quartic classifications are optional history, not hidden premises for
the complete interval theorem. Local manifests, historical result pages
and optional supplements are indexes; follow the explicit proof route.

## Checks And Their Limits

The serial replay verifies hashed source identity and rejects four malformed
manifest variants. Its arithmetic children include independent implementations,
all tail kernel rows, 500 raw-weight products, 499 upward-rounded cumulative
counts, exact factor prices and tiny Hasse/Taylor controls in small fields.

Passing those tests does not prove all universal geometry, establish an
exhaustive original-source bridge, certify optimality of the row endpoint,
or show acceptance by maintainers. No large numerical audit is necessary
to review the two analytic mechanisms.

At integration, independently reconstruct the argument from these sources
and bind SOURCE_CONTRACT.md to the upstream residual. Until then the
correct impact label remains LOCAL_ONLY.
