# Moving quartic projection graphs with full original accounting

Status: PROVED restricted source payments. Use this consumer's canonical
normalized source, LOW_500, actual eleven-dimensional degree-<J carrier,
4801<=J<=169999 and the unchanged KoalaBear field. Suppose all but
<=4 represented LOW pairs lie on ONE polynomial projection graph

    (a,b)=(Phi(z),Psi(z)), Phi,Psi in F(X)[z],
    max(deg_z Phi,deg_z Psi)=4,
    A0*Phi(z)+B0*Psi(z)=z,
    gcd(A0,B0)=1, h=max(deg A0,deg B0)<=2379.

Let d be the ACTUAL kernel dimension of
(v,w)->A0*v+B0*w on V x V. Let r be the maximum component dimension
of the entire nonempty input coefficient locus; the required jet theorem
gives r<=ceil(d/4), and d<=11. An empty locus needs no pair count.
In any of the following cases the whole
source, including the original near allowance, pays:

1. d>=6;
2. d>=4 and h<=1532;
3. d>=4 and at least 28881 original coordinates admit no joint
   agreement with any represented pair on the graph;
4. d=3 or 4 with actual coefficient dimension zero;
5. d=5 with actual coefficient dimension at most one.

A uniform total for these cases is

    N=|Gamma|+134944 <=274015369961868939,
    reserve >=965358149526148.                         (QG)

Case 1 has the sharper total <=80344841708572299. No graph cover,
height or forbidden-coordinate assertion is supplied for an arbitrary source.
Those implications in the current quartic residual belong to the kernel child.

## 1. The input, dimension and degree remain coupled

The already required projective-jet supplier's `moving_projection_graphs.md`
gives input ambient dimension 22-d, true polynomial degree bound J+h,
and coefficient equations of degree <=4. For any actual dimension bound r,
the proper-section and scalar LIST proof therefore gives pair count

    M<=4^(22-d-r)*Q_h^r,
    Q_h=(1048577-h)/(66973-h).                         (1)

The numerator changes to n'-J-h+1 when restricting only the on-graph
LIST count to a smaller domain of size n'. All components remain covered.
Input z recovers the original pair uniquely, so no extra multiplicity
factor or field extension is suppressed. The characteristic guard p>=J
and the agreement guard J+66972>=J+h hold throughout this source range.

For h<=2379, Q_h<17. Checking d=6,...,11 at r=ceil(d/4) gives

    M<=4^14*17^2=77577846784.                         (2)

For h<=1532, Q_h<16, since
16*(66973-1532)-(1048577-1532)=11>0. Checking d=4,...,11 gives

    M<=4^19=274877906944,                             (3)

attained as an upper envelope at d=4 and d=5. These checks use only
eight integer kernel dimensions, not a scan over fields or source words.

## 2. Coordinates with no on-graph agreement

Suppose a set Z of at least 28881 original coordinates has no joint
agreement with any represented on-graph pair. Their agreement remains
at least J+66972 on D minus Z. If that domain is smaller than the
required agreement the list is empty. Otherwise the scalar LIST proof
applies there with the SAME degree bound J+h. Its ratio is at most

    Q'_h=(1048577-28881-h)/(66973-h)
        <=1017317/64594 <16,

where monotonicity in h gives the endpoint 2379. Thus (3) applies
for every d>=4. The scalar received word is A0*u+B0*v on this actual
subset. Z is not removed from the source code, its bad supports, complete
cores, scalar defects or finite labels. In particular the label price
below still uses the ORIGINAL n-A=981604, not a smaller complement.

## 3. Smaller-dimensional graph loci

At d=3,r=0 equation (1) gives M<=4^19; d=4,r=0 gives 4^18.
At d=5,r<=1 use Q_h<17 and M<=4^16*17<4^19. The actual smaller
dimension is required; the default upper bound ceil(d/4) is not silently
lowered. These are proved sufficient cases, not assumed properties of
every graph or an inferred infinite family from finite samples.

## 4. Original finite source price

HIGH_500 has weight >=5500 on the same completed-basis resource
C=23067643444721720934. At most 981604 original LOW labels belong
to each graph or off-graph pair. Thus, including the <=4 exceptions,

    N<=C//5500+134944+981604*(M+4).

Substituting (3) proves (QG); substituting (2) gives the sharper case-1
total. The graph is affine-smooth over F(X), but no separate singular
allowance is needed in any event: every pair satisfying its exact
polynomial parametrization is counted by (1). Specialized coefficient
or denominator zeros remove no original coordinates from the proof.
The one resource and original near are counted once, with actual pair
counts rather than old /501-discounted gains.

## 5. Connection to the forced quartic, without a reverse dependency

The kernel child proves that a remaining quartic has <=4 off-curve pairs
and weighted excess H<=9517 on 8764..9526. A polynomial projection
graph has highest binary form c*(A0 Y+B0 Z)^4: choose a complementary
linear output coordinate over F(X), whose graph is quartic in z.
Gauss division gives c in F[X], so 4h<=H. Thus h<=2379, and through
9294 the sharper H<=6130 gives h<=1532.

If one off-curve pair exists, that child's root argument supplies the
28881 no-agreement coordinates in case 3. Consequently an UNPAID
quartic that is a polynomial projection graph must have either d<=2,
or d=3 with coefficient dimension one, or

    d=4, coefficient dimension one, OR d=5, dimension two;
    J>=9295, h>=1533, and EVERY LOW_500 pair on the quartic.

Non-projection quartics remain outside this classification. The kernel
child owns those forced hypotheses and the exhaustive residual, while
the generic jet theorem and finite source resource already suffice for
the payments proved here. No new conditional node, normalized interval,
unrestricted endpoint, higher-rank theorem or prize closure is claimed.
