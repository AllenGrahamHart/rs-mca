# Dependencies

The current `double_point_cubic_tail.md` closure uses the already
required multiplicity kernel and finite source consumer. Existing critical
suppliers now supply the cumulative raw-weight bound, the new high-height
component envelope and the GP tail envelope. The bounded-degree large-
line complement is proved in `cubic_tail_line_partition.md`. No new
node, edge or speculative premise; the consumer does not require its
kernel child in reverse. Earlier stages below retain their own scopes.

The new full closure in `quartic_strip_payment.md` uses only the
independent sections 1--5 of `quartic_obstruction.md` and the finite
consumer's `quartic_multiplicity_payment.md`. The consumer now requires
`mca_multiplicity_interpolation_curve_escape`, which in turn requires
the existing quadratic-graph supplier's intersection degree lemma.
This adds one PROVED node and two req edges, with the kernel's own
consumer requirement unchanged. The older quartic height/projection
refinements are not necessary for this closure. No reverse dependency
from the old factor dichotomy to the new closure is permitted.

The quartic multiplier refinement is proved in
`quartic_multiplier_geometry.md`, using the old full-kernel degree-four
conclusion, not its own refined statement. The finite consumer separately
owns `quartic_projection_graph_payment.md`, using only its already
required generic jet and resource suppliers. This node supplies the
actual height/exception/forbidden-agreement hypotheses. No reverse
dependency, new edge, speculative normal-form premise or red promotion.

The `low_cutoff_strip.md` theorem extends full normalized coverage to
8763 with existing requirements only. The finite consumer owns the
LOW-101/HIGH resource conversion, and its component supplier owns the
4622-pair envelope. The degree-eight full kernel and exhaustive factor
assembly are proved here. No source-cover hypothesis, reverse dependency,
new node/edge or original-red promotion is introduced.

The finite consumer now additionally requires the PROVED projective
split-product supplier. Section 6 of `quartic_obstruction.md` uses its
completed source price to exclude the exact constant-direction family
x*y^3=R!=0. The kernel's own requirement is unchanged; one new node
and two req edges are added upstream of it. There is no reverse dependence,
quartic-cover premise or original-red promotion. Earlier paragraphs below
record previous stages, before this added quartic-family payment.

The PROVED `quartic_obstruction.md` narrows 8656..9526 to one
geometrically integral quartic containing all but <=64 LOW pairs, unless
N<=274979661975561635. The height-aware line split and extended cubic
suppliers pay every reducible degree<=4 pattern, with direct pair counts
and one resource/near charge. `quartic_audit.md` and the two quartic
verifiers record the audit. Existing requirements suffice; no node/edge
is added. The quartic and the full interval remain unpaid. Earlier
records below retain their scopes; complete coverage still ends at 8655.

The component-list cubic supplier is now inherited through the same
finite consumer. It pays the last d=7 class, while the independent
d=10 supplier pays that other class. `cubic_strip_payment.md` combines
them with this node's exhaustive full-kernel classification, closing
7117..8655. Neither supplier requires this node, so no circular proof.

The geometric-progression cubic supplier is required through the same
finite consumer. It pays d=10 in all coefficient dimensions; this node
combines that with its own <=64 off-curve coverage to leave only
d=7,dim Y=3,1301<=h<=4327. The supplier does not require this node.

The weighted residual refinement inherits the new proved cubic-incidence
supplier through the same finite consumer. That supplier rederives its
normal form and never requires this node. This node supplies the exhaustive
curve coverage; the supplier supplies the counts. No new kernel edge or
speculative residual child is needed.

The affine-singularity refinement uses the already required jet supplier
through the finite consumer: the moving projection/kernel dimension
bound pays the infinity-singular class. The original cubic reduction's
sections 1--4 first supply a one-place curve; its section 5 and the
normal-form companion then refine it. The latter is not assumed to
prove the earlier reduction. No new edge or conditional node is added.

- `rate_half_mca_cancelled_low_core_relation_payment` -> this node (req):
  normalized source, partitioned margin ledger, conic and line prices,
  parallel-fiber bound, and their proved coefficient-degree machinery.
- This node -> `rate_half_mca_global_core_rank_support_distance_router`
  (ev): closes the printed 4801..7116 interval, and now restricts any
  unpaid source on 7117..8655 to the one-place singular cubic class.

The cubic continuation uses the same required finite consumer, now
with its PROVED smooth-cubic and normalization-unit suppliers. The
degree-eight kernel, asymmetric pair bound and exhaustive factor split
are proved locally in `cubic_obstruction.md`. No extra edge is needed.

No speculative conditional child, dependency cycle, or original red
promotion is introduced. The finite supplier does not require this node.
