# The full normalized cubic strip is paid

Continuation: `low_cutoff_strip.md` now closes the additional
8656..8763 interval at a new cutoff and exact resource conversion.
Combined coverage is 4801..8763 at the larger bound (S3) below.
The old cutoff-500 theorem and its endpoint failure remain valid at
their stated scope; they are not extrapolated to prove the extension.

Status: PROVED. Use exactly this node's canonical normalized source,
N=|Gamma|+134944 and original finite slope units.

For EVERY source with 7117<=J<=8655,

    N<=274979661292365251<B=274980728111395087,
    reserve=1066819029836.                              (S3)

The proof is a composition of the already required source-complete
dichotomy and the two new component counts:

1. `cubic_obstruction.md` uses the entire degree-eight kernel to force
   common-factor degree <=3 and <=64 off-curve pairs. Its complete factor
   ledger pays every other case or leaves the affine-singular cubic.
2. `weighted_cubic_residual.md` and the finite consumer's weighted
   supplier pay all projection-kernel/height/dimension cases except
   d=7,dim Y=3,1301<=h<=4327 and d=10,dim Y=4,1<=h<=865,
   with paid alternative 274979661292365251.
3. The geometric-progression supplier pays all d=10 coefficient dimensions
   by <=82951498428798039, keeping every exceptional coordinate.
4. The new component-list supplier pays the d=7 class by
   <=73164604161759423, counting every top-dimensional component and
   the entire lower-dimensional complement in original coefficient degree.

Both new totals are below the common paid-alternative bound, proving (S3).
Resource/near accounting is complete inside each applicable case; these
whole-source alternative bounds are combined by MAXIMUM, not added.
No residual curve hypothesis is imposed on the source in (S3).

The interval has 1539 integer J-values and follows the already proved
4801..7116 strip without a gap. Hence every canonical normalized source
on 4801..8655 pays by (S3), the larger of the two strip bounds.
The original degree-eight kernel surplus is positive at 8655 and negative
at 8656; no extrapolation beyond that endpoint is claimed.

The remaining normalized interval is 8656..169999. This is not all rank
twelve, higher error ranks, an unrestricted original-row adjacent endpoint
or an active-v4 certificate. Exhaustive original-source normalization/near
and owner/add-back transport remain separate obligations. Both prizes
stay open. The earlier residual refinements remain valid historical
theorems but no longer describe an unpaid case on 7117..8655.
