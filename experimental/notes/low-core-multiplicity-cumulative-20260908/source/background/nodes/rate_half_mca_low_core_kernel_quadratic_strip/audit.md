# Hand audit

The current `cubic_tail_audit.md` records complete source coverage
through 9821, including the cumulative weight identity, actual <2A
factor height, 256 exceptions and bounded-degree large-line complement.
It introduces no new premise or dependency. Earlier audits below retain
their original numerical scopes and do not supersede this endpoint.

## Double-point whole-strip closure

`quartic_strip_payment.md` uses only the independent original sections
1--5 of `quartic_obstruction.md` plus the new generic multiplicity
payment. The earlier <=64 exception allowance is kept; later quartic
height and family refinements are not premises of the closure. Both
whole-source alternatives include original labels, HIGH and near and
combine by maximum. The endpoint is 9526. The larger restricted
quartic price and degree-three double-point cover through 9821 do not
extend full source coverage; factor height is now <2A. The supplier's
hand proof and two independent tiny arithmetic/control implementations
are audited locally; external review remains due.

## LOW-101 interval extension

`low_cutoff_strip.md` closes every source on 8656..8763. Checked the
unchanged raw selection, complete-core union and original field, the
new >=11/1200 LOW/HIGH weights, and the exact 1189/1200 discount
derived from their ONE resource. The strict new kernel surplus is 1
at 8763; the old cutoff-500 surplus is not reused. The line alternative
is valid on the same LOW_500 family, while its small-line bound passes
to the LOW_101 subset. Cubic and conic bounds use direct pair counts.
The adjusted d=7 component argument counts every top and lower component,
one possible singular pair and all original coordinates. All factor
patterns of degree <=3 and the off-gcd allowance are priced.

The primary and independent `verify_low_cutoff_strip*` scripts pass
normally and with -O. The latter independently excludes 4623 pairs by
Cauchy and checks integer resource division. Peak RSS is below 16 MiB;
no Modal or large computation. Global diagnostics remain unchanged.
The new normalized full interval is 4801..8763. The older paragraphs
below record earlier cutoffs and stages, not the current unpaid frontier.

The PROVED `quartic_obstruction.md` narrows 8656..9526 to one
geometrically integral quartic containing all but <=64 LOW pairs, unless
N<=274979661975561635. The height-aware line split and extended cubic
suppliers pay every reducible degree<=4 pattern, with direct pair counts
and one resource/near charge. `quartic_audit.md` and the two quartic
verifiers record the audit. Existing requirements suffice; no node/edge
is added. The quartic and the full interval remain unpaid. Earlier
records below retain their scopes; complete coverage still ends at 8655.

## Full cubic strip

`cubic_strip_payment.md` combines the exhaustive earlier kernel/factor
ledger with the independently supplied d=10 and d=7 counts. Both are
below 274979661292365251, so whole-source alternatives combine by
maximum, not addition. Every 7117..8655 source pays; the next normalized
integer 8656 is outside this coverage. The new d=7 supplier separately
counts ALL components and handles constant/exceptional fibers before
claiming the finite bound. Its node-local audit records the proof and
tiny arithmetic checks. External hand review and original-row/owner
transport remain outstanding; the earlier sections below are historical.

## Weighted residual refinement

The new twelve-entry projection-kernel count pays all but d=7,10,11,
and those pay unless their coefficient dimension is maximal. The stronger
residual dichotomy uses 261013572486287276, not the old smaller paid
constant. Off-curve exceptions remain <=64; the singular pair is priced
inside the cubic. Exact examples prevent deleting the three patterns by
an unjustified carrier-dimension drop. No interval closure is claimed.

## Affine-singularity refinement

The moving projection/kernel bound is already a required supplier.
Checked that the primitive highest binary cube forces h<=17580,
that an infinity-singular one-place cubic is a polynomial graph in that
linear projection, and that the new graph gain plus <=64 exceptions
fits below the paid alternative. Completing the square gives a repeated
root defined over F(X), followed by the explicit (2,3) parametrization.
No square-root field extension or original slope transformation is used.
At most the singular pair is excluded from rational parameter recovery.
The proof first uses the earlier one-place conclusion, then refines it;
the normal form is not a premise of the kernel argument.

## Cubic obstruction continuation

Checked the degree-eight full-kernel surplus 960724-111J, strict
positivity through 8655, and the <=64 off-factor count. The constant
line is populated, so its intercept has degree <J. Its received locus
is split at 600000 coordinates, using (N,A,K)=(600000,74089,8655)
on the line and (457230,66973,8655) off it. Joint Johnson gives
132 and 50 pairs. The off-line cap includes ALL other LOW pairs,
including off-G exceptions, when the line's received locus is large.
The original coupled constant-line resource bound is reused once.

All degree-<=3 factor patterns are exhausted except a geometrically
integral singular cubic with one normalization point above infinity.
The unit supplier pays rational curves with two or more boundary
points; nongeometric irreducible cubics have at most six rational
points. The latter bound uses geometric reducedness at degree <p,
component descent and a nonzero partial derivative. Both nodal and
cuspidal examples show the remaining dimension-one extrapolation false.

Primary exact check PASS: 0.01 s, 11520 KiB RSS. Independent
monomial/asymmetric-row audit PASS: 0.01 s, 10240 KiB RSS. The
next kernel endpoint and one-unit-small recipe caps are rejected;
these are certificate controls, not unsafe-source constructions.
The initial equal-half bound stopping at 8487 was superseded before
banking this continuation, not mistaken for a genuine source obstruction.
External independent hand review remains due.

## Earlier quadratic strip

Verdict: NO ISSUE at the printed restricted-source scope; external
independent hand review is outstanding.

Checked: kernel over the original field; entire complete union; strict
weighted root bound; primitive Gauss division; additive weighted degree;
all interpolation dimensions positive; gcd of ALL equations, not one
chosen equation; coprime residual equations over the infinite function
field; Bezout exceptions; all quadratic factor types; disjoint received
coordinate sets for nonparallel constant lines; the intersection pair
charged once; unchanged original slope labels; coupled resource used
once; no claim at the failing next endpoint.

The 43-pair bound has a direct selected-agreement/Cauchy proof independent
of the earlier scalar compiler. Both arithmetic checks use small integers
and constant space. Four hostile checks reject endpoint 7117, cap 42,
a one-unit underpayment, and omission of an exceptional pair allowance.
These test arithmetic/contracts, not universal algebraic geometry.

Remaining gaps are outside the theorem: J>=7117, higher original ranks,
and the upstream active-architecture coverage/owner bridge.
