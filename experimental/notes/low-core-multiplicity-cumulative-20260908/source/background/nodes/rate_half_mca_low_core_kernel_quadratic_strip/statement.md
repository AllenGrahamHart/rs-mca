# Full-kernel source coverage through 9821

Status: PROVED, subject to independent external hand review.

The [double-point cubic-tail theorem](double_point_cubic_tail.md) now
pays EVERY source on 9527..9821 at N<=274778805314695460,
reserve 201922796699627. It retains the actual degree<=3 cover,
<2A factor height and 256 exceptions. Cumulative LOW accounting pays
d=7 at low height; the component/GP estimates pay the other cubic
cases. A large factor line is handled by counting its bounded-degree
complement on the actual smaller agreement domain.

Full normalized coverage is therefore 4801..9821 at the previous
uniform bound 274979661975561635. This adds 295 J-values with no
assumed source cover, new node, edge or original-red promotion. The
remaining range is 9822..169999. Higher original ranks, original-source,
near/owner transport, unrestricted endpoints and both prizes remain open.

## Preceding coverage through 9526

The paragraphs below record earlier stages, not the current unpaid set.

The [double-point strip theorem](quartic_strip_payment.md) closes EVERY
canonical normalized source on 8764..9526, without a curve hypothesis:
N<=274979661975561635, reserve 1066135833452. The prior full W8
factor dichotomy supplies either its already-paid alternative or one
quartic with <=64 off-curve pairs. Multiplicity interpolation now bounds
ALL rich pairs on that quartic by 68, regardless of genus, singularities,
projection kernel or isolated coefficient components. Its original source
total is <=4194117119656075; the two whole-source alternatives combine
by maximum, with original labels, HIGH resource and near retained.

Thus full normalized coverage is 4801..9526, adding 763 J-values. The
remaining normalized range is 9527..169999. The quartic-only price
extends to 9821, NOT arbitrary-source coverage. Higher original ranks,
original-source/near/owner transport, unrestricted endpoints and both
prizes remain open. One PROVED generic supplier is added upstream of
the finite consumer; no original red is promoted.

## Earlier refinements and interval stages

The following paragraphs record their original scopes. The quartic
restrictions are no longer unpaid cases on 8764..9526 and are not needed
for the new closure, which uses the original 64-pair dichotomy directly.

The [multiplier-space refinement](quartic_multiplier_geometry.md) now
controls the remaining quartic on 8764..9526. Its leading weighted
excess H has the precise two-piece ceiling (805 at 8764, <=9517
overall). Off-curve LOW_500 pairs number at most 0/1/2/3/4 on the
successive intervals ending 9014/9276/9444/9524/9526. The complete
LOW core union has size >=2009300-110J. With no off-pair its received
values lie on G and the full kernel is exactly G times all allowed
multipliers. This uses the ENTIRE kernel, not a selected relation.

The [moving projection-graph payment](../rate_half_mca_cancelled_low_core_relation_payment/quartic_projection_graph_payment.md)
pays every quartic polynomial projection graph with d>=6, and every
such graph with d>=4 through 9294. Even one off-curve pair pays d>=4
through 9526 by supplying forbidden on-graph agreements. The only
unpaid projection-graph types can have d<=2; d=3 with coefficient
dimension one; or d=4/d=5 of dimension one/two, respectively, where
J>=9295,h>=1533 and all LOW pairs lie on G. General non-projection
quartics remain. No new interval, node, edge or original red closes.

The [LOW-101 strip theorem](low_cutoff_strip.md) now pays EVERY source
on 8656..8763 by N<=274903465748372176, reserve 77262363022911.
It changes the accounting cutoff, not the raw selection, and keeps both
LOW and HIGH contributions to the same completed-basis resource. The
full kernel forces degree <=3 again, and the adjusted d=7 component
bound pays all resulting cubic cases. Thus 108 new J-values are closed:
the combined normalized 4801..8763 interval pays at the earlier uniform
bound 274979661292365251. The unpaid range now begins at 8764.
The quartic statements below remain true on their printed intervals,
but only their 8764..9526 portion remains unpaid overall.

The new [quartic restriction](quartic_obstruction.md) proves on EVERY
source with 8656<=J<=9526: either N<=274979661975561635<B,
or all but <=64 LOW pairs lie on ONE geometrically integral quartic
over F(X), of primitive weighted degree <J+66972. The full degree-eight
kernel forces degree <=4. The [height-aware line split](height_aware_line_split.md)
and extended cubic counts pay all reducible degree-four patterns, including
two conics and a cubic plus any line. The reserve of the paid alternative
is 1066135833452. This narrows 871 J-values but does not close them.
The [split-product payment](../rate_half_mca_cancelled_low_core_relation_payment/quartic_split_product_payment.md)
now excludes quartics x*y^3=R!=0 after constant invertible factor
directions and rational X offsets from this residual: their total including
64 off-curve pairs is <=164828561948185643. The covered full interval
now ends at 8763; the general quartic beyond that endpoint is still unpaid.

The latest [full cubic-strip payment](cubic_strip_payment.md) closes EVERY
canonical normalized source on 7117..8655 at N<=274979661292365251,
reserve 1066819029836, without a curve hypothesis. It combines the
full-kernel classification with the d=10 and d=7 payments. Together with
the quadratic strip below, 4801..8655 is paid; the unpaid normalized
range now begins at 8656. The earlier residual descriptions below retain
their historical scopes but are no longer unpaid on 7117..8655.

Use exactly the normalized source contract of
`rate_half_mca_cancelled_low_core_relation_payment`: the unchanged field
F of size 2130706433^6, n=1048576+J, polynomial degree <J,
agreement m=67472+J, one common eleven-dimensional polynomial carrier,
empty universal carrier core, and the canonical maximal-raw selection on
ALL full-bad supports of the fixed carrier. LOW means raw in 1,...,500;
each represented pair has its COMPLETE joint agreement core. Keep every
original finite slope label, HIGH label, the original margin resource,
and the single original near add-back 134944.

For EVERY such source with 4801<=J<=7116, without a relation or cover
hypothesis, the whole original slope count satisfies

    N_original <= 272837082962714299
               < 274980728111395087 = floor(|F|/2^128).

The reserve is 2143645148680788. The full interpolation kernel forces
all LOW pairs, except at most 100 pairs, onto a polynomial curve of
total pair-degree at most two. Every resulting factor pattern is paid.

This closes this normalized rank-twelve strip, not all rank twelve,
higher original error ranks, an active Grande Finale v4 ledger atom,
an unrestricted safe endpoint, or either prize. The previously open
normalized interval 4801..169999 becomes 7117..169999 at this scope.
No unsafe assertion follows from failure of the dimension test at 7117.

## Proved residual restriction on 7117..8655

The latest [residual refinement](weighted_cubic_residual.md) gives
N<=274979661292365251<B or only d=7,dim Y=3,1301<=h<=4327.
The geometric-progression cubic supplier pays ALL d=10 coefficient
dimensions, whole N<=82951498428798039, including <=64 off-curve pairs.
Dimension four has <=1251 nonsingular pairs and N<=4194118281875211.
Thus the previous second pattern is eliminated without changing the
paid-alternative bound. Earlier theorems retain their weaker scopes.

The [cubic obstruction](cubic_obstruction.md) proves that on this next
interval either N=|Gamma|+134944<=255637082913634099<B, or all but
at most 64 represented LOW pairs lie on one geometrically integral
cubic with an AFFINE singular point, rational geometric normalization
and exactly one point above infinity. Any source with N>B must have
the latter form.

The full degree-eight kernel has common factor degree <=3. A new
asymmetric joint Johnson argument makes every populated constant line
small (<=132 pairs), unless the whole source already pays. All
other cubic factor patterns are paid by the existing conic/smooth-cubic
and new normalization-unit suppliers. Both nodal and cuspidal
one-boundary cubics remain; their polynomial families forbid a blind
extension of the dimension-one bound.

The existing jet supplier now pays the singularity-at-infinity case,
using its forced moving projection height <=17580. The
[explicit remaining normal form](affine_singular_normal_form.md) is
f=f_s+(tau^2-lambda)*(P+tau*Q) over the ORIGINAL F(X), with
P,Q independent. All but <=65 LOW pairs have this nonsingular
parametrization; the additional exception is the possible singular pair.
No polynomial degree bound on tau or free coordinate transport is assumed.

This is a PROVED necessary restriction, not a new speculative condition
or interval closure. Using the stronger on-line agreement avoids the
equal-half certificate's failure at 8488 and reaches the kernel endpoint
8655. The unresolved normalized
interval remains 7117..169999. These new local results are not in the
immutable public quadratic-strip packet at 23844ac8.
