# Audit of the next quartic residual

Refinement: the constant-direction nonzero split-product family x*y^3=R
is now paid by the new projective supplier and the finite consumer's
`quartic_split_product_payment.md`. The geometric fiber degree is
binom(a+b,a)*3^b; four affine-offset cases give <=163644855660 rich
pairs. Its completed source total, including the same 64 exceptions, is
164828561948185643, below the unchanged paid alternative. All scaling
and common-zero cases are charged; arbitrary moving directions and
general integral quartics remain outside the theorem. Two independent
tiny verifier scripts accompany that new supplier. This refinement adds
one PROVED supplier and two req edges through the finite consumer;
the no-new-edge statements below describe the preceding kernel stage.

The conclusion is an unconditional dichotomy at the printed normalized
source, not a paid interval or a proved quartic hypothesis. The current
full interval closure remains 4801..8655.

Hand-audited interfaces:

- Full W8 interpolation, Gauss division and strict root bound are unchanged.
  The g>=5 dimension contradiction is new; the old g<=3 test is not reused.
- Line height lowers on-line pair collisions but raises the off-line
  relation degree. Both effects enter the asymmetric cutoff 592000+16h.
  The large-line alternative bounds ALL off-line LOW pairs, including
  off-G pairs, so their allowance is not added twice.
- The d=10 degree-sixteen double-fiber polynomial includes infinity.
  Its vanishing identically identifies the singular pair, which is excluded
  only from the nonsingular count and later charged. Receiver descent is
  never assumed; original label or defect coordinates are not deleted.
- The d=7 extension keeps the SAME h in slot and agreement budgets.
  The old independent rectangle has negative denominator on this larger
  interval; the new derivative identity is therefore load-bearing.
- Pair counts, not /501-discounted gains, are used with the /5500 base.
  The separately completed large-line case uses its original /501 coupled
  expression. Alternative whole-source bounds combine by maximum.
- Every cubic coefficient component is counted. Smaller dimensions and
  singular/off-curve pairs retain their original degree and label costs.
- All degree partitions <=4 are exhausted; geometrically nonintegral
  irreducible quartics have at most 12 rational pairs in characteristic >4.
  Only ONE geometrically integral quartic remains, not an arbitrary cover.

Checks: `verify_quartic_obstruction.py` and its independent audit reproduce
the kernel coefficients, line denominator envelopes, all cubic prices and
factor sums. New independent checks also accompany both extended cubic
suppliers. These are tiny standard-library arithmetic/algebra controls,
not universal geometry proofs or external independent hand review.

Dependencies are the existing finite consumer and its two cubic suppliers.
No new node, edge, speculative premise, original-red promotion or cycle
is needed. Old public packet 72f99be8 is immutable and contains NONE
of this next-interval continuation. Original-row/owner transport, higher
ranks, the quartic class and both prizes remain open.
