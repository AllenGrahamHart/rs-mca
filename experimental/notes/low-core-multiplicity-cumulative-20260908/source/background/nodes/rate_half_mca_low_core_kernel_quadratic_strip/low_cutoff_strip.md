# LOW cutoff 101 closes another 108 normalized dimensions

Status: PROVED. On EVERY canonical normalized source of this node with
8656<=J<=8763, in the unchanged KoalaBear field and original slope units,

    N=|Gamma|+134944 <=274903465748372176,
    B=274980728111395087,
    reserve >=77262363022911.                         (LC101)

Combined with the existing strip, EVERY canonical normalized source on
4801..8763 is paid at the earlier uniform bound 274979661292365251.
There is no assumed cubic or quartic description of the source. The
remaining normalized range begins at 8764, not 8656.

Use the SAME source, actual eleven-dimensional carrier, empty universal
carrier core, all-full-bad-support maximal-raw selection and complete
joint cores. Do not reselect supports or recompute their raw margins.
Only the accounting threshold changes: LOW_101 has raw 1..101;
HIGH_101 has raw >=102. In particular LOW_101 is a subset of LOW_500.

## 1. Retain both sides of the completed-basis resource

The finite consumer's `low_cutoff_accounting.md` owns this new
resource conversion independently of the kernel argument below.

The existing required completed-basis supplier gives

    sum_gamma w(r_gamma)<=F(J)<=C=23067643444721720934.

Its universal lower bound is 12*67472/(67472+11)>11, so every
LOW_101 record consumes at least 11 units. Every HIGH_101 record
consumes at least 1200 units, as follows. For r>=1200 its truncated
weight suffices. For 102<=r<=1200 the completed weight b(r) has
logarithmic derivative at least

    1/r-11/(67473-r)>0,

since 12*1200<67473. It increases with the agreement parameter m,
whose value is >=67472+11. Bernoulli's product inequality gives

    b(102)>=1224*(1-1122/67473)>1200,
    1224*(67473-1122)-1200*67473=246024>0.

Let L be the ACTUAL LOW_101 label count. The one resource implies

    |Gamma| <=floor((C+1189*L)/1200).                  (1)

Every LOW_101 pair has >=A=J+67371 joint agreements and at most
n-A=981205 finite LOW labels, using the complement of its OWN complete
core. Thus a cover by at most M curve pairs and 64 off-curve pairs gives

    P(M)=floor((C+1189*981205*(M+64))/1200)+134944.    (2)

This is a fresh discount derived from the SAME pointwise resource on
both classes, not an old /501 gain inserted into another HIGH budget.
No curve group is classified HIGH, and near is added exactly once.

## 2. The entire kernel forces degree at most three again

Put w=J-1 and use the full W8 interpolation kernel E on the complete
LOW_101 core union, with strict weighted degree <A. Its dimension is
at least D8-n, where D8=sum_(i=0)^8 (i+1)*(A-i*w). All terms are
positive here; at J=8763, A-8w=6038.

Every kernel polynomial vanishes identically on every LOW_101 pair by
the strict root bound. If its primitive gcd has pair degree >=4, Gauss
division bounds the kernel dimension by

    D_mult4=sum_(i=0)^4 (i+1)*(A-(i+4)*w).

But

    D8-n-D_mult4=972694-111J>=1>0.                   (3)

It is exactly 1 at 8763 and -110 at 8764. Hence the gcd has degree
<=3. Two coprime residual combinations leave at most 64 off-gcd pairs.
This repeats the complete-kernel proof with the ACTUAL new agreement,
not the old failed inequality or an assumed curve cover.

## 3. All lines and conics are affordable

Apply the existing `height_aware_line_split.md` to LOW_500 on this
same source. Either the whole source already pays at 255637083438792239,
or EVERY populated rational line has <=696 LOW_500 pairs and hence
<=696 LOW_101 pairs. There is no transfer of a per-group HIGH budget:
the large-line case is an independently completed whole-source alternative.

In the other case, use the previous direct pair cap 127031877504 for
each irreducible conic. It counts all A_500-rich pairs, so also the
A_101-rich subset. The degree<=3 patterns then cost

    at most three lines: P(3*696)=19223038296266967;
    one conic and one line: P(127031877504+696)=142724777436737573.

Repeated factors, fewer factors, or no gcd factor only reduce these
counts. P includes the off-gcd pairs and one original resource/near base.

## 4. Every irreducible cubic factor is affordable

The existing geometric classification is unchanged. Smooth cubics and
rational multi-boundary cubics have dimension <=1 and the old direct
pair cap 163774741769, giving P(M)=178446579638349529. A nongeometrically
integral irreducible cubic has at most six rational points.

A one-boundary cubic singular at infinity is a polynomial projection
graph. Its primitive weighted equation now gives

    h<=floor((67373-2J)/3)<=16687<17580.

The already proved projection cap 73222472421 therefore applies even
at the old, lower A_500 agreement and gives P(M)=90410701637663229.

For affine-singular one-boundary cubics, the general weighted supplier
uses projection kernel d and r=ceil(d/3), now at

    Q_h=(1048577-h)/(67372-h),
    M<=floor(2^d*3^(22-d-r)*Q_h^r).

For d>=1 use h<=8762/floor(10/(11-d)), with h=0 at d=11.
For d=0 use dimension zero directly. The exact table in the verifier
checks every d outside {7,10}; its largest total is
P(262822344599)=274741711077757253, at d=11, below (LC101).

For d=7 and h<=600, the same weighted expression gives

    M<=floor(2^7*3^12*(1047977/66772)^3)=262988722817,
    P(M)=274903465748372176.                          (4)

For d=7 and h>=601, the required component supplier's `cutoff101.md`
counts <=4622 pairs on each top component and the ENTIRE smaller-
dimensional complement. Its pair bound 255040048958 includes a possible
singular pair, but no off-curve pairs. Thus P(M)=267175680601112099.

For d=10 use the existing next-interval GP pair bound 80530893115;
it already counts every A_500-rich pair on the cubic through J=9526.
The new subset is smaller, and P(M)=97516025857888150. Its dimension-four
count, singular pair and lower-dimensional complement are all included.

These cases exhaust every possible cubic. Since a cubic uses the whole
degree<=3 gcd, it need not be combined with another curve group. Every
LOW pair is assigned once, the off-gcd set is retained, and alternatives
are combined by MAXIMUM. Equation (4) is the largest total, proving (LC101).

## Scope

The new interval has 108 integer J-values. Neither the original field,
row, source quantifiers nor complete support selection has changed. The
raw cutoff changes which records are charged HIGH, not the meaning of
their margins or pair noncontainment. The former cubic proof remains
valid at its original cutoff and interval; it is not extrapolated.

This does not resolve arbitrary normalized J>=8764, higher error ranks,
an unrestricted original-row endpoint, active-v4 owner/near transport,
ordinary LIST or either prize. The previous integral-quartic restriction
now concerns the genuinely unpaid 8764..9526 portion of its interval.
