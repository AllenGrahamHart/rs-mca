# Dependency Sub-DAG

The PROVED continuation `single_fiber_payment.md` extends d=10 payment
through J=9526, with uniform N<=83243563858163463 and <=760
nonsingular pairs in the dimension-four case on 8656..9526. Its
independent checks are `verify_single_fiber.py` and
`verify_single_fiber_audit.py`. The degree-sixteen identity includes
infinity and pays an agreement loss <=21h without changing original
labels. Existing weighted/GP dependencies suffice; no edge or node is
added. The earlier-stage records below retain their original scopes.

```text
mca_polynomial_map_projective_jet_dimension
mca_core_completed_basis_margin_resource
    -> mca_singular_cubic_weighted_incidence_payment
        -> mca_geometric_progression_singular_cubic_payment [this node]
            -> rate_half_mca_cancelled_low_core_relation_payment
                -> rate_half_mca_low_core_kernel_quadratic_strip
```

This node has one requirement, the weighted cubic supplier. It supplies
the carrier identity, normal-form derivation, smaller-dimensional pair
counts, completed-HIGH resource and complete-core label prerequisites.
The curve descent, maximal-family parameter theorem and projective
variable-list count are proved in this node; they are not speculative
conditional children.

The finite consumer owns the incoming requirement from this node.
The full-kernel consumer supplies its own <=64 exceptional-pair coverage,
which is a printed source-class hypothesis here, not a reverse dependency.
No evidence edge to a grand root is promoted directly to a requirement.
The global error-rank/router TARGET stays open with supplier evidence only.

There is no source transfer from the finite zero-dimensional selected set
to a positive-dimensional algebraic family. Full coefficient dimension is
tested as one of the existing cases. No field-size reduction is used to
alter the original slope denominator.
