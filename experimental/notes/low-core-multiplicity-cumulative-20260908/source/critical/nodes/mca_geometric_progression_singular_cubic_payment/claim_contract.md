# Claim Contract

The original application is MCA over F of size 2130706433^6, with
normalized `(n,K,m)=(1048576+J,J,67472+J)`, `7117<=J<=9526`,
and an actual eleven-dimensional polynomial carrier V of degree <J.
The pair carrier is `(h_*+V) x V`, with component degree <J.
The empty universal carrier core means exactly

    {x: V(x)=0, v(x)=0, u(x)=h_*(x)} is empty.

It does NOT mean V is basepoint-free everywhere. The common factor q
is nonzero only on the complete pair-core union; other q roots, B roots
and original scalar-defect labels are not deleted.

Each label is a distinct original finite affine slope with a size-m
FULL-code-bad support and an explanation in h_*+V. Canonical selection
maximizes raw mismatch over all such supports and all such explanations.
LOW means this raw maximum is <=500; HIGH means >=501. The existing
selection theorem gives complete minimizing pair cores of size >=m-500
and retains all nonempty scalar defects. Raw mismatch is not the present
projection-kernel dimension d or coefficient height h.

The fixed cubic is geometrically integral over F(X), affine-singular,
with one geometric infinity point smooth there. At most 64 LOW pairs
are off the curve. Its primitive direction (A,B) has d=10 on V x V.
The coefficient locus is the FULL algebraic equation locus in the affine
carrier, extended in constants for geometry. Dimension four is not inferred
from finite points; it is one case of the earlier proved dimension bound.
All smaller dimensions are counted separately by the weighted theorem.

The generic maximal-family theorem uses k(T), k algebraically closed,
char zero or >3, and exact polynomial degree ceilings ten. It does not
assert a rational-polynomial receiver descent. The finite application
additionally satisfies p>=J, as required by its weighted supplier.

The count first bounds nonsingular polynomial pairs via unique degree-three
sections and coordinate-specific lists, then adds a possible singular pair
and the <=64 off-curve pairs. Pair count is converted to original labels
with the complete-core defect ledger. HIGH and original near 134944 are
counted once. A local normalized source total N is not automatically the
original unrestricted row numerator without exhaustive normalization/near
transport; active-owner use also needs its own chronology and charges.

The original two-list constants apply on 7117..8655. The separately
proved `single_fiber_payment.md` gives N<=83243563858163463 on
8656..9526; its maximal-dimensional count has <=760 nonsingular pairs.
Deleting bad/multivalued fibers from an auxiliary count costs at most 21h
agreements per pair and does not delete source coordinates or labels.

No J>9526 assertion, general d=7 theorem, original red closure, arbitrary
ordinary LIST statement, endpoint maximality or prize resolution is claimed.
The generic parametrization and finite payment are hand proofs. Small
verifiers check arithmetic and hostile controls, not universal geometry.
