# Audit and provenance

The completed-HIGH refinement also consumes the directly required
completed-basis resource. Its monotonicity/Bernoulli proof gives weight
>=5500 for every raw>=501, while every LOW weight is merely dropped
as nonnegative. It does not incorrectly transport a raw minimum or add
overlapping resource counts. The h=1301 calculation is a failed sufficient
bound only. This refinement pays d=11, leaving two residual patterns.

This continues the affine-singular cubic residual exported at
1e83800283f850b939c42d4abe1e5aea92bfbef8. It is NEW local mathematics,
not part of that immutable packet or its public #1175 comment.

The weight-two/weight-three coordinate structure is specific to a cubic
with a smooth unique infinity point. A finite power cover reduces its
degree accounting to the already proved ordinary hypersurface lemma.
No external weighted Bezout theorem or speculative height theorem is
assumed. The elementary iterated-intersection proof supplies the height
bound with its exact dependence on kernel codimension.

The key audit catch is the pure-cover issue: a pure degree cover can
introduce constant-x components, invalidating scalar incidence. The proof
instead charges every dimension of the ACTUAL equation locus. The
nonpure degree budget and its adaptive-cut proof are explicit. Branch
points of the power cover are avoided only for the finite counted set;
no code coordinate or singular pair is removed.

The function-field normal form and bounded rational-parameter space are
reconstructed here, so the supplier has no reverse dependence on the
kernel node it helps. Its projective-infinity dimension proof uses the
original output carrier, hence the characteristic guard is p>=K.

The primary rational table and independent degree-six integer-cover
calculation are tiny checks, not proofs of the universal geometry. The
three remaining dimension patterns have exact algebraic controls, and
the height bound has an attained monomial-carrier example.

Independent external hand review remains due. No upstream acceptance,
bankable first-match atom, large experiment or formal certification follows.

Recorded replays: weighted table 0.03 seconds / 11520 KiB, completed-HIGH
check 0.06 seconds / 11264 KiB, independent audit 0.01 seconds / 10496 KiB,
all PASS under RAMguard. Manifest/affected-closure checks PASS; global
preexisting DAG/crosswalk diagnostics are unchanged, not repaired.

The final source-contract audit corrected the GP prefactor guard: q is
nonzero only on the complete-core union, not necessarily on the entire
domain. A V-root with v(x)!=0 is outside the universal core and outside
every allowed joint core, yet may carry defects. The algebraic carrier
identity and all weighted counts remain valid because none divides by q.
