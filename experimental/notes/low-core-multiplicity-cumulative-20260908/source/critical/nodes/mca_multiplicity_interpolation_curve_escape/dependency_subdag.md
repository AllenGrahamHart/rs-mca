# Dependency ownership

    mca_low_core_quadratic_graph_payment (PROVED)
        -> mca_multiplicity_interpolation_curve_escape (PROVED)
        -> rate_half_mca_cancelled_low_core_relation_payment (PROVED)
        -> rate_half_mca_low_core_kernel_quadratic_strip (PROVED)

Arrows here run supplier to consumer. The generic supplier supplies the
plane intersection degree lemma already proved in its companion file.
Hasse-jet counting, substitution multiplicity, Gauss division and the
escape dimension inequality are proved locally. The finite consumer owns
original LOW labels, HIGH resource and near. The full-kernel child owns
exhaustive coverage, not an assumed single-curve description.

The global arbitrary-source router still receives evidence only. No
reverse consumer dependency, speculative child or new router requirement.
