# Claim Contract

The `cutoff101.md` companion uses 8656..8763, 601<=h<=4381 and
joint agreement >=J+67371 instead of the cutoff-500 constants below.
It bounds all cubic pairs by 255040048958, including one possible
singular pair, but includes neither off-curve pairs nor resource/near.
Those costs belong to the finite consumer. The same geometric source
and polynomial models apply; no old bound is extrapolated.

The original object is MCA in the unchanged KoalaBear field
F of size 2130706433^6. The normalized source has
`(n,K,m)=(1048576+J,J,67472+J)`, 7117<=J<=9526, and actual
carrier dimension eleven. All pair components have degree <J.
The empty universal carrier core means exactly
`{x:V(x)=0,v(x)=0,u(x)=h_*(x)}=empty`, not global basepoint freeness.

Labels are distinct original finite slopes with size-m FULL-code-bad
supports and explanations in the fixed affine carrier. Selection maximizes
raw mismatch over all such supports and explanations. LOW raw<=500
pairs have complete joint cores of size >=m-500 and all nonempty scalar
defects retained. HIGH raw>=501 uses the already proved weight >=5500.

The fixed cubic is geometrically integral over F(X), singular affine and
smooth at its unique point at infinity. At most 64 LOW pairs lie off it.
Its primitive direction has d=7 on V x V. For the earlier 7117..8655
bound use 1301<=h<=4327; for the `next_interval.md` bound on
8656..9526 use 1301<=h<=4762. The latter total is
N<=241058823023306107 and does not inherit the earlier 315-pair cap.
d is a projection-kernel dimension and h its coefficient height, neither
a raw margin nor trade height. The full algebraic coefficient locus has
dimension <=3; no infinite family is inferred from sampled finite points.

The per-component parameter normalization is over the algebraic closure
of the constant field. It changes neither source field size nor slope
units. Component count is in original pair-coefficient degree, and every
geometric component is covered. The lower-dimensional part is explicitly
separated from all three-dimensional components before applying its
weighted degree budget. No dimension-two assertion is made for the whole
dimension-three locus.

All original coordinates remain accounted for. A common component
agreement is subtracted from both domain and agreement. Constant fibers
with the wrong received value have empty lists. Other fibers have at most
two or three parameter values. Poles/zeros in the original parameter change
do not authorize coordinate deletion: the transformed coefficient model
is proved polynomial there. Received values are arbitrary at each coordinate.

The source numerator is |Gamma| plus the original near allowance 134944.
Singular and off-curve pairs are charged, and one global HIGH resource is
used. Original-row use requires actual exhaustive normalization and near
transport; active-v4 use additionally requires its owner chronology and
inherited charges. The supplier does not assume every source is cubic;
the finite kernel consumer now provides exhaustive cubic coverage through
8763 using the new cutoff. Its next interval may instead have an irreducible quartic;
that case is not asserted paid by this supplier.

Arithmetic checks are not proofs of component geometry. Independent
external hand review is outstanding. No J>9526, higher-rank, LIST,
unrestricted endpoint or prize closure is claimed.
