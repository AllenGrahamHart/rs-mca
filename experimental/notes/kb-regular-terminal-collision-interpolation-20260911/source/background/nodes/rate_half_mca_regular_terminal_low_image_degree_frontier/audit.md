# Audit

Check actual pair ownership, the binomial jet cost and product-rank guard
before reading any numerical threshold. Interpolation dimension alone is
not sufficient if every solution vanishes on the image curve.

The finite primary and independent audit use different Hilbert evaluations
and reconstruct the strict integer price. Whole-degree monotonicity requires
ell>=M-1 and retained nonnegative v. Original q1, maximum q2, raw weight and
rational L_t are source-pinned. A fully recomputed lower image threshold
must be rejected at affordability, not just at a stale arithmetic field.

Both normal and optimized replays pass104 strict prices and52 image gates.
The independent audit imports no primary or selector and rejects46 corruptions.
A lower eta with all values recomputed fails at strict incidence price;
a smaller ell with all values recomputed fails at the product-rank gate.
The least integer-scaled gap is7140 (actual incidence slack3570), and the
maximum interpolant degree is207. Each focused check used under22MiB and
0.21seconds. Certificate SHA256:
6d5677e763c53bd135cbf244f75c3e974bf97e21a4cee285a53d3ac561332f4e.

Universal algebra and original ownership rely on the written proofs.
Small controls are not official MCA witnesses. Independent external
mathematical review remains due; no low-image or whole-row closure is claimed.
