# Claim Contract

All points, eigenspaces, interpolation conditions and product ranks are over
the ORIGINAL field F. Coordinates have nonzero evaluation on U and lie off
the declared F-eigenpolynomial roots. Every r_p is attained by agreeing
actual pairs at an actual coordinate, not by filling a formal affine hull.

Each unordered actual pair is assigned to at most one retained projective
evaluation point. Fat-point conditions cost binom(r_p,2), not r_p-1.
Their nonzero pullback requires PRODUCT RANK greater than the condition
count; full ternary-form dimension alone is insufficient.

Image equation degree eta, actual polynomial degree and map fibre degree
are distinct. Singularities, inseparability and exceptional finite fibres
are not excluded. Hasse/Taylor coefficients avoid characteristic restrictions.
The result counts core incidences; MCA weight conversion needs the original
owner theorem and an explicit finite consumer. No Prize bound is asserted.
