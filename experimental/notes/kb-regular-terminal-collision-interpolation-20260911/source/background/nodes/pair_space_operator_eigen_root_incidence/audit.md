# Audit

The e=2 proof distinguishes the repeated and simple eigenvalues; rank-one
coordinates need not be roots of both eigenpolynomials. For e=3, common
zeros would have joint rank zero and invalidate the two-budget charge.

A small split-spectrum cube control attains3*D*q2/2 exactly. A partially
split control refutes the naive D*q2 charge. Multiplying the sharp control
by a common factor refutes omission of the common-zero guard. These are
small algebraic controls, not official large-agreement MCA witnesses.

The verifier borrows only the prior small matrix/eigenvector helpers.
The universal proof and finite consumer remain independently reviewable.
Normal and optimized replays pass. The sharp split cube has incidence24;
the repeated-spectrum example has20>16, rejecting the naive D*q2 bound;
the common-zero example has32>30 and is rejected by the required guard.
The actual-subset pair-mass consequence follows from the exact second moment,
not a generic-map or source-exhaustion claim.
External mathematical review remains due.
