# Audit And Limits

The universal colour inclusion and fixed-frame source transfer are hand
proofs. Tests are controls, not exhaustive proofs over deployed sources.

Frozen certificate:2416bytes, SHA256

    5f36b1890c14b70ac7d3c80fc8df2035b284404f85d757a2cb9ef36115049382.

Primary normal/O:0.24/0.35seconds,18048/21368KiB RSS.
Independent normal/O:0.29/0.37seconds,17920/20876KiB RSS.
The initial resumed independent replay also passed in0.36seconds/17536KiB.

The independent audit imports neither the new primary nor the old
shifted primary. It reconstructs the original44 resource from the
pinned unshifted256-branch auditor. Tuple universes are independently
counted by the FIRST bank position, not complement subtraction.
It checks the full-J factor monotonicity,122 exhaustive small tuple
banks and1000 LOW/HIGH cost inequalities.

A concrete rounding counterexample has U=20,beta=7,hit=8:
floor(hit/beta)=1 but floor(floor(U/beta)*hit/U)=0.
Keeping W+1 avoids that invalid transfer.

39 altered scope/ledger certificates are rejected, six with recomputed
totals. This checks the exact declared certificate contract. In particular,
the linear union-overcount mutation is looser mathematics, not itself
a false upper-bound theorem; rejection is not evidence that every altered
ledger is mathematically false.

Fresh inherited unshifted audit:0.85seconds/13184KiB. It checks765
whole-interval branch gates,512 coefficient transfers, the endpoint
floors and24 adjacent wrong floors. The hand ownership proof remains
essential to turning those polynomials into source bounds.

The generic controls pass normal/O in0.04/0.06seconds,10752/13868KiB.
They exhaust two F17 source fixtures,102/40 bad supports and five labels
each, including zero evaluations and a noncanonical raw-one bank violation.

All runs are serial under RAMguard tiny. No Modal, spending, large
enumeration or original-source search. The numerical next-bank failure
is NOT an unsafe witness or optimality proof. Independent external
mathematical review remains due.
