# Dense Low-Height Conic Receivers Pay The Entire Original Source

Status: PROVED locally; independent external mathematical review remains due.

Use any original KoalaBear MCA error-rank12 normalization, any valid
original bad-support/minimizing-pair assignment, the original field
F_(2130706433^6), and

    J9965..21499, (n,K,m)=(1048576+J,J,67472+J), dim V=11,

with empty universal carrier core and original near add-back134944.

Suppose a polynomial Q(X,Y,Z) over F defines a NONSINGULAR projective
conic over F(X), and its original pair-weighted degree satisfies

    max_(Q_ij!=0) [deg_X Q_ij+(i+j)*(J-1)] <=2J.

Let E be the actual coordinate exception set

    E={x:Q(x,u(x),v(x))!=0}.

If |E|<=276035, the ENTIRE original bad-slope set satisfies

    |Z_bad|<=274980278712737789 <274980728111395087,
    reserve=449398657298.

There is no P1/P2 pair-rank or projection assumption. No arbitrary
conic description is asserted for other sources. All original raw values,
owners and near are retained; the original source is not punctured.

## Primitive-Degree-One Terminal Corollary

Suppose a retained pair family has a common expression

    f=f_*+F(X)*M(X)*y,

where f_* has component degree<J, F is nonzero, M is a2x2 polynomial
matrix with entries of degree<=1 and nonzero determinant, and deg F<=J-2.
If its represented y lie on one nonsingular constant-coefficient conic
and the union U of its COMPLETE ORIGINAL joint cores has complement
at most276035, then the same whole-source bound holds.

This includes a sufficiently dense original-source lift of the local
parabola obstruction. It does NOT assert that every terminal lies on a
conic or has this union size.

The recipe at276036 exceptions exceeds budget; this is NOT an unsafe
source or maximal-safe Prize endpoint. Other rank12 sources, higher
original error ranks, ordinary LIST, active-v4 atoms, the adjacent safe
row and both Prize problems remain open.
