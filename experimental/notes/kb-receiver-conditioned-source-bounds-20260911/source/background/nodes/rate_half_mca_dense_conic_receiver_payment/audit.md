# Audit

The proof is an original-source conic/exception partition, not an MCA
theorem applied to a punctured source. Actual raw and owners never change.
The geometric hypothesis is an explicit receiver condition; no universal
conic cover is asserted. The two classes receive one shared44 resource.

Hand checks:

- Q(X,a,b) for an off-conic pair is a nonzero polynomial of degree<=2J;
  all coefficient/content roots count against that same degree.
- The exception-domain agreement is d-t-J, with positive excess
  d-t-2J>=24431 over original polynomial degree J.
- Joint LIST uses shared polynomial dimension11, not pair rank22.
- The whole-J off-ratio derivative has positive numerator2E0-d+t-1.
- Rank-two, constant-direction and moving-direction conics are exhausted;
  the moving bound dominates throughout the actual q_t range.
- Defect disjointness bounds per-pair RAW WEIGHT, not just label count.
- The telescoping identity pays all original higher raw and adds near once.
- The adjugate terminal lift keeps roots of F and det M, preserves conic
  nonsingularity over F(X), and has weighted degree<=2J.

Certificate27338bytes, SHA256:
47fa0cf46238c8647c89a7980dd53f7266e51dfa948124b9aa726c377300c834.

The primary reconstructs43 rows at both exception endpoints and the
original shifted44 resource. The independent audit uses the unshifted
256-branch resource auditor, not the new primary. It checks all conic
types, shared-carrier product inequalities,1000 exact raw identities,
15 polynomial conic lifts and4 retained content/determinant roots.

Thirty-nine mutations are rejected, including seven recomputed source
ledgers with missing classes, wrong shared dimension, raw loss, missing
HIGH, missing near or an omitted cumulative cutoff. The adjacent276036
recipe really exceeds budget; it is not an unsafe source.

Initial certificate construction0.25seconds/18176KiB; independent
normal0.37seconds/19456KiB. Final normal/O replay and graph checks are
recorded in the cycle receipt. These are finite exact checks supporting
universal hand proofs, not an exhaustive official-source enumeration.
Inherited geometric source proofs were read; independent external
mathematical review remains due. No Modal, spending or compute request.
