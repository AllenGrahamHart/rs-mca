# Audit

Hand proof checks entire colour classes in COMPLETE scalar agreement,
not only the old selected support. The distinction is necessary.
Scaling the projective fibre or changing h_* within its affine carrier
preserves the partition.

The tiny verifier exhausts142 full-code-bad supports across two F17
sources, every carrier explanation and every minimizer. The first
has a constant one-dimensional carrier. The second uses span{X},
an affine h_* and an actual zero-evaluation coordinate. Full-code
badness is checked independently by interpolation, not assumed from
positive carrier raw.

Each source has three retained canonically low labels at cutoffs1 and2.
Complete defects, unique global minimizers and owned independent tuples
are checked. The zero source charges its exceptional label globally.
Both exhibit a noncanonical raw-one defect in a three-point colour,
while that label's actual maximum raw is three.

These are finite controls, not official received-word enumeration.
Universal field/degree claims are proved in proof.md.
Initial replay0.05seconds/10880KiB. Resumed normal/O replays pass in
0.04/0.06seconds,10752/13868KiB RSS. Graph checks are recorded in the
cycle receipt. External mathematical review remains due.
