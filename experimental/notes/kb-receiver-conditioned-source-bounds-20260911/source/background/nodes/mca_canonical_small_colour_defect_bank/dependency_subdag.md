# Dependency Sub-DAG

Required PROVED supplier:
mca_maximal_margin_complete_core_selection.

The receiver-colour identity is proved directly in proof.md, using the
same definitions as mca_receiver_fiber_peeling. No child-source LIST
payment from that node is used here.

Finite consumer:
rate_half_mca_small_colour_source_payment.

There is no direct requirement edge into a critical TARGET and no
conjectured resource lower bound. The generic tuple statement accepts
a supplied proved cost; the finite consumer instantiates it explicitly.
