# Couple The Trimmed Core Cost With Original HIGH And Singletons

Use the SAME fixed-frame canonical selection and incidence normals as
the required bounded-colour source theorem. Its source-transfer proof
precedes any cap hypothesis: full-code badness gives record rank12,
the literal empty universal carrier core makes joint-core evaluations
nonzero, and independent tuples determine original labels uniquely.
The receiver, dim11 carrier, degree and labels stay fixed. New raw or
explanation rank is not claimed equal to the original assignment.

Put R=1048576,d=67472,D=d-2=67470,J0=9965,J1=21499,
M=D+J and U(J)=(R+J)_falling_12.

## 1. Keep Both Costs Inside The SAME Core

Let F_10(k)=P_10(D)*min q(k-10) be the required full128-branch
rank10 basis lower bound, P_10(D)=prod_(i=1)^9(D+i).
It is nonnegative and nondecreasing on10<=k<=J1.

If e43<=E with43<=E<=4639, every class larger than43 has size<=E.
For every record of raw r<=2, its complete joint core has at least M
points. The generic trimmed-profile corollary gives

    B_11(core)>=(M-E)*F_10(J-43)+E*F_10(J-E)=:G_E(J).

There are at most E HEAVY coordinates in the whole domain, so charging
E inside every counted core only weakens this bound. No heavy group
is given a separate tuple budget or assumed to exhaust the core.
J-E>=5326>10, and M-E>0 throughout the parameter box.

All r actual defects can be inserted in12 recoverable positions of
every complete-core basis. Thus r<=2 owns at least r*beta_E(J)
original independent tuples, where beta_E=12*G_E.

## 2. Prove All J And E, Not Just Numerical Endpoints

Every branch in the already certified rank10 tree has nonnegative
coefficients in x=k-10. In addition, this node verifies exactly

    q'(0)*(R+J0-11)>12*q(J1-10).                    (STRONG)

For ANY k in[10,J1], increasing q,q' imply

    q'(k-10)/q(k-10)>12/(R+J0-11)>=U'(J)/U(J).

In particular both F_10(J-43)/U(J) and F_10(J-E)/U(J) increase
with J: each branch ratio increases, and so does its finite minimum.
Multiplication of the first by the positive increasing M-E, and the
second by the nonnegative constant E, preserves increase. Their sum
G_E/U therefore increases. Thus U/beta_E decreases on the WHOLE
J interval for every fixed E in the printed range.

Increasing E only lowers G_E. Indeed F_10(J-E)<=F_10(J-43);
if f>=e, replacing F_10(J-f) by the no-smaller F_10(J-e) shows

    G_f-G_e<=(f-e)*(F_10(J-e)-F_10(J-43))<=0.

This is an order argument for a minimum of branches, not an unjustified
derivative across its switches. Hence W_E=floor(U(J0)/beta_E(J0))
is nondecreasing in E, and U(J)/beta_E(J)<W_E+1 everywhere.
The exact values are

    W_43  =541038523546369464,
    W_3000=544892386482580591,
    W_4639=549959818359688305.

The128(STRONG) gates are proved with exact rational arithmetic.
The parent supplies all127 input-branch shape gates and the full tree.

## 3. Original HIGH And The Singleton Bank

The required original resource gives beta44(J)*min(r,44) tuples for
EVERY raw value, and U/beta44<W44+1 with W44=581590844909990298.
For r>=3 this costs at least3*beta44. Set

    eta_E=min(beta_E,3*beta44/2).

Every retained record therefore owns eta_E*min(r,2) tuples on one
universe. As floor(2*(W44+1)/3)<W_43<=W_E,

    sum min(r,2)<=W_E.                             (MASS)

Canonical raw-one defects lie in singleton receiver-colour classes,
after the generic zero-label peel. It removes at most J-11<=21488
labels GLOBALLY. At zero evaluations with v=0 no defect is possible.
For raw1 all tuples from the improved core cost touch the singleton bank.

Let n_min=1058541 and H(n,b)=(n)_12-(n-b)_12. The bank fraction
H(n,b)/(n)_12 decreases with n and increases with b. Therefore, if
sigma1<=b in the stated rows,

    N1<=L_E(b)=floor((W_E+1)*H(n_min,b)/(n_min)_12).

Keep W_E+1 BEFORE restricting the rational tuple universe; an already
floored W_E cannot be multiplied by the bank fraction safely.

Since2*N=sum min(r,2)+N1 for positive raw, the whole source obeys

    floor((W_E+L_E(b))/2)+21488+134944.              (PAY)

The certificate gives the two stated totals. Adjacent E4640,b0,
E3000,b825 and E4639,b1 fail this recipe; none is an unsafe source
or an optimal mathematical threshold.

## 4. Full-Profile Improvement Beyond The Named Classes

For ANY actual receiver-colour profile w, use the generic trimmed
cost Phi_M(w) instead of M*F_10(J-max w). The old pointwise source
proof applies with

    beta_trim=12*Phi_M(w),
    alpha=max(beta_trim,beta44),
    eta=min(alpha,3*beta44/2).

Keep its ACTUAL upper universes

    U_col=12!*e_12(w),
    V_col=12!*(e_12(w)-e_12(w with singleton classes removed)).

Then W=floor(U_col/eta), L=floor(V_col/alpha) give

    source<=floor((W+min(W,L))/2)+(J-11)+134944.      (PROFILE)

The complete-core contraction supplies every new LOW tuple cost;
same-colour proportional normals supply the upper universes. This
only strengthens the old pointwise bound, since Phi_M is at least the
old maximum-only cost. No arbitrary profile is asserted realizable,
and no guarantee that(PROFILE) fits budget is inferred.

The core profile is obtained by filling the required mass from
largest colour classes first, not by declaring all cores to have the
largest fibre. Universal control of the remaining profile/core geometry,
higher error ranks and BOTH Prizes remains open.
