# Audit And Scope

Certificate2924bytes, SHA256

    851c08cdff1ce32262bc2bb73e56b6ed610028403f29f4633a32576757a4c012.

Primary construction0.37seconds/18304KiB.
Frozen primary normal/O0.26/0.37seconds,18688/21412KiB.
Independent initial normal/O0.35/0.44seconds,18432/20980KiB.
With the final profile controls, normal/O0.52/0.54seconds,18304/20968KiB.
Initial independent replay0.42seconds/18048KiB.

Both implementations reconstruct the COMPLETE128-branch rank10 tree at
cutoff2. There are127 input branches, four whole-interval Bernstein
shape tests each, and128 uniform C/J derivative gates. The primary uses
shifted polynomials; the auditor reconstructs unshifted degree through
the pinned OLD independent polynomial helpers. It imports neither the
new primary nor the old shifted primary. The all-HIGH44 resource is
independently reconstructed from the old256-branch tree.

The auditor counts singleton-hit tuples by their FIRST bank position.
3000 toy LOW/HIGH costs verify the effective minimum and the exact
raw-two source identity. Max-instead-of-min and premature integer-floor
controls exhibit why those changes are invalid in general.

A separate F97 geometry fixture uses V=span{1,X^4,X^8}. Its full
evaluation fibres have size4, but each splits into two receiver colours
of size2. A complete pair core of48 points has97152 actual independent
ordered triples. Full core-fibre contraction lower-bounds them by88320.
This checks the distinction between receiver colours, full evaluation
fibres and fibres inside the counted core. It is a basis-geometry control,
not an original rank12 MCA source.

37 mutated scope/ledger certificates are rejected, including six with
recomputed totals. These verify the exact certificate contract, not
the claim that every rejected variant is a false theorem.

The profile appendix has a hand proof for arbitrary actual colour sizes,
including C>286. Fifteen exhaustive tiny weighted partitions compare
elementary-symmetric counts with distinct-class ordered tuples and
singleton hits. They do not prove that arbitrary profiles fit the Prize
budget or that all distinct-class tuples are independent.

Fresh generic canonical F17 controls pass in0.01seconds/10880KiB:
102/40 full-code-bad supports, complete canonical minimizers, zero
labels and an actual noncanonical raw-one bank violation.
The parent HIGH proof was freshly replayed in the preceding cycle;
its source bytes remain pinned and unchanged.

Only tiny route-deciding diagnostics preceded this proof. We did not
launch a large colour histogram or original-source census. All runs are
serial under RAMguard tiny. No Modal, spending or compute request.
Finite tests supplement the hand source-transfer proof; independent
external mathematical review remains due.
