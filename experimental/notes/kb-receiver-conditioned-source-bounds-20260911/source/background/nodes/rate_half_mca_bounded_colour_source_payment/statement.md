# Bounded Receiver Colours And Sparse Singletons Pay Original Sources

Status: PROVED locally; independent external mathematical review is due.

Fix the original KoalaBear MCA row F=F_(2130706433^6), length2097152,
degree1048576, agreement1116048 and target2^-128. Keep any original
post-near error-rank12 normalized source with

    J9965..21499, (n,K,m)=(1048576+J,J,67472+J),
    fixed h_*+V, dim_F V=11, empty literal universal carrier core.

On each NONZERO projective evaluation fibre ev_x=lambda_x*ell,
partition by the receiver colour

    ((u(x)-h_*(x))/lambda_x, v(x)/lambda_x).

Let C bound EVERY such colour-class size and sigma1 count coordinates
in singleton colour classes. These are receiver colours, not evaluation
fibres, and not only classes represented by one selected pair.

Each of the following hypotheses pays the ENTIRE original bad-slope set:

| Max colour size | Max singleton coordinates | Whole source bound | Reserve below B* |
| --- | ---: | ---: | ---: |
| 2 | 1717 | 274979228268047446 | 1499843347641 |
| 43 | 1465 | 274977964220092532 | 2763891302555 |
| 286 | 0 | 274980091354143171 | 636757251916 |

Here B*=274980728111395087. Each row covers ALL11535 original degrees
and imposes NO P1/P2 pair-rank or projection condition.
It includes every HIGH record, global zero-evaluation labels<=21488
and original near134944, each charged once.

In particular every source whose nonzero receiver-colour classes have
sizes2..286 is paid. No bound on sigma43 is imposed. For C<=43,
sigma43>=1048587, so the preceding sigma43<=53067 source theorem
cannot supply this conclusion.

Canonical selection stays inside the fixed original frame; no original
error-rank test is repeated after changing explanations. The proof
couples an improved LOW core cost with the proved original HIGH cost
on ONE tuple universe, then prices canonical raw-one defects in the
singleton bank. There is no universal bounded-colour assertion.

The exact recipe also gives a two-parameter sufficient condition for
2<=C<=286, as printed in proof.md. Failed adjacent recipes are not
unsafe sources, optimal true colour thresholds or Prize endpoints.
The [profile resource](profile_resource.md) additionally bounds EVERY
such original source using its actual colour sizes, without C<=286
or a singleton-count premise. Its elementary-symmetric tuple universe
and pointwise core costs need not fit the Prize budget; no universal
payment is inferred from that unconditional counting inequality.
Exhaustive original source coverage, higher error ranks, ordinary LIST
and BOTH Prize problems remain OPEN.
