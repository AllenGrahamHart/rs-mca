# Source-Collision Counting Pays 500 Whole Rank-Twelve Degrees

K3 / DIRECT / LOCAL_ONLY. KoalaBear MCA, q=2130706433^6,
(n0,k0,m0)=(2097152,1048576,1116048), target 2^-128,
B*=274980728111395087. Codex acting for AllenGrahamHart, 2026-09-08.
Complete local proofs submitted for independent mathematical review.
Neither an active-v4 atom nor unrestricted Prize closure is claimed.

## Exact Contribution And Integration Point

For EVERY normalized source in the printed contract,

~~~text
(n,K,m)=(1048576+J,J,67472+J), actual polynomial carrier dimension 11,
full-code-bad witnesses, empty universal core, J=22500..22999:

|Gamma|+134944 <= 274938028871508001,
reserve below B* = 42699239887086.
~~~

There is no additional source-density, fiber-count, curve-cover or
coefficient-field premise. Nonuniversal carrier zeros are retained.
The normalized domain need not be smooth; the same original field and
finite slope denominator remain in force.

For an original complete post-near selection of error affine rank twelve,
J=k0-g with g the intersection of COMPLETE scalar agreement sets. The
new theorem pays all g=1025577..1026076, not merely one geometry class.
The cumulative all-carrier high interval becomes J=22500..169999, with
cap 274938028871508001. The larger union cap must replace the preceding
274929007493481160. The overall maximum with the other paid intervals
remains 274979661975561635, reserve 1066135833452.

Any over-budget original line must, for EVERY complete post-near selection,
have either error affine rank >=13 or rank twelve with

~~~text
J=9941..22499, g=1026077..1038635, 12559 integers.
~~~

The remaining degree-6000 fiber restriction is useful on J=21000..22499;
the earlier clustered-arc gate is additionally useful on 20481..22499.
Their full historical theorem scopes are unchanged. This result fits the
same-source balanced-core slope-counting program of #1175, not a new
owner charge to add to other whole-line alternatives.

## Three Proof Suppliers

### 1. Flat-Split Original Root Moment

[Statement](source/critical/nodes/mca_flat_split_root_moment_basis/statement.md),
[proof](source/critical/nodes/mca_flat_split_root_moment_basis/proof.md).

Let a complete core have N=D+K nonzero evaluation points, actual rank s,
and original projective fibers c_i bounded by h. A complete rank-t flat
has a points. Its annihilator quotient has rank ell=s-t and degree k=K-a.
Writing S2=sum c_i^2 gives

~~~text
S2 <= a*h + min((N-a)*h, Q(N-a,k,ell)),
Q(v,k,1)=v^2,
Q(v,k,ell)=max(v*(k-1)/(ell-1), v+(k-ell+1)*(k-ell)) for ell>=2.
~~~

The quotient fibers COARSEN the original outside fibers; quotient fibers
are not assumed to obey h. Feed this improved first moment into the
original frozen hereditary convex basis product F_(s-1):

~~~text
B_s >= N*F_(s-1)(D,K-S2/N).
~~~

If full-source ordered collisions are at most C, then S2<=N+C as well.
This is a root-step improvement, not recalibration of the profile at a
fictional real child degree. The real product argument must be >=1.

### 2. Bonferroni Resource After One Secant Exception Set

[Statement](source/critical/nodes/mca_fiber_collision_bonferroni_resource/statement.md),
[proof](source/critical/nodes/mca_fiber_collision_bonferroni_resource/proof.md).

On the FULL source, let a_i<=A be nonzero projective fiber sizes and
T=sum a_i(a_i-1). The existing secant theorem gives one exceptional set
E, |E|<=z+T/2. Outside it, every owned independent r-tuple uses distinct
nonzero fibers. The following explicit upper resource includes both
overlapping and disjoint pair-event corrections:

~~~text
U(n,r,A,T)=(n)_r - binom(r,2)*T*(n-2)_(r-2)
  +3*binom(r,3)*(A-2)*T*(n-3)_(r-3)
  +3*binom(r,4)*T^2*(n-4)_(r-4).
~~~

All falling factorials count distinct coordinates. Carrier zeros may be
padded by singleton classes to upper-bound the resource, but are not
new agreement points. A printed derivative inequality guarantees that U
decreases over the ENTIRE possible range 0<=T<=n(A-1).

### 3. Exhaustive Finite Source Split

[Statement](source/critical/nodes/rate_half_mca_collision_profile_interval/statement.md),
[proof](source/critical/nodes/rate_half_mca_collision_profile_interval/proof.md),
[primary](source/critical/nodes/rate_half_mca_collision_profile_interval/verify.py),
[independent audit](source/critical/nodes/rate_half_mca_collision_profile_interval/verify_audit.py).

First, a full source fiber of size >=J-6000 already pays the whole source.
Otherwise every fiber is <=J-6001. Split at T0=180000000:

- T<=T0 improves every LOW core's original root moment, without removing
  labels or charging secant exceptions.
- T>T0 uses U at T0 and ONE conservative secant/zero-label exception
  allowance. LOW costs use the unrestricted flat-split moment.

In both branches, all maximizing-flat ranks 1..10 and sizes are included.
Earlier signed arbitrary-flat completions and guarded quotient products
may improve the same record's lower cost. Negative tangent coefficients
use the upper extension ratio. All HIGH raw margins remain accounted for.
LOW and HIGH consume ONE shared resource divided by their minimum cost.

Whole-box bounds, not source samples, cover 32 degree blocks of width 16
and 40960 rank/size boxes. Both branches have exact rational costs and
floors. The two implementations agree on every certificate entry:

~~~text
SHA256 5ace68871be07e08317ea359ac9a73f80c7a9b9a325e5390a0d6e66b9ecdc5a2
maximum 274938028871508001
at low T, J=22628..22643, t=5, a=18059..18235.
~~~

This is the maximum of a proof certificate, not an actual extremal line
or a claim that the chosen bounds are optimal. Each engine rejects
163840 adjacent wrong floors; the primary also rejects eight broken
covers. The integer audit does not import the primary or Fraction; it
reuses the preceding INDEPENDENT signed-completion and quadratic helpers.

## Hand-Audit Checks And Remaining Scope

[Assembly section 15](source/critical/nodes/rate_half_mca_rank_twelve_paid_interval_assembly/proof.md)
checks the original error-rank gauge, complete shared core, saturated
full-code-bad witnesses, original finite labels and field. Only universal
core cancellation is allowed. Nonuniversal zeros and the enclosing
carrier survive; remaining explanations need not span that carrier.
The single original near allowance is included once in each alternative,
not summed repeatedly or combined with independent budgets per core.

Dependencies retain the prior every-carrier theorem for the union claim,
not just its weaker fiber gate. The atlas and scalar census are separate
inventories and are not paid-interval premises. Generic moment controls
include 312 actual complete flats (89 nonmaximizers); collision controls
check 6517 small histograms and both necessary overlap terms.

The [publication validation](VALIDATION.md) records bounded replays on
the frozen sources. Arithmetic checks and graph/hash validation do not
formally certify the general hand proofs or constitute external review.
Hughes's resource, rank/near and support machinery remain credited
prerequisites; no broad literature novelty is claimed for moment or
inclusion-exclusion techniques.

Still open: the stated lower rank-twelve gap, original ranks >=13,
unrestricted MCA at agreement 1116048, ordinary LIST and both full Prize
problems. No new false-source witness, v4 atom or changed safe endpoint
is asserted. The newer weighted-collision prototype and its incomplete
generic packet, all exploratory scripts and unrelated research changes
are excluded from this export. No large compute request is needed.
