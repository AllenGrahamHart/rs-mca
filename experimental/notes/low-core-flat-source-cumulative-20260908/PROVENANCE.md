# Provenance And Dependency DAG

Agent: Codex acting for AllenGrahamHart, 2026-09-08. K3 / DIRECT /
LOCAL_ONLY. Complete local proofs submitted for independent review.

## Pins And Attribution

- Main: `93fba1be3f3299b0ba4708d88715377bbb656e45`.
- Hughes's open #1175: `6c59f9aa75b897c9274e94c7aa8864acd26a85ea`.
- #1174: `1b613fc669158a690a52b64f0eeb440f10672f1e`.
- Divergent near-reduction #1160: `c5f4ea7a0c78828c901ae5f3428894a8b2e2806b`.
- Previous companion: `6562b80720397fbc5f0815d53715c72c15c8f6f5`.
- Codex origin HEAD: `3b51e86d2595f28a02e842c81effca1dfcf98e77`.
  This is a dirty snapshot: per-file hashes, not HEAD alone, freeze the proofs.
- Canonical Fable committed pin: `0dd5b324482194208be0289f76ed3f0817648a46`.
  Read-only; no audit or alteration of its entire uncommitted tree.

Main and #1175 were checked live and remain unchanged. The five most recently
updated open PRs and the latest #1175 discussion were checked for coordination;
this is not a fresh mathematical audit of the entire queue. The current
workboard permits direct original-numerator theorems outside the v4 ledger.

Hughes's nonuniform margin resource, support-wise near correction, error-rank
gauge, raw-low descent and gluing reductions are credited prerequisites or
adjacent results. Their separate unmerged branches are not represented as
an already integrated source tree. The local reconstructed suppliers are
included with their own proofs and upstream provenance. Older evaluation-flat,
clone-tolerant basis and polynomial root arguments are antecedents, not new
inventions. No general literature novelty claim is made.

## New Contribution

1. Exact complete-fiber contraction of polynomial evaluation bases,
   a rank-three quadratic seed and a two-profile quadratic induction.
2. Eight fixed rational certificates pay EVERY normalized carrier on
   53000..65000 by 274171207928811099, including the original allowance.
   The union with the earlier high interval is 53000..169999, at the
   unchanged total 274929007493481160.
3. The existing original-source assembly now leaves J=9941..52999,
   removing 12000 additional integer values without a new source premise.
4. An actual raw-one construction limits uniform isolated-core pricing
   with the coarse global resource. It does not construct an unsafe line.

Maximum-density counts, the original-source gauge, saturated-support
transport and single near add-back are already in the parent contribution;
they are not claimed as new in this extension. The elementary polynomial
contraction and concavity proof imports no external extremal theorem.
Publication makes the generic contraction's r>=2 scope explicit; the
finite seed and ladder use ranks 3..11 and are unchanged. Higher-rank and
remaining rank-twelve classes are not paid. No active compiler integer is set.

## Dependency Structure

~~~text
nonuniform support-margin resource -> completed-basis resource
    -> maximum-density flat/core-basis resource
    -> every-carrier interval 65000..169999

completed-basis resource -> exact fiber contraction / quadratic seed
    + every-carrier interval 65000..169999
    -> quadratic interval / union 53000..169999

prior full-kernel, multiplicity, curve and raw-margin suppliers
    -> every-carrier lower strip 4801..9940

near correction + scalar descent + padded Johnson + prior completed bases
    -> lower-rank payment and rank-twelve common-core extremes

common-core saturated-support transport
    + common-core extremes + high interval + lower strip
    -> original rank-twelve paid-interval assembly
    -> restriction J=9941..52999 or original rank >=13
~~~

`SOURCE_MANIFEST.json` includes the exact 38-node requirement inventory
of the assembly in `interval_extension_requirements`. The exporter checked
that these local nodes are PROVED and acyclic, and the replay checks
reachability, acyclicity and the presence of their statements/proofs.
This is a source inventory, not formal verification of the mathematics.
The local assembly gives evidence to the unresolved original-source router;
it does not promote that TARGET or the Prize roots.

## Snapshot Boundaries

Relative to parent 6562b807, 401 source files remain byte-for-byte unchanged.
Eighteen sources are added and five assembly sources are revised, for 424
sources. Only small `.md`
and `.py` sources are vendored; no generated DAG, large output, credentials,
exploratory contraction script, package environment or Modal job is included.
Not every historical helper is part of the selected bounded replay.

The prior sixteen older statement/contract/result/dependency descriptions have since
changed locally. The manifest lists them in `retained_historical_descriptions`;
they remain frozen here. Their narrower historical summaries are not the
authority for the new results. In particular the low-strip extension is
proved in its already vendored `raw_weighted_strip.md` and earlier interval
companions, not inferred by relabelling an older statement. The lower-strip
node's local promotion from background to critical does not change the
published source paths or proof bytes.

In addition, historical maximum-density and normalized-consumer summaries
retain their earlier 65000 start and 64999 gap endpoint. Their valid
narrower theorems remain suppliers; the current contraction and assembly
statements supersede those dated residual descriptions. No historical
summary is used to infer the new interval.

`fiber_contraction_extension` records the immediate parent, its manifest
hash, every added path and each revised source's previous hash. The Git
parent preserves the full earlier assembly. `interval_extension_requirements`
is updated to the current 38-node graph; the older parent metadata remains
provenance for the previous publication stage, not the current inventory.

[EARLIER_PROVENANCE.md](EARLIER_PROVENANCE.md) retains older source-class provenance.
The new contraction and revised assembly links in README are the current
entry points. Source-control custody and review submission do not imply
maintainer acceptance. Inserting the direct result into Grande Finale v4
still requires its separate first-match ownership contract.
