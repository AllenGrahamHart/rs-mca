# Claim contract

- Count distinct finite selected slopes, once each. Each has one exact
  support of size m and a full-code pair-noncontainment witness.
- Use the ACTUAL F-dimension s and untruncated raw minimum r over C'.
- The new count applies only at 1<=r<=d; otherwise retain the old count.
- g and z belong to the fixed global incidence representation, not a
  separately optimized source per selected slope.
- Take max of recordwise tuple lower bounds. They overlap and cannot be added.
- The uniform alpha floor requires d>=s(s+1). No field extension,
  subfield restriction, smoothness, chosen core union or source relation.
- Sharpness concerns a single support's basis factor. It does not certify
  a whole extremal received family, optimal MCA spectrum or either prize.
