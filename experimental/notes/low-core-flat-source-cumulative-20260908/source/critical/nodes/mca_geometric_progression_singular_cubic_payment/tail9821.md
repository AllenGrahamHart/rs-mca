# The d=10 source class through J=9821

Status: PROVED on the same canonical normalized source with
9527<=J<=9821, LOW_500 and <=256 off-cubic pairs. No interpolation
factor-height assumption is needed. Then

    N<=83344622367408351.                             (GP-T)

The existing GP carrier theorem gives h<=floor((J-1)/10)<=982.
If the coefficient dimension is four, the generic maximal-family model
and single_fiber_payment.md cover all nonsingular bounded pairs with
degree-three parameter sections. Its degree-sixteen double-fiber and
five-bad-fiber argument loses at most 21h agreements per pair, retaining
all original coordinates in the source ledger.

Use the valid uniform relaxations

    n_max=1058397, a_min=76499, h_max=982,
    a1_min=a_min-21h_max=55877, collision_max=2946.

The single-list Johnson denominator is 4201567>0, and its floor is

    floor(1058397*(55877-2946)/4201567)=13333.

Together with one singular pair, the maximal-dimensional case has at
most 13334 curve pairs. Smaller coefficient dimensions use the same
weighted theorem at the actual height ceiling:

    M<=floor(2^10*3^9*(1047595/65991)^3)=80633845345.

This latter number already includes the singular case and dominates
13334. With all 256 off-curve pairs and one original HIGH/near base,

    N<=4194116990084347+981604*(80633845345+256)
      =83344622367408351.

The generic carrier, curve-descent, maximal-model and double-fiber proofs
do not depend on the old numerical endpoint 9526. Their parameter degree,
polynomial coefficient bounds and actual field remain the same here.
Only the honest agreement/domain/height envelope has changed. This is
a source-class theorem, not an arbitrary-source cover or a prize closure.
