# Dependency subgraph

The later `completed_high_weight.md` additionally requires
`mca_core_completed_basis_margin_resource` directly. That proved resource
gives HIGH weight >=5500 without changing the canonical source or selector.
It pays d=11 and d=7 at h<=1300, leaving only the two printed patterns.

    mca_low_core_quadratic_graph_payment
        -> mca_polynomial_map_projective_jet_dimension
            -> mca_singular_cubic_weighted_incidence_payment
                -> rate_half_mca_cancelled_low_core_relation_payment
                    -> rate_half_mca_low_core_kernel_quadratic_strip

Arrows here are supplier-to-consumer requirements. The new node requires
the jet supplier, inheriting the ordinary hypersurface-degree theorem,
scalar incidence argument and original margin resource transitively.
The finite consumer owns the new incoming requirement.

The height lemma, nonpure degree budget and finite-cover weighted count
are proved locally. The function-field cubic normal form is independently
derived here, without requiring the full-kernel consumer's normal-form
file. Thus there is no reverse dependency or proof cycle.

The full-kernel node continues to send only evidence to the open global
rank/support-distance router. No speculative residual child is created.
