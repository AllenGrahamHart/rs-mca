# Contract

Object: distinct original finite MCA slopes of one fixed received pair.
Actual carrier dimension s>=2; degree <K; full-code-bad same supports;
empty universal carrier core. Carrier zeros outside that core remain.
The selected minimizing pair is frozen before grouping. Raw maxima are
not needed. Colors use BOTH normalized received components, not raw values.

Q must pay arbitrary child words, degree K-a, agreement m-a, and affine
dimension AT MOST s-1. It cannot require an inherited post-near condition.
One complete fiber locator is used; at most a-t group labels are charged.
No slope quotient, base-field drop, uncharged padding or child near event.

The theorem's Q is a quantified input to an elementary composition rule,
not a new conjectural DAG leaf. The finite consumer supplies proved caps.
All sum/max choices and the finite multiplicity h are explicit in the proof.
