# Audit

Verdict: NO ISSUE at the printed generic scope; external independent review due.

The final outside-completion span is proper, so BOTH the density and
polynomial root bounds apply. The original quotient basis and inside
tuple are recoverable from an interleaved basis. The proof does not assume
newly appended outside vectors fail to enlarge the span's intersection with
the flat; it uses only the already present inside points as a lower count.

Actual E_(b+1) is bounded relative to E_b without dividing by E_b, including
rank-deficient inside sets. Signed backward elimination uses an upper ratio
when the tail coefficient is negative. Box calibrations are fixed and all
factors remain positive because a<c. A negative inner expression is not
multiplied into a separate lower quotient factor.

Nine exact F_37 polynomial controls have actual ranks 4,5,6 and flat ranks
1,2,3. Their low monomials and root-space bounds prove maximum density;
the verifier checks exact independent subsets, all disjoint inside classes,
inside extension identities and 45 tangent reductions. A wrong signed-ratio
shortcut is rejected by a sharp three-point independent-pair example.
Normal run: 0.08 seconds, 13440 KiB peak RSS, RAMguard tiny.
The same explicit checks also pass directly under -O.

These finite controls supplement the universal hand proof. The quotient
quadratic, finite interval and original-source transport are separate
consumer obligations, not inferred from these small carriers.
