# Dependency Sub-DAG

    mca_maximum_density_flat_core_basis_resource
        -> mca_density_aware_flat_completion_resource
    mca_flat_inside_tangent_basis_resource
        -> mca_density_aware_flat_completion_resource

The first supplies the actual quotient and disjoint inside classes; the
second supplies complete-core packing with the old defect and two-cost
resource interface. The finite consumer must discharge its chosen quotient
lower count and every parameter box. No open premise is introduced.
