# Claim contract

- The affine-product polynomial and NONZERO level are fixed over F(X),
  not varied with the polynomial pair. A rationally centered binary form
  is included, but concurrence is not needed. Component degrees <K
  give fixed pole bounds in the auxiliary algebraic function field.
- At least two nonparallel directions occur after affine splitting. A
  repeated-factor form is permitted; parallel-only products are not.
- The splitting extension is used only over the algebraic closure to
  prove coefficient dimension. It never changes the challenge field or
  its probability denominator. Its degree is not a hidden numerical cost.
- Finitely many divisor patterns imply finitely many projective function
  classes, not finitely many pairs. Each component retains scalar freedom;
  two independent directions and the remaining degree-e level equation
  reduce it to <=1. No false assumption of homogeneous concurrence is used.
- Degree e^(2s-1) is retained to count ALL coefficient components.
  No enumeration or uniform bound on the number of divisors is assumed.
- The LIST lemma counts JOINT agreements. Nonuniversal coordinates use
  a proper component hyperplane containing the joint section, not two
  independent scalar-list estimates. Universal joint agreements cost <=K-1.
- Group gains use raw mismatch ownership outside each complete pair core.
  HIGH labels, the global margin resource and the original near add-back
  are retained once across all groups.
- The actual hyperbola control is not a deployed unsafe source. General
  conics with rank-one quadratic part, arbitrary cubics and arbitrary
  factors of the forced degree-seven relation remain outside the claim.
- Provenance: direct local continuation of the forced-plane-relation
  route, with primary standard function-field facts cited in audit.md.
  No unmerged PR or speculative conditional is a required supplier.
