# Result

PROVED: one complete heavy receiver color retains its own full-occupancy
tuple cost. For uniform light/heavy costs and a FULL proved child cap,

    |Gamma|<=floor(U/beta_L+max(0,1-beta_H/beta_L)*(Q+a-t)).

All original labels use one resource; exceptions are explicit. This is
a receiver-fiber counting theorem, not a free child census or original-row
closure. The finite source consumer supplies Q and its exact cost boxes.
The new content is full heavy-core occupancy and separate costs; receiver
transport, fiber basis counts and completed-core tuples are predecessors.
