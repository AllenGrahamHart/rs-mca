# Claim Contract

Object: distinct original finite MCA labels on one fixed received pair.
The source and full-code-bad support hypotheses are exactly those of
receiver-fiber peeling, with actual dimension s>=2 and empty universal
carrier core. A is a COMPLETE nonzero evaluation fiber, not a selected
support intersection. Minimized receiver pairs and their colors are fixed.

The strict heavy color has t>a/2; all other colors have size<=a-t.
For LOW raw<=T, the complete pair core contains the entire heavy color.
Root capacity a<=K-s+1<K<=M permits an M-point core subset containing it.
Its incidence cost is computed at the ACTUAL t, including core points
outside the old selected support. All tuples identify the original label.

Q is a quantified FULL child cap for dimension<=s-1, degree K-a,
length n-a and agreement m-a over the same field, with full-code badness
and no child near hypothesis. It is not an asserted universal constant
or a conjectural DAG leaf. The finite consumer supplies a proved Q.
At most a-t exceptional original labels remain explicitly charged.

There is one common resource U>=beta_L*L+beta_H*H, beta_L>0,beta_H>=0.
The coefficient of the child cap is max(0,1-beta_H/beta_L), never a
negative multiple of an upper bound. Parameter-box costs must be uniform
lower bounds on the entire box. No optimized-envelope shape is assumed.

No original near allowance, original rank bound, unrestricted row payment
or Prize conclusion is included. The numerical controls test resource
algebra and actual polynomial basis counts, not every received source.
