# Audit

The chosen fiber and receiver colors are complete. Distinct colors are
disjoint, so a strict heavy color is unique and other occupancies are
bounded by a-t. The heavy color lies in its entire pair core. Since
t<=a<K<=M, it can be retained in the M-point subset without discarding
any of its coordinates. Universal-core emptiness excludes zero evaluation
rows there. The original defect remains recoverable in s+1 positions;
core points outside the old witness are allowed by the completed resource.

F_A is positive and log-concave separately on either side of c. Thus
light minima must include the kink c when it lies in the interval.
The finite application has a<c and needs no kink case. Heavy HIGH records
retain their separate HIGH cost; a LOW-only statement is not used for them.

The heavy child cap is FULL and the a-t original exceptions remain.
When beta_H>beta_L, the rearranged coefficient is negative: H>=0 is
used, not H<=H_up. The control L=5,H=0,U=10,beta_L=2,beta_H=4,H_up=3
rejects the invalid bound2 and verifies the corrected bound5.

Normal and optimized controls pass5376 exact resource cases, six actual
F17 polynomial cores with occupancies0..5 (including c=2), and three
invalid cost guards. These are controls, not a full source enumeration.
The universal source argument is the hand proof. No issue was found at
its printed scope; independent external mathematical review remains due.
Timings are retained in the dated correspondence record.
