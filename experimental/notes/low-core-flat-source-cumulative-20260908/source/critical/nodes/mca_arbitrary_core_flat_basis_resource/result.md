# Result

PROVED: the counting interface works for the ACTUAL dense-flag upper flat,
without requiring that flat to maximize density. Integer rank caps and
separately certified balanced quotients can be retained. The finite
rank-eight-density consumer pays a new source class; this generic result
is not itself an MCA or LIST row bound.
