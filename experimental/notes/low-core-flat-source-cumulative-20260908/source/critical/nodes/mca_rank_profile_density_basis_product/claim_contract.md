# Claim Contract

Actual rank s>=2 and degree<K polynomials are evaluated on N=D+K
distinct points, with no zero evaluation functional. Every proper rank-j
flat has at most j*h COORDINATES. D>=1 and K0<=K<=K1 with K0>=s.

The coefficients in statement.md are fixed for the entire box. At each
actual complete-flat descendant, b>=j, k=K-b and n=N-b. Its projective
fibers are complete and lift to original rank-(j+1) flats.

Jensen uses the resulting fixed-profile polynomial, whose coefficients
are nonnegative in x-1. Its arguments are >=1. No convexity is asserted
for an optimized envelope, nor does a relaxed argument describe a
fractional-degree polynomial source.

Only polynomial bases are counted. No received-word descent, original
slope upper bound, new near theorem, field drop or unrestricted endpoint
follows without a separately proved consumer.

Falsifier: a polynomial evaluation space meeting the exact hypotheses
whose ordered basis count is below (PROFILE), or an actual descendant
violating its printed moment coefficient.
