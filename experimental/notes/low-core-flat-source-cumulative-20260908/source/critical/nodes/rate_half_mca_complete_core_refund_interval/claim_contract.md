# Contract

- The same normalized KoalaBear source as the previous receiver-flat
  interval: actual dimension eleven, full-code-bad supports, empty
  universal core, unchanged field and original finite labels.
- Density <=h_7 pays directly; otherwise polynomial roots force j<=6.
  No additional carrier classification is hypothesized.
- Ranks 1..5 use full joint-core occupancy and separate valid LOW/HIGH
  minima for heavy and light groups. The heavy child includes exceptions
  and requires no post-near property. Its coefficient is nonnegative.
- Each parameter box covers an entire real region, not its vertices only.
  Joint log-concavity, fixed degree/density branches and monotone child
  bounds justify the rank-1..5 corners. Rank six uses sign-aware interval
  bounds and positivity before multiplying lower counts.
- Rank-six occupancy varies per record: MINIMUM charge on one shared
  resource means MAXIMUM quotient, never a sum over its occupancy bins.
- Each integer J belongs to a tested block. Real calibration parameters
  need not be field-realizable sources. No numerical survival premise.
- One original near allowance appears in the totals; the downstream
  original-source assembly must not add it again.

Falsifiers: an uncharged heavy label, invalid complete-core tuple point,
wrong coefficient sign or uncovered size/occupancy region; or an exact
contract source exceeding NEW/UNION. Broader rank and Prize claims are open.
