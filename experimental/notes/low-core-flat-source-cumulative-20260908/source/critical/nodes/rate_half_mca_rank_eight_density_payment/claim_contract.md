# Claim Contract

Same normalized (n,K,m)=(1048576+J,J,67472+J), dimension eleven and
same original distinct finite labels. Require full-code-bad supports and
empty universal carrier core. The density is COORDINATE density over all
nonzero carrier evaluations; h<=(J-3)/8 is a substantive source premise.

Fix minimizing pairs and M=J+67466 core subsets after normalization.
Core evaluations are nonzero; select an actual flag only when needed.
The flag upper flat need not maximize source density. Its quotient counts
bases only and deletes no receiver coordinates or exceptional labels.
Positive per-record costs consume one shared tuple budget; alternatives
combine by maximum. Add the original near allowance once.

Scope is all J=23000..29999 under this gate, not all sources at those
J. The original assembly owns transport and every-selection quantifiers.
No upper census of all dense labels or active-v4 ownership is asserted.
