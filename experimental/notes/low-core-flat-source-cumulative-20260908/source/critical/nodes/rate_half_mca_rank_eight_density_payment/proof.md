# Proof By Paying Each Actual Dense-Flag Core

Write D=67466, M=J+D. Fix normalized minimizing pairs and one M-point
nonzero core H for each LOW record raw<=6. Empty universal carrier core
makes these evaluations nonzero; polynomial roots make their rank eleven.
The chosen pair and original witness supply a genuine defect.

## 1. Old Density Region And The Recordwise Alternative

For J<=24537, 30*(J-3)/8<=J+D, so the required bounded-density supplier
already pays the entire source by 268913508505087358.

For 24538..29999, a core with at least P_11(D,J) ordered bases has
12*P_11 tuples after defect insertion. Otherwise the actual flag theorem
gives F<G, ranks t-1,t, sizes a,b, with

    (11-t)b-(10-t)a>M, a>=t-1, b<=J-11+t.

Its rank bound is t<=7 on this interval, as proved by the mass supplier's
root-capacity argument. We use only that flag geometry, not a presumed
number of exceptional labels. The source density implies integer caps

    c_r(J)=floor(r*(J-3)/8), 1<=r<=10,
    b>=floor((J+D+(10-t)(t-1))/(11-t))+1,
    b<=min(J-11+t,c_t(J)).

Combining the strict lower b bound before rounding with b<=t*(J-3)/8
gives the necessary onset

    J> [8D+8(10-t)(t-1)+3t(11-t)]/[t(11-t)-8].          (ONSET)

The denominator is positive for t=1..7. This discards impossible ranks,
not original labels. In particular t=7 starts no earlier than 26998;
the integer size interval is empty at 26998 and first nonempty at 26999.

## 2. Count Through The Actual Upper Flag Flat

Apply the arbitrary-core-flat supplier to G, whose COMPLETE coordinate
set inside H has exactly b points. It need not maximize density.
Let ell=11-t. Its auxiliary quotient has degree J-b, length D+J-b,
rank ell and density at most

    min(J-b-ell+1,(t+1)*(J-3)/8-b).

For every certificate box the hereditary balanced-product gate holds,
so the quotient has at least P_ell(D,J-b) bases. The supplier also gives
its greedy quotient count. Take their maximum, never their sum.

Use its completion factors and inside ratios, retaining the INTEGER caps:

    d_i(J)=max(D+1+i,J+D-floor((10-i)*(J-3)/8)),
    E_(i+1)>=max(b-floor(i*(J-3)/8),0)*E_i,
    E_(i+1)<=(b-i)*E_i.

The six tangent choices are q_i=c*b1/4 for c=0,1,2,3,4, and
q_i=max(b0-floor(i*(J1-3)/8),0). Each is fixed for the WHOLE box and
lies below D+1. Negative tail coefficients use upper ratios.

## 3. Why The Finite Boxes Cover Every Source

The consecutive degree ranges and step sizes are

    24538..26998 /128; 26999..27015 /1; 27016..27199 /16;
    27200..27999 /64; 28000..29999 /128.

Truncate the last block at each endpoint. For each t intersect its degree
block with ONSET; call the resulting endpoints J0,J1. The b lower endpoint
uses J0, the upper endpoint uses J1. These enclose every actual b. Partition
that entire integer interval into at most eight consecutive bins with
step max(1,ceil(number_of_integers/8)). Empty intervals contribute nothing.

All d_i(J), and the outside factors
max(D+ell-i,J+D-floor((t+i)*(J-3)/8)), are nondecreasing on these degrees.
For a flat rank r<=7, J-floor(r*(J-3)/8) is nondecreasing. At r=8 the
factor is constant D+3; at r=9,10 the root branch D+11-r dominates.
This proves the endpoint direction despite the floor discontinuities.

On a size bin [b0,b1], set k0=J0-b1,k1=J1-b0 and h1=(J1-3)/8.
The certificate checks ell<=k0<=k1 and

    floor(ell^2/4)*min(k1-ell+1,(t+1)*h1-b0)<=D+k0.

It therefore verifies the balanced gate for EVERY actual quotient in
the box. P_ell is increasing in degree, so P_ell(D,k0) is a lower count.
The other lower quotient count uses J0 and b1. Smaller d_i(J0), upper
caps floor(i*(J1-3)/8), and the two b endpoints enter the supplier's
signed BOX formula. All completion factors and selected inner counts
are checked positive. No fractional-degree polynomial source is posited.

The original tuple resource uses its upper degree endpoint
U=(R+J1)_falling_12. The no-flag LOW cost uses the lower original block
endpoint in 12*P_11; all HIGH records retain
(d+J0)*P_d*10488/125. Every LOW record belongs to one basis-rich or flag
case and has at least the minimum of these proved costs. A label with
several flags is NOT counted several times. All labels consume ONE resource.
Source cases combine by MAXIMUM; they are not separate additive budgets.

## 4. Exact Payment And Original-Source Scope

Rational and independent scaled-integer certificates agree on all 2038
flag-cost boxes and all 78 no-flag/HIGH comparisons. Their maximum total,
including ONE original near allowance, is 274977202549132026 at
J=26999,t=7,b=23621. This is a complete finite arithmetic certificate
for the preceding all-source inequalities, not a numerical source search.

Original-source transport is owned by the existing assembly. It retains
the field, labels, full-code badness and empty universal carrier core.
Any maximizing flat of rank j has h<=1+(J-11)/j. For j>=8 this is
at most (J-3)/8, so PAYMENT excludes all such maximizing ranks on an
over-budget source. Every maximizing flat, not just a chosen one, is <=7.

The chosen auxiliary quotient is a basis-counting device; no receiver
or exceptional label is removed. This pays a larger source class and
bypasses the need for an upper census there, but supplies no unrestricted
upper census, new whole-degree interval or endpoint closure.
