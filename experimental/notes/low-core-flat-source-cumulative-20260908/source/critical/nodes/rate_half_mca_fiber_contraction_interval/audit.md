# Audit

The primary checker uses normalized quadratic coefficients. The independent
audit starts from unnormalized polynomial products, composes the two
contraction profiles, and checks the global squared-factor remainder.
All eight upward coefficient mutations are rejected at an actual contact
point of the sufficient profile certificate. Two incorrect LOW floors
are rejected by integer cross products. These are certificate mutations,
not counterexamples to an MCA source theorem.

The exact derivative inequality proves the entire J interval, not a
numerical grid. The root-space/fiber reduction and its two concavity
arguments are hand proofs. The original-source bridge is inherited by
the downstream assembly, not inferred from arithmetic in this node.
Local proof with focused controls; independent external review remains due.
