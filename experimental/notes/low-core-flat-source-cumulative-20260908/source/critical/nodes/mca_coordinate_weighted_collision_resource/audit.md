# Hand Audit And Controls

The weighted tuple count is bounded by the unweighted Bonferroni resource
plus the marked sum over ALL distinct-coordinate tuples. Factoring
T*(n-4)_(r-4) proves CREDIT, including T=0, without an unnecessary
derivative-monotonicity assumption on the augmented polynomial.

The source moment is taken after auxiliary full zero-locator division,
then bounded in original n,K. This is not receiver descent. The core
weighted basis count sums actual first-coordinate completions and uses
ordered-basis symmetry exactly s times, not s+1. Defect insertion then
gives s+1 recoverable positions. The two multiplicities are distinct.

The generic control checks1452 weighted histograms,150 tangent comparisons
including both coefficient signs, and three actual polynomial cores. The
rank-three F17 core (K,M)=(12,13) has C=90,186 ordered bases and marked
weight1620, attaining3*C*F2=1620. Replacing3 by4 gives false2160.
An unfunded lambda=1 already exceeds the unweighted resource on one
two-point fiber and eight singletons. Four invalid guards are rejected.
The hand proof explicitly requires D>=1 and h>=1; no implicit extension
to an unproved convex domain. External independent mathematical review
remains due; controls are not formal proof or an unrestricted MCA census.
