# Contract

Unit: nonnegative weighted ordered independent original-coordinate tuples,
not a new received-word source or a separate budget for each core.

The source, actual rank s, full-code-bad explanations, complete nonzero
source fibers, coordinate weights and lambda are fixed before counting
records. Lambda satisfies CREDIT uniformly; SOURCE requires n>K>=s>=3.
The same secant/zero exceptional SET is charged once. No zero coordinate
is silently cancelled or made to agree by singleton padding.

CREDIT is sufficient for the resource part of this node. The marked-core
and tangent inequalities hold for every lambda>=0 and may be combined
with a separate proved upper bound for the SAME weighted source resource.
Such a consumer need not reimpose the older sufficient CREDIT inequality.

Core claims require M=D+K, D>=1, actual nonzero rank s, all-proper-flat
density<=h, K-h>=1, and the fixed ORIGINAL hereditary profile. C is the
CORE collision count, not the full-source T. Source weights dominate
restricted core-fiber weights. Exact contraction removes a whole CORE
fiber only for auxiliary basis counting. The original defect is retained.

Tangents require the entire relaxed C interval to stay in x>=1. A negative
linear coefficient uses Cmax; a positive one uses Cmin. Tangent points may
vary by core type; lambda may not. No numerical interval, original-rank
restriction, unrestricted row or Prize conclusion is supplied here.
