# Audit

The full-code-contained exchange neighbor is the delicate branch: root
uniqueness first proves its second polynomial equals the previous V-member.
Assuming carrier membership without that argument would be a gap.
The required common agreement count for two low minimizers is
m-1-2T>=K, giving precisely the printed strict guard 2T<d.

The rank-one collision may use an unselected scalar explanation. Taking
the maximum only for a previously fixed h would not justify its exclusion.
Existence of a bad m-subset for that alternative is constructed explicitly
from K points in one core and a defect in the other.

The controls falsify omitted maximality and the equality boundary, include
pair-contained exchange neighbors, and exhibit both hard row spaces after
full carrier maximization. Their small field arithmetic corroborates the
hand proofs; no exhaustive deployed-field search is undertaken.
Independent external review remains outstanding.

Local replay on 2026-09-07 passed in 0.30 seconds at 12800 KiB RSS
under ramguard tiny. It checked the three guard controls, all 23 labels
in each sextic-field family, all two-point-pinned scalar explanations,
their exact raw minima, and both 40x36 full row-space matrices.
