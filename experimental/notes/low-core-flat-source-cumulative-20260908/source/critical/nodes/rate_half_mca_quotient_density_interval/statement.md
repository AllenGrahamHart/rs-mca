# Every Normalized KoalaBear Source From J=32000

On |F|=2130706433^6, let n=1048576+J, K=J, m=67472+J. Fix an
actual dimension-eleven carrier V of degree <J, its affine translate and
one received pair. The universal carrier core is empty. Each distinct
original finite label has a scalar-agreeing size-m support bad in the
FULL degree-<J pair code. No maximal-raw selection is required.

For EVERY such source and every integer J=32000..39999,

    |Gamma|+134944 <= 261925431454675420.             (NEW)

No additional structural carrier hypothesis is imposed. Quotient quadratic
basis bounds and density-aware final completion are combined with actual
inside-tuple ratio constraints. The same fixed receiver determines heavy
groups, whose actual tuple consumption is charged on one original resource.
Large rank-one fibers use the already proved full receiver-fiber payment.

The four integer-degree blocks have 112640 record-cost boxes and 14080
source-parameter boxes in total. Exact integer implementations agree on
every cost and source bound through streaming digests. Analytic inequalities
prove coverage BETWEEN the checked corners; this is not a sampled assertion.

With the required preceding interval, EVERY source on 32000..169999 has

    |Gamma|+134944 <= 274929007493481160.             (UNION)

The original-source assembly applies its already proved gauge and complete
shared-core transport downstream, with its one original near allowance.
This theorem does not itself prove an error-rank bound, an active-v4 owner
charge, the unrestricted adjacent row, ordinary LIST or either Prize problem.
