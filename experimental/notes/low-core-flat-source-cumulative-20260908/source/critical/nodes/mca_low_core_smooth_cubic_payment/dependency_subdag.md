# Required dependency sub-DAG

    elliptic_curve_bounded_polynomial_section_dimension --req--> this node
    mca_low_core_homogeneous_level_payment ---------------req--> this node
    this node --req--> rate_half_mca_cancelled_low_core_relation_payment

The first supplier owns the geometric dimension theorem. The second
owns the raw-weighted group argument and requires the graph supplier's
general joint algebraic LIST/degree-cover machinery. Its affine-product
IDENTITY is not assumed here; only those general proved lemmas are used.

The finite consumer owns resource endpoints, original near transport and
mixed-factor composition. This node's two incoming req edges and the
consumer's new req edge are acyclic. The arbitrary-source router remains
a TARGET with evidence only. No new conditional node is created.
