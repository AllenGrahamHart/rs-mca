# Hand Audit And Independent Certificate

The source-fiber gate is an actual whole-family bound and is included
in the final maximum. Its failure bounds every core's rank-one occupancy.
Core-local density is chosen separately for each LOW record; no favorable
intersection with a globally maximizing source flat is presumed.

Below (J-5)/6, deficiency implies a first excess flat. Minimality caps
every smaller rank. Root capacity excludes excess ranks eight and nine.
Above the threshold, root capacity excludes maximizing ranks >=6.
These alternatives cover every record without imposing new source premises.

Full-core occupancy enables auxiliary quotient counting. Rank clipping
k0=max(ell,J0-b1) is valid because the ACTUAL quotient has dimension ell.
Both quadratic engines check all their coefficient, tangent, downward
rounding, positivity and degree-hull gates. The root-balanced gate is
uniform throughout the hull, not checked only at a sampled source.

The tangent at each inside rank is independent and fixed on its box.
The backwards choice records valid coefficients; no claim of an optimal
calibration is needed. The audit recomputes derivatives by omitted products,
chooses integer tangents independently, reconstructs the complete telescoping
coefficient vector, and checks its elimination against the adaptive result.
Negative tails use upper ratios and no negative lower count is multiplied.

Low-branch floor complements are nondecreasing in J. High-branch completion
ranks exceed the chosen flat rank, giving the opposite J corner in (J,k).
All integer sizes and degrees are covered; neighboring records or suitable
flats do not receive separate source budgets. Near is included once.

Primary and independent audit agree on every cost in 63312 boxes:
41957 low-density first-excess boxes, 21355 high-density maximizing-flat
boxes, and 63 basic basis-rich/HIGH comparisons. Both reject 126624 adjacent
wrong floors; five broken degree covers fail. The maximum is
273019482620216244 at J=28000..28031, high density, rank one, k=2001..2032.
Every full tangent vector is checked independently. Whole-certificate digest:

    a34884087a5dd4e1ddfabb9413b89f4bfa4fc02dd6dc7389ed8e5bea6c5f3dc9

Primary normal: 5.08 seconds /21732 KiB; -O: 4.05 seconds /23892 KiB.
Audit normal: 9.05 seconds /21040 KiB; -O: 8.76 seconds /23404 KiB.
All serial under RAMguard tiny; no Modal spending or field-scale search.

An early unbanked probe omitted actual-rank clipping of the rectangular
degree hull and failed its quadratic gate. It was not used as a certificate.
Exploratory scripts are not dependencies. The current exact checks supplement
the universal hand proof, not an external acceptance or formal verification.
Verdict: no issue at the printed scope; independent external review is due.
