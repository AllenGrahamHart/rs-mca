# Exact Source Contract

Row: q=2130706433^6, R=1048576, d=67472,
(n,K,m)=(R+J,J,d+J), 28000<=J<=29999.
The carrier has ACTUAL polynomial dimension eleven, original distinct
finite labels, full-code-bad supports and empty UNIVERSAL carrier core.
Retain nonuniversal carrier zeros. Fix minimizing pairs after normalization;
no preservation of earlier raw values is assumed.

Every source is covered, with no added flat/density premise. First pay
a complete source fiber >=J-2000; otherwise core fibers are <=J-2001.
Core-local density below/above (J-5)/6 is an exhaustive recordwise split.
The minimal-excess and maximizing flats are complete on that SAME core.

Annihilator quotients count bases only. The existing uniform quadratic
algorithm is used at its proved rank/gap/degree gates, not by importing the
parent's narrower finite interval. The balanced product is used only after
checking its density gate. Signed tangents are valid, not asserted optimal.

All records consume ONE original tuple resource; all whole-source
alternatives combine by maximum, with ONE original 134944 near allowance.
Original-source transport and the residual corollary are owned by the
assembly, which is a consumer, not a premise of this normalized theorem.
The interval union uses the earlier refined interval as an explicit input.

Falsifiers: an uncovered actual record or size, invalid endpoint direction,
unproved quotient gate, double-counted independent tuple or a normalized
source exceeding 273019482620216244 including the stated allowance.
