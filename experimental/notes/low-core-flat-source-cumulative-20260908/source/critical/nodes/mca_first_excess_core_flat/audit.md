# Hand Audit And Controls

The dense-flag inequality implies b>N/(s-r) because its subtracted term
is nonnegative. Least-rank selection gives every smaller-rank cap with
the correct weak integer floor. A nonspanned chosen flat would give an
even smaller excess flat; nonzero evaluations exclude rank zero.
Injective full-locator division of its annihilator proves b<=K-s+t.

The two alternatives can overlap. The generic proof does not require
density, smoothness, maximal raw margins or a received-word descent.

Five actual F_17/F_23 controls cover 147 complete spanned flats: two
basis-product equalities with no excess flat; deficient ranks three/four
with least excess rank one; and a basis-rich source with least excess
rank two. Four nonminimal excess choices fail the all-smaller-rank cap
check. Strict integer onset and permitted-rank boundary controls pass.

Normal replay: 0.11 seconds /13568 KiB peak RSS.
Optimized replay: 0.24 seconds /16936 KiB. Guards remain active under -O.
These are small controls, not a proof by finite-field enumeration.
Verdict: no issue at the printed scope; external independent review is due.
