# Proof With A Larger Proved Johnson Anchor

Put R=1048576,d=67472,D=d-6,H=52999 and near=134944.
The source fiber has a<=J-10 by actual polynomial root-space capacity.
The receiver-fiber peeling theorem applies with theta=1/11 and T=6.
There are at most ten heavy actual receiver colors. Their full-locator
children have degree J-a<=4700 and retain full-code badness and all but
the explicitly charged a-t original labels.

Use the existing padded-Johnson certificate at L=4700, multiplicity20:

    N=1053276, M=72172, k0=4699, sigma=41,
    4*20^2*M^2>=41^2*N*k0,
    X=1442208, Y=307, Z=31400,
    Q_4700=2*X*Y^2*Z+(N-M+1)*Y+Z=8536194661768235.

The separate square-root and rational ceilings are checked exactly.
Padding over the SAME field gives this FULL cap for every positive child
degree <=4700 and arbitrary received values. It requires no child near
event or empty-core hypothesis. The older anchor is a credited supplier,
not a new Johnson theorem or empirical monotonicity claim.

For the light labels, monotonicity on 0<=t<=a/11 gives the lower basis
count F_A(0). It increases in a, hence is at least

    B_0(J)=(D+J)*(D+1)*product_(i=1)^9(D+J-4690-i).

All ten nonconstant factors have logarithmic derivative >=1/(D+H).
Since 10*(R+23000-11)>12*(D+H), the tuple quotient U(J)/(12*B_0(J))
decreases on the ENTIRE 23000..H interval. Its floor at23000 is below
161394160592554522. This latter number is the required older theorem's
uniform raw>=7 quotient on14000..H; only its weight arithmetic is used,
not its old degree-2000 fiber premise.

The disjoint heavy colors and remaining light group therefore give

    |Gamma|+near
      <=10*(Q_4700+H)+161394160592554522+near
       =246756107210901806.

The a-t exceptions are bounded by H per heavy color. All heavy groups
and the light group refer to the same original receiver partition.
This is not a sum over alternative fibers, and near is not added twice.
