# Dependency Sub-DAG

Required, all PROVED:

- `mca_balanced_basis_or_dense_core_flag`: the actual record-core alternative.
- `rate_half_mca_balanced_density_payment`: only its explicit P(J), whole-
  interval comparison arithmetic and HIGH cost; NOT its density premise.
- `mca_core_completed_basis_margin_resource`: disjoint original tuples and
  the universal recordwise cost, including on exceptional records.

This node --req--> `rate_half_mca_rank_twelve_paid_interval_assembly`.
The assembly owns the original-source/near interface. This node does not
require the assembly, so no cycle is introduced. There is no new red or
conditional upper-census leaf. The original router keeps evidence only.
