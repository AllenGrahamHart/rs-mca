# Proof: Charge Both Sides Of The Recordwise Alternative

## 1. Flag-Free LOW And All HIGH Records Have The Good Cost

Use one original selected family and minimizing pair per label. On a LOW
record raw<=6, fix M=J+67466 points of its complete joint core. Their
carrier evaluations are nonzero by empty universal core, and the original
witness provides an actual defect. If the core has no FLAG, the required
balanced-basis-or-flag theorem gives at least P(J) ordered bases. Defect
insertion then gives at least 12*P(J) independent original incidence tuples.

Every HIGH record raw>=7 retains the proved cost
(d+J)*P_d*10488/125. This follows from the earlier completed-basis weight
and the exact inequalities in the numerical supplier. Define

    beta(J)=min(12*P(J),(d+J)*P_d*10488/125).

Every nonexceptional record has at least beta tuples. The numerical
supplier proves G(J)<=268913508505087358 throughout 23000..29999:
the LOW quotient decreases by its strict log-derivative comparison, and
the HIGH quotient has the printed coarse endpoint bound. Only these
ARITHMETIC statements are imported from that supplier; its source-density
hypothesis is neither asserted nor used here.

## 2. Exceptional Records Still Consume At Least One Eighth

Empty universal carrier core implies g=0 in the completed-basis ledger:
a universally satisfied zero normal would have V(x)=0,v(x)=0,u(x)=h_*(x).
The universal weight alpha_11=12*d/(d+11) therefore gives EVERY record
at least

    sigma(J)=12*d*(d+J)*P_d/(d+11)

independent tuples. It applies to the exceptional LOW records too. Because
P is increasing and J in [L,E]=[23000,29999], the exact certificate

    8*d*(d+L)*P_d >= (d+11)*P(E)

implies sigma(J)>=12*P(J)/8>=beta(J)/8.

These are lower bounds on sets of independent tuples for the SAME label.
The full-core basis count and the older selected-support count may use
different coordinates; their maximum is still a valid lower bound for
the union of that label's tuple sets. No two distinct finite labels share
an independent tuple. Thus, with N_labels=|Gamma|,

    U(J) >= beta(J)*(N_labels-e)+(beta(J)/8)*e,
    N_labels <= U(J)/beta(J)+(7/8)*e.                    (RESOURCE)

Flag-bearing records may also be basis-rich. That only improves their
actual tuple cost; no exclusivity or equality is assumed. Multiple flags
never multiply one label's count or create additional resource budgets.

## 3. Keep The Strict Integer Boundary

Let Z_bound=N_labels+134944. By the definition of G(J), RESOURCE gives

    Z_bound < G(J)+1+(7/8)*e.

If the INTEGER Z_bound>B*, then B*+1<=Z_bound. Hence
e>8*(B*-G(J))/7. Taking the smallest admissible integer proves MASS.
Flooring U/beta BEFORE adding a fractional exceptional contribution would
not be valid; the strict comparison above is the reason for the final +1.
The uniform numerical bound on G gives 6933965264351691. The pointwise
values in the statement are direct exact rational evaluations, not scans
or asserted unrestricted MCA bounds.

## 4. Rank And Quotient-Degree Consequences

The actual flag theorem gives a>=t-1, b<=J-11+t and t<=9. Substitution
into FLAG yields the necessary strict inequality

    (10-t)*J > 67466+111-11*t.

For t=8 this would require 2*J>67489, impossible at J<=29999;
t=9 is also impossible. Thus every exceptional flag has t<=7. Solving
FLAG using a>=t-1 gives the printed lower b bound.

For t=7, b>=floor((J+67484)/4)+1. Therefore
J-b<=J-floor((J+67484)/4)-1, a nondecreasing function of integer J.
Its maximum at 29999 is 5628. The full nonzero source flat associated
to G contains at least these b actual pair-core coordinates. Its
annihilator has actual dimension 11-t=4, and division by its full locator
has degree bound <=J-b. No receiver equation, original label or exception
is discarded: only an auxiliary annihilator is asserted here. A received-
word descent would need the separate same-source transport and charges.

## 5. Original-Line Use And Remaining Task

The existing original assembly normalizes a complete post-near rank-twelve
selection without losing any original labels and with actual carrier rank
eleven. Its original bad-slope count is <=|Gamma|+134944. Thus an original
over-budget line in this degree interval satisfies MASS for every fixed
choice described above. The assembly owns this transport, not a new near
removal or a claim that normalized counts are the entire original count.

The result is a lower bound on the number of records that need further
payment. It supplies NO upper census for them. A census below the printed
necessary mass would rule out this source, but no conjectural census is
introduced into the DAG. The lower J region and original ranks >=13
remain independent obstacles.
