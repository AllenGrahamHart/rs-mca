# Claim Contract

Same normalized field and received pair, actual dimension-eleven carrier,
degree J=23000..29999, original distinct finite labels, full-code-bad exact
supports and empty universal carrier core. No source flat-density premise.
Choose minimizing pairs and M-point complete-core subsets once. Exceptions
are labels whose OWN fixed core contains a strict nested-flat witness.

Output: if |Gamma|+134944 exceeds B*, at least the printed number of
distinct original labels are exceptional. The comparison G(J) is not an
unconditional MCA bound. Integer strictness and one eighth of the prior
cost are retained. Each label is counted once regardless of its flags.
The original assembly owns the unique near add-back and label transport.
Quotient degree bounds concern polynomial annihilators, not an uncharged
received-word descent. No upper census, endpoint, LIST or Prize closure.
