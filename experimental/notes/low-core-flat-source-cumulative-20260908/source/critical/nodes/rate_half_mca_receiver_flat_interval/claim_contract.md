# Exact Contract

Same normalized KoalaBear field, dimension-eleven carrier, full-code-bad
selected supports and empty universal core as the prior interval theorem.
No new fiber-size, flat-rank, raw-maximality or receiver-field premise.

The density split at h_6 is exhaustive and forces j<=5 otherwise.
Rank-five coupling is valid here because a<c and c>=100. The existing
quadratic certificate proves every record's half-HIGH tuple charge.

Heavy projected pairs use FULL anchored caps U_j and retain the actual
agreement m-t and empty universal core. Their a-t exceptional original
slopes are charged. LIGHT and HEAVY share one resource, yielding factor
1/2 on the heavy cap. Near=134944 is included exactly once.

Convexity in t and a and log-derivative bounds in J justify all 80 indexed
endpoint certificates; they are not a sample of arbitrary source rows.
Original-source assembly and the remaining ranks/interval are downstream.
