# Result

PROVED: evaluation occupancy <=j*h in every j-dimensional subspace,
1<=j<s, supplies

    beta(r)=(s+1)r*prod_(j=0)^(s-1)(m-r-g-jh)

independent ordered tuples per eligible selected record. Combining it
by maximum with the existing completed weight yields one stronger global
slope resource. Under the printed monotonicity and HIGH-weight conditions,
the minimum record count bounds the entire source.

The consumer uses this without a pair-curve assumption: unclustered arcs
pay on normalized 10000..169999; small-fiber arcs and all full progression
carriers pay on 23000..169999. Those finite constants and their source
binding are consumer-owned. Arbitrary carriers remain unresolved.

The generic proof needs no maximal-raw selector, finite-field scan,
interpolation matrix or receiver descent. Both prizes remain open.
