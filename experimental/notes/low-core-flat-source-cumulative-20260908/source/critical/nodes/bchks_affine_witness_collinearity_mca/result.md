# Result

HARVESTED / PROVED at the printed rounded constant. A complete hand
argument bounds the entire MCA numerator linearly in n at fixed
Johnson gap and rate, for all finite fields and evaluation domains.

The exact-gate continuation allows all m>=1 with the max(Y,Z_0)
degree bound and uses X<=m*a directly. Its separate collinearity load
proof includes m=1,2. The consumer now certifies actual KoalaBear
radius 609163 (previously 600000), all pairs and all ranks, and the
stronger near-cap formula ceil((65537/65536)*sqrt(n*(k-1))).
The original weaker certificates remain valid. The new supplier tests
passed 19243 small exact cases. This is safe-side bracket movement,
not actual-code optimality or the target crossing at 1116048.

Independent hand review and upstream integration remain outstanding.
The original prize roots and beyond-Johnson counting problems stay open.
