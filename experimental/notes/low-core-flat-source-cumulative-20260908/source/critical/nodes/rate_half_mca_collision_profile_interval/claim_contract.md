# Contract

Object: all selected original finite labels on ONE normalized received pair
and an actual dimension-eleven polynomial carrier. The field, degree,
agreement, full-code pair noncontainment and empty universal carrier core
are those in statement.md. Nonuniversal carrier zeros are retained.

J=22500..22999 is an entire integer interval, not a list of sampled rows.
Every source is included: a large source fiber is already paid; otherwise
all source fibers satisfy the upper gate needed for the exhaustive T split.
T is the ordered equal-fiber pair statistic on the FULL source. It is not
a core statistic chosen separately for each label or a receiver-color count.

Every LOW core chooses its own maximizing flat. All ranks1..10 and all
possible sizes are covered, with no h>(J-4)/7 premise. The root moment
is changed but the remaining original-rank profile coefficients are frozen.
Quotient moment bounds are not mistaken for quotient basis counts or a
received-word descent. Negative tangent tails use upper extension ratios.

Low T pays no secant exception set. High T pays exactly one such set and
uses one decreasing-in-T tuple resource. All records share that resource;
HIGH and LOW costs combine by minimum. Source alternatives combine by
maximum. The explicit134944 allowance is original near only, never a
fresh child near event. The source assembly owns original-rank transport.

Falsifiers: a missed actual source/rank/size, an invalid moment or derivative
inequality under the printed guards, a wrong whole-box lower cost, double
tuple ownership, uncharged labels, or a source exceeding PAYMENT. An
over-budget upper estimate on a lower J is not an unsafe received line.
No proof of cap optimality, original rank boundedness or either full Prize.
