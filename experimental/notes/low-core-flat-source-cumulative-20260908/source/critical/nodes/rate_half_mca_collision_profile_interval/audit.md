# Hand Audit And Exact Certificate

The source split is checked before any per-core choice: a large source
fiber pays the full family, otherwise the source maximum A is justified.
The source T bounds all restricted core pair counts at once. The high-T
derivative guard covers the entire possible T interval, not just T0.
The negative resource term uses the lower n endpoint. Carrier-zero and
same-fiber secant exceptions form ONE set; original near stays separate.

Flat completeness makes the annihilator quotient nonzero off the flat.
The quotient coarsens original outside fibers; the h bound is used on
original fibers only. First-step moment improvement retains the original
rank profile thereafter. The primary now coerces integral density inputs
to Fraction to prevent accidental floating-point division in this formula.

Every maximizing rank1..10 is covered, including the old low-density case.
All whole-box dimension, positive product and tangent-domain guards pass.
Only maximizing-flat quotient density is simplified to h; this would not
hold without the equality a=t*h. Twelve defect insertion positions and
the raw-HIGH weight are the existing proved original-tuple interface.

The primary uses exact Fraction products. The independently written audit
uses integer numerator/denominator pairs, unrolled profile products and
independently expanded source collision formulas. It imports only the
earlier INDEPENDENT signed-completion/quadratic audit for those unchanged
generic formulas, not this primary or Fraction. Every tangent coefficient
vector is independently reconstructed by that helper.

Both engines cover40960 boxes,32 degree blocks and both T branches;
10955 boxes retain the older signed-completion cost. Both reject163840
adjacent wrong floors and agree on every cost, cap, maximum and digest:
5ace68871be07e08317ea359ac9a73f80c7a9b9a325e5390a0d6e66b9ecdc5a2.
The primary also rejects eight broken degree/size covers. The initial
calibration runs intentionally did not pass the unset digest guard;
their matching full certificates were then frozen. No numerical
counterexample or mathematical failure is inferred from those guard exits.

The final normal/-O timings and local DAG replay are recorded in
notes/correspondence/COLLISION_PROFILE_INTERVAL_20260908.md. All computation
is tiny, serial, standard-library and RAMguard bounded. These exact finite
certificates audit the arithmetic; generality comes from the hand proof.
External independent mathematical review remains due. No optimality,
unrestricted endpoint or Prize closure is certified by these controls.
