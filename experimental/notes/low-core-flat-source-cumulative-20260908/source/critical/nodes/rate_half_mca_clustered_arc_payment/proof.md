# Proof With A Distinct-Fiber Resource

Put R=1048576,d=67472,s=11,h=2048,F0=560,T=500,
L=20481,H=22999 and P=product_(i=1)^10(d+i).

## 1. Pay One Exceptional Slope Set

The required secant theorem gives E with

    |E| <= z+sum_i a_i*(a_i-1)/2
         <= z+(n-z)*(h-1)/2 <= n*(h-1)/2,

because h>=3. Here z is the actual number of carrier-zero coordinates,
not an assumed zero. Empty universal core lets their agreeing labels be
charged. On our whole J interval a uniform integer exception cap is

    E_max=floor((R+H)*(h-1)/2)=1096757012.

Every remaining independent tuple uses twelve distinct nonzero fibers.
Padding the fiber sizes to560 entries and applying the proved balance
envelope gives a resource upper bound

    U(J)=(560)_falling_12 * (R+J)^12/560^12.            (U)

This is a rational bound on tuples in the SAME source. No fractional
coordinate domain or independently maximized received child is introduced.

## 2. Uniform Record Costs, Including All HIGH Margins

The projective-arc gate and fiber size<=h imply that every proper
rank-j flat contains at most j*h nonzero original evaluations. Thus the
required flat-core basis theorem applies. Universal-core emptiness
implies the common satisfied zero-normal count g=0, even when z>0.

For raw mismatch r<=T its completed-core tuple cost is at least

    beta(r,J)=12*r*product_(i=0)^10(d+J-r-i*h).

The supplier proves beta(r,J)>=beta(1,J) throughout1<=r<=T when
12*T<m-10*h. The weakest endpoint gives

    d+L-10*h=67473>6000.

So put b(J)=12*product_(i=0)^10(d+J-1-i*h)>0.
For raw>=501, the completed cumulative weight gives the SAME record at
least5500*m*P independent tuples. This is a proved weight at gap67472,
not an additional high-family budget. All factors of b(J) increase with
J; exact integer multiplication checks

    5500*(d+L)*P > b(H).

Consequently every HIGH record also has cost>=b(J) for all L<=J<=H.
Every label outside E, LOW or HIGH, is charged on the one resource (U).
The completed-core tuples remain in the same explanation's complete scalar
agreement set, so they satisfy the secant theorem's distinct-fiber rule.

## 3. Bound The Entire J Interval Analytically

Apart from a positive constant, U(J)/b(J) is

    (R+J)^12 / product_(i=0)^10(d+J-1-i*h).

Its logarithmic derivative is at most

    12/(R+L) - 11/(d+H-1) < 0,

since11*(R+L)-12*(d+H-1)=10673987>0. This proves the bound on
the ENTIRE real interval, hence every integer J in it. The exception
allowance E_max was bounded at the opposite endpoint separately.

The exact result, including one original near allowance, is therefore

    floor(U(L)/b(L))+E_max+134944=274545534639685994.

Independent rational and integer-scaled evaluations check this floor and
the435193471709093 reserve. The count is over original distinct labels;
the near allowance is not introduced recursively. The assembly supplies
the original post-near transport and shows that this is below its existing
whole-line maximum274979661975561635.

## 4. A Genuine Smooth-Domain Carrier

The prime p=2130706433 has p-1 divisible by2^21. Exact modular arithmetic
gives omega=3^((p-1)/2^21)=1213133211, with
omega^(2^21)=1 and omega^(2^20)=-1. Its order is exactly2^21.
Define

    A_j={omega^(j+1024*t):0<=t<2048}, 0<=j<1024.

Each A_j is a full fiber of X^2048 on the smooth domain and has2048
points; different j give different nonzero fiber values. Use
D=A_0 union ... union A_521 union {omega^522}. Then |D|=1069057=R+L.
The degree-<L carrier in the statement has actual dimension eleven and
no zero evaluations. Its projective evaluation vectors are Vandermonde
vectors (1,y,...,y^10). Any at most eleven different values are independent.
There are523 fibers, each of size at most2048, so all three gates hold.

The exact histogram, rather than the relaxed560-slot resource, gives the
stronger source total272166503326104174 by the same proof. This example
shows feasibility of the carrier gates and is not an assertion that its
selected explanations must minimally span V or that it is an unsafe line.

The previously proved progression payment begins atJ=23000, and the
unclustered-arc theorem forbids repeated projective evaluations. Neither
already covers this J=20481 domain. The present source-class payment
comes from the explicitly filtered resource, not an out-of-scope import.
