# Audit

The important change is the numerator resource, not a new unconditional
minimum basis cost. Receiver secant slopes are charged once before the
remaining tuple count is restricted. Carrier-zero labels are included;
the exception inequality absorbs their count because2048>=3.

The fiber count is a bound on NONZERO projective classes, not a selected
core's number of colors. The cap2048 counts original coordinates, not
distinct receiver colors. The arc condition implies every proper-flat
occupancy bound and retains polynomial-root and full-code-badness scopes.

The LOW monotonicity guard is6000<67473. The HIGH lower count at J=20481
exceeds the LOW cost at J=22999, so one b(J) covers both cases everywhere.
The all-J ratio decreases by an explicit derivative bound. No endpoint
extrapolation of an optimizer, source enumeration or free near removal
is involved. Independent code checks exact floors and failed scope/cap
mutations; the generic supplier checks actual small-field receivers.

The smooth-domain example uses an exact-order root and a compressed
fiber histogram; no million-point list is allocated. Any supplied receiver
still has to satisfy the actual bad-support contract. This demonstrates
a nonempty carrier class, not existence of a dangerous or full-rank
original selected family. Earlier progression results start at23000 and
cannot be used to dismiss this lower-degree example.

The statement is complete at its gates; independent external hand review
remains due. No original red or entire residual J interval is promoted.
