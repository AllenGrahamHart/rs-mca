# Result

PROVED: coefficient(r-1)/(2(n-r)) funds the coordinate weight a_i-1 on
the entire distinct-fiber tuple resource for every n>r>=2. It is the
largest uniform coefficient for this relaxation, with equality already
on a polynomially realizable one-doubleton partition for r>=4.

This removes the previous maximum-fiber/collision-mass penalties from
the resource credit. It also blocks attempts to increase a uniform credit
further using only this unrefined partition resource. Stronger source or
tuple information would be needed. No unrestricted MCA or Prize closure.
