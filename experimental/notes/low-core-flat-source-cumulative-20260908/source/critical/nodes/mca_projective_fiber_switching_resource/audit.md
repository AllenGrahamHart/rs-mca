# Hand Audit

Every outgoing switch starts from r distinct source fibers and inserts a
different coordinate from one retained fiber into a different position.
It produces exactly one doubled fiber and r distinct coordinates. A target
has two orientations, and the removed coordinate must lie outside the
UNION of its represented fibers. Incoming count is exactly2(n-|S_b|),
not2(n-r+1); |S_b|>=r because the tuple coordinates are distinct.

One doubleton and n-2 singletons makes both inequalities equalities with
positive marked mass. The polynomial carrier span{1,g,Xg,...} realizes
that partition and actual rank for g=X(X-1), but does not realize an unsafe
MCA line or every tuple as an independent agreeing tuple. This scope
restriction is essential to the optimality claim.

Primary controls cover4317 histogram/rank cases,245 sharp partitions,
312 direct switch targets and an actual rank-three F17 carrier. The
independent checker counts subsets of integer partitions and uses scaled
integer inequalities, with no primary, Fraction or dynamic-symmetric import.
Both orientation-factor and denominator mutations fail on the sharp family;
using a_i instead of a_i-1 fails already for all-singleton fibers.

No derivative monotonicity, maximum fiber A, collision cap Tstar or receiver
descent is used for the new credit. The cost inequalities of the earlier
weighted node hold for every lambda>=0: only its resource argument used
the old sufficient CREDIT guard. The source exception allowance still
needs pricing by a consumer. External independent mathematical review
remains due; the controls are not formal proof or a full Prize certificate.
