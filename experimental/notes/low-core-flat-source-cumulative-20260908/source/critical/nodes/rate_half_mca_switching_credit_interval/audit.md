# Hand Audit And Exact Controls

The key scope check is that the older marked-core/tangent inequalities
hold for ANY lambda>=0. Their proof used the older CREDIT guard only
for its resource upper bound. The new sharp switching theorem supplies
that bound for the same coordinate weights, with the same exception set.
This does not apply a conditional cost theorem outside its hypotheses.

The source cap still prices all secant/zero exceptions. The large-fiber
alternative, empty universal core, full-code badness and original finite
labels are unchanged. One lambda is fixed before counting any core.
Every generic moment/profile/completion inequality is checked on the new
range; only the preceding finite payment itself remains restricted to its
old interval. Core C is not identified with full-source T.

Both exact engines cover19 degree blocks,40489 boxes,18068 old-count
supplements and no exclusions. Rank one has18601 boxes; each other rank
has2432. The common digest for every source credit, collision endpoint,
tangent maximum, marked bonus, final cost and cap is
e9087d3555bafce49cba6c75e09c0c6eb45a5025d057f5f20e9b762f48c0fa86.
Each engine checks80978 wrong adjacent floors; the primary rejects four
broken degree covers. Exact source/size endpoints are included.

The integer audit reuses the prior INDEPENDENT integer cost formulas,
not the primary or Fraction. It reconstructs its own degree/size cover,
sharp rational credit and Euclidean divisions. Formula agreement supports
the finite arithmetic, not external review or formal proof of the source
argument. Runtime/RSS are in the correspondence record. An intentionally
unset digest stopped the first calibration only after it had printed the
complete expected maximum. Final digest-locked primary and independent
replays pass normally and under-O. The primary takes27.69/27.83s and
19712/21468KiB peak RSS; the independent takes9.84/11.05s and
19072/20932KiB. These runs are serial under RAMguard tiny.

The first coarse cover showed an over-budget relaxation, not an unsafe
line. Only the rank-one size cover was refined, once, with a fixed1024
choice. The unsuccessful inside/outside moment prototypes are not proof
dependencies or evidence of an actual-source counterexample. No Modal,
large-memory enumeration, omitted box or Prize closure is claimed.
