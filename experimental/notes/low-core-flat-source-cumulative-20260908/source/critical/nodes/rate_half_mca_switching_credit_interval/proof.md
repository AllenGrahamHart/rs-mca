# Proof By Sharp Source Credit And Guarded Existing Core Costs

Set D=67466,R=1048576,d=67472. Fix the normalized source, actual
rank-eleven carrier, finite labels and full-code-bad explanations first.

## 1. Same Source Alternatives, Stronger Resource

A complete nonzero source fiber of size>=J-6000 already pays the whole
source by266180883463176443 including near, throughout21000..52999.
Otherwise every such fiber has size<=J-6001. Source coordinate weights
are w_x=a_i-1 on full nonzero fibers and zero on carrier-zero coordinates.
Let T=sum_i a_i(a_i-1), and let z count carrier zeros.

On J0..J1 put n0=R+J0,n1=R+J1. Root capacity gives z<=J1-11.
The weighted supplier's polynomial SOURCE bound and n0>=10*(J1-10)
give T<=Tmax=floor(n1*(J1-11)/10). This is an auxiliary moment bound,
not removal of zero labels or receiver descent.

Use the sharp switching theorem with ONE common

    lambda=11/(2(n1-12)).

It bounds the entire source-weighted independent-tuple resource outside
the SAME secant/zero exception set by(n)_12<=(n1)_12. No old Bonferroni
CREDIT restriction on this lambda is imposed. The exception set has size
at mostJ1-11+floor(Tmax/2). The source moment is still used to price this
exception set and bound core collisions; it no longer penalizes lambda.

## 2. Whole-Core Weighted Costs Are Valid For This Lambda

Every LOW raw<=6 record has a nonzero joint core of size M=D+J, spanning
the actual carrier because M>J. Its minimizing explanation and witness
remain fixed. Completed-core bases plus one actual witness defect give
twelve recoverable independent original-coordinate tuples per ordered
basis. Nonnegative coordinate weights preserve this counting lower bound.

Choose any maximizing complete proper core flat, rank t and size a.
Then1<=t<=10, t<=a<=J-11+t; at t=1 also a<=J-6001. All cores
are covered, including low density and nonunique maximizing rank.

For a box a0..a1 put M0=D+J0,M1=D+J1,h1=a1/t,ell=11-t,k1=J1-a0.
Freeze the ORIGINAL rank-eleven profile mu for(D,J0,J1,11,h1). Set

    Cmin=a0(a0-1) at t=1, otherwise0,
    Cmax=min(M1(h1-1),M1((J1-1)mu_11-1),Tmax,
       a1*h1+min((D+k1)*h1,Q(D+k1,k1,ell))-M0).

These are the generic original-fiber and complete-flat moment bounds,
with Q(v,k,1)=v^2 and the proved quadratic moment envelope at higher
rank. Quotient fibers coarsen original outside fibers; they are not given
the original h1 cap. No box is discarded. The certificate verifies
Cmin<=Cmax,J0-h1>=1,J0-1-Cmax/M0>=1 in every case.

Let F=F_10 be the fixed original-profile child polynomial and G=F(J0-h1).
For C0=Cmin+i(Cmax-Cmin)/4, i=0..4, put x0=J0-1-C0/M0. The previous
weighted-cost theorem, valid for EVERY lambda>=0, gives

    L(C0)=M0*F(x0)+C0*F'(x0)
             +(11*lambda*G-F'(x0))*C_endpoint,

using Cmin if the coefficient is nonnegative and Cmax otherwise. Thus
12*max_i L(C0) is a valid LOW cost in the NEW resource. The old CREDIT
guard belonged only to the previous resource proof, not these core
inequalities. No optimized-profile convexity or label-specific lambda
is assumed. These five tangents bound entire collision intervals.

## 3. Supplementary Generic Counts And HIGH Records

Retain the same B_old constructed in the preceding weighted interval's
proof section4: arbitrary complete-flat signed completion, full-core greedy
and original profile counts, and the maximum of guarded quotient bounds.
Here the actual parameters still satisfy a1<D+1,ell<=k0<=k1<D,
with k0=max(ell,J0-a1). Hence every GENERIC inequality used there applies
on this new degree range. Rank-one quotients use D+k0 directly. Balanced
products are used only at their hereditary guard; signed tangents use
the correct lower or upper ratio. We reuse these formulas, not the prior
finite payment outside its stated range beginning at21800.

The same marked basis count supplies bonus11*lambda*Cmin*G. If the
preliminary cap exceeds the fixed trigger274960000000000000, increase
the LOW cost to the maximum with12*(B_old+bonus). This counts the SAME
basis collection and weighted resource, not two independent resources.

Every HIGH raw>=7 record retains the generic nonnegative-weight cost

    beta_high=(d+J0)*product_(i=1)^10(d+i)*10488/125.

The proved weight argument covers7..84 and all raw>=84, including raw>d.
LOW and HIGH divide the one resource by their minimum cost. The former
source/quotient receiver and original labels are never replaced.

## 4. Fixed Exact Certificate

Partition21500..21799 into width16 degree blocks, truncating the last.
At rank t use size range t..J1-11+t, further capped by J1-6001 for t=1.
Use bin width ceil((top-t+1)/P_t), with P_1=1024 and P_t=128 for t>=2.
These are explicit gap-free covers of all integers; each box represents
a proved uniform inequality, not a representative sample.

For each box the whole-source upper bound is

    floor((n1)_12/min(beta_low,beta_high))
       +J1-11+floor(Tmax/2)+134944.

Both exact implementations cover19 degree blocks,40489 boxes,18601 at
rank one and2432 at each rank2..10. Every source credit, collision
endpoint, tangent maximum, marked bonus, final cost and cap agrees.
Their maximum is274956328426911303 atJ21564..21579,t1,a3633..3648.
The18068 old-count supplements and every exact certificate entry have digest
e9087d3555bafce49cba6c75e09c0c6eb45a5025d057f5f20e9b762f48c0fa86.
Each checks80978 adjacent wrong floors. The primary also rejects four
broken degree covers. The bound is a certificate maximum, not an
extremal-line construction or a proof of optimality for the row.

The source-fiber alternative is smaller, so PAYMENT holds for every source
at the stated scope. One secant/zero exception set and original near134944
are included once. Surviving explanations need not span the retained
enclosing carrier. No source/core collision consistency hypothesis or
experimental split-moment refinement is used.

## 5. Union And Original Interface

The preceding weighted theorem pays21800..169999 by274954262108377832.
Taking the MAXIMUM with the new larger bound proves the21500..169999
union. The original assembly must supply its exact complete-core gauge
and full-code-bad normalization before this theorem is applied. That
transport maps21500..21799 to300 complete g values1026777..1027076.
Neither the remaining lower interval nor higher original ranks, LIST or
the full Prize problems are proved by this finite certificate.
