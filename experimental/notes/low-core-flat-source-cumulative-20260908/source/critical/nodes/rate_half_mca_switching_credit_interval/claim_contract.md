# Contract

Original field q=2130706433^6, normalized(n,K,m)=(1048576+J,J,67472+J),
21500<=J<=21799, actual polynomial carrier dimension11, full-code-bad
selected witnesses and empty universal core. Keep nonuniversal zeros,
original finite labels, received pair and field denominator.

The source-fiber>=J-6000 theorem supplies one whole-source alternative.
Otherwise weights a_i-1 and the single switching lambda are source-fixed.
The old Bonferroni CREDIT guard is NOT a premise: the new resource proof
replaces it. SOURCE still bounds T for exceptions and core collisions.
The core-cost proof was and remains valid for every nonnegative lambda.

All maximizing ranks1..10 and their full integer size ranges are covered.
Rank one uses1024 fixed size bins and the others128; there is no adaptive
pruning. The original-rank profile is frozen, all convex arguments are
guarded, signed coefficients use their correct endpoints, and every HIGH
margin is counted. Auxiliary quotient counts are not receiver descent.

Only generic formulas from the preceding weighted certificate are reused
at their individually checked hypotheses. Its older finite payment is
not invoked below21800. It is separately required for the high-union claim.
The new total includes one source exception set and one original near
allowance. Original normalization remains the downstream assembly's proof.

The switching constant is sharp for its partition resource, not for this
finite row. No source-consistency conjecture, experimental split-moment
count, original-rank bound or unrestricted endpoint is assumed or supplied.
