# Claim Contract

- Exact normalized KoalaBear source: fixed field, actual dimension eleven,
  fixed carrier/translate/received pair, original finite labels, full-code
  badness and empty universal carrier core. Nonuniversal zeros may remain.
- All J=30000..31999, all actual maximum-density flat ranks and sizes,
  all complete-core record occupancies and all projected receiver groups.
- The separately paid integer near-full rank-one class is included in
  the final maximum; no unpriced carrier-size assumption remains.
- Three smaller J blocks replace the coarse relaxation. Eight size slices,
  64 record bins, eight heavy bins and all proof gates remain unchanged.
- Record costs share one original tuple budget. Whole-source alternatives
  combine by maximum. Original near is added once, never inside children.
- Exact arithmetic is load-bearing, alongside the hand-proved coverage.
  A partial block replay is not a replay of the complete interval.
- No original-source transport, rank bound, full adjacent row or Prize
  conclusion is supplied without the named downstream assembly.
