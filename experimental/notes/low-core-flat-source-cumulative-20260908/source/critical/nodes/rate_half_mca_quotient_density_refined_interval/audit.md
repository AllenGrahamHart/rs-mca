# Audit

The scope audit checks the 30000 endpoint before reusing formulas proved
numerically in the parent only from 32000. Required root/density branches,
positive completion factors, actual quotient degree/gap gates and receiver
ratio signs all extend. The existing receiver-fiber payment covers the
removed integer class because 30000..31999 lies inside 14000..52999.

The same-source, original-label and complete-core proofs are unchanged.
The grid classifies record occupancies separately from source alternatives;
smaller degree blocks do not supply new tuple budgets. No-heavy sources
retain the first source bin. The full child exception charge and original
near allowance are included exactly as in the parent.

All three blocks have independent exact certificates, including every
record-cost and source-bound digest, not just the final maxima. The
independent wrapper uses the parent's independent cost engine and rational
source accounting. Neither audit imports its primary counterpart.
Both adjacent wrong floors are rejected for all 10560 source boxes;
five broken degree covers are rejected by the primary wrapper.

All three blocks pass normally and directly under -O with explicit checks:

    primary normal: 12.46 seconds, 21948 KiB RSS;
    independent normal: 16.81 seconds, 22512 KiB RSS;
    primary -O: 16.06 seconds, 23576 KiB RSS;
    independent -O: 16.66 seconds, 24264 KiB RSS.

The --start option replays one complete block and explicitly reports
its partial scope. Each script prints progress after every flat rank.
Runs are serial under RAMguard tiny; no original-field enumeration,
large table, Modal job or spending is used. The independent corridor pin
is also explicitly checked in the final optimized replay.

The initial spot probes were diagnostic only. In particular the J=28000
selected boxes remain over budget even at singleton degree blocks; this
does not construct an unsafe source or prove that every refinement fails.
No further scan was substituted for the scope proof. The gain is precisely
the smaller block relaxation and no novelty claim is made for the geometry.

Independent external mathematical review remains due. The ordinary field
is never enumerated, and a matching digest does not prove analytic coverage.
