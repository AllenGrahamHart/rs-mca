# Local hand audit

The entire weighted polynomial space is used. Counting conditions only
gives an upper bound on interpolation rank, which is the correct direction
for the kernel lower bound. Double points cost four, not three, conditions
in the original ambient three variables. The received word is never
differentiated. Substitution into the maximal ideal proves multiplicity
even in small characteristic. All A joint agreement points are retained.

The comparison is against the whole G-divisible subspace. Primitive Gauss
division prevents rational-denominator quotients from evading the weighted
degree charge. The strict positive difference supplies an actual nonzero
Q not divisible by G, not merely a larger abstract dimension estimate.
Irreducibility then gives a proper plane intersection. This prices every
function-field point, including singular points and isolated coefficient
solutions, without a rational-parameter model.

The finite check independently evaluates every J in 8764..9821. A second
implementation enumerates weighted monomials and translated Taylor
coefficients on tiny controls, with no import of the primary verifier.
Their arithmetic does not replace the universal hand proof or external
review. The original-source assembly retains its finite-field labels and
near allowance separately. No field-sized interpolation matrix is built.

Provenance: derived locally from the existing full-kernel argument by
changing point multiplicity. The only required inherited geometric fact
is the previously proved hypersurface intersection degree bound. This is
not claimed as a new invention of multiplicity interpolation; the new
local result is the explicit curve-escape criterion and its finite use.

The first full integer replay caught an omitted third monomial block
transition at 9568/9569. The original two-piece calculation was valid
through the full-strip endpoint 9526 but was incorrectly extrapolated
beyond 9568. The corrected three-piece gap stays positive through 9821,
with gap 80, then is -284. This changes the restricted payment range,
not the exhaustive full-source endpoint 9526. Both implementations now
test the additional transition explicitly; no graph was compiled with
the incorrect gap formula.
