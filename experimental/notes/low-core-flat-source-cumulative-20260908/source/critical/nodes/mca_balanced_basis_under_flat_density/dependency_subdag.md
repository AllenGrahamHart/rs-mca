# Dependency Sub-DAG

`mca_fiber_contraction_core_basis_resource` --req--> this node.
Its full-locator identity, common-root-space capacity and completed-basis
incidence interface are used. Its quadratic profile theorem is not used.

This node --req--> `rate_half_mca_balanced_density_payment` --req-->
`rate_half_mca_rank_twelve_paid_interval_assembly`.
All required statements are proved. No speculative child is introduced.
The large-fiber guard counterexample is a specialization of the supplier's
existing construction, not an assertion of new unsafe MCA data.
