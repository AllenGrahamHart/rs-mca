# Contract

Actual polynomial carrier, full-code-bad same supports, original finite
slopes, and empty universal core. The projected pair is its restriction
on a COMPLETE nonzero evaluation flat. Different projected core sets may
overlap; only their hyperplane-distance intersection bound is used.

The full locator and the a-t exceptional labels are explicit. The child
retains empty universal core but need not retain a post-near condition or
the selected explanation span. The cap U is for all required anchored
families and the unchanged field, not independently normalized quotients.

SHARED requires an actual common tuple lower charge eta*beta_max on every
record. Its proof uses the same original tuple resource for both groups.
Finite consumers must supply that charge, beta<=beta_max and their child
caps from proved statements. No speculative DAG premise is introduced.
