# Contract

- Object: ordered independent evaluation bases, then original MCA tuples.
- Coordinates: distinct original field points; evaluations nonzero on the
  counted core, not necessarily on the entire received domain.
- Rank and degree: actual rank r, degree <K, N=D+K. Contraction removes
  the ENTIRE projective fiber and leaves the same gap D.
- Quadratic step: 4<=r<=E<=D, C>=0, B>=D*C, and a universal rank-(r-1)
  child bound on every integer degree r-1..E. These are explicit sufficient
  hypotheses, not an assertion about arbitrary quadratic approximations.
- Real endpoint arguments are relaxations of actual integer partitions.
- MCA: fixed carrier/receiver, full-code-bad supports, universal core zero,
  distinct finite original labels; one resource, not a sum of maxima.
- No source normalization, carrier class conjecture, field descent or
  unrestricted prize payment follows without its separate consumer.
