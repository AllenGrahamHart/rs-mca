# Claim Contract

Same normalized source, actual dimension11, original field and finite labels,
full-code-bad supports and empty universal core. No extra density premise.
The new source-fiber gate is a proved whole-source alternative, not an
assumption that large fibers are absent. Its failure gives k>=8001 at t=1.

Every LOW record chooses its own actual core and maximizing flat where
needed. All possible ranks and integer quotient sizes are covered. Rank
profiles are fixed on whole degree boxes; signed counts use valid corners.
One original tuple resource and one original near allowance suffice.

Falsifiers: a source satisfying the exact hypotheses exceeding the total,
an omitted fiber exception or parameter box, or an invalid profile/corner
bound. The older narrow interval is not extrapolated; its formula scope
is rechecked in the proof. No whole-source rank bound or Prize closure.
