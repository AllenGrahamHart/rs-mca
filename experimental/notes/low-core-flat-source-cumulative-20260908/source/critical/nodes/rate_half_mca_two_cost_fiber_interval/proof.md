# Proof: Replace The Source-Fiber Cutoff In The Rank-Profile Argument

Use D=67466,R=1048576,d=67472. All pairs, raw margins and M=J+D
point complete-core subsets are selected AFTER fixing the normalized
source. Original finite labels and full-code-badness are retained.

## 1. A Stronger Exhaustive Source Split

If any complete nonzero SOURCE fiber has size a>=J-8000, the required
two-cost source theorem pays the whole source by265879110627611677,
including original near. Its stated23000..52999 scope includes this
entire interval. Otherwise every source fiber, and hence every rank-one
fiber inside any LOW core subset, has size<=J-8001.

The preceding rank-profile theorem's geometric proof now applies with
that stronger restriction. LOW raw<=6 cores have actual rank eleven
and nonzero evaluations by the original source contract. Split each
core by its OWN maximum density at(J-4)/7. The low-density branch
uses the rank-profile product; otherwise a maximizing complete flat
has rank1<=t<=6, size b, and

    floor(t*(J-4)/7)+1<=b<=J-11+t.

The only new geometric restriction is b<=J-8001 at t=1. Equivalently
its auxiliary quotient degree k=J-b is at least8001, not4701.
No original source is discarded by the split.

## 2. Uniform Bounds And Full Parameter Cover

The earlier proof's rank-profile theorem is valid for all positive gaps
and degree boxes, not only the parent's24000..26499 interval.
Use consecutive width16 degree blocks on23000..23999, truncating the last.
For rank t put ell=11-t. All quotient degrees lie between
ell (8001 at t=1) and J1-floor(t*(J1-4)/7)-1.
Partition that full integer interval into at most128 bins before inserting
the existing root-balanced threshold floor((D+f*(ell-1))/(f-1))+1,
f=floor(ell*ell/4).

On every k0..k1 bin retain the same four proved quotient counts: greedy,
general quadratic, guarded root-balanced and the uniform rank profile.
All actual ranks and degree guards remain valid:
5<=ell<=10, ell<=k0<=k1<24000<D. Proper-flat density is bounded
by min(k1-ell+1,(J1-k0)/t). The root coefficient uses k1 and the
density coefficient uses k0; neither is a sampled source profile.

The arbitrary-flat inside count uses the previously proved complements
max(D+11-r,D+J-floor(r*(J-k)/t)). They increase with k and use
the lower J corner for r<=t, the upper corner for r>=t. In particular
t=6,r=5 is handled correctly. Signed negative tails retain upper
extension ratios. The independent audit reconstructs full tangent vectors.
Also retain the valid full-core greedy and rank-profile lower counts.

The LOW density threshold, root rank split, positivity and corner arguments
hold throughout the new interval; no old narrow J theorem is assumed
outside its printed source scope. Raw-HIGH>=7 keeps its existing cost
(d+J)*P_d*10488/125. All record cases share ONE resource
(R+J1)_falling_12, with twelve defect positions per counted core basis.

## 3. Exact Certificate And Original-Scope Limit

Primary and independent exact engines cover63 basic comparisons and48357
high-density boxes. Their maximum, including one original near, is
274846585959022192 at J=23000..23015,t=6,k=3021..3046.
The separately paid degree-8000 source class is smaller and is included
in the final maximum. All degrees, possible flat ranks and sizes are covered.

The older union24000..169999 has total274929007493481160, which is
larger than the new total. Their maximum proves the stated union
23000..169999. Neither near allowances nor alternative whole-source
budgets are added. Auxiliary quotients only count polynomial bases;
no receiver, label, field, original support or nonuniversal zero is lost.
The source assembly separately binds this to the initial rank-twelve core.
