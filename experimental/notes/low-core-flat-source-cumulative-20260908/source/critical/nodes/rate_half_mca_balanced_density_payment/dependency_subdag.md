# Dependency Sub-DAG

Required, all PROVED:

- `mca_balanced_basis_under_flat_density`: the full core-basis product.
- `mca_core_completed_basis_margin_resource`: same-source defect tuples and
  HIGH mismatch weight, including untruncated large margins.
- `mca_maximum_density_flat_core_basis_resource`: actual proper-flat
  maximizers, coordinate density and their polynomial root-space capacity.

This node --req--> `rate_half_mca_rank_twelve_paid_interval_assembly`.
The assembly transports original labels and adds near once. The new result
does not require or alter the published quotient-density interval theorem.
The existing interval assembly's original red router remains TARGET.
