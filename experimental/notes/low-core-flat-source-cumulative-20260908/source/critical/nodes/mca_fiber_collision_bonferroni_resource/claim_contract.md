# Contract

The class sizes and T belong to one fixed ORIGINAL carrier on its complete
evaluation domain. The receiver enters only through the single exceptional
slope set. A bounds all nonzero source fibers, not just fibers in one core.

T counts ordered pairs, whereas its exception allowance is T/2 unordered
pairs. The r-tuple coordinates are distinct. The disjoint-pair intersection
upper bound is T^2, not an unjustified equality or an independence claim.

A consumer using monotonicity must verify its entire T interval. Zero
coordinates are padded only in the resource upper bound and are charged
as actual labels by the secant theorem. No child near or new denominator.
