# Claim Contract

The source is the SAME normalized KoalaBear receiver on the SAME field,
with actual polynomial dimension11, distinct original finite slope labels,
full-code-bad supports and empty universal carrier core. Nonuniversal zeros
are retained. Every original raw margin is selected after normalization.

The whole-source fiber branch is paid first. Remaining LOW records split
exhaustively by their OWN core density; this is not a global source premise.
All maximum-density flats and full-locator quotients count bases only.
No received child, exception removal or new near event is inferred.

Each rank profile is fixed uniformly across its entire degree box.
Its root coefficient uses the UPPER degree and its density coefficient the
LOWER degree. Jensen is applied to a proved fixed-profile polynomial,
not to an optimized numerical envelope.

Finite certificates cover ALL degrees24000..26499 and every allowed flat
rank and size. All record cases consume the SAME original tuple resource.
Whole-source bounds combine by maximum. The original near is included once.

Falsifier: a source satisfying these exact hypotheses with more than the
printed near-inclusive cap; an uncovered parameter box; or an invalid
profile/coefficient/corner/rounding gate. A larger upper estimate at a lower
degree is not a counterexample to MCA or a proved impossibility theorem.

No higher-error-rank bound, lower-degree payment, active-v4 owner atom,
ordinary LIST bound, external acceptance or full Prize closure is claimed.
