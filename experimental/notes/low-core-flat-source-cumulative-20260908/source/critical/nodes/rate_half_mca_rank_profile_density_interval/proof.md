# Proof Using Frozen Rank Profiles On Every Parameter Box

Put D=67466, R=1048576, d=67472 and s=11. Minimize pairs only AFTER
normalizing the fixed original source. The preceding quantitative interval
supplies the same core and tuple geometry. We verify its scope here rather
than apply its narrower J interval as an assumption.

## 1. Exhaustive Source And Core Cases

One complete nonzero SOURCE evaluation fiber of size >=J-4700 pays the
whole source by the required receiver-fiber theorem, since this interval
lies within its 23000..52999 scope. Its total is 246756107210901806,
including original near. Otherwise every rank-one fiber of any core
subset has size <=J-4701.

For each raw<=6 record choose an M=J+D point subset H of its complete
joint core. Universal-core emptiness makes its evaluations nonzero, and
M>J makes their actual rank eleven. Let h_H be maximum proper-flat
coordinate density inside this H, and split at h0(J)=(J-4)/7.

If h_H<=h0, the new rank-profile lemma counts its bases directly.
Otherwise choose a maximizing flat of rank t and complete size b=t*h_H.
Root capacity gives h_H<=1+(J-11)/t, excluding t>=7. Thus

    1<=t<=6, floor(t*(J-4)/7)+1<=b<=J-11+t.

At t=1 also b<=J-4701. This exhausts each actual LOW core without
assuming density on the original carrier. Raw-HIGH>=7 retains the proved
cost (d+J)*P_d*10488/125, P_d=product_(i=1)^10(d+i).
Only that weight arithmetic is imported from the older density supplier.

## 2. Uniform Profile And Quotient Counts

Partition 24000..26499 into consecutive degree blocks J0..J1 of width32,
truncating the last. The low-density core uses the rank-profile theorem
with dimension11, degree interval J0..J1 and density bound (J1-4)/7.
Its rank-specific moment coefficients are fixed on the WHOLE box;
the product is evaluated at scalar argument J0.

For a high-density flat set ell=11-t and k=J-b. Its complete annihilator
quotient has actual rank ell, degree <k, length D+k and nonzero evaluations.
Every proper quotient flat has density <=h_H; root capacity also gives
the density bound k-ell+1. These are polynomial basis counts, not receiver
descent.

Every actual k lies between ell (4701 at t=1) and
J1-floor(t*(J1-4)/7)-1. Partition that integer interval into at most64
equal-width bins before inserting the root-balanced threshold
floor((D+f*(ell-1))/(f-1))+1, f=floor(ell*ell/4).
For each bin k0..k1 put b0=J0-k1, b1=J1-k0.

The new quotient profile uses the ENTIRE degree interval k0..k1 and
the bound min(k1-ell+1,b1/t) on proper-flat density. In particular,
the root-density coefficient uses k1, NOT k0. Its separate density
coefficient uses k0. Using a point profile at k0 for all larger degrees
would not be justified.

Take the maximum with the inherited greedy, general quadratic and
guarded root-balanced quotient counts. Their actual rank and degree
guards hold: 5<=ell<=10, ell<=k0<=k1<26499<D. The earlier quadratic
construction is valid on this full range; its old whole-source J scope
is not needed. Every coefficient, tangent and rounding gate is checked.

## 3. Signed Inside Counts And Shared Original Resource

The complement for a core rank r is

    c_r(J,k)=max(D+11-r,D+J-floor(r*(J-k)/t)).

It increases with k. In J it increases when r<=t and decreases when
r>=t, with equality at r=t. Thus its lower box corner uses k0 and
J0 when r<=t, J1 otherwise. This explicitly includes t=6,r=5.
These statements follow from floor increments for ALL integers, not
from comparing sampled values.

Use those lower complements at ranks10 down to ell, and upper inside
caps floor(i*b1/t), in the arbitrary-flat signed BOX count. Each selected
integer tangent is valid; negative tails use upper extension ratios.
The independent implementation reconstructs the full coefficient vector.
The quotient greedy lower count is

    (D+k0)*product_(i=1)^(ell-1)max(D+ell-i,D+k0-floor(i*b1/t)).

Also retain the full-core greedy count
(D+J0)*product_(r=1)^10 c_r(J_corner,k0), and the full rank-profile
count with degree J0..J1 and density <=b1/t. Maximize the valid
counts, never add counts that may describe the same bases.

Every core basis admits the original defect in twelve recoverable positions.
Original finite labels have disjoint independent incidence tuples.
All labels, across all core cases, therefore share ONE resource at most
(R+J1)_falling_12. Divide it by the smallest applicable record cost,
including the original raw-HIGH cost. Do not assign independent budgets
to different flats, cores, quotients or source choices.

## 4. Exact Finite Certificate And Union

The primary rational and independent integer-scaled implementations agree
on 79 basic comparisons and 30565 high-density boxes. The maximum,
including original near, is 273123695048315164 at

    J0=24000,J1=24031,t=1,k0=4701,k1=4949.

The separate whole-source fiber bound is smaller. Each integer degree,
flat rank and possible size is covered; the code certifies the preceding
analytic box estimates, not a sampled-source extrapolation.

The preceding quantitative interval and its inherited union pay every
carrier on26500..169999 by274929007493481160. The maximum of that
total and this theorem's total gives the enlarged union24000..169999
with no repeated near allowance. The original-source assembly owns
transport back to the initial rank-twelve selection. No original finite
label, nonuniversal zero, full-code-badness condition or field is changed.
