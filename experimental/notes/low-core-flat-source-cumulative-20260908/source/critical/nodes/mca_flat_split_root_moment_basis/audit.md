# Audit

The flat partitions complete original fibers, so no fiber is split across
the two moment terms. Quotient merging has the correct upper-bound
direction for squares. Nonzero outside evaluations require completeness.
The ell=1 quotient uses its full square rather than dividing by ell-1.

The first moment may beat the balanced coefficient, but subsequent profile
coefficients are not changed or recalibrated at a real degree. Jensen uses
the inherited positive convex product. Whole-box numerator upper bounds
are divided by the lower N endpoint. Real arguments are checked to be>=1.

Small actual-polynomial controls compare the split moment, the root mean,
and basis counts, including nonmaximizing complete flats. Exact arithmetic
is supplementary to the universal hand proof. External review remains due.

Both normal and -O controls pass. Integral density inputs are explicitly
coerced to Fraction before division; two integer/Fraction comparison
controls guard against accidental floating-point root moments. The finite
consumer independently reconstructs the moment and frozen product with
integer numerator/denominator arithmetic across all40960 parameter boxes.
