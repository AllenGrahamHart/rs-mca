# Result

PROVED: maximum-density hybrid and coupled inside-class core-basis lower
bounds, and their single-resource distinct-slope consequence at the exact
contract in statement.md. One requirement: completed-basis margin resource.

The genuinely new coupling keeps inside-rank-deficient cores rather than
assuming the core fills its occupied flat. The finite consumer uses it
to cover every normalized source on 65000..169999; this generic node does
not own original near, transport or row-level conclusions.

Two independent small checkers agree. Classical root-space bounds, greedy
basis counts and convexity are not claimed as new general methods.
Antecedents are Hughes's margin resource and the local completed-basis,
bounded-flat and projective-fiber arguments. No external extremal theorem
or conjectural flat classification is imported. External review remains due.
