# Local Hand Audit

The maximum exists on a finite evaluation configuration, and choosing the
span of its contained evaluations is legitimate. All ranks 1..10 remain
possible in this generic theorem; low-rank concentration is not assumed.
The polynomial common-root-space bound, not individual-vector independence,
gives K-11+i on a proper i-flat.

The quotient density estimate subtracts ALL a original points of the
chosen flat from its proper preimage. Subtracting only t core points would
not prove i*h. Dividing its locator leaves the actual degree excess e.
Common carrier zeros are excluded from a joint core by g=0, but need not
be removed from the original source or its defects.

In the basis construction, the first l outside points are a quotient
basis, so their span intersects F trivially. After adjoining b independent
inside points the intersection is exactly their span. Later outside choices
can only enlarge that intersection; at least z_b inside points remain
in the growing span. This proves the outside choice bound.

Every final tuple recovers the inside positions, their order and the
first l outside positions. Different b classes are disjoint. The tangent
bound may be negative on individual choices: sum it FIRST using
sum y_b=E_(b+1). Only the resulting nonnegative C_(j,b)E_b terms may
be dropped. This is valid even when the actual inside rank is below j.
That termwise nonnegativity argument is restricted to j<=4.

The rank-five supplement keeps C_3=-165c-110 and C_4=-165 explicitly.
Actual E_3<=t*E_2 and E_4<=t^2*E_2 bound their negative contribution;
for t<=a<=c the remaining E_2 coefficient is positive at c>=100.
The same logarithmic derivative sign argument works with its separately
checked seed bound. Five actual inside-rank controls include all deficient
ranks. No claim is made for j>=6 or j=5,a>c.

The sign proof for log g uses g'>0, g''>=0 and g'''<=0 on [0,c].
Initial strict log-concavity propagates since
(g*g''-g'^2)'=g*g'''-g'*g''<=0. Beyond c the formula is positive linear;
the kink is included as a separate endpoint. P has positive factors.

Old and new record counts may overlap and combine by maximum. Distinct
original labels cannot share an independent incidence tuple. LOW/HIGH
divide one global budget, not a sum of independently maximized budgets.

Independent finite arithmetic and small algebraic controls pass; these
are not formal proof certification or external independent review.
Verdict at the printed scope: NO ISSUE. Original-source transport is not
part of this supplier and is not inferred from its status.
