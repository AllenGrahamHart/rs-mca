# Exact Contract

- Actual dimension eleven and degree <K; g=0; m=K+d; full-code-bad
  selected supports; distinct original finite slopes, one fixed receiver.
- 1<=T<=d-11; M=m-T, c>=12. Common V-zeros may lie outside the
  cores or be scalar defects; none is deleted from the source.
- Maximum density counts nonzero ORIGINAL evaluation coordinates, not
  projective classes. The maximum is over proper subspaces of V*, and
  the chosen flat includes all its nonzero original evaluations.
- Only ranks j<=4 use the inside-extension coefficient positivity and
  three-point minimum. Larger j retain the hybrid bound only.
- The locator quotient inherits both its actual remaining degree and
  the density cap from preimages containing the complete flat.
- Counts with different numbers of inside coordinates are disjoint.
  The tangent terms cancel via the exact identity sum y_b=E_(b+1).
  Old/new bounds on the SAME completed tuples combine only by maximum.
- No conjectured flat dimension, curve cover, free outside independence,
  receiver descent, canonical selector or original-row transport is used.

Falsifiers: an admissible core below (HYBRID) or (FLAT), or a valid
selected family violating (SOURCE). Small controls must include inside
sets whose actual rank is lower than that of the chosen flat. External
independent review is due; arithmetic alone does not establish geometry.
