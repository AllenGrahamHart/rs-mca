# Claim Contract

Object: an actual polynomial evaluation space and its ordered bases.
Input: N=D+K distinct points, D>=1, actual dimension 2<=s<=K and
nonzero evaluations. NO proper-flat density hypothesis is assumed.
Output: a sufficient balanced count or an actual nested complete-flat
witness satisfying the STRICT inequality FLAG. The alternatives may overlap.

Flat sizes are relative to the fixed counted core H, not the whole received
word domain. The descent lowers actual rank and degree by complete-fiber
locator division. The real Jensen arguments only bound integer-degree
children. For MCA the selected pair, explanation, core subset and original
finite slope remain fixed. No witness count, new near event, slope
projection, received-word descent or prize safety is supplied here.
