# Dependency Sub-DAG

`mca_balanced_basis_under_flat_density` --req--> this node.
We use its capped-moment proof and explicit product's convexity, and its
proved full-fiber contraction ancestor; we DO NOT assume its density gate.

This node --req--> `rate_half_mca_dense_core_flag_mass` --req-->
`rate_half_mca_rank_twelve_paid_interval_assembly`.
All inputs are proved. No conjectural census or hypothetical shape is
introduced as a new leaf. The desired upper census remains future work.
