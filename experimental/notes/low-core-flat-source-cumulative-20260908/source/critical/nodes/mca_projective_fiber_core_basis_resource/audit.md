# Local Hand Audit

The polynomial annihilator has dimension s-1 over the original field.
Dividing its actual fiber locator gives degree <K-a, not degree <s-1
unless a=K-s+1. The excess e is load-bearing. The small repeated-square
control refutes its unsupported removal from the outside-basis count.

g=0 excludes zero evaluations from an actual joint core, not from the
whole source. An outside carrier-zero defect still supplies an independent
incidence normal and is retained in the controls and global denominator.

The outside evaluation count uses the dimension of a polynomial COMMON
root space, not a false independence assertion about all outside points.
First choose a basis modulo the actual fiber; then distinguish exactly
one inside point from no inside points. This distinction makes these
two core-basis counts disjoint. A tuple in the second count has its first
s-1 positions fixed by its own order, so it is not multiply represented.

Positive log-concavity gives a lower endpoint minimum, not an upper one.
The possible kink c=M-K+1 must be included; it cannot be omitted merely
because both separate pieces are log-concave. The same count is valid on
an arbitrary fixed m-T subset of each actual core, retaining every defect.

The resulting completed tuples remain original support tuples. They pin
both slope and explanation coefficients. Old/new completed counts can
overlap and combine only by maximum on the same budget.

Finite application uses LOW<=6 and the proved HIGH>=7 weight floor.
Its four analytic lower envelopes retain both t-endpoints and the kink.
Logarithmic derivative bounds justify all-J extension; tests of endpoints
alone would not suffice. The higher-J source-class hypotheses are not
claimed for every carrier. External independent review remains due.
