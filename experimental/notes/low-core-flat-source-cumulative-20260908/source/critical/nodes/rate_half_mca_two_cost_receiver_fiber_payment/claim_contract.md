# Claim Contract

Fixed normalized original source, actual polynomial dimension11, full-code
badness, original finite labels, one field and empty universal carrier core.
One qualifying COMPLETE nonzero fiber suffices. Root capacity bounds it by
J-10; its actual receiver colors, not guessed core intersections, define
the heavy/light partition.

Every LOW heavy core includes ALL of its color's coordinates before bases
are counted. Separate heavy and light costs are lower bounds on the SAME
independent tuple resource. Positive-part handling is mandatory.
All exceptional original labels are charged before full-locator division.
Q10 is a FULL dimension-at-most-ten cap, not a degree-8000 Johnson claim,
post-near cap, zero-core assumption or active-v4 allocation.

The quadratic input is used on its printed general11..65000 basis scope.
The raw-HIGH inequality is arithmetic, not an imported old fiber premise.
Uniform a/J monotonicity and occupancy endpoint minima prove every box.
All integer degrees and real occupancy ratios in the stated gates are covered.

Falsifier: a source satisfying one printed fiber gate exceeding its total;
loss of an original label; an invalid full-child scope; or a parameter
box whose claimed costs exceed the guaranteed record counts.
No source existence, unrestricted row, higher-rank or Prize claim follows.
