# Local Hand And Independent Arithmetic Audit

## Exhaustiveness

The h<=h_5 branch covers all possible maximizing-flat dimensions by the
hybrid root/density estimate. In its complement, the actual common-root
bound forces j<=4; this is not a structural conjecture. Use the closed
interval j*h_5<=a<=J-11+j as a valid relaxation.

For each fixed J, all min(e+i,i*h) changes occur at a=j*h_(j+i).
Above j*h_5, switches with j+i>5 lie outside the range. Endpoints r=j
and r=5 are the range boundaries. The three t endpoints and the extra
a=c kink exhaust every positive log-concave a-piece.

At moving a=j*h_r, profile choices are fixed for all J>11. Only a=c
changes the g formula or activates t=c. The t=0 and t=c quotients
decrease. For t=a, both sides of c are log-convex by the printed exact
curvature bounds. At constant a=c, profile switches are exactly the
same J_(j,r), and the remaining pieces are convex. The only extra outer
boundary is j=1,J=169999. Thus 91 indexed endpoint checks are complete.

All relaxed J,a,t values keep e>=0 and every quotient factor positive.
The finite proof does not presume that fractional endpoint parameters
are actual sources. They bound the real intervals containing the sources.

## Independent Arithmetic

verify.py uses Fraction products. verify_audit.py uses integer pairs and
a common scale, with the degree-twelve/degree-eleven scale correction
explicit in the denominator. The independent audit constructs the g
polynomial by coefficient convolution to check its initial curvature.
Both produce the same four maxima and low-density endpoint; the audit
rejects all 91 off-by-one floor mutations.

The resource ceiling is checked at both ends, with convexity proved in
the text. HIGH>=7 has floor 10488/125, including raw>d; LOW<=6 has
12*B tuples. Their larger quotient, not sum, plus one 134944 equals
274929007493481160. Its exact field reserve is 51720617913927.

## Scope

Actual dimension eleven, degree<J, distinct original finite slopes,
full-code-bad supports and g=0 are retained. No receiver descent,
pair-curve cover, fiber condition or field change remains as a premise.
This is stronger than the earlier high-J source-class payments.

Original post-near exhaustion and add-back are not consequences of matching
the normalized parameters. Higher ranks and LIST are separate. The original
router remains TARGET with evidence only; no row or prize is closed.
Verdict: NO ISSUE at the normalized-source scope. External independent
review and any formalization remain separate from these local audits.
