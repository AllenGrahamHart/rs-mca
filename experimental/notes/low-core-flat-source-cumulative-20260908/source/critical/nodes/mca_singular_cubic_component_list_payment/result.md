# Result

PROVED: the `cutoff101.md` companion counts all rich cubic pairs by
255040048958 on 8656..8763 at h>=601, with <=4622 per top component
and all lower components included. With the finite consumer's separately
proved resource conversion and full-kernel coverage, this closes 108
additional normalized dimensions. Full coverage now ends at 8763; older
cutoff-specific counts below retain their historical scopes.

The PROVED continuation `next_interval.md` extends the d=7 high-height
source bound to 8656..9526, 1301<=h<=4762, with N<=241058823023306107.
Coupling h in both Johnson budgets gives <=4271 pairs per top component;
all lower-dimensional components are counted. The new checks are
`verify_next_interval.py` and `verify_next_interval_audit.py`; the old
independent height rectangle fails here. No requirement changes or
quartic closure follows. Earlier records below keep their narrower scopes.

CLOSED: the remaining d=7 affine-singular source class with
1301<=h<=4327 on normalized 7117..8655, N<=73164604161759423.

All three-dimensional components are counted, at most 315 nonsingular
pairs each and at most 3^16 components. The lower-dimensional complement
has <=56703328989 pairs. Singular/off-curve and resource/near add-back
are included in the whole source bound.

The full-kernel consumer can therefore close all 1539 remaining J-values
7117..8655, with no assumed curve description, at its already established
paid-alternative bound 274979661292365251. The combined normalized
interval 4801..8655 is paid. The unpaid range begins at 8656.

No original red, unrestricted endpoint, higher original rank, ordinary
LIST theorem or prize closes. The generic component machinery and the
full lower-strip coverage form a future grouped K3/K4 export candidate,
not an automatic upstream bankability claim.
