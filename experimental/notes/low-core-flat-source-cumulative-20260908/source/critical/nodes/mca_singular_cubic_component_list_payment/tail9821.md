# The d=7 high-height class on the double-point tail

Status: PROVED. Use the unchanged canonical normalized source on
9527<=J<=9821, LOW_500, d=7 and 1601<=h<=4910, with <=256
off-cubic pairs. The generic component model and degree-cost proofs
already in this node apply. Then the whole original source satisfies

    N<=142942788280886491.                            (C7T)

The finite consumer separately pays h<=1600 by cumulative LOW weights;
that low-height payment is not a premise of this theorem.

## 1. Count every three-dimensional component

The true parameter degree satisfies

    2<=ell<=floor((J-1-h)/3)<=2739<=2740.

After retaining and optimizing the identical agreement coordinates, the
existing component-list proof gives

    a=66973+h+3ell, S=2097154+4h+7ell,
    M_component<=floor(S*(a-ell)/(a^2-S*ell)).

For fixed ell, S=4a+b with b=1829262-5ell>0. Its derivative in a
has numerator -(b+12ell)*a^2-6b*ell*a-b^2*ell<0, as proved in
next_interval.md. Its denominator increases with a. Thus lower h to
1600, giving a0=68573+3ell and S0=2103554+7ell. The numerator
increases with ell, while the denominator

    68573^2-1692116ell+2ell^2

decreases through ell=2740. At that relaxed endpoint,

    a0=76793, S0=2122734, denominator=80873689>0,
    floor(S0*(a0-ell)/denominator)=1943.

This positivity justifies all the monotonicity steps. Every top component
therefore contains at most 1943 nonsingular rich pairs. The original
projective-degree budget still allows at most 3^16 top components;
its proof depends on the actual 22 coefficient coordinates, not this J
endpoint or the height of a selected interpolation equation.

## 2. Keep the whole lower-dimensional complement

The height theorem gives h<=floor((J-1)/2)<=4910. The existing
weighted argument applied to the complement of ALL top components gives

    M_lower<=floor(2^7*3^13*(1043667/62063)^2)
            =57709146976.

Include one possible singular pair and 256 off-curve pairs. With the
same /5500 resource/near base 4194116990084347 and 981604 original
labels per pair,

    N<=4194116990084347
       +981604*(1943*3^16+57709146976+1+256)
      =142942788280886491.

No pure-locus assumption, receiver descent or deleted coordinate is used.
The earlier J/height rectangles are not extrapolated. In particular this
proof uses ell<=2740 and h>=1600 TOGETHER in the component estimate.
