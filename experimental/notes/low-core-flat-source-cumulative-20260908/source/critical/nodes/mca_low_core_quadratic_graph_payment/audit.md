# Audit

## Moving-axis equality exclusion

Hand-checked the rational parameterization without square roots,
closed coefficient transport with a quadratic inverse, variation-gcd
normalization, and applicability of the ONE-output tangent argument
without an input-carrier assumption. If an odd-s component attains
the ceil bound, every generic coprime second derivative lies in
one fixed tangent space; dimension and closure force linear variation.
Polarization then places both weighted product spaces in the SAME V.
The elementary 2r-1 leading-degree bound and Cayley--Hamilton force
the quadratic-coefficient ratio constant, contradicting the moving gate.

Degree and LIST incidence return to original polynomial-pair coordinates;
the rational parameter's height is never substituted for K. The JOINT
lemma previously proved by the affine-product child is now shared in
this node's algebraic-list companion, with no reverse requirement edge.

`verify_moving_parabola.py` checks 20358 canonical labels, the
constant-direction and separate-carrier counterfamilies, and the
product-space endpoint (0.12 s, 15488 KiB). The exclusion of all
previous degree-seven factor covers is a hand parameter-degree proof.
Numerical endpoint audits remain with the finite consumer; external
independent proof review is still due.

## Rational X-coefficients

Hand-checked the variation-gcd normalization, nonconstant evaluation
at EVERY algebraic x, primitive derivative content, generic full degree,
and avoidance of finitely many proper closed conditions. These supply
two coprime derivative values; neither coprimality nor finite-field
genericity is assumed. Their tangent images lie in one fixed rational
output space. The quadratic intersection can have dimension one; for
e>=3 it is zero. D=0 and zero-dimensional components are separate.
Clearing a fixed X denominator preserves coefficient equation degree e.

`verify_rational_graph_controls.py` checks a 20358-label canonical
rational-quadratic source, both tangent intersection endpoints, and a
pole at which scalar agreement is not joint agreement. Its canonical
maximality and exclusion from every old degree-two-through-seven
polynomial-X graph are hand arguments in `rational_graph_controls.md`.
Replay: 0.06 seconds, 15488 KiB. The independent numerical endpoint
certificate is unchanged; external independent proof review remains due.

## Uniform polynomial-graph extension

The maximal varying coefficient D, not merely the largest degree
attained on a component, is used after removing fixed higher coefficients.
The leading derivative coefficient is a nonzero polynomial in a
nonconstant function lambda, so it cannot cancel identically over the
algebraically closed base. A point in that nonempty open set supplies
degree delta>=(e-1)D. Common leading degrees then give r=6 for
quadratics and r=5 for higher degrees in the dimension-eleven consumer.
The D=0 case has dimension <=1; zero-dimensional components remain
covered with the degree charge. The characteristic must not divide e.

The received-data certificate is checked separately by its strict weighted
degree root argument. An actual raw-one source defeats the unguarded
pointwise-fit shortcut. The weighted quadratic control has 20358
canonical labels and is not equivalent to the previous square graph under
any constant matrix and polynomial offset; the latter universal exclusion
is a hand coefficient proof, not a matrix enumeration. A characteristic-
three cubic family enforces the new characteristic gate.

The generalized degree-e section argument is the same proved proper-
hypersurface argument, not a new genericity assumption. Exact finite
degree endpoint checks live in the consumer. No new conditional node or
universal algebraic-description assertion is introduced.

## Earlier square-graph audit

Hand audit, 2026-09-07:

- Tangent vectors are taken in a component whose maximal polynomial
  degree is D, so deg w<=D is justified even at a point where the full
  nonreduced coefficient scheme has a larger tangent space.
- Differentiation gives 2aw in V; characteristic two is excluded and has
  an explicit dimension-three counterfamily in a dimension-four carrier.
- Leading-degree sets of W and aW both sit in the SAME V. Their
  overlap is at most one degree for D>0; D=0 is treated separately.
- Every geometric component, including isolated points, is covered before
  taking s-r generic quadratic sections. Properness of every cut is
  explicit. Taking coefficients over the algebraic closure is legitimate
  for an upper bound on the original field's polynomial points.
- LIST incidence subtracts universally agreeing coordinates, never
  pretending that a hyperplane containing a component lowers its dimension.
  Proper hyperplane sections retain degree <=Delta. The degree-two
  zero-dimensional control rejects omitting this degree factor.
- Matrix/offset transformation is confined to pair geometry. Full-code
  badness, finite labels, complete-core complements, and the HIGH margin
  charge remain in original coordinates.

The hypersurface degree statement was checked in Vakil's primary notes,
PDF page 84, and its Hilbert-polynomial proof is stated locally. This
standard algebraic input is not a probabilistic or heuristic premise.

`verify_small.py` checks the exact 20358-label field encoding and every
parameter triple of the actual 27-pair canonical control. The all-label
and all-explanation assertions are established algebraically in
`controls.md`, not by exhaustive codeword enumeration. Separate finite
integer verifiers live with the consumer. External independent proof
review remains outstanding; no numerical test alone certifies the theorem.
