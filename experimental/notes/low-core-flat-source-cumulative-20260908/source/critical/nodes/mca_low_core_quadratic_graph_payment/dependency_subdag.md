# Required dependency sub-DAG

The moving-parabola continuation uses this same two-tangent proof
owner. The shared algebraic-list companion now includes the JOINT
incidence argument previously proved in the affine-product child.
No reverse requirement on that child is introduced, so there is no
cycle. The finite consumer combines the separate conic cases and
owns all finite budgets and mixed-cover prices. No new node or edge.

The rational-X-coefficient extension proves its two-tangent argument
locally and reuses the same algebraic LIST degree lemma. It adds no
required node or edge. Parallel-fiber and automatic interpolation
costs belong to the finite consumer, not to this generic supplier.

    mca_nonuniform_support_margin_resource [PROVED]
      -> mca_low_core_quadratic_graph_payment [PROVED]
      -> rate_half_mca_cancelled_low_core_relation_payment [PROVED]
      -> prize_full_threshold_brackets [PROVED]

This node proves its algebraic-coefficient LIST bound and quadratic-locus
dimension estimate in full. It does not import the numerical ordinary
Johnson or MCA Johnson/Hensel caps. The finite consumer already owns the
empty-universal-core resource and original-row near add-back.

The polynomial-graph companion generalizes the same dimension/degree
argument and supplies its own degree-guarded received-data root bridge.
No new supplier, edge, universal-description target or speculative
per-degree child is required. The node ID retains its quadratic origin.

The arbitrary-source router gets only the finite consumer's existing
outgoing evidence edge. No universal-parabola premise or new red target
is added. Placement under critical/nodes follows actual grand requirement
ancestry, not the location of the older background supplier.
