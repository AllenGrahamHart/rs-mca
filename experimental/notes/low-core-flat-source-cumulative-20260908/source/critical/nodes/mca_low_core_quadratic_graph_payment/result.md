# Result

PROVED: a nonsingular rank-one conic with genuinely moving quadratic
direction has coefficient dimension <=floor(s/2) in a common carrier
s>=2, characteristic not two. Excluding the odd-s tangent equality
case gives the improvement. Original-pair degree charge 2^(2s-r)
and JOINT LIST incidence supply the count. Together with earlier
suppliers this pays every individual nonsingular conic in the finite
consumer, not every possible combination of conics.

The actual 27-pair moving-parabola control is outside all previous
degree-seven affordable factor covers. Constant-direction and unrelated-
carrier controls guard the strengthened dimension statement. No original
red or unrestricted bracket moves, and neither prize is resolved.

PROVED extension to Psi in F(X)[Y], with X-only denominator and
e=deg_Y Psi nonzero in F. Two coprime derivative tangent images
replace the old leading-degree method. All dimension/degree and finite
endpoint bounds are unchanged. A pole-safe received-data certificate
supplies the identity. The new actual canonical rational quadratic is
not a constant-matrix polynomial-offset image of any old polynomial-X
graph of degree two through seven. This feeds an automatic interpolation
payment in the existing finite consumer; general plane curves stay open.

PROVED uniform extension: polynomial graphs b=Psi(X,a) of Y-degree
e>=2 nonzero in F have parameter dimension <=floor((s+1)/2) for
e=2 and <=max(1,floor(s/2)) for e>=3, even with arbitrary polynomial
X-coefficients. Degree charge e^(s-r_e) and scalar LIST incidence give
the full original label bound. The finite consumer pays all degrees two
through seven with total <=167137715680311148. An optional degree-
guarded received-data certificate supplies the exact pair identity.

An actual weighted quadratic is outside the old constant-coefficient
parabola class. Characteristic-three and pointwise-fit controls enforce
the new proof's precise hypotheses. The general source-description gap,
original red statuses, unrestricted brackets and both prizes remain open.

## Earlier square-graph result

PROVED: a quadratic graph in a common polynomial carrier has parameter
dimension at most floor((s+1)/2) in odd characteristic. An explicit
degree-charged algebraic LIST argument bounds its represented LOW pairs
by floor(2^(s-r)*Q^r), and their original finite labels by complete own-
core complements. The original high-margin resource is retained once.

The finite KoalaBear consumer obtains whole original total
<=46570195123304300. The actual canonical 27-pair control requires
fourteen rational lines, so the nonlinear mechanism is not contained in
the previously paid cover classes at small scope. No affordable-curve
description of arbitrary sources, unrestricted bracket change, original
red closure or resolution of either prize is claimed.
