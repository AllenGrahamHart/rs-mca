# Hand Audit And Exact Certificate

The proof uses one lambda per source/degree box, not one independently
optimized lambda per core. Full-source collisions T fund coordinate
weights; the core's C controls its marked basis cost. No additional
source-consistency exclusion is needed. All56320 boxes are covered.

Each Cmax term is an upper bound on TOTAL core collisions: use M1 for
positive upper moments and subtract M0 for the flat-split square sum.
Both relaxed child arguments stay>=1. Convex tangents use Cmax for a
negative coefficient. G retains the fixed original-rank profile. The
polynomial source moment handles nonuniversal zeros only as an auxiliary
locator quotient, not receiver descent.

The old signed-completion bound applies at all ranks1..10 here, since
the full flat size is<D+1. The rank-one quotient is counted directly;
all higher quotient products retain their actual-degree and hereditary
guards. The marked bonus is added to a lower unweighted count of the SAME
core bases, never to a separate tuple resource. Nonnegative source weights
keep all existing HIGH costs valid after exception removal.

Primary Fraction and separately written integer-pair engines agree on
every source credit, collision endpoint, tangent maximum, bonus, final cost
and cap, with SHA256
b0d340b813df77e827f49d3b4d273c8de17c42214f12e4823ee1d87edc8aaafb.
Both cover44 degree blocks,56320 boxes and12990 old-count supplements.
Each rejects112640 adjacent wrong floors. The primary also rejects four
broken covers. The integer derivative uses a common denominator for value
and derivative, rather than the primary's Fraction recurrence; it imports
only the preceding INDEPENDENT profile/inside/quadratic helpers, not the
primary or Fraction.

The first primary calibration printed the complete expected maximum and
digest before stopping at an intentionally unset digest guard. That was
not a mathematical counterexample or RAM crash. The fixed digest must
pass both final engines. Runtime/RSS and final replays are recorded in
notes/correspondence/WEIGHTED_COLLISION_INTERVAL_20260908.md. No original
field enumeration or Modal work is needed. External independent review
of the general hand argument remains due. The original source assembly
must still own its normalization and near-addback proof.
