# Proof By A Single Source-Weighted Resource

Put D=67466,R=1048576,d=67472,r=12. Fix the source, actual rank-eleven
polynomial carrier, original finite labels and full-code-bad explanations.

## 1. Exhaustive Source Alternatives

If one complete nonzero SOURCE projective fiber has size>=J-6000, the
required two-cost receiver-fiber theorem pays the whole source, including
original near, by266180883463176443 on21000..52999. This covers our interval.
Otherwise all source fibers have size<=J-6001. Let z count carrier zeros
and T=sum_i a_i(a_i-1) over complete nonzero source fibers. Root capacity
gives z<=J-11; these coordinates and their agreeing labels are retained.

On a degree box J0..J1 let n0=R+J0,n1=R+J1,A=J1-6001. The weighted
supplier's polynomial SOURCE bound and n0>=10*(J1-10) give the uniform
integer cap

    T<=Tmax=floor(n1*(J1-11)/10).

This moment bound uses an auxiliary polynomial quotient by the full zero
locator only. It does not change the received pair, field or finite labels.

Assign each source coordinate weight w_x=a_i-1 on its full nonzero fiber,
and zero weight on carrier zeros. Use the SAME rational for every label:

    lambda=[66(n0-2)(n0-3)-660(A-2)(n1-3)-1485*Tmax]
              /[12(n1-1)(n1-2)(n1-3)].

Its numerator is positive on every certificate box. The whole-box CREDIT
theorem therefore bounds the total weighted independent-tuple resource,
after the ONE secant/zero-label exceptional set E, by(n)_12<=(n1)_12.
Here|E|<=z+T/2<=J1-11+floor(Tmax/2). No grid in T, guessed source
consistency inequality or label-dependent choice of lambda is needed.

## 2. Every LOW Core And Its Own Collision Interval

For each LOW raw<=6 record choose M=D+J points in its actual joint core.
All have nonzero evaluations, since a zero-evaluation joint-core point
would belong to the universal carrier core. Since M>J they span V*.
The completed-basis supplier inserts one actual witness defect in twelve
recoverable positions. This counts original independent incidence tuples
even when some chosen core points lie outside the old size-m witness.
Outside E, their weights belong to the one source resource above.

Choose any maximizing proper complete flat in this core. Let its rank be
t, size a, density h=a/t, so all proper flats have coordinate density<=h.
Root capacity gives1<=t<=10 and t<=a<=J-11+t. At t=1 the source-fiber
cap further gives a<=J-6001. These conditions cover every core, including
low density; maximizing rank need not be unique.

Let C be the ordered collision count of this CORE's restricted fibers.
It differs from source T, but C<=T. On a box a0..a1 put

    M0=D+J0, M1=D+J1, h1=a1/t, ell=11-t, k1=J1-a0.

Freeze the original rank-eleven profile mu_3..mu_11 for(D,J0,J1,11,h1).
Let F=F_10 denote its increasing convex child product. Valid collision
endpoints are

    Cmin=a0(a0-1) if t=1, and0 otherwise,
    Cmax=min(M1(h1-1), M1((J1-1)mu_11-1), Tmax,
       a1*h1+min((D+k1)*h1,Q(D+k1,k1,ell))-M0).

The first bound follows from core fibers<=h1. The second is the ORIGINAL
rank-profile moment. The third is the source cap. The last is the proved
complete-flat split of the ORIGINAL fiber square sum, minus M>=M0.
Q is the full-fiber second-moment upper bound, with Q(v,k,1)=v^2.
The quotient fibers coarsen original outside fibers; they are not assumed
to have size<=h1. All positive numerators are bounded above at M1,k1,a1;
subtracting M0 is in the upper-bound direction. Cmin follows from the
actual rank-one flat when present. The certificate checks Cmin<=Cmax in
every box, so no incompatible box needs exclusion.

## 3. Weighted Convex Tangents

The exact first-coordinate identity in the weighted supplier gives marked
ordered-basis weight at least11*C*G, where G=F(J0-h1). The unweighted
Jensen cost is bounded below by M0*F(J0-1-C/M0). Both statements retain
the ORIGINAL descendant profile. The certificate verifies

    J0-h1>=1, J0-1-Cmax/M0>=1.

For each of the five fixed C0=Cmin+i(Cmax-Cmin)/4, i=0..4, put
x0=J0-1-C0/M0. The lower bound on weighted core bases is

    L(C0)=M0*F(x0)+C0*F'(x0)
           +(11*lambda*G-F'(x0))*C_endpoint,

using Cmin when the coefficient is nonnegative and Cmax otherwise. The
weighted supplier proves this across the entire collision interval, not
only at five sampled collision counts. Taking the maximum of these five
valid tangents gives L>0. This choice need not optimize the true envelope.
The same fixed lambda is used for every core; only tangent calibration
and lower costs vary by record type. Defect insertion gives cost12L.

## 4. Retain Earlier Unweighted Counts With A Valid Weight Bonus

The earlier arbitrary-flat theorem applies at EVERY t=1..10 because
a1<D+1 and the core flat is complete. Put k0=max(ell,J0-a1), and

    d_i=max(D+1+i,M0-floor((10-i)*a1/t)), 0<=i<t,
    c_i=floor(i*a1/t), 1<=i<t.

Its signed BOX elimination gives the inside factor I using lower extension
ratios max(a0-c_i,0), upper ratios a1-i, and the sign-correct final a
endpoint. Every d_i>a1. The annihilator quotient has actual degree in
k0..k1, rank ell and unchanged positive gap D. Because the flat is
MAXIMIZING, quotient proper-flat density is<=h1: a rank-i quotient flat
lifts to an original rank-(t+i) flat containing all a=t*h points.

For ell>=2, take the maximum Qbasis of its greedy count, proved general
quadratic count, balanced product ONLY when its hereditary guard holds,
and quotient rank-profile product at density min(k1-ell+1,h1). The generic
quadratic formulas apply since ell<=k0<=k1<D, regardless of the older
finite supplier's narrower J interval. For ell=1 use exactly D+k0;
do not invoke a balanced formula with a zero denominator.

Also retain the full-core greedy product and original-rank profile count:

    B_old=max(Qbasis*I,
      M0*product_(j=1)^10 max(D+11-j,M0-floor(j*a1/t)),
      F_11(D,J0)).

For the SAME weighted basis collection its lower cost is at least
B_old+11*lambda*Cmin*G. Adding this minimum marked contribution to an
independent unweighted lower count is valid. It is not adding two separate
basis counts. Whenever the preliminary whole-source cap exceeds the fixed
trigger274960000000000000, increase12L to the maximum with
12*(B_old+11*lambda*Cmin*G). Every increase is independently justified;
there is no adaptive box refinement or unproved calibration premise.

## 5. HIGH Records, Exact Cover And One Add-Back

The existing generic HIGH weight proof gives every raw>=7 record cost at
least

    beta_high=(d+J0)*product_(i=1)^10(d+i)*10488/125.

For7<=raw<=84 use12raw*(1-11raw/(d+1)), increasing on this interval
and already larger at7. For raw>=84 the truncated weight is>=84,
including raw>d. Nonnegative coordinate weighting only increases these
unweighted costs. All surviving LOW and HIGH records share ONE resource;
divide by the minimum applicable cost, not a separate budget per class.

Partition21800..22499 into width16 degree blocks, truncating the last.
For each t the size range is t..J1-11+t, further truncated toJ1-6001
at rank one. Divide it into bins of width ceil((top-t+1)/128), truncating
the last. Each formula above holds on its ENTIRE box. The final cap is

    floor((n1)_12/min(beta_low,beta_high))
       +J1-11+floor(Tmax/2)+134944.

The two independent engines cover44 degree blocks and56320 boxes,
5632 for each rank; no exclusions. They agree on every common source
credit, collision endpoint, tangent maximum, marked bonus, final cost
and cap, with digest
b0d340b813df77e827f49d3b4d273c8de17c42214f12e4823ee1d87edc8aaafb.
12990 boxes use the old-count supplement. Each engine rejects112640
adjacent wrong floors. Their maximum is274954262108377832 at
(J0,J1,t,a0,a1)=(22056,22071,5,19554,19726). The separately paid
large-source-fiber branch is smaller. This proves PAYMENT for EVERY
source at the stated scope. It is not an extremal-line construction.

The exception set and original near134944 occur once in the final cap.
Removing exceptions does not recompute the actual enclosing carrier or
require its surviving explanations to span it. Auxiliary quotient moments
and basis counts never change the receiver or challenge denominator.

## 6. Union And Original Transport Interface

The required collision-profile theorem already pays22500..169999 by
274938028871508001. Taking the maximum with PAYMENT pays21800..169999
by274954262108377832; retaining the old smaller union cap would be wrong.
The original assembly must check its rank-twelve gauge and full-code-bad
normalization before applying this statement. That transport sends our
interval to700 whole original shared-core sizes1026077..1026776.
No theorem about the remaining lower J gap, ranks>=13 or either full
Prize problem is inferred from the finite arithmetic alone.
