# Result

PROVED: an explicit continuous-loss balanced basis product for every
actual polynomial source with floor(s^2/4)*h<=lambda*N and1<=lambda<=2.
This extends the exact lambda=1 gate without discarding the estimate as
soon as the density bound is missed slightly.

The result is a generic basis-counting supplier, not an MCA or LIST
endpoint. It is LOCAL and used by the new all-carrier finite interval.
