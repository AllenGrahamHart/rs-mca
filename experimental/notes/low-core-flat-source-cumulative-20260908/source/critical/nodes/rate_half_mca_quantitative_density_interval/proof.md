# Proof With Quantitative Core And Quotient Products

Write D=67466, R=1048576, d=67472. Fix minimizing pairs AFTER the
original normalization, on the same fixed dimension-eleven carrier.

## 1. Exhaustive Source And Record Splits

If any complete nonzero SOURCE projective fiber has size >=J-4700,
the required degree-4700 receiver-fiber theorem pays the WHOLE source
by246756107210901806 including original near. Otherwise every rank-one
fiber of any nonzero core subset has at most J-4701 points.

For each raw<=6 record choose an M=J+D point subset H of its complete
joint core. Empty universal carrier core makes its evaluations nonzero;
M>J gives actual rank eleven. The original defect remains available.
Let h_H be maximum proper-flat coordinate density on this H and put

    h_0(J)=(J-4)/7=1+(J-11)/7.

If h_H<=h_0, apply the quantitative density product directly to H.
There is no first-excess subcase. Otherwise choose a maximizing core
flat of rank t and size b=t*h_H. The polynomial root bound
h_H<=1+(J-11)/t excludes t>=7. Hence1<=t<=6 and

    floor(t*(J-4)/7)+1<=b<=J-11+t.

At t=1 also b<=J-4701 from the source-fiber split. These are exhaustive
alternatives for every LOW record. HIGH raw>=7 retains the proved cost
(d+J)*P_d*10488/125, P_d=product_(i=1)^10(d+i), including all larger
raw margins. Only that arithmetic is imported, not an older density gate.

## 2. Quantitative Products On Whole Boxes

Let F_r(D,k;lambda) be the required quantitative product. It is increasing
in k>=1 and decreasing in1<=lambda<=2. For a rank-r quotient with
actual degree k in[k0,k1], lengthD+k and proper-flat density <=h1, set

    lambda1=max(1,floor(r^2/4)*h1/(D+k0)).

If lambda1<=2, F_r(D,k0;lambda1) is a lower basis count throughout
the hull. Otherwise this particular bound is omitted, not assumed true.
All polynomial degrees remain actual integers; only lower polynomials
and their scalar arguments are relaxed.

For the low-density core branch on J0..J1, use r=11,k0=J0 and
h1=(J1-4)/7. Its lambda1<=2 gate passes on every degree block.
For a high-density core the same formula uses h1=b1/t. It is an optional
additional whole-core count, on exactly the same H and original label.

## 3. A Maximizing Core Flat Has Known Occupancy

For the high-density branch write ell=11-t and k=J-b. The arbitrary-
complete-core-flat lemma gives an ACTUAL rank-ell polynomial quotient
of degree<k, lengthD+k, and nonzero evaluations. Since b=t*h_H, a
proper quotient rank-i flat has <=i*h_H coordinates. Root capacity
also bounds its density by k-ell+1.

Use consecutive integer J blocks of width32 on26500..27999. In each
block all actual k lie between ell (4701 when t=1) and
J1-floor(t*(J1-4)/7)-1. Partition this complete interval into at most64
equal-width integer bins, inserting the existing root-balanced threshold
floor((D+f*(ell-1))/(f-1))+1, f=floor(ell^2/4).

On a bin k0..k1 put b0=J0-k1, b1=J1-k0. All actual sizes lie in
that interval. Set h1=b1/t. The quotient density is at most

    min(k1-ell+1,h1).

Its lower basis count is the maximum of the proved greedy count below,
the existing checked general quadratic, the guarded root-balanced product,
and the quantitative product from section2. The old narrower J interval
is not used as a premise for its general quadratic construction.

## 4. Correct Corners And Coupled Counts

For a proper rank r, define

    c_r^comp(J,k)=max(D+11-r,J+D-floor(r*(J-k)/t)).

For fixed J this is nondecreasing in k. For fixed k it is nondecreasing
in J when r<=t and nonincreasing when r>=t; r=t is constant in J.
This follows directly from the integer floor increments. Thus its lower
box corner uses k0 and J0 when r<=t, J1 otherwise. The change matters
at t=6,r=5; blindly reusing the earlier t<=5 corner would be invalid.

Use these lower complements at ranks10,9,...,ell for the arbitrary-flat
completion factors, and upper inside caps floor(i*b1/t). The earlier
signed BOX algorithm, with one valid integer tangent per inside rank,
gives a positive inner lower count. The quotient greedy lower count is

    (D+k0)*product_(i=1)^(ell-1)max(D+ell-i,D+k0-floor(i*b1/t)).

Also retain the independent full-core greedy count
(D+J0)*product_(r=1)^10 c_r^comp(J_corner,k0), and the optional full
quantitative product. Take the MAXIMUM of these valid basis counts.
Signed tails use upper extension ratios when negative. The independent
audit reconstructs each full coefficient vector from its chosen tangents.

Insert the original defect in twelve recoverable positions per basis.
Different original finite labels have disjoint independent incidence tuples.
All records, however their core cases differ, consume the ONE resource
(R+J1)_falling_12. Divide by the smallest applicable record cost, not by
separately budgeted core classes. HIGH and low-density costs use J0.

## 5. Finite Certificate And Original Source

The two exact implementations agree on all18189 high-density boxes and
47 basic comparisons. Their maximum, including original near, is
272896493994028693 at J=26500..26531,t=1,k=21339..21620.
The separate whole-source fiber bound is smaller and included in this
maximum. Every integer degree, flat rank and possible size is covered.
This is an exact arithmetic certificate for the preceding analytic box
bounds, not extrapolation from sampled sources.

The existing first-excess interval pays28000..169999 by274929007493481160.
The union26500..169999 therefore retains that larger bound. Original-
source transport, near chronology and the residual corollary belong to
the assembly. No auxiliary quotient changes the original receiver, field,
finite labels or full-code-bad support contract. No larger-rank source or
unrestricted Prize endpoint is proved here.
