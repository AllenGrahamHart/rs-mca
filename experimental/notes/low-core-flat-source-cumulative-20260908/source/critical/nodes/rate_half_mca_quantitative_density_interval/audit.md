# Hand Audit And Independent Certificate

The degree-4700 gate is a WHOLE-source alternative, whose failure bounds
every core fiber. The low/high CORE-density split at(J-4)/7 is exhaustive
and distinct from the raw<=6/raw>=7 split. Root capacity forces a high
maximizing core flat to have rank<=6, with its full known occupancy.

Every core and quotient quantitative product uses a uniform upper loss
factor and is omitted if that factor exceeds two. Increasing degree and
decreasing-loss monotonicity are proved by the new supplier. The quotient
has density<=min(k-ell+1,h_H); it is only a polynomial counting device.

High-rank completion now includes t=6,r=5. The correct minimum in J is
the LOWER endpoint when r<t; the old t<=5-only high corner cannot simply
be reused. Primary code uses monotonicity, while the independent audit
computes both corners. Integer floor increments justify every endpoint.
The independent full-core greedy count and positive quantitative count
may be maximized with, not added to, the coupled quotient count.

All18189 record boxes and47 basic comparisons match between a rational
recurrence and independently unrolled integer-scaled products. The audit
also reconstructs the full signed tangent vector using the prior independent
helper. Negative tails retain upper ratios. Both engines reject36378
adjacent wrong floors; five broken degree covers fail. Maximum:

    272896493994028693 at J=26500..26531,t=1,k=21339..21620.

The separate source-fiber total is smaller and included. Digest:

    42390ea68dfb9360ab741636c071e3a88523c371bf013fc0857016faa8d1eab0

Primary normal:2.55s/19584KiB; -O:2.71s/21284KiB.
Audit normal:2.47s/18944KiB; -O:2.54s/20548KiB.
All serial under RAMguard tiny, no Modal spending or source enumeration.

Earlier point probes identified the loss at hard density gates; they are
not proof dependencies. The hand argument proves whole-box source coverage.
Verdict: no issue at the printed scope; external independent review due.
Original ranks>=13 and unrestricted MCA/LIST remain open.
