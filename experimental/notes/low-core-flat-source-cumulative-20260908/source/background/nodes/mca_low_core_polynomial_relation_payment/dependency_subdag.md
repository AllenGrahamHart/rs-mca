# Dependency sub-DAG

    mca_nonuniform_support_margin_resource [PROVED]
    list_two_component_relation_dichotomy [PROVED]
      -> mca_low_core_polynomial_relation_payment [PROVED]
      -> rate_half_mca_low_core_polynomial_relation_payment [PROVED]

The generic supplier does not assume numerical ordinary-list caps.
The finite consumer supplies them, handles the constant primitive row
through the preceding defect-one theorem, and pays smaller unions.

The degree-free companion proves the primitive identity directly from
an actual F(X)-pencil. It bypasses the bounded-row root-extraction step,
while retaining the same margin supplier. The existing
`rate_half_mca_cancelled_low_core_relation_payment` requirement already
imports this node and the ordinary LIST compiler; its finite cover
payments need no added edge. No affordable-cover existence target is
introduced, and the unrestricted router receives evidence only.
