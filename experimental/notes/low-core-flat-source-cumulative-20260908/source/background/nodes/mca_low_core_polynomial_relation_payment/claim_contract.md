# Contract and provenance

- Degree-free companion: affine lines in F(X)^2 of actual represented
  LOW polynomial pairs. No old height or remainder bound is imposed.
  Primitive polynomial normalization and coverage give the pair/source
  identity. Cover groups partition labels and share ONE global resource.
- Nonconstant direction gives a same-carrier dimension drop, independent
  of its height. The groupwise constant-direction anchor specialization
  uses the finite consumer's empty universal carrier core explicitly.
- The actual canonical F_(19^6) control has eighteen preferred raw-one
  labels and zero full E-row space at ell=2; it is not a deployed unsafe
  source. The 27-point cover guard is only a polynomial-family example,
  not an actual over-budget MCA counterexample.
- Exact original finite slopes, selected supports, raw minimizers and
  global margin resource. Only the low complete-core union is used for
  polynomial relations, at A=m-T and row-degree cutoff ell=d-T.
- Supplier `list_two_component_relation_dichotomy` provides the exact
  polynomial relation and scalar parameterization on this actual domain.
- Covered-core source agreement proves primitive compatibility at EVERY
  gcd root on U. No gcd root is deleted on the original full domain.
- BOTH pair components have directions in the same C'. Equal dimensions
  in two unrelated spaces do not imply the one-dimensional rank drop.
- Nonconstant primitive rows cost up to n exceptional finite slopes.
  The actual control shows this charge cannot simply be omitted.
- Ordinary scalar-list inputs are explicit; the finite consumer proves
  them. No higher-order MDS, independent-source or genericity assumption.
- Nonclaim: existence of a bounded relation or affordable rational-pencil
  cover on every possible low-core union; full error-rank-twelve payment;
  arbitrary MCA safety.
