# Audit

Hand checks: both low cores have at least m-T coordinates; their union is
the SAME U_T; overlap K, not K-1, identifies both polynomial components.
Complete-core outside ownership retains distinct original slope labels.
All high records remain charged at T+1. The min(C,...) endpoint covers
an empty low family and avoids an impossible negative residual resource.

verify.py checks actual F_7 scalar agreements, all 49 candidate degree-<2
minimizers, pair noncontainment and complete cores for a positive example
and the one-point-sharp negative control. It does not search a deployed
field or replace the general hand proof. External review remains due.
