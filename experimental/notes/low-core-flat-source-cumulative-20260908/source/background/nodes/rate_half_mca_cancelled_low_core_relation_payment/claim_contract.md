# Claim contract

The required [maximum-density interval theorem](../../../critical/nodes/rate_half_mca_maximum_density_high_interval/statement.md)
now pays EVERY normalized source on 65000..169999 by
N=|Gamma|+134944<=274929007493481160, reserve 51720617913927.
No fiber-size, occupied-flat-rank or pair-curve hypothesis remains there.
A maximum-density flat is selected from the actual carrier; the hybrid
bound or coupled inside-class count covers every possible case.

Together with the earlier full lower strip, the remaining normalized gap
in this lane is 9941..64999. This removes 105000 integer J values.
Original source/near/owner transport, higher ranks and both prizes remain
open. The original router receives only evidence and remains TARGET.

Dependency: completed-basis resource -> maximum-density flat resource
-> high-interval payment -> this finite consumer. No cycle or unproved
premise is introduced. The following paragraphs describe earlier stages,
not the current unpaid set; their source-class bounds remain valid.

The [curve-free carrier theorem](projective_arc_carrier_payment.md) pays
unclustered projective arcs on 10000..169999 and the weaker bounded-flat
condition on 23000..169999. The latter includes every full progression
carrier, even after a constant-field extension. All LOW/HIGH records use
one tuple budget; no cubic or quartic cover is assumed. An unpaid high-J
source must have a heavy evaluation flat and cannot have a nonconstant
rational multiplier with intersection dimension >=10. The new incoming
requirement is the proved flat-core-basis supplier, not a guessed source
classification. Arbitrary-carrier coverage still ends at 9940; all original
source/near/owner and prize obligations remain. Earlier stages follow.

The [raw-weighted cutoff-125 theorem](raw_weighted_cutoff125.md) pays
EVERY cubic source class on 9822..9940 at N<=274811931500367244,
with actual <2A factor height and all 256 exceptions. It uses the
endpoint resource, full per-pair raw SUM budget and a harmonic chord;
H124<=6 has a short dyadic proof. Existing component and GP suppliers
provide the complementary pair counts. The kernel child owns exhaustive
coverage through 9940; no source cover or new req is assumed here.
Earlier statements below are the preceding stages.

- The cumulative LOW counts all use the EXISTING raw selection. The
  same weights dominate 11*min(r,500), so count every raw<=t label
  in the exact cumulative conversion, not an independently reselected
  support family. Complete joint agreement is >=m-t and the original
  per-pair label cap is n-m+t. The cubic-tail cover, actual <2A factor
  height and <=256 exceptions are explicit; no arbitrary-source cover
  or old <A/64 scope is assumed. See `cubic_tail_payment.md`.

- The multiplicity quartic payment requires only ONE irreducible quartic
  containing all but <=64 LOW pairs on 8764..9821. It counts every
  rich pair by <=68, with full four-condition double points and all
  three weighted monomial regimes. No height, smoothness or parameter-
  family premise remains. This consumer still does not assume the cover
  exists for an arbitrary source; its full-kernel child proves exhaustive
  coverage only through 9526. Original labels, HIGH and near are kept.

- Quartic projection graphs require exact polynomial Phi,Psi of maximum
  input degree four and A0*Phi+B0*Psi=z with primitive A0,B0. The
  input has true degree <J+h and ambient dimension 22-d, not eleven
  coordinates of unchanged degree. The finite source-class guards are
  h<=2379, <=4 off-pairs and the printed d/dimension/height alternatives.
  Forbidden coordinates are removed from the on-graph LIST count only;
  every original defect, HIGH label, off-pair and near allowance remains.
  General quartics or a smaller dimension of the whole locus are not assumed.

- LOW_101 means the raw minima 1..101 in the EXISTING canonical selection,
  not a reselection. Every LOW record has weight >=11 and every remaining
  record has weight >=1200 on the same completed-basis resource. Hence
  use floor((C+1189*L)/1200), not C/1200+an old /501-discounted gain.
  At most 981205 original labels per LOW_101 pair and one original near
  allowance are retained. Actual cover existence belongs to the kernel child.

- Split-product quartics require an EXACT nonzero product x*y^3=R
  after a fixed GL_2(F) matrix and rational X offsets. Moving directions,
  general rational quartics and an inferred product cover are excluded.
  The new supplier counts actual jointly agreeing pairs with all four
  offset cases. This consumer charges <=64 off-curve pairs, complete
  LOW labels and one completed-HIGH/near base; it does not use /501
  discounted gains with the /5500 budget. See `quartic_split_product_payment.md`.

- Weighted affine-singular cubic payment uses the ACTUAL coefficient locus,
  the finite-projection condition, Q_h>=3 and the primitive height lemma.
  Curve coverage and <=64 off-curve pairs are separate source obligations.
  The completed-basis HIGH weight >=5500 gives paid alternative
  274979661292365251; only d=7,dim Y=3,h>=1301 or d=10,dim Y=4
  remain, with their proved height ceilings. The d=11 class is paid.

- Moving projections require an exact polynomial-in-input graph pair
  and A*Phi+B*Psi=Z, with A,B primitive. Input dimension is 22-d,
  family dimension ceil(d/e), and input degree bound J+h. Do not
  assume the input belongs to V or keeps degree <J.
- h<=51391 pays cubic projection graphs. The sharper cubic and
  ordinary conic prices use the printed height or primitive weighted
  equation. Arbitrary moving conics keep their older price without it.
- One-place cubics with finite singularities have a quadratic/cubic
  normalization, not a linear input. They remain outside the linear-input
  graph theorem; the separate weighted theorem above now counts them.

- A nonconstant normalization unit and its inverse give a separate
  dimension-one cubic price. Two distinct points above infinity on a
  rational normalization suffice. A single boundary point, rationality,
  an integral function alone, or a constant unit does not suffice.
- This expands only the exceptional cubic class, not the stronger
  nonconstant-j price. All divisor patterns and exceptional pair points
  remain in the original degree cover. Resource and near occur once.

- The smooth-cubic class requires geometric projective smoothness over
  F(X), in characteristic zero or >3. Nonconstant j gives the stronger
  zero-dimensional coefficient count; singular specialized X-fibers are
  allowed. The Weierstrass model is used for dimension only.
- General smooth cubics have the old exceptional cubic price. Cubics
  with the proved zero-dimensional condition are ordinary degree-three
  factors. This expands the two degree-seven mixed-cover rules, without
  combining two expensive exceptions or constant-direction lines for free.
- Singular generic cubics remain outside this new theorem. Actual factor
  coverage, original transport and one resource/near allowance remain
  required. No unproved classification is introduced as a dependency.

- The projective-jet graph theorem requires characteristic zero or p>=J.
  This holds on the normalized KoalaBear rows. Its bound ceil(11/e)
  retains degree e^(11-ceil(11/e)) and pays actual graph degrees 2..9.
  No genericity assumption is made on the code domain; auxiliary points
  in its dimension proof are chosen over the algebraic closure.
- The new theorem still needs an actual graph identity. It supersedes
  the old degree-eight numerical recipe limitation, not a counterexample
  or an arbitrary nongraph residual. The original moving-conic theorem
  with only characteristic !=2 remains valid in its broader scope.

- Every individual nonsingular conic is paid on the current J band.
  Nonsingularity is for the projective conic over F(X), not a sample
  of fibers. Its quadratic part has rank two, or rank one with constant
  or moving direction. Each case has its own proved supplier and price.
- Moving direction uses original coefficient dimension <=5 and degree
  2^17. Never apply LIST to the higher-degree rational parameter or
  replace the common carrier by unrelated component carriers.
- The new total-degree-seven mixed cover allows at most ONE moving
  conic and five ordinary degrees. It does not also include the older
  expensive cubic exception for free. Multiple moving conics and mixed
  constant-direction lines are not silently certified.
- A geometrically singular irreducible quadratic over F(X) has at most
  one affine rational point. Reducible quadratics must instead be priced
  by their actual linear factors, with constant directions distinguished.

- Affine-product levels: fixed geometrically affine-split polynomial
  and NONZERO fixed level, with at least two nonparallel directions.
  Rationally centered homogeneous levels are included; concurrence
  is not needed. Parallel-only products remain excluded.
  The coefficient dimension is <=1 but the full degree charge e^21
  remains. Use the JOINT list bound and cumulative RAW-weighted gains.
- The mixed cover has total component degree <=7, with at most one
  affine-product level cubic. Other allowed factors are nonconstant
  rational pencils, rational-X polynomial graphs of degree 2..7 and
  affine-product level quadratics. Constant-direction lines do NOT cost W.
- Factor recognition is a proved sufficient source test, not a theorem
  that every forced plane relation passes it. Resource and near occur
  once after partitioning pairs and their labels, never once per factor.

- Automatic interpolation: |U_500| is STRICTLY less than the printed
  coefficient dimension in `automatic_graph_interpolation.md`. This
  forces a nonzero homogeneous relation, not necessarily one with a
  nonzero output coefficient. All possible cases are paid separately.
- Rational-X graphs use the new two-tangent supplier proof, not the
  earlier polynomial-X leading-degree argument. X-only denominators
  may vanish on the domain; their roots are retained. Y-dependent
  denominators and general plane-curve payments are outside the claim.
- Seven constant fibers share ONE component row. Pairwise union
  intersections have size <=J-1, so all but one group have complement
  >=524289. Only the designated group uses the coupled full-resource
  expression. The other six gains cost <=895247541220804 each.
- U_500 is not U_6. Plane interpolation on 4801<=J<=10244 supplies
  a relation but not a general component count or a whole-strip payment.

- Earlier polynomial-X graphs: represented LOW_500 pairs satisfy
  b=Psi(X,a), Psi in F[X,Y], 2<=deg_Y Psi<=7, after one fixed
  constant matrix and degree-<J polynomial offset. All such degrees share
  original total <=167137715680311148; quadratics retain the sharper
  <=46570195123304300 even with polynomial X-coefficients. This is
  an exact pair identity, not an arbitrary high-degree received fit.
- The separate received-data bridge has a strict weighted-degree guard.
  No existence assertion for arbitrary sources follows. Characteristic
  does not divide the graph degree; this holds on the printed KoalaBear
  class. The later X-rational extension above supersedes this limitation;
  general conics are not included. A failed degree-eight recipe is not unsafety.
- Quadratic-graph companion: one fixed GL_2(F) matrix and polynomial
  pair offset, component degrees <J, send actual LOW_500 pairs to b=a^2.
  On 4801<=J<=169999 the original total is <=46570195123304300.
  The proof counts a nonlinear algebraic coefficient family, retaining
  degree charge 32 and dimension six. Only pair geometry is transformed;
  all labels, bad supports, own-core complements and HIGH charges stay
  in original coordinates. Characteristic is odd. No arbitrary conic or
  rational-coefficient graph is silently included in this source class.
- Degree-free cover companion: on 4801<=J<=169999, at most thirteen
  nonconstant-direction F(X)-lines covering actual LOW_500 pairs pays
  original total <=269643200488601452. One constant plus one
  nonconstant line pays <=272837082864553899. No row-height or
  remainder-degree gate. Group gains use only ordinary LIST certificates;
  the original margin resource and near charge are used ONCE.
- Cover existence is an explicit restricted-source premise, not a new
  conjecture or consequence of carrier dimension. Actual small canonical
  ZERO-row-space examples demonstrate strictness of the removed degree
  gate, not deployed-row unsafe witnesses or complete residual coverage.
- Complete U_6 companion: |U'_6|<=J+400000 pays the whole source with
  original total <=274980427687989169, on 4801<=J<=169999. There
  is no row/subfield or low-rank premise for this class. Two fixed ordinary
  Johnson certificates bound JOINT pairs in one shared carrier of dimension
  ten after anchoring; no numerical MCA Johnson/Hensel cap is imported.
- The labels-per-pair count charges n-|H_f|, not n-|U_6|. An unpaid
  normalized source has |U'_6|>=J+400001 and the lifted original union
  >=1448577. This also bounds U_500 for the SAME family. The full
  E-row space on U_500 is not identified with that on U_6.
- Seven-margin companion: ONLY 4801<=J<=169999; every raw>=7 gives
  original total <=274928364476952114. On the complete raw<=500 union,
  a nonzero row of degree <=66972 with remainder degree <=m-7 also
  suffices. No extra raw/subfield premise is added to the row assertion.
- Raw<=6 rank<=8 gives whole original total <=274971532963425180.
  Any unpaid raw<=6 subfamily has >=61100872739557 labels and rank
  >=9, not necessarily full rank or empty common agreement. Test ranks
  after canonical selection. This raw cutoff is not the trade-height h.
- Low-span companion: on 4801<=J<=169999, error rank <=11 of the
  selected raw<=500 subfamily pays the entire family, total
  <=199054477120667562. Canonical complete-core selection is performed
  only after cancellation on the fixed carrier, then error rank retested.
  Every unpaid LOW family has >=234554688218295064 labels, error rank
  twelve, empty common scalar agreement and only rank-two cross-core
  differences. No old margin, explanation or rank invariance is assumed.
- Count: selected finite slopes, once each, in the original KoalaBear
  field. Add 134944 only when returning to the original post-near family.
- Source: arbitrary domain, degree bound J, actual carrier dimension
  eleven, and EMPTY universal carrier core. Neither smoothness nor a
  post-near child assertion is needed.
- Row: (R+J,J,d+J), R=1048576, d=67472, 4801<=J<=254999.
- Relation: on COMPLETE raw<=500 pair-core union, row degree <=66972,
  remainder degree <J+66972. All high labels remain charged globally.
- Extension: remainder degree <=m-139 suffices, with total
  274482706723995445. In the added band the nonzero polynomial
  R*f-Q bounds the COMPLETE pair core by deg Q, hence raw>=139.
  The union cannot be replaced by an arbitrary subdomain while leaving
  additional pair agreements outside it uncharged. No E-valuedness gate.
- Coverage: constant and nonconstant primitive rows, small and empty
  low unions. The gcd-root compatibility is inherited from coverage by
  actual low pair cores, not assumed on arbitrary unrelated coordinates.
- Normalization: whole-common-core cancellation with saturated supports;
  do not transport an old arbitrary witness's margin through reselection.
- Nonclaims: arbitrary high-remainder relation-free payment, error ranks >=13, all-row safety,
  a new unconditional prize bracket, or upstream acceptance.
