# Required dependency sub-DAG

The required [maximum-density interval theorem](../../../critical/nodes/rate_half_mca_maximum_density_high_interval/statement.md)
now pays EVERY normalized source on 65000..169999 by
N=|Gamma|+134944<=274929007493481160, reserve 51720617913927.
No fiber-size, occupied-flat-rank or pair-curve hypothesis remains there.
A maximum-density flat is selected from the actual carrier; the hybrid
bound or coupled inside-class count covers every possible case.

Together with the earlier full lower strip, the remaining normalized gap
in this lane is 9941..64999. This removes 105000 integer J values.
Original source/near/owner transport, higher ranks and both prizes remain
open. The original router receives only evidence and remains TARGET.

Dependency: completed-basis resource -> maximum-density flat resource
-> high-interval payment -> this finite consumer. No cycle or unproved
premise is introduced. The following paragraphs describe earlier stages,
not the current unpaid set; their source-class bounds remain valid.

The [curve-free carrier theorem](projective_arc_carrier_payment.md) pays
unclustered projective arcs on 10000..169999 and the weaker bounded-flat
condition on 23000..169999. The latter includes every full progression
carrier, even after a constant-field extension. All LOW/HIGH records use
one tuple budget; no cubic or quartic cover is assumed. An unpaid high-J
source must have a heavy evaluation flat and cannot have a nonconstant
rational multiplier with intersection dimension >=10. The new incoming
requirement is the proved flat-core-basis supplier, not a guessed source
classification. Arbitrary-carrier coverage still ends at 9940; all original
source/near/owner and prize obligations remain. Earlier stages follow.

The [raw-weighted cutoff-125 theorem](raw_weighted_cutoff125.md) pays
EVERY cubic source class on 9822..9940 at N<=274811931500367244,
with actual <2A factor height and all 256 exceptions. It uses the
endpoint resource, full per-pair raw SUM budget and a harmonic chord;
H124<=6 has a short dyadic proof. Existing component and GP suppliers
provide the complementary pair counts. The kernel child owns exhaustive
coverage through 9940; no source cover or new req is assumed here.
Earlier statements below are the preceding stages.

The cumulative accounting and cubic-tail prices strengthen existing
required suppliers only: completed-basis weights, component lists and
the GP single-fiber argument. This consumer owns their original finite
label/resource/near assembly. The full-kernel child owns exhaustive
cover and factor partitioning. No new node, edge or reverse dependency.

New supplier 18, `mca_multiplicity_interpolation_curve_escape`, proves
a characteristic-free Hasse-jet/Gauss/Bezout curve count and requires
only the existing quadratic-graph supplier's intersection-degree lemma.
This consumer owns the finite quartic price and original source ledger
in `quartic_multiplicity_payment.md`. The kernel child independently
owns exhaustive coverage through 9526. One PROVED node and two req
edges are added; no reverse dependency or speculative curve-cover child.

The quartic projection-graph payment uses the existing generic jet
supplier's moving-projection theorem and the existing /5500 resource.
This consumer proves its numerical source-class prices. The kernel
child independently supplies forced height, off-pair and forbidden-
agreement hypotheses. There is no reverse requirement or new node/edge.

The LOW-101 accounting uses only existing requirements: completed-basis
weights, the general weighted cubic count and the component supplier's
new `cutoff101.md`. No new node/edge. Resource conversion is proved in
`low_cutoff_accounting.md`, without requiring the full-kernel child; that
child separately owns its new exhaustive interval theorem through 8763.

New supplier 17, `mca_projective_split_product_list_payment`, requires
the existing affine-product supplier and proves the projective fiber count
with its four affine-offset cases. This consumer's
`quartic_split_product_payment.md` supplies the original LOW/HIGH/near
accounting. This adds one PROVED critical node and two req edges overall,
with no reverse dependency or speculative source-cover premise. Earlier
paragraphs below record the dependency changes at their own stages.

The existing GP and component-list requirements now also supply their
8656..9526 source-class bounds: N<=83243563858163463 for d=10,
and N<=241058823023306107 for d=7 at h>=1301. Their generic proofs
and new list estimates have no reverse dependency on this consumer or
its full-kernel child. That child separately proves the quartic reduction
and factor ledger. No new node, edge or speculative requirement is added.

Supplier 16, `mca_singular_cubic_component_list_payment`, requires
supplier 14 and proves the polynomial component model, original-degree
component count and 315-pair list envelope. It pays the last d=7 case;
the full-kernel child now covers 7117..8655 with no remaining cubic
assumption. No reverse requirement or speculative child is introduced.

Supplier 15, `mca_geometric_progression_singular_cubic_payment`, requires
supplier 14 and proves the curve-descent/maximal-family/list-recovery
chain. It pays the entire d=10 cubic class, leaving only the d=7 pattern
in the full-kernel child. No reverse dependency or speculative child.

New supplier 14, `mca_singular_cubic_weighted_incidence_payment`, requires
the existing jet supplier and independently proves the normal form,
height bound and weighted incidence. It has no reverse dependency on this
consumer or the full-kernel child. This is one new proved requirement.

Supplier 11's moving-projection companion now provides the coupled
dimension/input-degree count and weighted curve-height consequences.
It uses its existing requirements and adds no node or edge. The full-kernel
child uses the same consumer to exclude infinity-singular cubics; its
remaining finite-singularity normal form is proved in that child.

The normalization-unit extension adds PROVED supplier 13. It requires
supplier 10's general degree/LIST/gain tools, not its affine-product
identity. The full-kernel child's new cubic obstruction uses this expanded
consumer; there is no reverse requirement from here to that child.

The smooth-cubic extension adds PROVED supplier 12, requiring the
standalone elliptic-section dimension theorem and supplier 10's general
joint LIST/group-gain arguments. Both new nodes lie on critical req
ancestry and live under critical/nodes. Their finite group constants
do not require this consumer; the source resource/near composition is
local here. There is no dependency cycle or new conditional premise.

The projective-jet extension adds PROVED supplier 11. Its new generic
dimension proof is independent of the finite consumer and requires only
supplier 9's degree-cover/LIST machinery and inherited margin resource.
The new supplier directly rederives the finite resource endpoint bound,
so its finite consequence introduces no circular citation. Earlier
paragraphs below describe historical additions at their own scopes.

The all-conic assembly uses existing suppliers 9 and 10. Moving
parabolas share supplier 9's two-tangent owner; its general algebraic
LIST companion now owns the JOINT argument, avoiding any reverse
edge to supplier 10. The conic case split, singular irreducible factor
price and finite mixed ledger are proved locally. No new node or edge.

The homogeneous-level continuation adds the PROVED supplier
`mca_low_core_homogeneous_level_payment`. Its fixed-divisor argument
and JOINT coefficient LIST extension are fully proved in that node.
It requires the graph supplier's general algebraic degree lemma, not
a graph hypothesis. The finite mixed-factor gain ledger is local.

The automatic interpolation companion combines the already required
rational-X graph supplier, degree-free pencil supplier, ordinary LIST
certificates and original margin resource. Parallel-fiber overlap and
homogeneous interpolation are proved locally. No additional node, edge,
graph-existence premise or speculative plane-curve child is introduced.

The degree-two-through-seven polynomial-coefficient extension uses the
same `mca_low_core_quadratic_graph_payment` node, whose ID retains its
origin. Its generic dimension/degree proof and received-data root bridge
are complete. No new node, edge or per-degree conditional is introduced.

The quadratic-graph companion adds the PROVED critical supplier
`mca_low_core_quadratic_graph_payment`. It proves its own nonlinear
coefficient LIST estimate and needs only the existing support-margin
resource. No numerical MCA/ordinary Johnson constants are used for this
new class. The arbitrary-source router still receives evidence only.

The degree-free rational-pencil cover companion expands the already
required `mca_low_core_polynomial_relation_payment` supplier. Its
certificates use the already required ordinary
`list_padded_johnson_dimension_descent`, not numerical MCA Hensel caps.
No new node/edge and no cover-existence conditional is introduced.

The current complete-U_6 payment uses supplier 5's shared-carrier JOINT
LIST extension, the core-completed weights already in supplier 7's
ancestry, and supplier 6's exact common-core lift. The finite count uses
two ordinary Johnson certificates, not the numerical MCA Johnson/Hensel
cap. Its pair-to-label conversion counts all noncore points and is proved
directly. No extra required node, edge or speculative hypothesis is added.

This consumer requires the following proved suppliers:

1. `mca_nonuniform_support_margin_resource`: pre-maximization resource,
   specialized to empty universal core.
2. `mca_received_pair_one_defect_weighted_list_reduction`: scalar kernel,
   incidence and weighted grouping argument, with zero scalar common core.
3. `mca_low_core_polynomial_relation_payment`: covered gcd roots,
   same-carrier dimension drop and charged preferred rational labels.
4. `mca_raw_low_union_overlap_payment`: small/empty union branch.
5. `list_padded_johnson_dimension_descent`: exact ordinary rank-ten caps.
6. `mca_common_core_low_margin_transport`: saturated-support transport.
7. `rate_half_mca_error_rank_twelve_common_core_forcing`: original-row
   routing into this exact interval and the original near add-back.
8. `mca_maximal_margin_complete_core_selection`: complete-agreement
   defect saturation and rank-one collision exclusion after reselection.
9. `mca_low_core_quadratic_graph_payment`: quadratic coefficient locus,
   degree-charged LIST bound and original-coordinate LOW label ownership.
10. `mca_low_core_homogeneous_level_payment`: fixed-divisor coefficient
    dimension, JOINT LIST incidence and raw-weighted curve group gains.
11. `mca_polynomial_map_projective_jet_dimension`: dimension ceil(s/e)
    in large characteristic and the degree-two-through-nine graph price.
12. `mca_low_core_smooth_cubic_payment`: smooth-cubic coefficient
    dimension <=1, nonconstant-j rigidity and original joint group prices.
13. `mca_normalization_unit_curve_payment`: two integral functions give
    both valuation bounds, dimension <=1 and the singular multi-boundary
    cubic price with the original degree charge.

All thirteen supply this node, which is required by the critical
`prize_full_threshold_brackets` and supplies an evidence edge to the TARGET
`rate_half_mca_global_core_rank_support_distance_router`. The latter
remains TARGET because relation-free and other row/rank cases are unpaid.

The two universal-core-zero specializations are proved in proof.md;
they are not inferred by substituting zero into an already maximized
resource formula. The numerical certificate needs no new conjecture.

The maximal-support low-span companion uses the core-completed basis
weights and error-rank gauge/dimension-ten cap already in supplier 7's
proved ancestry. Its finite arithmetic is checked both by products with
Fractions and independently by binomial coefficients with integers.
Only supplier 8 is a new required edge; no speculative child is added.

The seven-margin companion uses the same core-completed weights and the
rank-seven padded-Johnson cap in supplier 7's ancestry. The bounded-row
branch reuses the elementary core-ceiling proof. All required suppliers
are already present: no edge, node or conjecture is added. Its short
integer relaxation separately certifies both paid source classes.

The bounded-remainder companion uses the elementary root bound and
suppliers 1, 6, 7; the degree-<A branch reuses this node's existing proof.
It adds no required edge or speculative child. The cyclic interpretation
uses a degree-adapted E-offset as explicitly supplied by the separate
row-saturation theorem; the bounded-remainder payment itself needs neither
that theorem nor any E-valuedness condition. No reverse edge is introduced.

The global metadata audit corrected four partial-source inputs to the open
router from req to supplier-owned ev. A red proof target is a logical leaf;
these paid special classes do not prove the whole arbitrary-source claim.
Their required edges into actual PROVED consumers remain unchanged.
