# Audit

## Cumulative weights and cubic tail

`cumulative_low_accounting.md` keeps the entire nested raw-margin profile
on one selected source and uses a convex chord bound, not a reused
discount. `cubic_tail_payment.md` retains the actual <2A factor height
and 256 exceptions. The component/GP envelopes are checked at J<=9821.
The full-kernel child's `cubic_tail_audit.md` checks the exhaustive
factor and large-line complement composition. No new node or req edge,
arbitrary-source cover assumption, original-red or prize closure.

## Multiplicity escape pays every quartic

The new supplier uses full multiplicity in X,Y,Z, not only pair jets.
Its strict kernel-dimension gap escapes the prescribed curve factor;
Bezout then counts all function-field pairs with no coefficient-component
omission. The finite price has three actual monomial regimes, including
the independently caught 9568/9569 transition. At most 68 curve pairs,
64 exceptions, 981604 labels per pair and one /5500 HIGH/near base
give N<=4194117119656075. The source-class range ends at 9821;
only the full-kernel child supplies all-source coverage through 9526.
No normalization, height or smoothness guard is needed. See the new
supplier's hand audit and independent tiny controls; external review is due.

## Quartic moving-projection refinements

`quartic_projection_graph_payment.md` uses the actual input degree J+h,
ambient dimension 22-d and maximum dimension of ALL coefficient components.
The smaller d=3/4/5 cases require the stated actual dimension drop. An
empty locus costs no pairs; no lower dimension is inferred from examples.
The kernel child, not this consumer, supplies the quartic height and
off-pair consequences. Removing impossible on-graph agreements changes
only the auxiliary LIST domain; each pair still costs up to 981604
ORIGINAL labels. One HIGH/near base and four off-pairs give the uniform
total 274015369961868939. Local hand and two independent-code arithmetic
audits pass; see the kernel's `quartic_multiplier_audit.md`. These are
restricted payments, not a general quartic count or external review.

## Projective split-product quartics

The new supplier proves the finite projective fiber bound and counts
all four affine-offset/scaling cases; it does not merely invoke a finite
number of unpriced divisor classes. Its <=163644855660 pair cap
has independent Pascal/Hilbert and polynomial/evaluation controls. This
consumer adds the same <=64 off-curve pairs, actual LOW labels and
one completed-HIGH/near base, giving N<=164828561948185643.
Rational offsets never remove original coordinates. A fixed GL_2(F)
matrix and a nonzero product are essential; no general moving-direction
or arbitrary-quartic assertion is inferred. See `quartic_split_product_payment.md`.
The source-class refinement and tests are local, not external review.

## Weighted affine-singular cubics

The new supplier retains all dimensions of the actual coefficient locus,
not an arbitrary pure cover with possibly constant projection. Its finite
power-cover shifts justify the exact lift division. All twelve kernel
dimensions are priced; the remaining three must attain dimension 3,4,4.
The new paid-alternative constant is larger than the old one, still below
budget. No old numerical bound is silently strengthened.

## Coupled moving projections

Supplier 11 retains the actual scalar input degree J+h and the
rank-nullity identity input dimension + kernel dimension =22. Its
coefficient dimension uses that same kernel, preventing two independent
worst cases from being charged together. Exact graph recovery prevents
an omitted fiber multiplicity. Polynomial projection coordinates keep
all original code points even at displayed output poles. Only primitive
weighted equations receive forced height bounds and ordinary conic
prices. The finite-singularity (2,3) class is not treated as a linear
projection graph. No additional required node or edge.

## Normalization-unit continuation

The new supplier keeps both monic equations, one fixed extension,
finite divisor classes and finite unit fibers. Auxiliary evaluation
makes the scalar-family map algebraic without a domain genericity
assumption. Singular points and all divisor classes remain in the
original e^21 cubic degree cover. A singular three-boundary cubic
passes; both one-boundary nodal and cuspidal families reject an
unrestricted singular-cubic claim. The unchanged exact group price
is independently inherited and recomputed locally.

## Smooth generic cubics

The new section theorem treats the all-smooth elliptic product separately
from the singular-fiber case, spreads an actual family before taking its
tangent, and injects coefficient variations using a curve isomorphism.
The finite count stays in the original 22 pair coordinates. Singular
generic cubics and unjustified constant-j rigidity have algebraic controls.
Two independent tiny arithmetic checks are provided with the new critical
supplier; they do not certify the classical surface theorems. Mixed covers
retain one original resource and near allowance. External review is due.

## All individual conics

Hand-checked the exhaustive quadratic-rank split, constant-direction
reduction by a CONSTANT component matrix, and singular irreducible
quadratic rational-point argument. Moving-axis dimensions use the
equality exclusion in the existing graph supplier. The cover and LIST
ratio are computed in original pair coordinates, retaining degree 2^17.
The exact point cap is 128*63^5=127031877504; its original LOW
label/gain cap is 124694999085436416. Only one resource and near
charge are added, including in the moving-plus-five-degree cover.

Primary rational/integer check: 0.03 s, 12160 KiB. Independent
binomial/integer audit: 0.01 s, 10496 KiB. Two-moving and moving-
plus-expensive-cubic recipes are explicitly not certified. The actual
small moving source, equality gates and prior-cover exclusion are
documented with the supplier. External hand review remains due.

## Homogeneous level and mixed-factor continuation

The new supplier's fixed-divisor argument bounds coefficient dimension,
not the number of divisor classes. The e^21 cover retains every
component. Exact gains use depth-dependent JOINT counts and cumulative
raw mismatch ownership before telescoping. Primary rational and
independent upward-rounded arithmetic agree: cubic gain
159185671413625180; cubic plus four nonconstant pencils has original
total 274028871902226632 and reserve 951856209168455.

The mixed ledger prices ordinary factors per total component degree;
the uniform graph endpoint uses Q<63/4 and checks degree seven once.
It does not give constant-direction pencils that price. Every label
belongs to one group and the original resource/near are used once.
New checks: 0.03 s / 12544 KiB primary, 0.43 s / 13184 KiB
independent (including existing pencil transitions and mutation guards).
The actual hyperbola and missing-hypothesis controls live with the new
supplier. External independent proof review is still outstanding.

## Automatic homogeneous interpolation

Hand-checked the strict number-of-unknowns test, weighted degree after
substitution, complete-core root argument including poles, and all
three cases of the output coefficient/numerator degree. A pure-input
relation has at most seven polynomial roots over F(X), not seven
unrelated slopes. Parallel-fiber union overlap limits all but one cost;
the shared original resource and near charge occur once. Small groups
are handled by core overlap and a single-pair bound.

Primary exact check: 0.23 seconds, 12800 KiB. Independent upward-
rounded audit: 0.47 seconds, 13312 KiB, rechecking the earlier coupled
expression, all 12220 tail LIST transitions, explicit monomial counts,
and invalid-price and nonstrict-kernel controls. No large interpolation
matrix is constructed. The plane relation is not recorded as a payment.

## Uniform polynomial-coefficient graphs

The generic companion uses the maximal VARYING coefficient and a
nonzero leading polynomial in that coefficient to rule out generic
cancellation. It yields r=6 for degree two and r=5 for higher degrees
in the same dimension-eleven carrier. The degree-seven endpoint gives
M<=123363917824 and L_LOW<=121094515191709696; one resource
and near add-back give total <=167137715680311148. Primary and
independent endpoint checks agree. No X-degree restriction is silently
added to the exact-identity class, and no unguarded pointwise fit is used
to establish it. The source bridge has its own explicit root-degree guard.
Characteristic-three and weighted-graph controls live with the supplier.

## Quadratic-graph continuation

The new generic supplier proves the algebraic coefficient dimension,
degree-charged scalar LIST bound and original-coordinate slope ownership.
The finite proof checks Q=1048577/66973<16, r=6, degree charge 32,
M<=536870912 and L_LOW<=526994634702848. One global resource and
one original near add-back give total <=46570195123304300.
Primary Fraction and independent binomial/integer checks agree. They do
not substitute arithmetic tests for the hand-proved geometric supplier.
Its actual 27-pair control lies outside the old covers; its characteristic-
two and zero-dimensional examples test two necessary proof guards.

## Degree-free rational-pencil continuation

Hand-checked the generic exact identity, Bezout parameterization, same-
carrier dimension drop and covered-core primitive compatibility without
height bounds. Partition pairs and their labels before counting. Global
F(J)/501 and the near charge occur once, not once per group. The
constant group reuses a numerical EXPRESSION envelope, not the old
whole-source theorem with other LOW groups silently discarded. Small
groups collapse to one pair by core overlap and are charged directly.

`verify_rational_pencil_cover.py` checks the exact group and full-source
totals; `verify_rational_pencil_cover_audit.py` independently checks every
ordinary LIST transition and upward-rounded envelope, including the
constant expression. Underpriced traces are rejected. Fourteen copies
of the rounded cost exceed budget: this recipe does not certify them.
The separate exact small-field verifier checks actual high-height ZERO
cores and the non-MCA polynomial-family cover guard. No empirical
coverage statement is substituted for the unproved general-source count.

## Complete very-low union continuation

The auxiliary joint LIST domain can be extended to J+400000 points
inside the original D; it keeps the field and degree unchanged and loses
no existing agreement. Anchoring uses a single agreeing polynomial pair,
with BOTH component differences in the same dimension-ten kernel.
Evaluation on V is nonzero on U_6 because the universal core is empty.
The slope conversion charges all points outside each complete H_f, not
merely outside U_6. The existing 23-label zero/cyclic controls, with
U_6=D, explicitly refute the latter omission and nearly attain the
correct 24-label toy cap. Canonicalization is not a payment hypothesis.
The original union floor is for the lifted selected family; it does not
identify row spaces on U_6 and U_500 or preserve an older selection.

## Full-weight continuation

The weight floor for ALL raw>=7 is proved in two ranges: monotonicity
of the core formula through raw 84, then the old truncated raw weight.
The core formula is not extrapolated past r=d. The resource endpoint is
the smaller CURRENT upper J=169999; the 254999 endpoint would not
justify this new number. The seven-margin remainder condition retains
the exact-relation branch for deg Q<m-500 and the complete-core guard.
The raw<=6 rank theorem uses the actual subfamily's gauge, retains all
raw>=7 labels in the global budget, and adds original near only once.
An independent integer/binomial check includes a simple weaker certificate
with small rational weights, so the two payments need no large-fraction
black box. External proof review remains outstanding.

## Canonical low-family continuation

The rank test is applied to LOW after maximizing support and explanation
inside the fixed normalized carrier. The gauge counts that subfamily on
the same row; the HIGH remainder stays in the original weighted budget.
Its original near charge is added once, with no extra child-near premise.
Full error rank twelve makes LOW's common scalar agreement equal the
literal universal carrier core, which was already empty before selection.
The new proof does not transfer an earlier selection's rank or core union.
The generic controls retain both zero/cyclic full row spaces and rank-two
cross incidences, so those hard classes are not declared eliminated.

## Bounded-remainder continuation

The root argument separates deg Q<A (possible polynomial identity) from
deg Q>=A (nonzero difference of degree exactly deg Q). Only the second
branch receives a forced margin floor. Complete cores, actual minimizing
pairs and the high-label resource are retained; arbitrary subdomain data
alone are insufficient. Original near 134944 is charged exactly once.
Saturated transport subtracts R_0*h_* before dividing by P_G; its degree
is below both relevant bounds, so the degree drop is legitimate.

`verify_bounded_row_remainder.py` replays the old 25740 transitions and
independently checks the resource endpoints with a binomial expression:
0.33 seconds, 12288 KiB peak RSS. Margin 139 pays; margin 138 fails
this uniform recipe only. `verify_row_remainder_controls.py` checks six
actual F_(13^6) slopes, raw two, complete union eight, and the FULL row
space by a 40x36 prime-field matrix. The kernel has dimension four,
including two offset dimensions; the row space is cyclic of dimension two.
The full-rank degree-three evaluation matrix excludes all old relations.
This complements the hand proof, not a deployed-field exhaustive search.

## Earlier certificate

Hand-audited points:

- The child's empty COMPLETE shared scalar core implies empty universal
  carrier core in the intrinsic rank gauge, not for a guessed enclosing
  rank. Saturated support transport retains every selected slope.
- The global resource uses g_0=0 before maximizing over zero normals.
  In both constant-kernel charts the scalar-list universal zeros are
  also absent; neither a separate maximum nor a missing denominator
  is used.
- Polynomial relations only concern low complete cores. High labels,
  outside coordinates and rational-direction exceptional slopes retain
  their charges. Small unions are paid without any relation assumption.
- Convexity covers the whole J interval; arbitrary-domain padding and
  exact block weights cover every outside count and depth. Both J
  endpoints and the last truncated outside interval are checked.
- Near 134944 belongs to the original row and is added once. No new
  near-removal is performed in the child.

Local primary exact check: 25740 integer transitions, 0.26 seconds,
12672 KiB peak RSS. Separate integer-transition/binomial-resource and
upward-Decimal envelope audit: 0.21 seconds, 12928 KiB. Two invalid
certificate mutations are rejected. The transport's actual F_17 check
took 0.04 seconds and 10880 KiB. No Modal use, spending or live tasks.

These computations certify finite inequalities around a printed proof;
they are not numerical survival evidence substituted for a theorem.
Independent external mathematical review remains outstanding, including
the existing rounded MCA supplier used only in the original-row routing.
Generated DAG, software and crosswalk replay remain separate obligations.
