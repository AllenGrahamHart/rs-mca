# Canonical low-family rank payment

Status: PROVED, 2026-09-07. This companion uses the remaining interval
4801<=J<=169999, not the older upper endpoint 254999. Fix the same
normalized KoalaBear row and ACTUAL dimension-eleven carrier:

    R=1048576, d=67472, n=R+J, K=J, m=d+J,
    q=2130706433^6, T=500, B=274980728111395087.

Its universal carrier core is empty. Preserve the given finite label set
and fixed affine carrier. AFTER common-core cancellation, choose for each
label a full-code-bad m-support and carrier explanation maximizing its
raw margin, as in the required maximum-margin selection theorem. Call
the selected raw<=500 labels LOW and the other labels HIGH. Let their
counts be L,H. Error rank below means the affine dimension over F of
the selected scalar error vectors on this normalized domain.

If the LOW subfamily has error rank at most eleven, the WHOLE original
near-inclusive count is at most

    199054477120667562 < B,
    reserve =75926250990727525.

This is a source-class payment, not merely a necessary rank restriction.
It does not assume that HIGH labels are absent. In fact the counting
proof also holds for noncanonical selections; canonicalization supplies
the stronger structural description of the unpaid family below.

## Weighted budget and low-family gauge

For the empty universal carrier core, the support resource is at most

    F(J)=(R+J)_falling_12 / ((d+J)*prod_{i=1}^{10}(d+i)).

Write y=d+J. Since R-d-11>0, dividing
prod_{i=0}^{11}(y+R-d-i) by y gives a sum of positive multiples of
nonnegative powers of y and y^(-1). Each term is convex for y>0.
Thus F is convex and its interval maximum occurs at an endpoint.
The endpoint ceilings are

    ceil F(4801)   =13195104981505077258,
    ceil F(169999) =23067643444721720934 = C.

The required core-completed-basis supplier gives every record weight
at least alpha=12*d/(d+11), since d>=11*12. A HIGH record also has
weight at least 501 by the original truncated margin count. These
weights use the SAME tuple budget, combined by maximum, not addition.
Consequently

    alpha*L + 501*H <= C.

The error-rank gauge, already in this consumer's proved ancestry,
applies to any selected subfamily. Rank<=11 gauges LOW into actual
explanation dimension <=10 on the SAME field, domain, row and labels.
Its field gate q>=2097152 holds. The proved uniform dimension-ten cap is

    L <= V10=156765527508668296.

No near assumption on this subfamily or on any shortened child is used.
For a singleton the inequality is immediate; otherwise its bad support
implies v is not a global degree-<J polynomial, which makes the usual
error-difference parameter map injective. Since 501>alpha, we obtain

    L+H <= [C+(501-alpha)*V10]/501.

Take the floor only at the end, then add the ORIGINAL near charge
134944 once. This gives the stated total and reserve. No normalization
exception is removed in this argument: we work on the already cancelled
empty-universal-core row with all its selected labels.

## What any unpaid source must contain

If the original count exceeds B, its selected non-near count obeys
N=L+H>=B-134944+1. Rearranging the same weighted inequality gives

    L >= ceil((501*(B-134944+1)-C)/(501-alpha))
      =234554688218295064 > V10.

Thus LOW itself has error rank exactly twelve. Indeed its explanation
parameters (gamma,lambda_1,...,lambda_11) have affine rank at most twelve,
and injectivity of their error map follows from v not being a codeword.
The rank test is made AFTER canonical reselection: preservation of a
previous selection's error rank is NOT claimed.

LOW's COMPLETE common scalar agreement is empty. To see this, a common
agreement coordinate would make the affine functional

    u(x)-h_*(x)+gamma*v(x)-sum_i lambda_i*c_i(x)

zero on a full-affine-rank subset of F^12, hence identically zero.
This forces u(x)=h_*(x), v(x)=0 and c_i(x)=0 for every i, contradicting
the fixed carrier's empty universal core. In particular the complete
LOW pair cores have empty intersection too.

The maximum-margin theorem applies because 2T=1000<67472. Each LOW
support contains ALL the defects of its complete scalar agreement set.
Its scalar agreement is exactly its complete pair core plus those <=500
defects. Every cross-core incidence is between pairs whose two difference
polynomials are F-linearly independent. Rank-one differences would supply
an alternative scalar explanation of the same label with raw>500.

## Scope and next obstruction

The earlier zero/cyclic full E-row-space alternatives and bounded-remainder
restrictions apply to this selected family's actual COMPLETE low-core
union. They are not transported unchanged from another selection. The
small exact controls in the generic supplier realize both alternatives
even after full carrier maximization; rank-two cross incidences remain.

This result pays low error rank <=11 throughout the residual J interval.
It does not pay rank-twelve LOW families, bound their cross-core mass,
pay original error ranks >=13, change the unrestricted first-safe bracket,
or solve either prize. The next mathematical problem is a count retaining
this full-rank, complete-core, rank-two compatibility, not another
independently maximized scalar-image count or per-height case campaign.
