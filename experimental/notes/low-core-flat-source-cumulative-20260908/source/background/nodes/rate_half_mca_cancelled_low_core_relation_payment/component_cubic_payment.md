# All d=7 components are paid

Continuation: on 8656..9526 and 1301<=h<=4762 the same supplier's
`next_interval.md` gives <=4271 pairs per top component, counts the
entire lower-dimensional complement, and pays N<=241058823023306107
including <=64 off-curve LOW pairs. The h<=1300 weighted price is
unchanged. The full-kernel child may use these groupwise counts with
other factors; one resource and near allowance enter once. Arbitrary
quartics remain outside this source-class theorem.

The required supplier `mca_singular_cubic_component_list_payment` proves
on the SAME normalized source and 7117..8655 that the remaining d=7,
1301<=h<=4327 affine-singular cubic class pays

    N=|Gamma|+134944<=73164604161759423<B.

It handles all geometric coefficient components: <=315 nonsingular pairs
on each of <=3^16 top-dimensional components, plus the lower-dimensional
complement bounded by the actual weighted lift budget. One singular pair,
<=64 off-curve pairs, original labels and one resource/near charge are kept.

This supplements the separately required d=10 supplier. The full-kernel
child supplies its own exhaustive coverage, so its last d=7 alternative
is now paid. Every source on 7117..8655 consequently has
N<=274979661292365251, with reserve 1066819029836. Together with
the quadratic strip, normalized 4801..8655 is covered; the next unpaid
integer is 8656. No curve hypothesis is imposed on that entire-strip
conclusion, because the kernel child proves the required dichotomy.

The generic supplier does not require this consumer or its child. Larger
J, higher original ranks, original-row normalization/near transport and
active-owner accounting remain separate work. Neither prize closes.
