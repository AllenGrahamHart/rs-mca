# Hand Audit And Control Scope

Proper joins have rank>=7 only for DISTINCT rank-six subspaces. The
intersection coordinate count uses the maximum over ALL proper flats,
including smaller-rank intersections. The condition h>(K-4)/7 is strict.
The dimension-one intersection conclusion follows only after the join is11.

Cauchy counts coordinate incidences within one H, not finite slope labels.
The coefficient a^2-Mh must be positive before division. The row's core
length J+67466, not its full domain length1048576+J, gives the caps25/17.
The near-maximum version chooses b coordinates per flat ONLY for packing;
the annihilator argument still uses each full actual locator. Its proper-
join and root comparisons retain the deficit e=6h-b. Its distinct rational
directions do not rely on equal original flat sizes. Its packing constant
must be recomputed, not copied from the maximum-family specialization.

The annihilator direct sum makes polynomial-pair representation unique.
Both components of a nonzero vector are nonzero by pairwise zero
annihilator intersections. The cross determinant vanishes on C outside
A union B, not automatically on their intersections. Its degree is bounded
by k_A+k_B-2, requiring STRICTLY more distinct roots. Coprime factorization
then gives a polynomial multiplier, not just a rational one.

The gcd of the multiplier space is retained in LOCATOR. Dropping its roots
or claiming P_C always divides the primitive R_C alone is not justified.
Distinct directions follow from dimensions4+4>5, with both spaces in the
same fixed two-anchor image. None of this compares different H or anchors.

The F193 maximal-flat fixture has singleton projective evaluation fibers
and three dense rank-six flats. Thus the new statement is not the earlier
fiber-secant theorem in different notation. The F1201 fixture realizes
the d_C=4 case with nonconstant primitive components. An actual F23
Reed-Solomon space supplies nonzero cross determinants when the root
threshold is omitted. A second F1201 source has an actual multiplier-gcd
root where the primitive R_C is nonzero, so the locator factor cannot be
dropped. These exact controls supplement the general hand proof.

The gcd-root fixture caught an initial verifier shortcut that interpreted
coefficients in a nonmonomial annihilator basis as monomial coefficients.
The checker now divides every actual basis polynomial by its full locator
and reconstructs both polynomial components before testing determinants.
This was a test-harness normalization error, not a change to the hand proof.

Finite constants are checked normally and under-O by separate rational
and integer implementations. External independent review remains due.
Verdict for the printed local hand proof: NO ISSUE.
