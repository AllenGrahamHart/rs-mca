# Dependency sub-DAG

```text
elementary polynomial algebra [proved in proof.md]
        -> list_two_component_relation_dichotomy [PROVED, requires=[]]
        -> prize_full_threshold_brackets [PROVED, consumer-owned req]
        -> existing grand-root assembly [still CONDITIONAL]
```

The source theorem is independent of prize arithmetic, dimension
compression, the coefficient-field MCA kernel theorem, and the root
assembly. The consumer composes it with its existing compression and
simple-pole equivalences. There is no reverse dependency.

The scalar-tail companion proves its source-fiber inequality internally
by polynomial root counting. It also has no imported requirement.
Its two-radius upper route is supplied through the SAME existing req
edge; no additional node or speculative scalar-bound child is added.

M_1 and M_0 are definitions in an exact equality, not new assumed
suppliers. No speculative conditional child is created. Generated
graph replay is deferred under the no-scripts instruction.
