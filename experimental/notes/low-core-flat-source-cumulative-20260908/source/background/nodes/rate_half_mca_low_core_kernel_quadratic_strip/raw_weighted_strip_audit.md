# Raw-Weighted Strip Audit

Local hand audit, 2026-09-08. External independent review remains due.

- Raw margins are fixed by the existing canonical all-support selector.
  LOW_125 is a subset of LOW_500, not a new source or new raw choice.
- The completed resource is bounded by F on the actual J interval.
  Convexity gives the maximum at an endpoint; both rational ceilings are
  checked. The old whole-interval maximum is not assumed attained here.
- S_t is a SUM OF RAW, not a label count times t. Complete defect sets
  are disjoint outside their own pair core, proving the stronger budget.
- The weighted chord has harmonic coefficient H_124-2+2/125.
  Convexity of 1/x on six dyadic blocks proves H_124<=6 by hand.
- The d=7 weighted theorem counts the whole nonpure coefficient locus,
  with actual parameter degree <J+h at every agreement depth.
- At high height, identical-agreement coordinates change the slot budget
  by -3g. Both derivative parameters b stay positive at the new cutoff.
  The same h is retained in slot and agreement expressions. The minimum
  denominator 37424700 is positive.
- The d=10 model and 21h loss are unchanged; the actual new endpoint
  and parameter height yield denominator 18172692. Infinity and all
  smaller coefficient dimensions remain included.
- The full kernel has actual <2A factor weight and <=225 exceptions.
  Pricing 256 is a relaxation, not an exception dropped from the count.
- The large-line proof uses original pair degrees on its outside domain.
  All other factors have total degree <=2. It neither prices arbitrary
  off-line families nor extrapolates the old unrestricted Johnson result.
- The bridge back to the /501 primary-line expression is proved anew:
  all raw>=126 records have completed weight >501. Without this bridge,
  medium margins 126..500 would be missing. No second source resource
  or near allowance is added to the primary-line expression.
- Every irreducible/reducible, repeated, empty and constant-gcd pattern is
  covered. Completed alternatives combine by maximum, never by sum.
- The all-multiplicity fence concerns only the printed sufficient dimension
  inequality. Convex shell counts cover every J in the interval, and the
  r>=5 argument is analytic; samples cannot substitute for that quantifier.

The primary check uses exact fractions and closed weighted dimensions.
The independent audit separately counts monomials, rounds each of 124
raw-budget terms upward and combines integer denominators, and directly
checks the Johnson cross products. No primary checker is imported.
Both also test the omitted-medium-margin and double-resource errors.

These checks support arithmetic, not formal certification of the geometric
proofs or original-source/owner transport. The new 119 dimensions are a
normalized-source closure only. Neither prize is complete.
