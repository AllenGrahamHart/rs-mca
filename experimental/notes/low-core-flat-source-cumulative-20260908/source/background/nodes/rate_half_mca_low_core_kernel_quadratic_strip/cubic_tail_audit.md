# Audit of cumulative accounting and the cubic tail

Local hand audit, 2026-09-08. No external independent review is claimed.

## Source and resource interfaces

The selection, actual carrier dimension eleven, field, degree bound and
complete cores are unchanged. L_t always counts raw<=t labels in the
SAME selected source. Their pair list has agreement >=m-t and every
pair has at most n-m+t labels. A pair counted at several depths is not
a duplicated source charge: the cumulative identity deliberately gives a
raw-r label the coefficient (500-r)/500.

The completed weight has eleven positive factors. Bernoulli gives
w(r)>=11r through 500; its positive logarithmic derivative handles
500..5500; the truncated resource handles the rest. This proves a bound
on every raw margin, not just those sampled in a numerical check.

The d=7 weighted pair count applies to the whole coefficient locus at
each agreement depth, with actual input degree <J+h. Every component
and the singular case are retained. Convexity bounds the cumulative sum
by two exact endpoint expressions. The old /501 gain is never inserted
into this /5500 conversion. One near allowance remains.

## Geometric and factor interfaces

- The generic double-point cover has degree<=3, <=256 off-gcd pairs
  and factor weighted degree <2A. The old <A/64 scope is not reused.
- The moving cubic graph height is <=41473, so the existing <=51391
  coupled projection result applies without a new normal-form premise.
- Other affine-singular kernel dimensions use the true J<=9821 carrier
  height, not a lower bound borrowed from the simple-point kernel.
- The d=7 high-height component estimate keeps h and parameter degree
  coupled, with positive denominator 80873689. Every top component and
  the full lower-dimensional complement are counted.
- The d=10 GP model is unchanged. Its new height, domain and agreement
  envelope pays the complete 21h loss and infinity; denominator 4201567
  is positive. All smaller coefficient dimensions remain covered.
- A large factor line uses a bounded-degree complement. Every off-line
  pair has enough agreements OUTSIDE its compatibility domain. Joint
  LIST uses the original degree bound, not an uncontrolled parameter.
  Lines have coefficient dimension <=11; the existing conic classification
  gives dimension <=6 and its original degree-2 cover costs 2^16.
- The primary-line expression already includes one /501 resource and
  near. All other factor and off-gcd labels are charged explicitly, with
  no second resource base. The unrestricted large-line theorem is NOT
  extrapolated beyond 9526.
- Repeated, empty, reducible, nongeometric and constant-gcd cases are
  included. Whole-source alternatives combine by MAXIMUM.

The primary checker verifies every tail kernel row, the raw-weight gates,
all cubic kernel dimensions, component/GP endpoints, line/complement
bounds and final source totals. The independent checker uses integer
cross multiplication, all 500 LOW weight products, 499 upward-rounded
nested counts, direct Cauchy inequalities and independent monomial sums.
It imports no primary-check code. These arithmetic controls support the
printed inequalities, not formal certification of the universal geometry.

The cumulative check also confirms that treating every LOW pair only at
t=500 would fail the budget at h=1600. Adding another resource base to
the large-line branch would likewise fail. These are guards on the
accounting recipe, not counterexamples to an MCA source theorem.

Full normalized coverage is 4801..9821. This is not a higher-rank,
unrestricted original-row/owner-transport, LIST or prize closure.
