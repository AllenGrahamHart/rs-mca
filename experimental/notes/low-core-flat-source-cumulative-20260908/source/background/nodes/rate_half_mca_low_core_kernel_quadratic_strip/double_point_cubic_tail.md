# The entire double-point cubic tail is paid

Status: PROVED. For EVERY canonical normalized source of this node on
9527<=J<=9821, without a curve-description hypothesis,

    N=|Gamma|+134944<=274778805314695460,
    B=274980728111395087,
    reserve>=201922796699627.                         (DT)

This closes 295 additional integer J values. Together with the earlier
intervals, EVERY normalized source on 4801..9821 pays at the previous
uniform bound 274979661975561635. The remaining normalized interval
starts at 9822, not 9527. No original-row endpoint or prize is asserted.

## 1. The full kernel, with the actual new scopes

The required multiplicity supplier's kernel_corollary.md gives gcd degree
<=3 for the entire double-point kernel and at most 256 off-gcd LOW
pairs on this interval. Its primitive factors have weighted degree <2A,
A=J+66972. These are proved source consequences, not assumed premises.
Do not reuse the old <A factor height or 64-pair exception allowance.

## 2. Exhaust every factor pattern

If the gcd has an irreducible cubic, the finite consumer's
cubic_tail_payment.md pays EVERY cubic type by (DT), including the
singular and isolated cases and all 256 exceptions.

Otherwise the factors are lines and conics of total degree <=3. Apply
cubic_tail_line_partition.md to any populated linear factor. If a factor
has more than 980 pairs, its completed whole-source alternative
273209735302733347 is below (DT). This uses small LIST counts on
the complement for the other degree-<=2 factors, not an extrapolated
unrestricted large-line Johnson claim.

In the other case, every populated line factor has <=980 pairs. An
irreducible conic has the existing direct pair bound 127031877504,
independent of its coefficient height. The remaining patterns cost at most

    base+981604*(3*980+256), or
    base+981604*(127031877504+980+256),
    base=4194116990084347.

Both are below (DT). Fewer, repeated or empty factors, or a constant
gcd with only exceptional pairs, only reduce these upper bounds.

Assign each pair and all its labels once. Each alternative retains all
HIGH records, original scalar defects, exceptions and one near allowance.
Whole-source alternatives combine by MAXIMUM, not addition. The largest
is the d=7,h<=1600 cumulative-margin price, proving (DT).

## 3. What actually changed

The critical completed-basis supplier now proves a linear low-margin
weight, giving a cumulative count conversion on the SAME selected source.
This pays the d=7 low-height class through 1600. The complementary
component estimate and the d=10 single-fiber estimate are checked at the
new endpoint. The large-line branch explicitly uses the bounded degree
of the remaining factor cover. No speculative conditional node is added.

Existing PROVED nodes are strengthened; no new node or edge is required.
The double-point degree-three test fails at 9822, which is a limitation
of this sufficient recipe, not an unsafe-source counterexample. Higher
original ranks, exhaustive original-source/near/owner transport,
unrestricted adjacent endpoints, ordinary LIST and both prizes remain open.
