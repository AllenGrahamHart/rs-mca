# Audit of the quartic multiplier and projection refinements

Local hand proof, 2026-09-08. No new external geometric theorem or
independent external review is claimed. The previous LOW-101 interval
closure remains intact; the current work narrows its quartic complement.

Checked proof interfaces:

- The residual quartic is the WHOLE degree-four gcd, so every multiplier
  has pair degree <=4 and strict weighted degree <S. Primitive Gauss
  division is over F[X], not an assumed rational-coefficient equality.
- The actual multiplier count has positive parts. Its last block disappears
  on part of the interval; the two-piece H ceiling retains this transition.
  H bounds the top binary coefficients, not all coefficients of G.
- Newton products use a CONSTANT projection separating at most five
  distinct polynomial pairs. At most ten constants are excluded, and the
  original field has more than ten elements. Their evaluations have a
  triangular independent block structure; no source coordinate is inverted.
- L>D(H)-R_t(H) is a strict inequality. The collinear polynomial controls
  attain the lower rank, and the dimension-equality toy retains an off-gcd
  point. Neither is represented as a canonical rank-eleven MCA source.
- Core density follows from the rank of the ENTIRE interpolation kernel.
  Received-curve coverage on U is asserted only when no LOW pair is off G.
- An off-G pair gives a nonzero polynomial G(X,f) of degree <=4w+H;
  its >=A core therefore supplies >=S forbidden ON-GRAPH agreements.
  The remaining-domain LIST bound removes no original slope, scalar
  defect, HIGH label, off-pair or near allowance.
- Quartic projection graphs keep the real input degree J+h, ambient
  dimension 22-d and all actual coefficient components. The d=3/4/5
  dimension drops are separate proved sufficient cases, not assumptions.
- The projection normal form is tested, not asserted for arbitrary rational
  quartics or curves with one point at infinity. Constant split products
  remain a distinct paid family, not polynomial projection graphs.

`verify_quartic_multiplier_geometry.py` checks the endpoint identities,
Newton surplus coefficients, projection envelopes and original source prices.
The independent audit checks all 763 integer J values by separate monomial
sums, exact F11 evaluation matrices of size at most 30x50, and integer
resource accounting. These are tiny tests, not field-sized computations,
universal geometric proofs or external peer review. Neither a matrix of
the actual interpolation system nor a CAS computation is constructed.

The unchanged consumer requirements supply the generic graph/jet bound
and the pointwise HIGH resource. The kernel owns forced height, exception
and density facts. This adds no node, edge, cycle, speculative premise,
complete normalized interval, original-red closure or prize claim.

Final replays on 2026-09-08 pass in normal and optimized modes. Fresh
normal peak RSS was 11520/10624 KiB. The focused threshold closure
has 71 acyclic PROVED nodes; sorted global diagnostics remain exactly
the pre-change 260 DAG errors and 11 crosswalk alias errors. Details and
the remaining all-components counting obligation are recorded in
`notes/correspondence/QUARTIC_MULTIPLIER_GEOMETRY_20260908.md`.
