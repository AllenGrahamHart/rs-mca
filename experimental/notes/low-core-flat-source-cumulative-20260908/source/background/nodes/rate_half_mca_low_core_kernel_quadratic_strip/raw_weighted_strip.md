# The entire raw-weighted strip through 9940 is paid

Status: PROVED. On EVERY exact canonical normalized source of this node
with 9822<=J<=9940,

    N=|Gamma|+134944<=274811931500367244,
    B=274980728111395087, reserve>=168796611027843.    (RWS)

This closes 119 additional integer J values, with no chosen-curve premise.
The combined interval is 4801..9940 at the earlier uniform bound
274979661975561635. Remaining normalized range: 9941..169999.

Keep the actual eleven-dimensional carrier, empty universal carrier core,
field and all-full-bad-support maximal-raw selection. Only accounting uses
T=125. Full scalar defects and complete pair cores are unchanged.

## 1. Obtain the actual cover

Put A=J+67347, w=J-1, n=1048576+J. For the full double-point kernel,
the required multiplicity supplier gives

    Phi_w(2A)-Phi_w(2A-4w)-4n=3618424-364J.

Throughout the interval the pair-degree cutoff is 15 and the shifted
cutoff is 11. The displayed gap is 43216 at 9822 and 264 at 9940.
It is -100 at 9941; no endpoint is extrapolated.

The full gcd therefore has degree <=3. There are at most 15^2=225
off-gcd LOW_125 pairs; conservatively retain the existing allowance 256.
Every primitive factor has weighted degree <2A, not <A.

## 2. Irreducible cubics

An irreducible cubic occupies the entire gcd degree budget. The finite
consumer's raw_weighted_cutoff125.md pays all its curve types, coefficient
dimensions and all 256 exceptions by (RWS), at actual <2A factor height.
That proof is independent of the existence of this full-kernel cover.

## 3. Reducible covers and large factor lines

The remaining factors are lines and conics of total degree <=3. A
populated factor line containing two distinct bounded pairs has primitive
identity A0*a+A1*b=Q, gcd(A0,A1)=1, h<=J-1<=9939. Its Q is polynomial
of degree <=J-1+h. Pairwise collisions on the line have size <=J-1-h.
Let S be its compatibility coordinates A0*u+A1*v=Q.

If |S|<=590000+16h, every represented line pair has at least 77169
joint agreements on S. Relaxing the collision bound to 9939-h gives

    denominator>=77169^2-(590000+16h)*(9939-h)
                =91044561+430976h+16h^2>=91044561,
    numerator<=749024*77169=57801433056.

Their Johnson ratio has floor 634. An insufficient domain gives no
pairs, and singleton/empty lines are already small.

Otherwise |D outside S|<=468515-16h. An off-line pair agrees on S
at no more than J-1+h positions by its nonzero compatibility polynomial.
It has at least 67348-h joint agreements outside S. Use the ORIGINAL
component degree bound <=9939 in this auxiliary joint LIST count:

    Q_out<=(458576-16h)/(57409-h)
          <=458576/57409<8.

The denominator is positive for h<=9939 and the ratio decreases.
If the actual outside domain is too small the list is empty.
Other factors have total degree <=2. The existing joint-incidence and
conic-dimension proofs give at most 2*8^11 pairs for two lines, or
2^16*8^6=2*8^11 for one conic. All singular conic types are included.

## 4. Retain the old coupled line ledger without losing medium margins

This is an additional accounting argument, not an invocation of the old
LOW_500 source cover. Let w_gamma be the completed-basis weight and use
its actual resource F(J). Exactly,

    |Gamma|<=F(J)/501+sum_gamma(1-w_gamma/501).

Every raw>=126 record has w_gamma>=11*126=1386>501. Its summand is
nonpositive and may be dropped. For a LOW_125 record, w_gamma>=raw,
so its summand is at most 1-raw/501. Consequently

    |Gamma|<=F(J)/501
              +sum_(LOW_125 gamma)(1-raw_gamma/501).   (5)

No raw in 126..500 is left unpaid. The old rational-pencil group bounds
apply to ANY such subset on the fixed line; their proof includes all
complete cores and does not require the other groups to disappear.
The primary-line gain, F(J)/501 and original near are jointly at most
255637082864553899, by rational_pencil_cover_payment.md section 3.
The nonconstant-direction alternative is smaller.

All other LOW_125 groups and all 256 exceptions are charged at full
label cost <=981229 per pair. Thus a large factor line gives

    N<=255637082864553899+981229*(2*8^11+256)
      =272494468975295659 < (RWS).

The primary expression already contains its resource and near. Do not
add the new /1375 base to it. Outside-domain restriction was only for
pair counting: no source coordinate, original defect or label was deleted.

## 5. Small lines and all remaining factor patterns

If no populated line is large, each has at most 634 represented pairs.
The existing direct conic cap 127031877504 at LOW_500 also bounds its
LOW_125 subset, independently of coefficient height. With

    base=9499980819316335, labels_per_pair=981229,

the reducible possibilities cost at most

    base+981229*(3*634+256), or
    base+981229*(127031877504+634+256).

Both lie below (RWS). Constant gcd, empty factors, repeated factors and
smaller total degree only reduce the count. Assign intersection pairs
and all their labels once. Combine completed whole-source alternatives
by maximum, proving the theorem.

## 6. Route boundary and nonclaims

The required multiplicity supplier's cubic_recipe_boundary.md proves
that for EVERY r>=1 and positive cutoff the same degree-three full-kernel
dimension criterion fails on 9981..169999. It uses convexity in J and
an analytic bound for all r>=5, not a bounded multiplicity scan.
This is a limit on one recipe, not a false source theorem or unsafe row.

Thus moving the cutoff/multiplicity can only give limited further mileage.
The next broad route needs sharper source structure, a different kernel
or prices for genuinely higher-degree covers. Normalized 9941..169999,
higher ranks, original-source/near/owner transport, ordinary LIST,
unrestricted adjacent endpoints and both prizes remain open.
