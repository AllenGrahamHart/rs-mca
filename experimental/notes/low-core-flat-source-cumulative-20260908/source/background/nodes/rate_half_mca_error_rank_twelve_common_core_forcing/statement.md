# Rank-twelve payment outside an intermediate common-core range

## Current core-completed-basis extension

The [new proved continuation](basis_child_payment.md) pays the entire
additional interval 793577<=g<=878576, or 170000<=J<=254999,
with original total <=270992495272115150 and reserve 3988232839279937.
It strengthens the support-margin basis count and applies that uniform
bound to the two-anchor theorem's ACTUAL degree-bounded second children.
No low-margin, subfield, polynomial-relation or cyclic-row premise is used.

Together with the older bounds below, rank twelve is paid for g<=878576
or g>=1043776. The unpaid rank-twelve interval is now
878577<=g<=1043775, equivalently 4801<=J<=169999. Exactly 85000
additional shortened dimensions are removed. Higher ranks and both
prizes remain open. The older proof and numerical gates remain valid.

## Earlier uniform-child payment

Status: PROVED, 2026-09-07. On the deployed KoalaBear row fix

```text
q=2130706433^6, n=2097152, K=1048576,
m=1116048, d=67472, B=floor(q/2^128)=274980728111395087.
```

Remove the intrinsic near set of at most 2d bad slopes. For EVERY
remaining bad slope select one explanation h_gamma with an exact size-m
same-support pair-noncontained witness. Let a be the affine rank, over
the ambient field, of its selected error words. Define the shared core
using COMPLETE agreement sets of those explanations:

```text
G = {x: r_0(x)+gamma*r_1(x)=h_gamma(x) for EVERY selected gamma},
g=|G|.
```

If a<=11, the existing rank-eleven theorem pays the full bad-slope
set. If a=12, it is also paid whenever either

```text
g<=793576             or             g>=1043776.
```

For the first branch a uniform total bound, including near slopes and
all normalization exceptions, is

```text
|Z_bad|<=273540953998915577 < B,
slack  =  1439774112479510.
```

The LARGE-core branch needs no rank bound at all: for ANY complete
post-near selection with g>=1043776, the total is at most
100000000000134944. Consequently, on ANY over-budget line, EVERY
complete post-near selection has g<=1043775 and either a>=13,
or else a=12 and

```text
793577<=g<=1043775.
```

In the latter case simultaneous cancellation of this shared core retains
every original slope, leaves empty COMPLETE common agreement, preserves
error affine rank twelve, and produces the unique row

```text
(R+K',K',d+K'), R=1048576, 4801<=K'<=254999.
```

The padded-Johnson V_9 extends the preceding small-core cutoff 623576
by 170000 values, retaining the original field and all exceptions. The
large-core branch now needs one fixed Johnson certificate and padding,
not the preceding 4799-row numerical premise.

This is not a bound on the intersection of arbitrarily selected m-subsets,
a payment of the intermediate range, an all-source rank bound, active-v4
ownership, full KoalaBear safety or a resolution of either prize.
