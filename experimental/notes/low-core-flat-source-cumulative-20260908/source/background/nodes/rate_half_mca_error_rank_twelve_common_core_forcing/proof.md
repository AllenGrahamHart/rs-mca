# Proof

## 1. Gauge and identify the intrinsic shared core

The required rank-eleven payment covers a<=11. Suppose a=12.
Use its reproduced error-rank gauge: choose (1,b) in

```text
E=span{(gamma-gamma_0,h_gamma-h_gamma0)}.
```

Set r_1^g=r_1-b, h_gamma^g=h_gamma-gamma*b, choose
h_*=h_gamma0^g, and let C' be the span of their differences.
The gauge is reversible, leaves all errors and supports unchanged,
and gives ACTUAL dimension s=11. Write T for the common evaluation
zero set of C', z=|T|<=K-s, and

```text
G_0={x in T: r_0(x)=h_*(x), r_1^g(x)=0}.
```

G_0 equals the shared complete agreement set G in the statement.
One inclusion is immediate. Conversely, if all selected errors vanish
at x, every (delta,c) in E satisfies c(x)=delta*r_1(x).
In particular b(x)=r_1(x), all gauged explanation differences vanish
there, and h_*(x)=r_0(x). Thus x belongs to G_0. This uses the
actual error-rank gauge, not an arbitrary larger enclosing space.

## 2. Remove common evaluation zeros with an explicit exception charge

Let e=z-g and
tau=#{x in T:r_1^g(x)!=0}<=e. Each such coordinate can agree
with at most one finite slope. Remove the union of those exceptional
slopes, costing at most tau; this is a union of labels, not a sum over
new owners. The other nonuniversal zero coordinates never agree.

Let P_T be the locator of T. On D minus T, define

```text
r'_0=(r_0-h_*)/P_T, r'_1=r_1^g/P_T,
h'_gamma=(h_gamma^g-h_*)/P_T.
```

The new explanation space has dimension s, degree <K'=K-z,
and no common evaluation zero on its remaining domain. Set

```text
n'=R+K', m'=m-g, d'=m'-K'=d+e.
```

Every retained complete agreement set meets T exactly in G. Hence it
has at least m' remaining points and is pair-noncontained in the full
degree-<K' code. Otherwise a pair explaining that complete remainder
would lift by P_T and addition of (h_*,0), explaining the original
complete agreement set, including G. This contradicts the selected bad
witness. An exact m'-point bad subset exists: if every such subset
were contained, explaining pairs on adjacent subsets would agree on
m'-1>=K' points and coincide, then glue over the whole set. This is
the usual connected one-point exchange argument.

If m'>n', no retained slope exists and the exception charge alone
pays this branch. Otherwise d'<=R. No post-near property is claimed
after normalization; the child theorems count all bad selected families.

## 3. Small shared core

The newly required padded-Johnson supplier proves the uniform explanation
dimension-nine cap, in the unchanged KoalaBear field,

```text
V_9=10755802499540570.
```

Its field-size gate |F|>=2R is satisfied on every child. The cap holds
on every row (R+J,J,d+J), 1<=J<=R. It also applies at higher
agreement with the same explanation: from any larger bad support select
an exact (d+J)-point bad subset by the same exchange argument.
Thus it bounds all second children of the normalized row, whose slack
is d'>=d and whose dimension is at most s-2=9.

Apply the required two-anchor fiber theorem to the normalized family.
Its comparison with the baseline row (K-g,d) gives

```text
|Z| <= tau + floor(V_9*max(F_bal(K-g),F_spike(K-g))).
```

Both F branches decrease with K-g. If g<=793576, then
K-g>=255000. At that endpoint the exact rational values are

```text
F_bal   =383277578467/15718615477,
F_spike =11153988094749115/438581833089399 > F_bal,
floor(V_9*F_spike)=273540953997732057.
```

Using the conservative tau<=z<=K=R and the ORIGINAL near add-back:

```text
|Z_bad| <=273540953997732057+1048576+134944
        =273540953998915577 < B.
```

The exact slack is 1439774112479510. No endpoint maximization or
independent worst-case fiber realization is assumed. The two-endpoint
envelope is a proved uniform inequality.

## 4. Very large shared core, without a rank hypothesis

This part applies to any complete selected family, not just rank twelve.
Families of at most one slope are immediate. Otherwise choose two distinct
slopes and their explanations, put

```text
B_0=(h_gamma1-h_gamma0)/(gamma1-gamma0),
A_0=h_gamma0-gamma0*B_0.
```

The pair (A_0,B_0) matches the received pair on G. If g>=K,
every h_gamma-A_0-gamma*B_0 has at least K roots and is zero.
The selected explanations are globally affine in the slope; a codeword
gauge makes them constant, so the scalar supplier's rank-zero cap
R-d+1=981105 pays the family.

Otherwise g<K. Subtract (A_0,B_0), divide by the locator of G,
and use the same lifting/exact-subset proof. This preserves ALL selected
slopes, with no exceptional charge, on

```text
(n',K',m')=(R+K',K',d+K'), K'=K-g.
```

The selected errors all vanished on G. Restriction to its complement
and division by the nonzero locator values are injective on their
affine-difference space, so the error affine rank is preserved. The
shared complete agreement set is now empty, by the definition of G.
For the rank-twelve case in particular, the gauge still has polynomial
dimension eleven and therefore K'>=11.

If g>=1043776, then 1<=K'<=4800. Pad this actual row, in the
SAME KoalaBear field, to J=4800 using the new supplier's padding proof.
All original bad slopes are retained and the new domain has distinct
points; it need not be smooth. The ONE fixed Johnson certificate at
N=R+J, A=d+J, D=J-1 uses t=31 and satisfies

```text
4*t^2*A^2 >= (2*t+1)^2*N*D,
L=ceil(sqrt((2*t+1)^2*N*D/4)),
Y=ceil(sqrt((2*t+1)^2*N/(4*D))),
Z=max(Y,ceil((2*t+1)^2*N/(12*D))),
L=2239638, Y=467, Z=72600,
H_up=2*L*Y^2*Z+(N-A+1)*Y+Z=70921548248995035<10^17.
```

The ceiling variables bound the required theorem's real quantities
upward. Padding proves the bound for every 1<=K'<=4800, including
K'=1, without applying the Johnson formula at degree zero. No
monotonicity of optimized Johnson constants is presumed. The previous
4799-case checks remain optional regression evidence, not proof premises.

Adding only the original near charge gives 100000000000134944<B.
Only one fixed exact certificate is needed in this branch. Independent external review
of the rounded Johnson supplier remains outstanding as recorded there.

## 5. Residual and nonclaims

Combining the two paid branches with the existing a<=11 theorem
proves the stated alternative for every over-budget line and EVERY
complete post-near selection. In every rank, g<=1043775 is necessary.
In its rank-twelve case,
793577<=g<=1043775 is equivalent to 4801<=K-g<=254999.
Cancellation in section 4 is still valid on this residual interval.

Nothing here pays that interval or error ranks thirteen and higher.
The global set G is the intersection of COMPLETE agreement sets of the
selected explanations, not the intersection of arbitrary chosen witnesses.
No first-match architecture, global crossing or grand prize is closed.
