# Audit

Hand checked finite and infinite projective kernels separately; low
records force the polynomial/carrier alignment; the finite kernel label
costs at most one. Repeated ordinary list words are NOT distinct slopes.
Each such word owns at most e/t labels by its complete core.

The t=max(1,m-a) convention is essential when a core has >=m points.
The proof uses pair noncontainment on the selected support, not an
unproved assertion that every complete core has <m points.

The list zero count is shown to be at most the GLOBAL zero-normal count
in both charts. Resource and list factors are eliminated jointly by
convexity. The n-m>=s condition is used explicitly in its coefficient
proof; the theorem does not silently extend beyond that condition.

The finite consumer includes actual small-field controls with repeated
list words, a finite kernel label, an infinite kernel and a common zero.
These audit the construction, not the general theorem's quantifiers.
External independent mathematical review remains due.

Low-core continuation: every use of core containment in the proof is
attached to raw<=T. The global tuple resource still includes the high
labels. The F_17 controls in low_core_localization.md have UNIQUE
minimizers, low-core defect one and full-core defect two, in both charts.
Thus the weaker premise is strict and does not rely on a tie-breaking
artifact. The finite consumer separately pays |U_T|<m.
