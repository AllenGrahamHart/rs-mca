# Dependency sub-DAG

```text
elementary evaluation kernel + root bound + scalar-support incidence
                              |
                              v
             mca_scalar_agreement_dimension_descent
                              |
                              req
                              v
       rate_half_mca_scan_free_error_rank_eleven_payment
                              ^
rank-one weighted-line cap ---+--- two-anchor near charge
```

The general supplier has no external mathematical requirements. The
rank-one constant, concrete field budget and error-rank gauge belong to
the consumer. No consumer or numerical cap is a reverse premise here.
The original raw-low suppliers remain proved alternatives, but are not
required for this dimension-only proof.

The two-anchor companion adds no premise: its joint fiber restriction
comes from the polynomial root bound on the intersection of two evaluation
kernels. The additional required consumer edge is

```text
mca_scalar_agreement_dimension_descent
                  |
                  req
                  v
rate_half_mca_error_rank_twelve_common_core_forcing
```

That consumer also requires the rank-eleven cap, the rounded Johnson MCA
theorem and the original near charge. None is a reverse dependency of
this general supplier; finite row constants enter only in the consumer.
