# Audit

Hand checks: exchange overlap is m-g-1>=K-g; full-pair containment
is equivalent in both directions; every b in C' vanishes on G;
row division applies to Q-R_0 h_*, not to Q alone; no received
division occurs at a locator root. Tied minimizers are preserved.

`verify_small.py` is an actual F_17 regression, not a proof premise.
It includes a nonzero h_* gauge, complete-core and row-kernel checks,
and an unsaturated support whose raw margin changes from two to one.
Independent external hand review remains outstanding.
