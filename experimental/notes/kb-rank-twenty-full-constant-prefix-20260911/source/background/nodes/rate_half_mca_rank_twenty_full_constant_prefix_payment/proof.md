# Stop At The One-Dimensional Quotient And Pack Its Actual Fibres

## Original-Source Alternatives

Write the unchanged-field normalized source parameters as
(n,K,m)=(R+J,J,d+J), R=1048576,d=67472. Its shared carrier V has
dimension11; pair differences have components in V of degree<J.
Retain each actual pair's ORIGINAL raw-t owner weight for t=1,2.
Its complete joint core has size at least A_t=d+J-t.

The height-zero source gates of
rate_half_mca_regular_rational_plane_terminal_bounds apply to ANY original
source here, not only sources of pair rank19. If an actual P43
constant-direction pencil union has original complement at most the
profile's gate g, the entire source costs at most270000000000000000.

Work on the complementary source branch. Every actual hereditary
constant-pencil fibre then has original complement e>=g+1: its complete
union is contained in the corresponding full original P43 pencil union.
This is a paid whole-source alternative, not an assumed absence of a
possible family. The inherited rank19 terminal L_t is NOT used below.

## Eight Actual Anchors, Not Nine

A constant-direction11 subspace inside V squared is delta*V for some
nonzero delta in F squared. Choose a second constant vector delta_prime
independent of delta. Subtracting the full first component gives

    W = delta*V direct_sum delta_prime*B, B subset V, dim B=9.

This changes coordinates in the proof, not any original finite label,
receiver, field or assigned raw value. Full generic projection is automatic.

After j regular anchors, divide only pair differences by their locator.
The formal nested product has shared dimension11-j, second dimension9-j,
pair dimension20-2j and polynomial degree<J-j. It still contains the
full constant subspace delta*V_j. Actual subsets need not fill this hull.

The common root set of B_j has size at most
(J-j)-(9-j)=J-9. Outside it both component evaluations have rank one,
the joint evaluation has rank two, and its section is the product of
the two kernels. Division by the next anchor preserves B_{j+1} subset V_{j+1}.

Every original pair has at least A_t-j remaining joint agreements.
Excluding b<=J-9 auxiliary root coordinates leaves at least A_t-j-b.
The weighted incidence ratio is increasing in b, so the step costs at most

    (R+J-j-(J-9))/(d+J-t-j-(J-9))
      = (R+9-j)/(d+9-j-t).

The denominator is positive. Excluded coordinates are paid through that
core loss, not silently treated as free. All original label weights persist.
For j=0,...,7 the exact product is

    F_t = product_(ell=2..9) (R+ell)/(d-t+ell).

At j=8 the pair/shared dimensions are4/3, with a full constant3 pencil H.
The whole pair space cannot have function-field rank one: a nonzero
component would inject a dimension4 space into the shared dimension3
carrier. Thus W/H has dimension1 and the compression supplier applies.
No additional ninth-anchor factor belongs in the final source price.

Extra common gcds are not discarded or charged twice: this argument
divides only the eight actual anchor factors and keeps the original
degree bound. The ensuing packing uses ORIGINAL complete-core unions.

## One Budget For All Quotient Fibres

For every occupied actual quotient fibre c let C_c be the union of its
ORIGINAL complete joint cores, and set

    S=n-K+1=R+1, x_c=|C_c|-K+1=S-e_c.

The rational-compression theorem at height h=0 gives

    d+1-t <= x_c <= S-g-1, sum_c x_c <= S,
    Omega_t <= t + sum_c (S-x_c)*L_3(x_c).

The inside charge t is GLOBAL. Its one preferred finite label, if one
exists, has only one original owner; an infinite preferred direction
adds no finite label. Scalar parameterization has dimension at most3,
not2. Its auxiliary length, degree and agreement are

    x+J-9, J-8, d+J-t-8.

Thus the same-field padded LIST parameters are
(r,w,Kmax,dimension)=(x-1,d-t,J1-8,3) on a profile ending at J1.
This degree relaxation covers every original J in that profile.
No receiver or affine base pair is divided.

## Exact Mass Envelope

Put T=floor(S/3)=349525. Each original x is an integer, so at most
two occupied fibres have x>T. Partition each small/large mass range
into intervals[l,u] of width at most1000. Padding to u gives the
certified integer LIST cap L_3(x)<=L_3(u) on the entire interval.

Choose

    alpha=max_small (S-l)*L_3(u)/l,
    beta=max(0,max_large ((S-l)*L_3(u)-alpha*l)).

For a small fibre, (S-x)L_3(x)<=alpha*x. For a large fibre it is at
most alpha*x+beta, since S-x decreases with x and alpha>=0.
Summing under the ONE packing constraint yields

    Omega_t <= t+S*alpha+2*beta = C_t.

The primary generates three-step scalar LIST traces; the independent
audit verifies their legal inequalities without importing the primary
or selector. It reconstructs both envelopes and the eight factors from
the actual shared/pair dimensions.

## Original Source Composition

The inherited W(J_low) resource bounds the original truncated raw mass.
Use the original nested identity, with both cutoffs in the same P2 hull:

    bad slopes <= floor(W(J_low)/3+F_1*C_1/2+F_2*C_2/6)+134944.

All higher raw and the one near allowance remain. The certificate covers
3166 complete mass boxes and9498 three-dimensional LIST transitions on
the five profiles. It gives the five residual prices in statement.md.
Taking MAXIMUM with the earlier270000000000000000 source alternatives
gives270378604704109625, reserve4602123407285462.

This is a new rank20 source-class payment, not a rank19 terminal-price
substitution and not an exhaustive rank20 or whole-row bound.
