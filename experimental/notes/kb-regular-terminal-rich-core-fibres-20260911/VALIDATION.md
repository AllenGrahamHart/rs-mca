# Validation And Limits

Workboard K3 / DIRECT, KoalaBear MCA, agreement1116048, target2^-128.
These are exact arithmetic and small geometry controls for local theorems;
they are not a formal verification or a proof of either Prize problem.

## Frozen Inventory

- Parent commit:918324a10b1d8235e5d06418cbfdca83544d339c.
- New sources:22files,95,671bytes, exactly two completed DAG nodes.
- Required closure:112 acyclic locally PROVED nodes.
- Inherited mathematical documents:337, unchanged from the research source
  when frozen and pinned to their published packets.
- Source hashes checked:1,194 inherited plus22new, totaling1,216.
- Source manifest SHA256:
  693c0120e5d2b9e9a868dc250af8f893ec0e234ccfc5f93c7f99f6742aecb5dd.
- Finite certificate SHA256:
  5fb6934b925a6f53b6a1063e61fdc684292d8249822cc9024d45e1fee091c991.

Node-local historical provenance retains its original local/unpublished
wording; this wrapper records the publication snapshot without rewriting
those frozen mathematical sources. Research HEAD is dirty, so the manifest
uses per-file digests rather than asserting clean-commit reproducibility.

## Fresh Checks

Replay validates the new packet and the inherited proof inventory, then
runs three new checks and one inherited independent audit, serially:

1. Eight F101 geometry controls and992 sampled pair directions cover split,
   repeated, nilpotent, partially split and nonsplit spectra; common zeros;
   retained rank-one evaluations; and the earlier exceptional triple fibre.
2. The finite primary reproduces13 complete profiles,52 spectral gates and
   104 exact Johnson floors and original-weight inequalities.
3. The independent finite audit imports neither the primary nor a LIST
   selector, recomputes the original available-weight envelope and rejects
   32 scope/coverage/price corruptions. One raises H and recomputes all
   dependent arithmetic, testing affordability rather than a stale field.
4. The inherited rational-plane audit independently checks92,042 mass
   boxes,184,084 proposed LIST transitions,544 original pencil gates and
   the original eight-anchor envelope;24 corruptions are rejected.

The packet replay also rejects50 malformed manifest mutations. Its custody
helper is the preceding packet's replay.py, pinned before import by SHA256
31e822f5063e30e59d4c709f9f0298431207090582751a71d6e55574c746dd2e.
This helper reuse is not an independent implementation of custody checks.
The independent arithmetic audit remains separate from the primary.

Both full modes pass: normal2.21seconds/28,480KiB peak RSS;
optimized2.63seconds/28,768KiB. Both ran under RAMguard tiny, serially,
with no Modal, paid computation or large field/source enumeration.
An initial runtime-selection bug on older non-node paths was fixed by
checking the path length and node prefix; both complete reruns passed.

## Not Established By Replay

The universal spectral geometry, profile monotonicity, original ownership
and rank9 source interpretation rely on the written proofs. Numerical
controls do not prove them. The frozen inherited graph is not a trusted
source registry, and unrelated inherited arithmetic was not freshly replayed.

No unrestricted rich-fibre bound, terminal census, rank19 payment, whole
degree closure, active-v4 atom, LIST row or Prize resolution is claimed.
The newer unfinished eigen-root incidence draft is deliberately excluded.

Audit verdict: OPEN GAP. The local packet passes its focused checks;
independent mathematical review and the K3 aggregate incidence bound remain.
