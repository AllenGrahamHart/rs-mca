# Dependency Sub-DAG

mca_weighted_pencil_raw_mass -> pair_space_operator_spectral_fibre_johnson.

The pair-intersection, root-set and Cauchy--Schwarz arguments are written
in full here. The required supplier supplies unchanged original raw
ownership only. This node feeds the finite rich-fibre frontier; no
unproved fibre bound or reverse dependency is installed as a requirement.
