# The Rich Fibre Is Supported By Two Actual Source Pairs

Let I be H_e+1 shared-core coordinates in Omega for the excessive terminal
from the main proof. The two actual pairs differ by

    H_a*((I-alpha*T)y,T(y)),  y and T(y) independent over F.

Thus y and T(y) vanish on I and are divisible by its locator P_I.
Their span is a two-dimensional POLYNOMIAL subspace of U with a common
factor of degree>=H_e+1. This is not a function-field-rank-one pair plane.

Every x in I has nonzero evaluation on U. Since it annihilates both
independent polynomials, all these evaluations have the same projective
class. Eigenpolynomial roots have been excluded, so no nonzero
F-eigenpolynomial vanishes at any x in I. A joint-rank-one evaluation
is nevertheless possible and was not silently omitted.

Now let V be the11-dimensional ORIGINAL normalized carrier before the
eight regular anchors. Their evaluation functionals are independent:
each step lowered the shared carrier dimension by exactly one. Their
common kernel V_A has dimension3, and V_A=H_a*U.

For x off the anchors, restriction of ev_x to V_A equals H_a(x) times
evaluation on U. The restrictions for x in I are nonzero and span one
dimension. Restriction V*->V_A* has kernel the span of the eight anchor
evaluations. Hence the anchor evaluations together with those of I span
a flat of rank EXACTLY8+1=9 in V*. It contains a+H_e+1 distinct domain
coordinates at which the two original terminal pairs jointly agree.

No received pair is divided globally, and no new original assignment is
chosen. The witness lies on actual complete joint cores; generic image
fibres, unattained affine points and arbitrary formal histories do not
supply it. The source's higher raw labels and all other pairs remain.
