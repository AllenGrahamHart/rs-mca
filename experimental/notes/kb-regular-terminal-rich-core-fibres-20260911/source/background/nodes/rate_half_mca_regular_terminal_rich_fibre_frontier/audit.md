# Audit

Universal hand checks: one polynomial per ORIGINAL-field eigenvalue requires
one-dimensional eigenspaces, forced by pencil-freeness. Common zeros must
be removed when there is no eigenvalue. Non-eigen differences force one
projective evaluation class, even at a joint-rank-one coordinate.

The contrapositive uses actual two-core intersections, not an unoccupied
large fibre. Its source flat has rank9 because all eight original anchor
functionals are independent and the additional restricted evaluation is
nonzero. Two scalar polynomials sharing a factor are not a rank-one pair
plane. The excluded-coordinate deletion changes neither owners nor weights.

The independent checker imports no primary verifier or selector. It checks
104 integer floors/weight inequalities, all52 spectral gates,13 complete
profiles and the unchanged original available-weight envelope. Its32
mutations include an increased intersection gate with ALL dependent
arithmetic recomputed, to test affordability rather than mere stale hashes.
Both normal and optimized runs are required. Hand mathematics is not
certified by these arithmetic checks; external review remains due.

No eigen-root-removal, generic-fibre or source-coverage shortcut is assumed.
The original rank19 source, whole-J gap and both Prizes remain open.
