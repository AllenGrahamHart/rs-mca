# Claim Contract

Object: subfamilies of ONE original finite-label MCA assignment.
Pair/shared dimensions:2s-3 and s, regular generic projection.
Partial class: constant(s-1) subspace, with no full constant s.
Degree: D is primitive shared degree; E is primitive scalar degree of
the maximal constant subspace; zeta=E-(s-2).
R>=d>t; all scalar padding and integer-packing guards must hold.

Weights and original labels are unchanged by anchors or full gcds.
The generic recurrence consumes actual upper bounds C2,U3,C4.
The degree-price transport proves how scalar LIST certificates supply
them; the finite consumer checks those certificates.
No assumption of actual affine occupation, uniform local allowance,
normalization cap, characteristic-zero geometry or all-source closure.
