# Degree-Refined Constant-Pencil Costs

Retain the original source degree J, anchor set of size a and owner
weights. For a scalar constant-pencil direction carrier, remove its
full scalar gcd after the anchors. Let E be its primitive degree and
z the distinct removed gcd roots on the actual core-union domain.
Then v=J-a-1-E-z>=0.

For a whole constant-pencil family with core-union complement e,
the scalar list has length R-e+E+1+v and agreement at least d-t+E+1+v.
Delete ANY v coordinates. Each word loses at most v agreements;
the remaining length is at least E+1, so polynomial restriction is
injective. The same-field LIST parameters are

    (r,w,Kmax,dimension)=(R-e,d-t,E+1,dimension).

For a quotient fibre in constant codimension-one compression, put
x=|C|-J+1. The same calculation gives parameters

    (r,w,Kmax,dimension)=(x-1,d-t,E+1,dimension).

Both statements allow arbitrary actual subfamilies and receivers.
The receiver is divided pointwise only where the gcd is nonzero.
An upper bound E<=B permits the uniform-in-degree LIST theorem with
Kmax=B+1; no monotonicity of a numerical selector is assumed.

The proved original constant-pencil gate gives e>=g+1.
The original rational-compression theorem gives

    d+1-t<=x<=R-g, sum x<=R+1=S.

There is ONE preferred finite slope for the common constant direction,
charged at most t globally. All other original defects of a pair are
charged outside its own core union. Thus a whole constant3 family costs

    U3(B)=max_e [t+e*LIST3(R-e,d-t,B+1)].

For compression with scalar dimension r=2 or4, a certified upper bound
C_r(B) for

    t+sum_x (S-x)*LIST_r(x-1,d-t,B+1)

is obtained from the existing two-large-fibre envelope. With
T=349525 and3*(T+1)>S, at most two integer masses exceed T.
A partition into boxes[lo,hi] with certified counts L(hi) gives

    alpha=max_small (S-lo)*L(hi)/lo,
    beta=max(0,max_large ((S-lo)*L(hi)-alpha*lo)),
    C_r(B)=t+alpha*S+2*beta.

All intervals are covered; padding is in the original field. The finite
consumer certifies every proposed scalar trace, complement/shifted-mass
box, and nonnegative price. The full-degree quotient annihilator, one
shared mass, and global inside charge are inherited without alteration.
