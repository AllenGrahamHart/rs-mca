# Apply The Constant-Hyperplane Recurrence To The Original Source

Separate the previously paid whole-source pencil alternatives first;
their total caps are at most270000000000000000. In the residual branch,
the original constant-pencil gates supply all hereditary union-complement
bounds used by the new degree-refined prices. This is the explicit source
antecedent, not a conjecture that every source avoids those alternatives.

A constant10 subspace with no constant11 is a maximal constant hyperplane
of the original shared11 carrier. The new supplier applies at every
regular stage s=11,...,3, including root-exceptional children that become
full constant. No normalization or generic-curve hypothesis is needed.

For each original J profile the certificate covers all degree-excess
zeta in[0,J1-10] by width2000 bands. Every base C_2,U3,C_4 price uses
proved scalar LIST traces and one shared original fibre mass.
The normalized recurrence gives an original raw-t weight bound

    M_t<=A_11,t*max_band P_11,t, t=1,2.

This is a bound on ACTUAL subfamilies of the same enclosure. Raw labels,
full gcds, original degrees and affine ranks are not redefined along a
branch. Degree-excess may decrease; prefix maxima retain every such child.

The original raw-resource identity and near134944 give

    floor(W(J_low)/3 + A_11,1*max P_11,1/2
                         + A_11,2*max P_11,2/6)+134944
      <=266908096047530929

over all13 original profiles. This pays the ENTIRE original bad-slope
set in the residual class, not just its low-raw component. Combining
the smaller already-paid alternatives by maximum proves the stated
constant-class cap270000000000000000.

If maximum pencil dimension is10, either a constant10 exists or a
nonconstant10 exists. The former cannot extend to constant11 by the
maximum hypothesis. The latter is paid by the earlier nonconstant
maximal-pencil source theorem. Their maximum is272127061148955779.

Nonconstant subspace dimension is at most shared dimension minus1
by polynomial-product growth. A rank-one subspace of dimension11 must
therefore have constant direction and occupy the entire shared carrier.
This proves the exhaustive source-level remainder at the printed rank.

Every scalar projection of W19 to V11 has kernel dimension at least8,
and that kernel is a constant pencil. Thus the maximum pencil dimension
cannot be below8. If one original-field projective scalar projection has
rank9, its kernel is a maximal constant10 pencil. A constant11 pencil
in another direction would intersect it trivially and require dimension21
inside W19, impossible. Such a rank9 projection source is therefore paid
by the constant-class theorem. This includes the projective direction at
infinity and never extends the coefficient field.

No individual failed constant4 terminal recipe is relabeled as a theorem,
and no unrestricted original source rank or Prize endpoint is inferred.
