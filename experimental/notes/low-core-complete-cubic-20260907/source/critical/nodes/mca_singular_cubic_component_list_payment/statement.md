# Componentwise singular-cubic list payment

Status: PROVED at the printed source contract. This pays the last d=7
pattern in the normalized KoalaBear lower-strip cubic reduction.

Let V have actual dimension eleven, degree <J, and let the pair carrier
be `(h_*+V) x V`. Use the canonical normalized source
`(n,K,m)=(1048576+J,J,67472+J)`, empty universal carrier core, maximal-raw
full-code-bad selection, LOW raw<=500 and complete pair cores. Retain
the original field of size 2130706433^6 and every finite slope label.

Suppose `7117<=J<=8655`, and all but <=64 LOW pairs lie on the fixed
geometrically integral affine-singular cubic, smooth at its unique point
at infinity. Let its primitive direction `(A,B)` have kernel dimension
d=7 on V x V and coefficient height `1301<=h<=4327`.
Its FULL algebraic coefficient locus has dimension at most three by the
required weighted supplier. Then

    N=|Gamma|+134944 <=73164604161759423
      < B=274980728111395087,
    reserve=201816123949635664.                         (C7)

No relation or source premise is inferred from finite samples.
The count covers every coefficient component, the singular pair and
the <=64 off-curve exceptions, with one HIGH resource and original near.

## Mechanism

On each positive-dimensional irreducible coefficient component, choose
a rational affine change of normalization parameter so that its members
are polynomials z(X) of degree <=ell, their differences have gcd one,
and their leading coefficient varies. The output coefficients of z^i
are then polynomials of degree <=J-1-i*ell. Their leading cubic vector
is `gamma(X)*(B,-A)`, with

    deg gamma+h+3ell<=J-1.

Every constant output fiber lies among the roots of gamma. All other
coordinate lists have size <=2 except among the roots of a nonzero
quadratic-projection coefficient of degree <=J-1-2ell+h, where <=3
suffice. No original coordinate is removed without accounting for its
agreement contribution. A uniform Johnson envelope gives <=315 rich
nonsingular pairs per three-dimensional component.

A cubic normalization map has no projective basepoints and is
birational on each such component. Hence its original coefficient degree
is at least 3^3=27. The ordinary nonpure degree budget permits at most
3^16=43046721 three-dimensional components. Pairs outside their union
are bounded by the weighted lower-dimensional budget, <=56703328989.
Adding one possible singular pair and 64 exceptions gives at most
70263046169 LOW pairs and the total (C7).

## Consumer Consequence

The full-kernel child already pays all other patterns, including d=10.
It therefore pays EVERY canonical normalized source on 7117..8655 by
N<=274979661292365251, reserve 1066819029836, without an assumed
cubic description. Combined with the earlier quadratic strip, the entire
4801..8655 interval is paid. The unpaid normalized range starts at 8656.

This consumer consequence is not an unrestricted original-row upper
bound or an active-v4 owner certificate. Higher J, higher original ranks,
ordinary LIST, adjacent endpoint maximality and both prizes remain open.
