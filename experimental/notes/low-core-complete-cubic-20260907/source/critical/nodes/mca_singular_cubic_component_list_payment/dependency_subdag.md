# Dependency Sub-DAG

```text
mca_polynomial_map_projective_jet_dimension
mca_core_completed_basis_margin_resource
    -> mca_singular_cubic_weighted_incidence_payment
        -> mca_singular_cubic_component_list_payment [this node]
            -> rate_half_mca_cancelled_low_core_relation_payment
                -> rate_half_mca_low_core_kernel_quadratic_strip
```

The weighted supplier provides the bounded rational normal form, original
dimension bound, nonpure degree budgets, finite cover, scalar incidence
and HIGH resource/label prerequisites. The polynomial component model,
degree cost and uniform 315-pair envelope are proved here. No speculative
conditional child or reverse consumer dependency is introduced.

The earlier d=10 supplier remains separately required by the finite
consumer. Together with its existing kernel coverage and earlier prices,
the present d=7 payment closes the whole 7117..8655 normalized interval.
The root router still receives supplier-owned evidence only and stays
TARGET. Closure of a restricted normalized interval is not a replacement
for the unrestricted source/row/owner proof.
