# Dependency subgraph

The moving-projection companion uses exactly the same requirements:
the local jet proof, existing algebraic LIST/degree cover and inherited
resource. Kernel dimension is counted before the degree charge. Its
finite consumer and the full-kernel strip child inherit the new result
without a new edge, reverse dependency or conditional premise.

`mca_low_core_quadratic_graph_payment` -> this node (req) supplies only
the proved coefficient degree cover, algebraic LIST incidence and original
slope accounting. Its older tangent dimension argument is not a premise
of the new projective-jet proof. The elementary Wronskian calculation and
projective dimension theorem prove the new dimension bound directly.

This node -> `rate_half_mca_cancelled_low_core_relation_payment` (req,
owned by that finite consumer) supplies the graph-degree-two-through-nine
price. The consumer's existing normalized resource remains separately
required; the new theorem does not derive that source contract.

There is no reverse edge to the graph supplier and no speculative child.
The full-kernel strip node inherits the stronger supplier without changing
its paid interval or bound. Its residual cubic class is sharpened by
excluding singularities at infinity. Original red nodes remain open.
