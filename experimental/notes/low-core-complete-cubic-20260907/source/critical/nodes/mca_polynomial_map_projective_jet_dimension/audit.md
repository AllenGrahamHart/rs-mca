# Audit

## Moving projection audit

Checked the rank-nullity identity N+d=2s and the leading-vector
inclusion in the SAME d-dimensional projection kernel. The scalar
projection of that kernel is injective in a nonzero leading output,
so the existing output-space jets apply. Original output degree K
controls the characteristic guard; actual input degree K+h controls
LIST. Phi,Psi make input-to-pair recovery unique, while polynomial
input evaluation retains even displayed output-pole coordinates.

Primitive coefficient content makes c polynomial in the highest binary
part c*(A Y+B Z)^e. The resulting weighted height bound is not
assumed for arbitrary curve descriptions. Direct coefficients at infinity
recognize moving parabola graphs and one-place cubics singular there.
The remaining affine-singular cubics have quadratic/cubic normalization
coordinates and are explicitly excluded. Controls include an actual
input outside V, the characteristic failure, sharp dimension, and a
dimension-four cusp family whose coordinate spans fill all eleven
carrier dimensions. No canonical unsafe source is claimed by those controls.

## Original jet audit

Verdict: NO ISSUE at the printed scope; independent external review due.

The output degree/characteristic guard makes every surviving polynomial
subspace generically jet-independent. The pure-power family is projective
and closed, so d root hyperplanes cannot miss a component of dimension d.
Zeros/poles of the leading coefficient are avoided before choosing the
auxiliary points. Taking infinity only gives CONTAINMENT in the leading
equations, which is all the dimension upper bound requires.

Affine offsets, fixed rational denominators and parameters of higher
degree are handled explicitly. The proof does not infer a property of
arbitrary evaluation coordinates from generic auxiliary coordinates.
Upward rounding is sharp. The characteristic-three cubic family is a real
counterexample if the degree/characteristic guard is dropped.

Only the finite graph bound imports the earlier proper-section degree
cover and original label ledger. Numerical replay checks arithmetic and
gate controls, not the universal projective statement. The source class
still requires an actual graph, not a pointwise empirical fit.

No unrestricted parameter interval, original red node, higher-rank branch
or prize closes. The older odd-characteristic moving-conic theorem is not
weakened to the stronger p>=K assumption used by this alternative proof.

Completed replay: primary exact arithmetic PASS (0.02 seconds, 11520 KiB);
independent integer/jet audit PASS (0.02 seconds, 10624 KiB), two cap
mutations rejected. Affected dependency closure and generated manifest
equality PASS; preexisting global and crosswalk diagnostics unchanged.
