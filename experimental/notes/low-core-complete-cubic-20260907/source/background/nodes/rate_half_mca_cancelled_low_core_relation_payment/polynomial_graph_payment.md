# Polynomial-coefficient graphs of degrees two through seven

Status: PROVED, 2026-09-07. Keep the normalized KoalaBear row and
empty universal carrier core, with s=11, T=500, 4801<=J<=169999,
n=1048576+J and m=67472+J. Suppose the represented LOW pairs,
after one fixed constant invertible matrix and degree-<J polynomial
pair offset, satisfy

    b=Psi(X,a), Psi in F[X,Y], 2<=deg_Y Psi<=7.

There is no bound on the X-degrees of the coefficients in this EXACT
PAIR IDENTITY formulation. An arbitrary rational-coefficient graph or a
received-word fit without the supplier's degree certificate is not covered.

The whole original source has near-inclusive total

    degree two:       <=46570195123304300,
    degrees three--seven: <=167137715680311148.

In particular the entire printed graph class has the uniform total

    <=167137715680311148 < B=274980728111395087,
    reserve=107843012431083939.                          (1)

This is a single parameterized proof and endpoint certificate, not seven
separate source assumptions or a new per-degree target campaign.

## Uniform count

The generic supplier's `polynomial_graphs.md` proves a coefficient-
variety dimension bound r=6 for e=2 and r=5 for e>=3. Its leading-
coefficient argument works even when the top coefficients of a are fixed
throughout a component: it first removes them, then uses the highest
coefficient that actually varies. The characteristic condition holds
because 2<=e<=7<2130706433.

The scalar LIST ratio is Q=1048577/66973<16. For e=2 the degree
charge is 2^5=32, so the preceding total remains unchanged, now for
arbitrary polynomial X-coefficients. For every 3<=e<=7,

    Delta=e^(11-5)<=7^6=117649,
    M_LOW <=7^6*16^5=123363917824,
    L_LOW <=981604*M_LOW=121094515191709696.

The single original resource obeys F(J)<=C=23067643444721720934;
its convexity gives the previously verified endpoint maximum. Add all
HIGH labels via F(J)/501 and the ORIGINAL near charge 134944 once:

    N_original <=floor(C/501)+121094515191709696+134944
                =167137715680311148.

The degree-two pair cap 536870912 is smaller than the displayed
uniform cap, so (1) includes it. No scalar/JOINT Johnson or MCA
Johnson/Hensel numerical certificate enters this new count.

## A concrete received-data supplier, not an assumed fit

One sufficient certificate is a graph relation on the COMPLETE LOW union
U with weighted polynomial degree

    max_i(deg_X p_i+i*(J-1)) < m-500.

Then each b-Psi(X,a) has more roots on its actual complete core than
its degree, yielding the exact pair identity. A certified smaller first-
component degree can replace J-1. This condition is sufficient only;
existence for arbitrary sources remains unproved.

The supplier's actual weighted-quadratic source b=X*a^2 cannot be
converted to the earlier square graph by any fixed constant matrix and
polynomial offset. Its fixed-carrier census has 20358 canonical labels.
Conversely, an actual small one-defect received-word fit fails to yield
any pair identity when the weighted degree bound is omitted.

The rounded degree-eight recipe exceeds budget, at 315864453456459628.
That is a failure of this chosen upper recipe, not an unsafe witness or
an impossibility theorem about degree eight. No effort is spent optimizing
that cutoff. The next issue is a source-compatible affordable algebraic
description of the remaining arbitrary family, not a degree-by-degree list.
No original red, unrestricted endpoint or prize closes here.
