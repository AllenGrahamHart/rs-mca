# The d=10 cubic pattern is completely paid

The required PROVED supplier
`mca_geometric_progression_singular_cubic_payment` applies to the SAME
canonical normalized source on 7117..8655, when the affine-singular
cubic has projection kernel d=10 and <=64 LOW pairs lie off it.
It retains the original field, degree ceiling, complete cores, arbitrary
receiver values in projection fibers and all scalar-defect coordinates.

In coefficient dimension four, the entire geometric-progression carrier
allows the curve, not the received word, to descend to F(A/B). The
maximal-family theorem puts all nonsingular bounded pairs into a common
degree-three polynomial parameter model. Two-coordinate lists have size
<=2 outside <=5 exceptional projective fibers and <=3 there. Two interval
budgets give <=1251 pairs. One singular pair and <=64 off-curve pairs
give whole source plus original near at most 4194118281875211.

In dimension <=3, the prior weighted supplier gives pair cap 80233354159
and whole source plus near <=82951498428798039. This larger bound is
the uniform d=10 result, not the sharper dimension-four number.

Both are below the current paid alternative 274979661292365251, so
the full-kernel child's residual on 7117..8655 now has only

    d=7, dim Y=3, 1301<=h<=4327.

Curve/off-curve coverage still comes from that child. This consumer does
not assert every arbitrary source has a cubic description. Resource and
near are each counted once. The earlier two-pattern theorem remains valid
but is superseded as the current residual. General d=7, larger J, higher
error ranks, unrestricted endpoints and both prizes remain open.
