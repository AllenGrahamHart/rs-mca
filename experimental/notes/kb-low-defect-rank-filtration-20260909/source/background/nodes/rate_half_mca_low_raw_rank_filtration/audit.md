# Hand And Arithmetic Audit

The new certificate uses D=d-125,E=21499, not the older D=d-6,E=65000.
Its eight fixed tangent recurrences must therefore be checked afresh.
The spike difference is affine after subtracting the shared quadratic;
the equal-fiber difference has an exact nonnegative squared-factor identity.
Those identities, not the degree stream, prove the universal basis bound.

All 1..125 defects are retained multiplicatively on one chosen core subset.
The separate HIGH gate covers every remaining raw value. It is unsafe to
truncate the enumeration at125 or assume a lower core count covers HIGH.
No source-fiber alternative or zero/secant deletion is needed in the mass
bound. Empty universal core only ensures nonzero joint-core evaluations.

The rank test is on the actual low-raw parameter subset AFTER selecting
records. A proper graph flat lowers explanation dimension by one because
its distinct slope projection has rank one. The bound is uniform in
selection; the canonical theorem makes these complete-set defect counts.

For an over-budget line use N>=B*-near+1, not B* or B*-near. Low-raw
counts are CEILINGS of the integer ledger. Source-class payments use
floors and add near once. The after-deletion assertion uses an extension
of the survivor selection, not an assumed preservation of earlier rank.

Primary rational checks and independent polynomial-convolution/integer
checks reconstruct all eight steps and whole-interval gates. Small
multi-defect tests belong to the raw-mass supplier. No original-field
search or Modal computation is needed. External independent review remains
due; the checks do not prove a full-rank low-defect census.

The explicit next census has three COMPLETE-core classes. Their deficit
weights are2943315,981106,327035, not one identical n-m+3 factor.
The sufficient ceiling includes the last three units permitted by flooring
after division by four. Its adjacent arithmetic is a consistency check
of this sufficient reduction, NOT optimality of the true MCA numerator.
The census bound is openly unresolved and is not a new DAG premise.
