# The Unpaid Rank-Twelve Family Already Has Many Tiny-Defect Labels

Status: PROVED by the explicit quadratic certificates and source transport.
External independent mathematical review remains due.

Original KoalaBear row: q=2130706433^6,n0=2097152,K0=1048576,
m0=1116048,d=67472,B*=274980728111395087,near=134944.
Normalize any complete post-near rank-twelve selection by the required
original-source assembly. Write J=K0-g for its complete shared-core
complement and suppose9941<=J<=21499. The unchanged-field child has

    (n,K,m)=(1048576+J,J,67472+J), dim V=11,
    empty universal carrier core, |Z_bad|<=|Gamma|+134944.

For EVERY valid selection of explanations h_gamma in the SAME h_*+V and
size-m full-code-bad supports, let raw_gamma be the minimum mismatch to
the second-component carrier V. Then

    sum_gamma min(raw_gamma,4) <=624373932788019251.    (W)

For J>=14000 the stronger uniform right side522680876725222604 holds.
The proof uses eight fixed rational quadratic certificates, not a scan
over sources, field elements or degrees. There is no new fiber-density
premise and no secant/zero deletion in(W).

## Exact Paid Source Classes

Let G_t={(gamma,h_gamma):raw_gamma<=t} in the actual12-dimensional
slope/explanation parameter space. Each of the following is sufficient
to pay the ENTIRE original line, including the one near allowance:

    no raw<=2 labels:                    |Z_bad|<=208124644262808027;
    affine dim G_2<=10:                  |Z_bad|<=215295179262501741;
    affine dim G_3<=11:                  |Z_bad|<=273667628828640978.

The last reserve is1313099282754109. For J>=14000, affine dim G_1<=10
also pays the line, using the explicit(W) endpoint and the existing
dimension-nine explanation cap; see proof.md for its exact formula.
The MIXED condition dim G_1<=10 AND dim G_2<=11 also pays the whole
interval by263965087598877649, even if G_3 has full dimension.
Affine dimension here concerns the ACTUAL selected parameter graph, not
an enclosing coefficient variety. Empty or singleton subsets are included.

## Necessary Structure Of Any Over-Budget Source

If |Z_bad|>B*, every valid selection has at least

    100284125772880591 labels with raw<=2;
    158516326552340442 labels with raw<=3.

The raw<=2 parameter graph has affine dimension at least11, so its
explanations span at least10 dimensions. The raw<=3 graph has FULL
affine dimension12 and its explanations span all11 carrier dimensions.

On J>=14000 there are at least27280579497297684 raw-one labels, whose
parameter graph has affine dimension at least11. These are uniform
necessary conditions, not assumptions that an over-budget source exists.

More precisely, L_1+L_2>=200568251545761181. Thus either the raw-one
graph has dimension at least11, or the raw<=2 graph has FULL dimension12.
If the former graph has dimension<=10, there are at least189812449046220611
raw<=2 labels. If the latter graph is proper, there are at least
43802724037092885 raw-one labels. These statements use one nested mass
identity; the low-raw sets are not treated as disjoint.

Under the required maximal-raw selection, each such low label has the
UNIQUE minimizing polynomial on its COMPLETE scalar agreement set;
raw counts all its complete-set defects. The restriction is therefore
to actual one/two/three-defect complete-core families, not a favorable
truncation of larger-defect explanations.

There are more than10^11 DISTINCT represented polynomial pairs with
complete joint-core size at least m-2. Indeed one fixed pair can own at
most n-m+2=981106 of the raw<=2 labels. Equal such complete cores
determine equal pairs by the polynomial root bound. This is a necessary
large interleaved pair family, not a proved upper census of it.

## A Concrete Sufficient Census

Partition the represented raw<=3 minimizing pairs by COMPLETE core size:
M1 counts |H|>=m-1, M2 counts |H|=m-2, M3 counts |H|=m-3.
The common source is paid whenever

    2943315*M1 +981106*M2 +327035*M3
      <=475548979657021324.                          (CENSUS)

This implication is proved; the displayed census bound for arbitrary
sources is NOT proved or assumed. It names a concrete next sufficient
bound without adding a speculative DAG requirement. Every over-budget
source necessarily violates(CENSUS).

Removing e labels reduces each lower population bound by at most e,
even under valid reselection of the survivors in the same carrier.
In particular all stated rank lower bounds survive the existing
secant/zero set of at most1149710068 labels.

This narrows the low-defect family that must be counted. It supplies no
upper bound for its full-rank mass, no new whole degree interval, no
original rank>=13 coverage, ordinary LIST theorem or full Prize closure.
