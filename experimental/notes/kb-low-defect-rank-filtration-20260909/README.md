# KoalaBear MCA: Low-Defect Mass And Actual Parameter-Graph Ranks

K3 / DIRECT companion to #1175, extending the
[parameter-graph contribution](../kb-parameter-graph-spread-20260908/README.md).
Two locally proved theorem nodes, submitted for independent mathematical
review. The results pay five whole source classes and constrain every
remaining over-budget rank-twelve source. They do not close a new degree
interval or the unrestricted row.

## Row And Quantifiers

Original field `q=2130706433^6`, `(n0,k0,m0)=(2097152,1048576,1116048)`,
target `2^-128`, finite challenge denominator `q`, budget
`B*=274980728111395087`. The unit is distinct finite bad slopes of one
received line, with full-code-bad supports, not polynomial-pair counts.

Normalize a complete post-near rank-twelve selection by its complete shared
core. Write `J=k0-g`. Throughout the remaining `9941<=J<=21499`, the child
has `(n,K,m)=(1048576+J,J,67472+J)` and an actual dimension-eleven
polynomial carrier `h_*+V`. Its universal carrier core is empty. The field,
original labels and nonuniversal carrier zeros are retained. Original near
costs at most `134944`, charged once.

For any valid selection in this SAME carrier, `raw_gamma` is the minimum
number of mismatches between `v` and a polynomial `b in V` on the selected
size-`m` support. Put `G_t={(gamma,h_gamma):raw_gamma<=t}` and `L_t=|G_t|`.
Graph dimension below is actual affine dimension, not dimension of an
enclosing coefficient variety.

## New Theorems

**One resource retaining every defect.** The generic theorem inserts each
selected defect into each joint-core ordered basis. The defect and its
position are recoverable, so this counts distinct tuples. With its printed
high-defect gate, all labels share the bound `sum min(raw,kappa)<=W`.
The exact nested identity is

```text
(t+1)|Gamma| - sum_(j=1)^t L_j = sum min(raw,t+1) <= W.
```

The finite consumer proves, using eight fixed rational quadratic
certificates and an all-high-defect gate,

```text
sum min(raw,4) <= 624373932788019251             (9941<=J<=21499),
sum min(raw,4) <= 522680876725222604             (14000<=J<=21499).
```

These bounds require no new fiber-density premise or exceptional-label
deletion. The basis certificates and derivative inequalities cover the
whole intervals; no finite-field enumeration supplies a proof hypothesis.

**Exact source-class payments.** Each condition below suffices to pay the
ENTIRE original line, with the one near allowance already included:

| Condition | Original distinct-slope cap |
|---|---:|
| No raw<=2 labels | 208124644262808027 |
| dim G_2<=10 | 215295179262501741 |
| dim G_3<=11 | 273667628828640978 |
| dim G_1<=10 and J>=14000 | 266718339612516531 |
| dim G_1<=10 and dim G_2<=11 | 263965087598877649 |

The largest cap is below `B*` by `1313099282754109`. These are alternatives,
not charges to add together. The credited same-support graph gauge lowers
explanation dimension before applying the existing padded scalar caps.

**Necessary structure of an over-budget source.** Every valid selection
would have at least `100284125772880591` raw<=2 labels and
`158516326552340442` raw<=3 labels. Their graph dimensions are at least
eleven and exactly twelve. Moreover, either the raw-one graph has dimension
at least eleven or the raw<=2 graph has full dimension twelve. For
`J>=14000`, at least `27280579497297684` raw-one labels already force
graph dimension at least eleven. The strict rank margins survive the
existing at-most-`1149710068` secant/zero exceptions, including valid
reselection of survivors in the same carrier.

Under the credited maximal-raw selection, these are actual complete-set
one/two/three-defect explanations. Any over-budget source represents more
than `10^11` distinct polynomial pairs with complete joint cores of size
at least `m-2`: a fixed pair owns at most `981106` of the raw<=2 labels.

## The Remaining Census Is Not Proved

The new generic owner ledger charges the deficit `4-raw` to actual
minimizing pairs. Let `M1,M2,M3` count represented raw<=3 pairs whose
COMPLETE joint cores have sizes `>=m-1`, `m-2`, and `m-3`, respectively.
The following inequality would suffice to pay the original source:

```text
2943315*M1 + 981106*M2 + 327035*M3 <= 475548979657021324.
```

Only this implication is proved. The displayed bound for arbitrary sources
remains OPEN and is not added as a proved DAG premise. Different pair owners
may reuse coordinates; the same-owner argument does not supply their census.
The adjacent integer check tests the sufficient ledger, not optimality of
the true MCA numerator.

## Review And Integration

1. [Generic statement](source/background/nodes/mca_raw_margin_basis_mass/statement.md)
   and [tuple/owner proof](source/background/nodes/mca_raw_margin_basis_mass/proof.md).
2. [Finite statement](source/background/nodes/rate_half_mca_low_raw_rank_filtration/statement.md)
   and [eight-certificate, rank and projection proof](source/background/nodes/rate_half_mca_low_raw_rank_filtration/proof.md).
3. [Dependency and custody manifest](SOURCE_MANIFEST.json) and
   [validation record](VALIDATION.md). The 75-node reachable proof graph
   reuses the two unchanged published supplier packets. The old interval
   assembly remains 72 nodes; no evidence edge becomes a required proof.

The natural K3 use is a source-owned low-defect pair census. This is not a
Grande Finale v4 `U_paid`, `U_Q` or `U_BC` atom: no first-match owner bridge
is claimed. The residual degree interval still has 11,559 integers.
Original ranks>=13, unrestricted MCA, ordinary LIST and both Prizes remain
open. The tentative cubic-interval experiment is excluded entirely.

## Bounded Replay

From the repository root, with Python 3.10+:

```bash
python3 -B experimental/notes/kb-low-defect-rank-filtration-20260909/replay.py
python3 -B -O experimental/notes/kb-low-defect-rank-filtration-20260909/replay.py
```

The runner resolves inputs independently of the working directory. It
checks custody and dependency structure, then runs three focused controls
serially with 20-second child timeouts. It does not replay the inherited
arithmetic suites. Hashes and controls do not certify the universal hand
proofs. No large computation or Modal funding is requested.
