# Pay The Double-Conic Regular Terminal Class

For the original KoalaBear MCA row q=2130706433^6, agreement1116048 and
B*=274980728111395087, retain the original rank12 residual on
J9965..21499, shared dimension11 and any valid original assignment.
Use a regular eight-anchor rank19 terminal, remove its full polynomial
gcd as in the required primitive-carrier theorem, and keep original raw
cutoffs t=1,2, actual pairs, labels, weights and degree slack.

After removing the previously paid original large-pencil alternatives,
suppose the terminal is pencil-free, with evaluation-image degree eta=2
AND kernel-direction-image degree chi=2. Then its original assigned
raw-t weight is at most the inherited L_t(J), for EVERY primitive degree
and every degree slack v>=0. This pays the ENTIRE double-conic class
across all11535 original degrees, not just a low-degree subrange.

Actual nonbase collision groups have at most6 or7 pairs, according to
the profile. Every actual rank-one evaluation plane has at most15 pairs;
rank-one evaluation fibres contribute at most30nu core incidences.
Exact subset-forest prices exclude the first unaffordable M-subset in
all13 profiles and both original raw cutoffs. Here D=2nu=kappa-1.

Consequently any excessive pencil-free terminal still has eta2..5 by
the required branch-energy frontier, but its eta2 alternative must have
chi=3 or4. Eta3..5 and the whole constant3 tail J14965..21499 remain
unpaid. Image and kernel degrees are different invariants, neither equal
to shift-pair h or to the normalization multiplicity nu.

The available-weight maximum272127061148955779 and reserve2853666962439308
are unchanged, NOT unrestricted rank19 bounds. No whole source degree,
rank19, higher original rank, active upstream atom, LIST row or Prize closes.
