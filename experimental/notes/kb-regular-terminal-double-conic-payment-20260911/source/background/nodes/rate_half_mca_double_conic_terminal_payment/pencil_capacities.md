# Use Original Pencil Gates At The Collision Line's Actual Height

Put R=1048576,d=67472. Original source length is n=R+J and each actual
P_t pair has complete core at least A=d+J-t. The parent's pencil_gates.md
proves that, once its whole-source alternatives are removed, EVERY actual
pencil subfamily of primitive height h has union complement at least g_h+1.
The nonconstant bins cover1..floor((J1-9)/2) for each original profile.

In the conic normal form, D=2nu and nu lies in that interval. At a nonbase
evaluation point, every agreeing group lies on an actual affine line.
If its direction is an F-eigenvector, the original constant-pencil theorem
bounds its pair count by q1=floor((R-g_0)/(d+1-t)). Otherwise the geometric
supplier proves its primitive ORIGINAL rational height is h=nu.

For this nonconstant pencil the scalar direction gcd has degree at most
J-1-h. Off its b common zeros in the actual union, different pairs cannot
jointly agree. If the group has m pairs and union complement e, then
m*(A-b)<=n-e-b. The ratio increases with b because n-e>=A. Hence

    m <= (n-e-b)/(A-b)
      <= (R+h-g_h)/(d+h+1-t).

The denominator is positive. This is a count of actual pairs, not a weight
or a census of a formal hull. Original anchors and the full gcd were
retained when identifying the height and source union.

For a fixed gate, the fractional-linear expression in h is monotone;
both endpoints therefore bound its entire bin. The exact certificate
checks every bin and both cutoffs against the pinned parent shards.
It yields qcap=6 on the first six profiles and qcap=7 on the remaining
seven, with q1<=7 throughout. The terminal's original weights have not
been reset by deleting anchors or gcd roots.
