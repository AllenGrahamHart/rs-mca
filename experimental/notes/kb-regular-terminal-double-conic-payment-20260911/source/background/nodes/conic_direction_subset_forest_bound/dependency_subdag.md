# Required Sub-DAG

conic_direction_subset_forest_bound [PROVED], requires[].

This is a self-contained quadratic-space/finite-graph/averaging argument.
It assumes no unresolved extremal graph theorem. The downstream original
MCA consumer must separately prove actual null-line geometry and capacities.
