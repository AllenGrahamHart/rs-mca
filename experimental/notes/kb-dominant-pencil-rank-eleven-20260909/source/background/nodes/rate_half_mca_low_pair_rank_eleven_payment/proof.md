# Use The Proved Pencil Restrictions Inside Every Affine Section

Put R=1048576,d=67472,E=21499,near=134944 and
T=274138707278280353. The original source is supplied by the required
finite chains; no choice below remeasures its original rank or complete core.
Assume affdim P_2<=11.

If P_2 is a rational pencil, the required complete pencil payment gives
the whole-source cap274136923022229951<T. If any large-pencil-section
gate applies, the required dominant-pencil payment gives cap T. Both
include all higher raw values and near once. It remains to consider
the complement of these proved source classes.

Then the affine hull Z_*=aff(P_2) has function-field direction rank two.
Every pencil's complete P_2 core union U obeys

    |U|<=n-250001 for nonconstant direction,
    |U|<=n-500001 for constant direction.             (UNION)

These statements follow from the already proved whole-source alternatives;
they are not new assumptions in the final theorem.

## 1. Derive A Hereditary Cap On Every Pencil Subspace

For t=1,2 let S_t be the distinct pairs in P_2 whose COMPLETE joint
core has size at least A_t=m-t. Consider ANY affine F-subspace Z of
degree-<J pairs, with dimension v and function-field direction rank<=1.
If |S_t intersect Z|<=1, the desired bound below is immediate.

Otherwise its rational pencil ell has primitive height h<J. Let U be
the complete-core union of ALL P_2 pairs on ell, not merely S_t intersect Z.
The required pencil normalization gives an injective polynomial parameter

    (a,b)=(a_*,b_*)+(A1,-A0)*H,  deg H<J-h.

Compatibility on U supplies one scalar receiver w. Every counted pair
has at least A_t agreements H=w on U. Its actual affine parameter hull
has dimension v'<=v. The scalar affine LIST theorem therefore gives

    |S_t intersect Z|
      <= ((|U|-J+h+1)/(d+h+1-t))^v'.                 (SUBSPACE)

This is applied only to nonempty lists, so |U|>=A_t>=J-h and the
ratio is at least one. Universal agreement coordinates are handled by
that scalar theorem; they are not assumed proper hyperplanes.

For constant direction h=0, (UNION) bounds the ratio by

    C_t=(R-500000)/(d+1-t).

For h>=1, (UNION) bounds it by (R-250000+h)/(d+1-t+h).
This ratio decreases with h because R-250000>d+1-t. Its maximum
at h=1 is

    P_t=(R-249999)/(d+2-t).

Exact cross multiplication shows 1<=C_t<=P_t. Consequently (SUBSPACE)
implies |S_t intersect Z|<=P_t^v for EVERY affine pencil subspace Z.
This is the hereditary hypothesis required by the new incidence supplier,
not a bound only on the total family of each full pencil.

## 2. Count The Actual Complete Pairs In The Rank-Two Hull

The determinant guard holds on the entire interval:

    A_t-2J+2=d-J+2-t>=45973>0.

Put H_t(J)=(R-J+2)/(d-J+2-t). This increases with J, since its
derivative has numerator R-d+t>0. Exact endpoint checks give

    H_t(J)<=H_t(E)<=P_t^2.

Apply the hereditary pencil-incidence theorem to S_t inside Z_*.
The family S_t need not span Z_*. Its affine dimension r<=11 and
function-field rank two give

    M_t=|S_t|<=H_t(J)*P_t^(r-2)<=H_t(E)*P_t^9.        (COUNT)

The two separate floors are101805808190 and101821603501. The final
price below retains the unfloored rational values. Children of a joint
anchor may be pencils: section1 pays them, so no repeated-rank-drop
assumption was made.

## 3. One All-Raw Resource And The Exact Pair-To-Slope Ledger

The required pencil supplier's basis_mass.md proves, BEFORE any tuple
refund and for every original record,

    sum min(raw,3)<=R0,
    floor R0=W3=613022127444579907.

The sum is an integer, so flooring this UNREFUNDED resource is valid.
This does not multiply a floored ratio by a fractional refund. The basis
proof's core-sixteen/HIGH187 argument is independent of a pencil hypothesis.

Let L_t=sum_(raw<=t) raw. The same-pair complete-core ownership proof
gives L_t<=(R-d+t)*M_t. It counts all selected defects for one pair as
disjoint original coordinates; different pairs may reuse coordinates.
Exact telescoping and (COUNT) imply

    |Gamma|+near
      <= floor(W3/3 + sum_(t=1)^2
             (R-d+t)/(t*(t+1))*H_t(E)*P_t^9) + near
       =270931433891625928<T.                        (BRANCH)

All raw>=3 labels have zero remaining deficit after the resource term.
No maximum raw value or separate HIGH budget is assumed.

## 4. Original Source And Remaining Rank

The preceding whole-pencil and large-pencil alternatives, and (BRANCH),
are exhaustive under affdim P_2<=11. Their maximum is T, proving
(RANK11) with original near included once. They are not summed as atoms.

For an over-budget original source, every valid selection and minimizing
assignment must therefore have pair affine dimension>=12. All previous
function-field-rank and pencil-section restrictions remain valid. This
does not assert that such larger families are impossible or paid, nor
that every original error rank is at most twelve.
