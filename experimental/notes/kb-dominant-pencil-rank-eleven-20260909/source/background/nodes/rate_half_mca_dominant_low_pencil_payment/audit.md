# Audit

The main risk is applying an earlier all-pairs-on-pencil theorem without
its premise. proof.md instead reuses its GROUPWISE gain inequalities on S,
keeps the all-record resource, and explicitly adds the off-pencil low deficit.
The tuple refund excludes internal U tuples for all retained records, not
only S owners. The finite kernel label and original near are charged once.

Off-pencil pairs are counted by the new universal root/Johnson proof,
using their actual height h. Same-pair disjoint defects justify the981106
label multiplier. Positive Johnson denominator is used only when A<=e;
A>e has zero exceptions. Its failure is not evidence of an unsafe row.

The positive integer denominator yields a deliberately coarse but sufficient
all-height cap. Three shared-carrier LIST certificates additionally pay
constant complements through500000 and arbitrary-direction complements
through250000, even outside the full-code Johnson range. All use dimension11
of the shared V, not an assumed bound on pair affine dimension. Padding
the auxiliary complement and relaxing degree/agreement only enlarge its list.
Original field availability and positive corridor gaps are checked.

Both finite implementations verify all33 new LIST steps and190/101 branch
boxes. Clipped source intervals do not shorten the older box endpoints or
floor R0 prematurely. The independent
implementation uses one common integer denominator and imports no primary
or compiler. The inherited basis/LIST audit remains required and is replayed
separately. Nine corrupted outside certificates and three invalid Johnson
gates are rejected. Hand proofs and independent external review remain necessary.

Audit verdict: NO ISSUE
