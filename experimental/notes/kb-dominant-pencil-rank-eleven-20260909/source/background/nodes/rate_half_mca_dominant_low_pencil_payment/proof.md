# Pay A Pencil And Prove Its Exceptions Are Affordable

Write R=1048576,d=67472,L=9965,E=21499,N0=981106,near=134944.
The required original-source and pair-span chain supplies the fixed source
in statement.md and sum min(raw,4)<=W=624373932788019251. Reselection
and minimizing-pair assignment do not alter its original rank/core gauge.

## 1. Count All Off-Pencil Pairs Before Projecting To Labels

Let S=P_2 intersect ell. Each hypothesis forces S nonempty: otherwise
e=n>500000 and e*(J-1)>67471^2 throughout the interval. Take the primitive
row A0*a+A1*b=Q. A represented pair gives deg Q<J+h. On the union U
of COMPLETE S cores, A0*u+A1*v=Q at every point, including gcd roots.

Every off-pencil pair in P_2 has at least m-2 joint agreements. The
required escape theorem gives at least

    A=d-h-1=67471-h

on D\U. It is positive because h<J<=E. If A>e there are no off-pairs.
Whenever (JC) holds, it gives the exact pair cap

    M_off<=floor(e*(A-J+1)/(A^2-e*(J-1))).             (OFF)

For one represented pair f, every assigned low label uses at least one
defect outside its COMPLETE core H_f. At such a point its scalar incidence
equation has at most one finite slope solution. Its selected defect sets
are therefore disjoint over its assigned labels. Since |H_f|>=m-2,
at most n-|H_f|<=R-d+2=N0 labels are assigned to f.

Hence the off-pencil low-label deficit sum is at most N0*M_off. This
is a conservative pair-to-slope factor, not an uncharged identification.
No high-record count or second full-source resource is added.

## 2. The Existing Pencil Ledgers Are Groupwise

Termwise telescoping on the SAME original record set gives

    |Gamma| = (sum min(raw,3))/3
                  +sum_(raw<=2)(1-raw/3).

Split only the last sum into S owners and off-pencil owners. For S the
credited rational-pencil ownership proof works independently of other
pairs: complete-core compatibility, scalar parameter dimension, preferred
labels and same-pair defect disjointness are groupwise facts.

For nonconstant direction h>=1 the common-carrier parameter dimension
is at most ten. The pair-span supplier's proof section3 bounds the S gain
using its actual union U and all preferred labels. Its base W/3 still
funds every record. Therefore its whole-source formula, with the extra
off-pencil deficit retained, is

    |Gamma|+near <=228260637755610995+N0*M_off.         (NC)

This imports the proved groupwise inequalities, not a theorem applied to
a falsely claimed all-pairs-on-pencil source.

For constant direction h=0, finite chart membership Q/A0 in h_*+V or
infinite chart membership Q/A1 in V follows from a pair in S. The
compatible-domain tuple refund applies to ALL original records after at
most one finite kernel label is charged, including off-pencil records.
Their original raw values and minimizing pairs stay fixed. The required
pencil proof thus gives its SAME refunded box envelope E(a,b), now plus
N0*M_off. Evaluation on V is nonzero on U by the empty universal core.

The all-raw core-sixteen/HIGH187 resource, unrounded R0, dimension-ten
scalar LIST caps and J-monotonicities are exactly the required supplier's.
No claim that off-pencil records own internal U tuples is needed: the
generic refund excludes them just as it excludes other retained labels.

## 3. A Crude Integer Bound Pays Every Nonconstant Height

Because A<=67471 and J-1>=9964, the strict integer gate (JC) gives

    e<=floor((67471^2-1)/9964)=456878.

For e>=A, the positive integer denominator in (OFF) is at least one.
Consequently

    M_off<=e*(A-J+1)<=456878*57507=26273683146.

This also covers the zero case e<A. Substitution into (NC) gives

    |Gamma|+near<=254037905932250471,
    reserve=20942822179144616.                        (NC-PRICE)

There is no enumeration of heights or possible fields here.

## 4. Constant Direction: Small Complement

Here A=67471. Put

    e0=floor((A^2-1)/(E-1))=211756.

For A<=e<=e0, the Johnson fraction increases with e and with J.
Indeed for s=J-1 its derivatives have nonnegative numerators
A^2*(A-s) and e*A*(e-A), respectively. At (e0,E), its denominator
is5353 and floor is1818617. If e<A the off count is zero.

The existing refunded envelope has maximum274136923022229951 on the
entire domain. Adding the integer off-label bound proves

    |Gamma|+near<=274136923022229951+N0*1818617
                =274138707278280353.                (SMALL)

## 5. Shared-Carrier LIST Pays Beyond The Direct Johnson Gate

All off-pencil components still lie in h_*+V and V with dim V=11.
In the AUXILIARY complement count, enlarge the polynomial degree bound
to E=21499 and add arbitrary distinct coordinates up to a fixed e_bar.
All old agreements and polynomial pairs survive. If their guaranteed
agreement is at least A_bar, use the shared-carrier compiler at

    (r,w,K_max,s)=(e_bar-E,A_bar-E,E,11).

Every w is positive, r>=w, and the original field supplies more than
e_bar coordinates. The degree-<J pairs are contained in this larger
degree-<E list, with the same carrier dimension. The source row and its
labels are not changed. The pair family's affine dimension is NOT used.

Three fixed certificates, each eleven steps, give

    e_bar   A_bar    joint pair cap
    400000  67471    3146811647
    500000  67471    54886611863
    250000  45973    14781874200.

The last agreement floor holds for every h<J because d-h-1>=d-E.
Thus every nonconstant pencil with e<=250000 satisfies (NC) with the
last cap, giving total242763223224476195. Together with section3 this
pays its (ANY) and (JC) alternatives, including nonpositive Johnson cases.

For constant directions, section4 already covers e<=211756. On the
remaining intervals use the first two caps with the prior exact E(a,b):

    e interval          added off-label cap          maximum total
    211757..400000      N0*3146811647                254493279590417819
    400001..500000      N0*54886611863               262886297505008376.

All these e are below R-d=981104, so |U|>=m and every previous
anchor/box gate holds. The old boxes are used unchanged on their
intersections with the two stated intervals. There are190 and101 box
evaluations; row400 intersects both disjoint source intervals. The maxima
occur in rows211 and400, respectively. Retain the entire prior refund
endpoint b even for a clipped boundary box.

The982-row supplier certificate is reused, not regenerated or duplicated.
Only a small three-row JSON artifact is added for the33 new LIST steps.
The primary reproduces the chosen steps; the independent verifier checks
their legality without needing compiler optimality or importing a primary/compiler.
The exact R0 numerator/denominator and integer ceilings are retained.

## 6. Exhaustion Of The Stated Source Class

For constant directions, (JC) implies e<=456878 and is contained in
(CONST). The small and two larger complement intervals therefore cover
all constant cases. Sections3 and5 cover nonconstant (JC) and (ANY),
including e<A with zero off-pairs. Taking the maximum of the five printed
whole-source prices, not their sum, proves (PAY). All raw values and one original
near charge are included.

Contraposition gives (NO-LARGE-PENCIL) because e is an integer. This
restricts pencil sections of genuine rank-two pair families; it does not
assume those families are covered by a small number of pencils. General
remaining J sources and original ranks>=13 are not paid by this theorem.
