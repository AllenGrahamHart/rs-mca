# Result

A proved bound on every affine pencil subspace propagates to all affine
pair spaces when one rank-two anchor costs at most P^2. Its first rank-two
step saves two pair dimensions without assuming that later steps do so.
The finite application derives all pencil bounds from source-owned core
union restrictions, so no conjectural leaf is introduced.
