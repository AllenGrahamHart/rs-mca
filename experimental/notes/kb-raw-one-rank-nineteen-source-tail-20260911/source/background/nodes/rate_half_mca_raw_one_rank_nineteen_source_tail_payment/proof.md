# Exhaust The Raw-One Geometry Before Composing The Original Resource

Use the original source and labels of the statement. Let V be its shared
dimension11 direction space. Fix an actual base pair f0 whenever P1 is
nonempty, and W=span_F(P1-f0) subset V squared. The raw-one count N1
equals the sum of its original raw weights. Empty P1 has N1=0.

First pay the original constant and all-height P43 pencil-union
alternatives by270000000000000000. These full-source gates are independent
of the P2 rank. On the residual source every hereditary P1 pencil union
has complement at least its applicable gate plus one. No low-rank source
payment or P2 regularity theorem is invoked at this step.

Let S be the sum of the two component images of W and r=dim W<=19.
The following four bounds exhaust the residual source.

## 1. Proper Component Carrier

If dim S<=10, enlarge only S to a dimension10 subspace of the ORIGINAL V.
Keep the affine offsets f0. The auxiliary weighted low-family theorem
applies to this raw-one subset with T=2; it neither needs an auxiliary
minimizer nor assumes that the rest of P2 occupies the same carrier.

As in section1 of rate_half_mca_low_raw_hyperplane_payment, deleting its
compatible universal core z gives k=J-z in[10,21499] and

    N1 <= floor F(k),
    F(k)=(R+k)_11/[11*(d+k-2)*product_(j=1)^9(d-2+j)].

Its consecutive ratio is at most one because
R+k-10-11*(d+k-2)>=91406>0 throughout this interval.
Thus

    N1<=floor F(10)=78301130139301820.

Only this auxiliary raw-mass assertion is used, not that supplier's
whole-source P2/carrier conclusion. The original source is not remeasured.

## 2. Small Generic Projection

Suppose S=V and the F(z)-rank of pi_z:W -> V, (a,b)->a+z*b, is at most9.
Every larger minor is the zero polynomial, so every constant ORIGINAL-F
specialization also has image dimension<=9.

Apply mca_constant_projection_singleton_fibre_census directly to P1,t=1.
The previous P1 certificate is a uniform raw-weight calculation throughout
J9965..21499 BEFORE its later J11925 whole-source composition.
Its actual cores have size at least d+J-1; two distinct pairs in one
constant-projection fibre give complement e<=R-2d+1=913633.
Original source gates give e>=569051 on a rich fibre.

The 35 complete bins use projected scalar dimension9, on-fibre scalar
dimension11 and the same ORIGINAL field. They give

    N1 <= 1+(R-d+1)*30106298254+2340755344065418
        =31878195092556089.

The singleton term and global preferred finite-label charge are retained.
No P2 image hypothesis is used or inferred.

## 3. A Generically Full Nineteen-Dimensional Enclosure

If pi_z(W) is already all V tensor F(z), extend W, if needed, to any
dimension19 ORIGINAL-F subspace of V squared. Generic fullness persists.

If instead its generic image has dimension10 and r<=18, choose a constant
a in V outside that image over F(z). Such an a exists: otherwise the
constant basis of V would lie in the proper image and span all V tensor
F(z), a contradiction. Then (a,0) is not in W, and adjoining it makes
the generic projection onto V. Its pair dimension is r+1<=19.
Extend further over F to dimension19 if necessary.

These are enclosure extensions only. They add no represented pairs,
labels, weights or supposed occupied points. Keep the actual affine base f0.
The [weighted transport proof](transport.md) reconstructs the t=1
all-coordinate induction for ANY actual subfamily of this enclosure.
It proves, uniformly throughout the original J gap,

    N1 <= G, floor G=180336660306614524.

The exact rational G and all43 degree-band root prices are frozen in
certificate.json. Since N1 is an integer, taking floor G here is valid.

## 4. The Remaining Quadratic Normal

Only S=V,r=19 and generic image dimension10 remain.
The pair-space projection dichotomy has codimension c=22-19=3.
Its shortest normal ell(z) has independent coefficients and degree d0
with1<=d0<=c-2=1. Hence ell(z)=ell0+z*ell1, and

    ell(z)((a-a0)+z*(b-b0))=0

holds as a POLYNOMIAL identity for every pair in P1. All these pair
lines are contained in ONE moving-normal degree2 parameter hypersurface.
No P2 pair line is asserted to lie there.

Use only the contained-line part of the quadratic-normal mass proof.
The moving-normal tuple theorem accepts this subfamily with T=7,e=2:
raw remains1 and every original joint core has enough points to select
m-7 of them. Its rank10 kernel-basis lower bound is uniform and does
not require containment of the full raw-seven family.

The previously proved uniform basis floor M5=222676884802638507 gives
retained raw mass at most floor(2*(M5+1)/5)=89070753921055403.
At most J-9<=21490 original finite labels have moving-zero evaluation;
each retained raw-one label weighs exactly1. Thus

    N1<=89070753921055403+21490=89070753921076893.

There is no noncontained-line term because every P1 line satisfies the
identity. There is no claim about P2 or G7 containment.

## 5. One Exhaustive Maximum And The Original Source

The four integer raw-one bounds are

    proper carrier:       78301130139301820,
    small projection:     31878195092556089,
    quadratic normal:     89070753921076893,
    generic enclosure:   180336660306614524.

Their MAXIMUM proves RAW. These are alternative source cases, not four
summable low-label budgets.

For every original positive raw value tau,

    1 = min(tau,2)/2 + 1_(tau=1)/2.

The original min-envelope theorem bounds sum min(tau,2) by
sum min(tau,9)<=W(J). Its proved full polynomial-tree quotient decreases
throughout the interval. Hence all higher-raw records and near once obey

    bad slopes<=floor((W(J)+N1)/2)+134944.

The exact unshifted-resource audit gives

    W(20617)=369631303721713659,
    W(20618)=369620187118249156.

At J20618 the resulting cap is274978423712566784; its reserve is
2304398828303. Monotonicity extends this to every J through21499.
Taking MAXIMUM with the earlier270000000000000000 source alternatives
does not change it. The J20617 recipe is274983982014299035>B*, which
certifies only the starting degree of this particular uniform recipe.

Every over-budget original assignment on J20618..21499 must therefore
have ACTUAL P1 affine pair rank at least20. This is a necessary restriction
on actual raw-one pairs, not an assertion that a formal hull is occupied.
