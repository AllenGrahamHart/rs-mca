# Transport The Weighted Enclosure Induction, Not The P2 Source Conclusion

This is the load-bearing scope transfer in case3 of proof.md.
The old all-coordinate certificate is used as a proposed arithmetic
ledger. Its old theorem, which assumes an original P2 rank bound, is NOT
invoked with that hypothesis changed. The induction below is reconstructed
for the original raw-one subfamily and the enlarged formal enclosure.

## Inputs That Survive The Transfer

Let f0+W contain ACTUAL P1, dim W19, inside the original V squared,
dim V11, with pi_z(W)=V tensor F(z). Only W need have these dimensions.
All actual pairs retain their ORIGINAL finite-label owners and raw weights.

At each actual joint-agreement coordinate, take the agreement section
of this formal affine enclosure; the actual child is its occupied subset.
No occupancy or actual-span hypothesis is needed by the all-coordinate
supplier or by its incidence inequality. "Actual regular anchors" here
means coordinates of the original domain where this enclosing direction
space has rank-two regular section, not an assumption that the actual
child has the full enclosing affine span.

Initially and after full shared gcd division, use primitive degree D0 and

    N=R+D0+1+v, A=d+D0+1-t+v, v>=0,
    kappa=D0-(s-1), t=1.

The initial kappa is at most J-11. Unused degree and deleted gcd-root
coordinates enter v exactly as in the all-coordinate proof. Every actual
pair has at least A remaining joint agreements. No original tau is reset
and no original full-source resource is applied to the auxiliary child.

## Two Tables And All Child Types

U[s,c] bounds arbitrary direction enclosures of pair dimension2s-c;
G[s,c] bounds full-generic enclosures along paths that remain in G.
The initial state is G[11,3]. All states have0<=c<=min(s,3).

For U, actual joint ranks0/1/2 lead to codimensions c-2,c-1,c.
The bad-coordinate sets have the nested bounds kappa+floor(c/2) and
kappa+c. The supplier's U inequality prices all three types.

For G, no rank-zero coordinate occurs. A rank-one child remains generic
and lowers c by one. A rank-two child preserves c but can lose generic
fullness at a kernel-root coordinate. Such a child is priced by U.
The two exceptional types share ONE set of size at most kappa+c; use
the maximum increment there. For c0/1 the forbidden types are omitted
only as justified by the algebraic supplier, not by a numerical shortcut.

Every branch lowers s and cannot increase kappa. In each of43 width500
bands covering0..21488, previously accumulated lower-degree prefix maxima
therefore bound every child. For any fixed child prices the incidence
ratio is affine over affine in kappa at v=0, so its endpoints suffice.
For v>=0 its supremum is bounded by that value and the child-price limit.
This proves each stage and band by induction, preserving original weights.

## Terminal Bounds Do Not Need P2 Occupancy

For s=c<=3, the small-terminal weighted theorem uses actual represented
pairs and ORIGINAL unions/weights inside the enclosing space.

- A zero-dimensional terminal has at most one pair, with original raw
  weight at most R-d+1.
- A whole function-field-rank-one terminal is constant by full-carrier
  rigidity. Its scalar LIST uses its own original union complement,
  and the one preferred finite label is charged once.
- A rank-two terminal uses the minimum of the determinant-anchor and
  same-field SHARED-carrier joint-LIST bounds. These count actual pairs;
  the original per-pair raw multiplier is retained.
- Nonconstant line inside defects have one global coordinate charge.
  Their integer populations and height-valid original source gates
  are included in the109-row line ledger.

All these statements apply to subfamilies of enclosures; none asserts
that every formal point is represented by an original explanation.

## Why The G33 Extra Allowance Is Still Available

The inherited extra L_1 is used ONLY in G[3,3] at D0<2*(H+1),
H=floor((21499-1)/10)=2149.
A path from G[11,3] to G[3,3] has eight steps, all preserving c3.
Any rank-one step would irreversibly lower c; any nonregular rank-two
step would enter U. Thus exactly the required eight regular enclosing
sections occur at actual common joint-agreement coordinates.

For a rank-two terminal containing a rank-one plane, reconstruct the
rational-plane proof: its actual scalar fibres, original core unions,
primitive height, shared mass budget and one global inside charge are
unchanged. The scalar dimension is2. The terminal envelope depends on
upper original degree21499 and the original pencil gates, not on P2 rank.

Otherwise it is pencil-free. The primitive carrier's nondegenerate plane
image has degree eta>=2 and D0=nu*eta. The small-degree guard gives
nu<=floor(D0/2)<=H. For eta2..5 the inherited normalization proof's
strict spectral energy inequalities cover every nu<=H and every v>=0.
For eta>=6 the preceding branch-energy/collision-interpolation proof
covers the entire degree corridor. Both arguments choose actual pairs
only after their original weight exceeds L_1. Their pair and eigen-root
counts depend on this terminal geometry, the original source gates and
upper degree, not on the affine rank of the full original P2 family.

In particular these arguments do not require nu to remain unchanged from
the initial source, or actual occupation of every intermediate enclosure.
Characteristic2130706433 exceeds all relevant carrier/image degrees.
Whole constant3 retains its separate scalar bound. U[3,3] never receives
this history-dependent L_1.

## Original Source Gates Remain Hereditary

Both the old all-height gates and the degree-adapted gates are proved
whole-source statements using the complete ORIGINAL P43 unions and the
original truncation44 resource. They are rank-independent.
Any retained P1 pencil union is contained in the corresponding original
P43 union, with the same primitive direction height. Consequently these
gates apply after their whole-source alternatives have been paid.

The final-profile degree-adapted gates use only the upper degree J1=21499
in their OFF/ON scalar-LIST relaxations. Their source proofs work for
every original J in9965..21499. The older all-height gates already have
that scope. No additional original low-pair hypothesis is introduced.

## Why The Old Profile's Lower Endpoint Is Not A Hypothesis

The frozen20965.jsonl file was originally grouped under J20965..21499.
Its WEIGHTED induction is valid throughout J9965..21499:

- The initial degree excess is bounded by J-11<=21488.
- Every LIST, determinant, line-height and primitive-degree ceiling uses
  J1=21499 or the current kappa band, never the profile lower endpoint.
- The rational-plane and normalization prices use J1, original gates,
  actual degree/slack and the eight-section history just checked.
- J0=20965 and its mass_floor occur only in the old final mixed-raw
  WHOLE-SOURCE composition, which is discarded here.

The independent checker rebuilds all43 t=1 bands and verifies the
same prices after changing only a COPY of the parent's lower J to9965
and20618. This is a dependency check supporting the explicit reasoning
above, not a proof by sampling original sources. Frozen supplier files
are never edited or falsely relabelled.

The maximum rational root price G is recorded exactly in certificate.json,
and floor G=180336660306614524. There are11051 legal LIST transitions,
3268 U/G recurrence identities and109 line-height rows. All original
anchor factors are already included in G; no extra factor belongs in
the final original-source identity.
