# Required Dependency Subdag

    mca_constant_projection_singleton_fibre_census -> this source payment
    rate_half_mca_min_envelope_raw_mass -> this source payment
    rate_half_mca_regular_rational_plane_terminal_bounds -> this source payment

All suppliers are PROVED. The first is applied DIRECTLY to t=1/P1.
The second supplies the complete original min(raw,9) resource and its
whole-interval monotonicity. The third supplies only rank-independent
whole-source pencil gates; no rank19 terminal L_t is used.

The earlier P2 projection source theorem is not invoked with weakened
hypotheses. Matching numerical t=1 bounds are independently compiled here.
Outgoing evidence targets rate_half_mca_global_core_rank_support_distance_router,
which remains TARGET with requires[].
