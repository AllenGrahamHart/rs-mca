# Charge All Higher Raw Values By Two, With No P2 Hypothesis

Write R=1048576,d=67472,E=21499. Keep the ORIGINAL assignment and
raw values on the standard complete rank12 normalized source.
P1 consists exactly of represented pairs whose assigned raw-one records
are retained. Its total raw weight is N1, the number of those labels.

## Independent Raw-One Census

Fix a nonzero original-field constant row rho with image dimension<=9
on P1. Every actual pair has joint core size at least A=d+J-1.
The rank-independent original height-zero source gates pay the entire
source by270000000000000000 if any full P43 constant-pencil union has
complement at most its profile gate.

On the remaining branch, every rich P1 rho-fibre has original complement
at least G+1=569051. Its union is contained in that full original P43
pencil union. This uses no rank or image premise on P2.

Apply mca_constant_projection_singleton_fibre_census directly to P1, t=1.
Two actual pairs force e<=R-2d+1=913633. On-fibre scalar parameters lie
in the original dimension11 shared carrier, whereas projected polynomials
lie in the asserted dimension-at-most9 affine space.

Use the same-field padded LIST parameters

    base: (R,d-1,E,9),
    on a complement bin [l,u]: (R-l,d-1,E,11),
    projected tail: (R,R-u,E,9).

Thirty-five complete bins of width at most10000 and709 legal dimension
transitions certify base Q=30106298254 and rich outside weight
2340755344065418. Singleton pair weight is at most R-d+1.
Inside defects across all fibres have one original preferred finite label,
charged once by1. Hence

    N1 <= 1+(R-d+1)*30106298254+2340755344065418
       = 31878195092556089 = C1.

This reconstructs a t=1 application of the GENERIC census. It does not
substitute P1 for P2 in the earlier source theorem: the new proof never
uses that theorem's source conclusion or its raw-two term.
The P2 projection and pair rank are entirely unrestricted.

## Original Higher-Raw Records

For EVERY positive integer raw value r,

    1 = min(r,2)/2 + 1_(r=1)/2.

The original minimum-envelope source theorem gives

    sum min(raw,2) <= sum min(raw,9) <= W(J),
    W(J)=floor((R+J)_12/beta_9(J)).

This funds ALL original higher-raw records, including the far tail.
No record is discarded and no auxiliary support resets its raw value.
The exact minimum over256 basis branches is used in beta_9, not an
optimistically chosen branch. The supplier proves W(J) nonincreasing
throughout the whole real source interval, so no scan of all J is needed.

The primary uses the shifted polynomial recurrence; the independent audit
reconstructs the unshifted-degree recurrence and its whole-interval gates.
At the two adjacent certificate endpoints,

    W(11924)=518101072106770916,
    W(11925)=518074353492715892.

Thus for every J>=11925 in the original gap,

    bad slopes <= floor((W(J)+N1)/2)+134944
               <= floor((W(11925)+C1)/2)+134944
                = 274976274292770934.

This residual cap exceeds270000000000000000, so MAXIMUM with the
earlier whole-source alternatives leaves it unchanged.
The reserve below B* is4453818624153. Empty P1 only decreases the bound.

At J11924 this same uniform recipe is274989633599798446>B*.
Monotonicity certifies the recipe's integer starting degree, not actual
source unsafety below it and not a maximal-safe Prize endpoint.
A sharper or differently composed estimate may pay earlier degrees.

## Remaining Source Restriction

If an original source on J11925..21499 exceeds budget, no constant
projection of P1 can have affine dimension<=9. Every such image lies
in the same dimension11 scalar carrier, so its dimension is10 or11.

More quantitatively, every excessive assignment must have

    N1 >= 2*(B*+1-134944)-W(J).

This is an original-label lower bound, not a formal pair-hull population.
Other original source classes and the row itself remain unresolved.
