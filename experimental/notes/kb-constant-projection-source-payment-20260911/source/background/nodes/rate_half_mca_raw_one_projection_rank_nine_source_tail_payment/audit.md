# Audit

Primary: shifted256-branch basis recurrence, same-field scalar LIST selector,
35 complement bins and709 LIST transitions, thirteen pinned source gates.
Independent: pinned OLD import-free unshifted-degree resource auditor and
OLD scalar-LIST legality checker. It imports neither this primary nor the
selector, reconstructs both exact resource ratios, and reprices all gates.

49 semantic mutations are rejected, including six fully repriced ledgers:
omitted singleton weight, omitted projected multiplicity, omitted global
inside charge, division by3, scalar dimension9 instead of11, and using
the maximum basis branch instead of the minimum.
The inside-charge deletion changes the raw-one bound but the final integer
floor absorbs one at these endpoints. It is rejected as an invalid raw
census, not presented as a demonstrated false final integer bound.
1000 exact positive-raw identity controls include raw2, which rules out
charging every higher-raw record by one third without a raw-two correction.

The full original resource audit was freshly replayed:765 branch gates,
512 intermediate-raw transfers, all endpoint floors and HIGH-tail coverage.
The generic census's original-label controls and analytic proof remain
separate suppliers. Small controls are not official source enumerations.
External independent mathematical review remains due.
