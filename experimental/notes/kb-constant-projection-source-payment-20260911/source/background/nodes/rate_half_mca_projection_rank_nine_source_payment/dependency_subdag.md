# Required Dependency Subdag

    mca_constant_projection_singleton_fibre_census -> this source payment
    rate_half_mca_min_envelope_raw_mass -> this source payment
    rate_half_mca_regular_rational_plane_terminal_bounds -> this source payment

The generic census requires the original weighted-owner and ordinary
same-field LIST suppliers. All requirements are PROVED.
The source gate dependency is used only for its rank-independent
whole-source alternatives; no rank19 L_t is imported.

Outgoing evidence: rate_half_mca_global_core_rank_support_distance_router.
The router remains TARGET with requires[]; evidence is not a required proof.
No speculative conditional leaf is introduced.
