# Direct Original-Source Projection Census

Write R=1048576,d=67472 and retain ORIGINAL finite labels and raw weights.
For cutoff t=1,2 each complete joint core has size at least A_t=d+J-t.
One actual pair therefore has total original raw weight at most R-d+t.
The fixed row rho maps each P_t into an affine scalar space of dimension<=9.

## First Pay Original Source Alternatives

The height-zero pencil gates of
rate_half_mca_regular_rational_plane_terminal_bounds pay the ENTIRE original
source by270000000000000000 if a full original P43 constant-pencil union
has complement at most its profile gate. These are rank-independent source
theorems, not the old rank19 terminal allowances.

All13 profile gates on J9965..21499 are at least G=569050.
On the remaining source branch every occupied rich rho-fibre of P_t has
original complement e>=G+1: its complete union is contained in the full
P43 fibre of the same original row and projected polynomial.
The source alternative is paid, not assumed impossible.

Inside one fibre, pair differences are delta*h, delta=(lambda1,-lambda0).
A nonzero constant component identifies h with a scalar multiple of a
component difference in V. Hence its affine scalar dimension is at most11
and its degree is<J. The generic census applies with projected dimension9
and on-fibre scalar dimension11, without requiring actual occupation of
the enclosing subspaces.

## Rich Fibres Have A Stronger Agreement Threshold

Two distinct pairs force |C_q|>=2*A_t-(J-1). Therefore

    e <= (R+J)-2*(d+J-t)+J-1 = R-2*d-1+2*t = E_t,
    E_1=913633, E_2=913635.

Most possible projection fibres can be charged as single pairs.
Only fibres with at least two actual pairs need the scalar parameter count;
they must satisfy this sharper projected agreement condition.

Use Jmax=21499 for the same-field padded dimension descent. For a complement
bin [l,u] in [G+1,E_t], the three legal ordinary LIST instances are

    all projected polynomials: (r,w,Kmax,s)=(R,d-t,Jmax,9);
    pairs inside a rich fibre: (R-l,d-t,Jmax,11);
    projected tail at e<=u:    (R,R-u,Jmax,9).

These bound length r+J, degree J and agreement w+J uniformly for every
actual1<=J<=Jmax. The scalar on-fibre count pads its OWN complete union
to R+J-l. The projected tail is counted on the unchanged original domain
and agrees on at least R+J-u points.
Every corridor has1<=w<=r and q>=r+Jmax. No original field is extended.

Partition each complement range into width-at-most10000 bins. There are
35 bins at each cutoff. The generic singleton/rich-fibre census gives

    Omega_t <= t+(R-d+t)*Q_t+sum_bins u*P_t(l)*Q_t(u).

The leading Q_t may count rich projections too. The tail caps can overlap
between bins. Both are harmless overcounts, not conserved resource budgets.
All inside defects use one global preferred finite label and cost at most t.
There is no anchor factor, quotient-one packing assumption or rank19 L_t.

The exact certified values are

    Q_1=30106298254, rich_1=2340755344065418,
    Omega_1<=31878195092556089;
    Q_2=30108989792, rich_2=2341210898039010,
    Omega_2<=31881321436908964.

## Original Slope Count

The proved original resource bounds sum min(raw,9) by
M=578501226347492453, so it also bounds sum min(raw,3).
For every positive integer raw, the original pointwise nested identity
gives1=min(raw,3)/3+raw*1_(raw<=1)/2+raw*1_(raw<=2)/6.
Consequently all original post-near slopes, including every higher raw,
together with the single near allowance satisfy

    bad slopes <= floor(M/3+Omega_1/2+Omega_2/6)+134944
               <=214086393235061966.

If P2 is empty the two raw terms vanish. Combining the residual bound
with the earlier whole-source alternatives by MAXIMUM yields
270000000000000000, reserve4980728111395087.
The70 exact bins have1418 legal dimension transitions; the independent
audit checks them and all13 source gates without importing the new primary.

## Full-Constant Rank20 Equivalence

Let W be the ACTUAL pair-direction span of P2 inside V squared, dimW=20.
A full constant-direction11 subspace is delta*V. Choose a nonzero
original-field row rho annihilating delta. Its kernel on W has dimension11,
so its image has dimension9, and the theorem pays the source.

Conversely for any constant row, the kernel inside V squared is precisely
delta*V of dimension11. An image of dimension at most9 on W would force
a kernel of dimension at least11; it is therefore exactly11, and W
contains delta*V. No image dimension below9 is possible at actual rank20.
Without that full constant subspace, every image has dimension10 or11,
with corresponding fibre-direction dimensions10 or9.

No conclusion about nonconstant pencils or exhaustive higher-rank coverage
is inferred. The original router and both Prize roots remain unresolved.
