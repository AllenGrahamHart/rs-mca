# Audit

The independent audit imports only the pinned PREVIOUS scalar-LIST legality
checker, not this primary or its selector. It checks all70 bins and1418
dimension transitions, the scalar dimensions11/9 separately, the lower
endpoint for on-fibre padding and upper endpoint for projection tails.
All13 original height-zero source gates are independently repriced.

The original min(raw,9) resource supplier was freshly replayed: three full
256-branch polynomial trees,765 shape checks and512 coefficient transfers.
That proves a uniform original resource, not a sampled J estimate.

62 semantic mutations are rejected, including THREE fully repriced cheaper
ledgers: omitted projected multiplicity, scalar dimension9 in place of11,
and omitted singleton weight. Outer hashes are bypassed in these tests.
Rejected recipes are invalid implementations of this certified proof,
not counterexamples to every possible stronger bound.

Primary and independent checks use tiny exact arithmetic, not field-wide
censuses. The generic supplier has six literal original-label fixtures,
including a sharp two-core threshold and a multidimensional projection.
The analytic original-owner/union proof remains a separate hand-review
obligation. Independent external mathematical review remains due.
