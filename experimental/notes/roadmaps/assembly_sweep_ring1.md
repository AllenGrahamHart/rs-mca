# Assembly sweep, ring 1 (the radial referee pass)

- **Protocol:** move radially outward from the grands; per red node,
  either PROMOTE (local implication established from the kids as
  stated -> CONDITIONAL, entry in the assembly-implications manifest,
  statement-pinned) or mark TRUE RED (local content genuinely open) or
  DEFER (a kid's packet must be read first). Generic verifier:
  `verify_assembly_implications.py`.

## Ring-1 verdicts

- **mca_safe -> PROMOTED (modus ponens).** The kid
  `safe_assembly_uniformity` IS the assembly implication, as an
  explicit node; granting all kids, the parent follows tautologically.
- **mca_unsafe -> PROMOTED (per-row aggregation).** The kid
  `unsafe_at_crossing` states the parent per admissible row; the
  family statement is definitional aggregation over rows; cap_theorem
  positions, zone_b feeds the collided branch.
- **adjacency_closing -> TRUE RED.** Its own statement prices the
  corridor at 1.12-2.17 grid steps even granting every mapped
  conjecture: the local content is the final quantitative corridor
  arithmetic. This is genuine open work, correctly red.
- **list_adjacency_closing -> TRUE RED.** The list mirror
  ("previously unpriced") — same quantitative character.
- **list_safe -> DEFERRED.** The chain (imgfib -> codegree conversion
  -> m-handling) is named, codegree's Theorems A/B/C are proved, but
  whether the conversion ARITHMETIC (landing at 2^-128 |F| above the
  window) is inside the codegree packet or missing as a node must be
  read before promotion. Next pass.
