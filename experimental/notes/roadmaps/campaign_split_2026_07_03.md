# Three-rate campaign — parallel work split (2026-07-03)

- **Status:** AUDIT / coordination. Two agent fleets, five tasks each,
  mutually exclusive; plus one external assignment. RAM rule: heavy
  compute (C5) runs SOLO; everything else is light.
- QA.19 is MOOT (multiplier route refuted; direct mod-p MITM needs no
  uniform-multiplier theorem — per-row enumeration is deterministic).

## FLEET A (roadmap lane subagents — in-branch packaging & writing)

```text
A-M1 [QX.13] Pair-ledger packet: repo-standard proof note + verifier
     for c(s,t) = min(s,t-1) (both codim cases, slope accounting,
     LD_sw bridge row, honest moment-level scope).
A-M2 [QX.11] Globalness packet: the top-core double count + the
     brute-force verifier; the strip-normal-form section states the
     L_tan = 1 vs 2 convention question explicitly against T0-T7.
A-M3 [QX.12] KLLM import SKELETON: the note structure with
     [CITATION NEEDED] slots for every constant, the uniform-slice
     variant requirements checklist, and the exact statements the
     composition consumes. NO invented constants.
A-M4 [gap1 terminal step] The remaining GAP-1 counting estimate:
     per-character counts summed against poly(n) x FM, using the #212
     tower product bound as given. Proof attempt; fallback = a precise
     obstruction report.
A-M5 [Q3R.5 + QX.15 draft] The per-rate dossier skeleton (Reading B
     semantics, three-rate headline) + the XR assembly master
     statement draft incorporating the wave-1 verdict.
```

## FLEET C (Codex subagents — compute, corpus, PR pipeline)

```text
C-1 [Q3R.2] Leak adjudication: classify all 10 post-strip link-leak
    candidates from the #209 corpus (strippable under the L_tan
    convention vs genuine unpaid tangent leak). Gates globalness at
    every rate.
C-2 [Q3R.3] Clean-rate corridor pipeline: concrete rows at rates 1/4,
    1/8, 1/16 (Row-C-class + pinned-class): locate crossings, verify
    per-row integrality margins, emit per-row certificate skeletons.
C-3 [Q3R.4 + QL.5/E22] The pullback-strip packet: package
    pma_pullback_lists, restate the primitive residual, charge the
    pullback stratum to the profile budget; merge the E15 challenger
    class (test: is the E15 challenger an instance of the pullback
    family?).
C-4 [QA.16-18 revised] The certifier pipeline post-impossibility:
    direct mod-p MITM bands (corrected costs: 2^40-2^50 buys w=12-16)
    + the exact B&B totality anchor, end-to-end at toy scale with one
    printed certificate.
C-5 [E24] The BKZ collision hunt at N' = 128 (RUN SOLO — heavy).
    Kernel lattice built exactly; reduced-basis profile + any sparse
    ternary vector reported. Either outcome is decisive-grade.
```

## EXTERNAL (GPT Pro, via project owner)

```text
X-1 [Q3R.1] The slack composition: beat 2^100 x FM at the clean-rate
    operating points using: the globalness theorem (poly link bound),
    the KLLM extraction from #211 (q^{-(g+t-1)/4} shape), the E_3
    bridges, and the pair ledger. All inputs pinned; the wave-1 margin
    tables attached as given data. Deliverable: the composition
    inequality chain with explicit losses, or the named gap.
```

Verification protocol unchanged: every packet replayed independently
before banking; negative results publish identically.
