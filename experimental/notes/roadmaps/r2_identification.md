# The R2 identification (statement-level referee packet)

- **Claim:** at every admissible row, the aperiodic-stratum obligation
  consumed by `mca_safe` and `aperiodic_zero_at_crossing` is EXACTLY
  the compiled form `r2_clean_rates` (R_post(u,v;A) <= 16 n^3), by the
  PROVED compiler/budget chain (xr_target_budget_audit + the poly-
  forcing compiler): the strip taxonomy identifies the aperiodic
  stratum with the post-strip primitive residue, and the budget audit
  performed the per-row conversion at all six candidates. The general
  `r2_rigidity` (n^B FM, all rows) is supporting generality, not a
  requirement — demoted to ev at both consumers.
- **Quantifier check:** mca_safe quantifies over admissible rows = the
  candidates the audit ran; aperiodic_zero_at_crossing asks for "R2
  (any poly)" — 16n^3 qualifies verbatim. Rate 1/2 enters via the
  coverage-gap wiring and the banked rate-portability note (the
  zero-residue closure is rate-agnostic).
- **Consequence:** the terminal-node campaign (x24/x81/x83, A3, the
  closure assembly, C1a/C1b, mid/large-h, B2b) is now ON the critical
  path of the safe side through r2_clean_rates <-
  xr_clean_residual_any_gate <- the smallcore/spread chain. The map
  shows what has been true since the dichotomy landed.
- **Pins:** r2_clean_rates sha256/16 = 09d963096b27ee33; r2_rigidity = c0b65ca10d409780.
