# Flip packet: imgfib

- **Node:** `imgfib`
- **Current critical label:** UNPROVED
- **Verdict:** PROMOTE-CONDITIONAL
- **Referee summary:** `imgfib` is a statement-level assembly over its hard
  children. If the petal-growth escape, Conjecture F plane-section statement,
  payment-completeness taxonomy, and dyadic profile arithmetic are granted,
  the L1 image-fiber bound follows by the L1 frontier packet.

## Statement

> For ALL received words U: #ImgFib_U(k+sigma) <= n^B once sigma log2(q_D) >= (1+eps) log2 C(n, k+sigma) and the quotient profile is budgeted.

## Req Children

| child | live status | critical label | role |
|---|---:|---:|---|
| `petal_growth` | TARGET | UNPROVED | Closes the populated full-petal growing-excess escape. |
| `conj_f` | CONJECTURE | UNPROVED | Supplies the divisor-plane rigidity dictionary for petal extras. |
| `l1_program_frontier` | PROVED | PROVED | States the L1 proof program is closed except the full-petal growing-excess frontier. |
| `dyadic_profile_evaluation` | PROVABLE | PROVABLE | Computes the 2-power quotient profile at the four rates. |
| `payment_completeness` | CONJECTURE | UNPROVED | Ensures the paid taxonomy covers every structured escape found by the classification route. |

## Referee Argument

The local implication is the L1 frontier packaged as a node. The cited list-side
note states that the target is exactly the `ImgFib_U` bound above the reserve,
with quotient profile budgeted, and that the proof program has one populated
escape: full-petal sunflowers with cofactor excess `d-ell -> infinity`.

Granting the children:

1. `l1_program_frontier` reduces the image-fiber theorem to the full-petal
   growing-excess branch.
2. `petal_growth` closes that branch uniformly.
3. `conj_f` identifies petal extras as paid or divisor/quotient-structured
   plane sections.
4. `payment_completeness` prevents an unpriced structured escape from being
   silently left outside the ledger.
5. `dyadic_profile_evaluation` supplies the quotient-profile arithmetic in the
   node's hypothesis.

No additional local theorem remains in `imgfib` once those children are
granted. The node should therefore become CONDITIONAL on its children, not
stay red.

## Caution

This is not a proof of the hard children. In particular `conj_f`,
`payment_completeness`, and `petal_growth` remain the mathematical content.
The promotion is only the parent assembly step.

## Evidence Pins

- `imgfib`: `0701cb2b6f534e11`
- `petal_growth`: `8f168d85016f2af5`
- `conj_f`: `fd5e16562c7aa220`
- `l1_program_frontier`: `72362a3fe8608709`
- `dyadic_profile_evaluation`: `05224c92c9c1190f`
- `payment_completeness`: `6bd02bac86a7bd31`

## Source Quotes

- `proof_sketch/s7_list_side.md` Section 4: the L1 input is
  `#ImgFib_U(a) <= n^B` above the reserve, with the one populated escape being
  full-petal sunflowers.
- `wp_consolidation_sketch_extracts.md` WP-5.1: the concrete open sub-battle
  is the full-petal sunflower growth; Conjecture F supplies the dictionary.
- `l1_full_list_quotient_proof_program.md` summary: all listed L1 components
  are proved except the aperiodic extension counting / growing-excess full
  petal frontier.
