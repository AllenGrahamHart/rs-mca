# Required dependency sub-DAG

    mca_low_core_homogeneous_level_payment --req--> this node
    this node --req--> rate_half_mca_cancelled_low_core_relation_payment

The existing supplier provides only its general divisor facts,
algebraic degree/joint LIST machinery and original group-gain argument.
Its affine-product identity is NOT a premise of this theorem. The new
unit dimension proof is local, with classical normalization inputs
identified in `audit.md`.

The finite consumer combines prices and feeds its existing full-kernel
strip child. No reverse dependency, speculative conditional or new
original red is introduced. This node lies on critical req ancestry.
