# Claim contract

The strongest finite claim additionally uses the completed-basis resource,
which gives HIGH weight >=5500. Its paid alternative is
274979661292365251; the remaining patterns are d=7,dim Y=3,h>=1301
and d=10,dim Y=4, with the printed height ceilings. The old three-case
raw-resource bound below is a valid baseline, not the final unresolved set.

The object counted first is actual bounded polynomial PAIRS on a fixed
geometrically integral cubic over the original function field. The curve
has one geometric point at infinity, smooth there, and an affine singular
point. The coefficient locus is the entire algebraic solution set in the
same affine VxV carrier, not the finite set of sampled F-points.

Characteristic zero or p>3 and p>=K is required. The primitive projection
A*a+B*b has coefficient height h and actual scalar degree <K+h. Its
image dimension is 2s-d, not s. The counting theorem assumes Q_h>=3
unless using the d=0 zero-dimensional conclusion.

The weighted proof retains every component of the actual equation locus.
Finite projection to x must hold on its subvarieties. Auxiliary power-cover
shifts avoid the finite counted set, allowing exact division by 2^(2s-d)*3^d.
These shifts and constant extensions are not changes to original labels.

The finite consequence additionally uses the established canonical
normalized source, original label ownership, one global resource and one
near allowance. A <=64 off-cubic exceptional set is a separate hypothesis
supplied by the full-kernel consumer. This supplier does not assert that
arbitrary sources admit its curve description.

No pole or singular pair is deleted. Rational parameter degree is bounded
only to prove dimension; it is not inserted into a false scalar LIST bound.
The three unpaid dimension patterns and the larger normalized interval
remain open. The new paid-alternative bound is 261013572486287276,
not the earlier smaller alternative 255637082913634099.
