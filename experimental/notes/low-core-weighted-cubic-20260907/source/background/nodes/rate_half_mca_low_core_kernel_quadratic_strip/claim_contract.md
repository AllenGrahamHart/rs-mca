# Contract

The weighted refinement with completed-HIGH weight >=5500 uses paid
alternative 274979661292365251 and retains d=7,dim Y=3,1301<=h<=4327
or d=10,dim Y=4,1<=h<=865. Y is the full geometric equation locus
in the fixed carrier, not the finite selected-point set. The singular
pair remains inside the cubic count; at most 64 pairs lie off it.

The cubic continuation is a dichotomy on 7117..8655, not a safe-row
assertion. A source exceeding the budget must have all but <=64 LOW
pairs on a geometrically integral singular cubic with ONE normalization
point above infinity and an AFFINE singular point. Nodal and cuspidal
cases are both retained. Its explicit (2,3) normal form excludes at most
one additional singular pair; tau and all coefficient vectors remain
rational in X. Neither a polynomial parameter nor unchanged transformed
degree/carrier is assumed.
The asymmetric-domain test uses original JOINT pair agreements. Constant-line
intercepts have degree <J because the line contains an actual pair.
No line with an arbitrary high-degree rational intercept is substituted.
The degree-three kernel and this classification stop at 8655. The
asymmetric cutoff uses different agreement levels on and off the line;
neither is substituted for the other. The earlier one-place refinement
is in the 1e838002 export; the new weighted/HIGH refinement is not.

Use the exact source and row in statement.md, not arbitrary polynomial
pairs or an arbitrary choice of supports. F is unchanged; all slopes are
the original finite slopes. The canonical selector, complete pair cores,
one common carrier, and empty universal core are load-bearing.

The new mathematics is the full interpolation kernel's common-factor
degree bound, the finite off-factor exception count, and the Johnson
argument for nonparallel constant lines. Inherited exact group prices are
from the required finite conic/interpolation supplier. Their full source
quantifiers and original-resource partition are retained.

Status is a proved restricted-source upper bound, NOT an unconditional
rank-twelve or prize-row bound and NOT an active-v4 bankable atom.
Independent external review and upstream source/owner transport remain
separate obligations. Arithmetic tests do not certify the hand proof.
