# Validation And Review Limits

The two theorem nodes are marked PROVED in the local research DAG at
their printed scopes. This is not an independent external review or a
machine-checked proof of the entire mathematical dependency closure.

## Frozen Provenance

- Parent companion commit: 1108726d01f96585a2e229fe1f6d2ab9ad77a82c.
- New packet: 22 source files, 90959 bytes, 97 required PROVED nodes.
- Inherited proof documents: 283, byte-compared against their latest
  pinned published snapshots. Later supplier packets supersede earlier
  snapshots only in the explicit order printed in the manifest.
- The replay checks all 984 listed inherited source hashes and 22 new
  source hashes. It checks acyclicity, full dependency reachability, node
  status, theorem scope, document identity and canonical source paths.
- Source manifest SHA256:
  `233dc3d60a09b03ecb9b1101600703b30e2942106e424fff9acd1762b05a11ef`.
- Exact pencil certificate SHA256:
  `1e699d294491f6c998c0764e111528daeb4e339a6e853323ad2ed05fd422d6ad`.

The research worktree is dirty. Per-file digests, not a clean-HEAD claim,
identify the exported mathematics. Earlier published sources are unchanged.
No unfinished envelope theorem or speculative rank consumer is included.

## Replay

From this repository root, with standard-library Python only:

```bash
python3 -B experimental/notes/kb-high44-pair-rank-extension-20260910/replay.py
python3 -B -O experimental/notes/kb-high44-pair-rank-extension-20260910/replay.py
```

The runner reconstructs a temporary source tree from hash-checked frozen
bytes. It runs four new checks and the inherited filtration and completed
HIGH checks serially, with a 20-second timeout per child. It does not use
the local prize worktree, network access, Modal, third-party packages or
field/source enumeration. Run under a memory guard on constrained hosts.

Both commands passed serially under RAMguard tiny (256 MiB / 60 seconds).
Measured complete replay: 0.86 seconds / 23244 KiB peak RSS normally,
and 1.36 seconds / 24268 KiB with optimization enabled. No Modal spend.

The pencil certificate has 23 constant boxes and 11 height bands, with
55 LIST traces and 582 transitions. The independent auditor imports
neither the primary checker nor the LIST compiler. It reconstructs the
eight original basis-polynomial identities, verifies each chosen LIST
transition and all interval gates, and rejects 10 corrupted arithmetic
or scope certificates. The rank consumer has a separate integer-ratio
implementation that checks both terminal types and final floors.

The replay additionally rejects 25 malformed manifests, including changed
scopes, missing/duplicate files, wrong source pins and dependency drift.
These are structural and arithmetic tests, not a substitute for the
universal original-owner, contraction, incidence and rigidity proofs.

## What Is Not Claimed

Only the four new and two named inherited arithmetic checks are freshly
replayed. Other inherited arithmetic, the global software/crosswalk and
the full upstream compiler are not freshly audited by this packet.
The hand review must still check the original tuple refund, ON/OFF owner
distinction and hereditary anchor application before external promotion.

The result narrows two actual low-pair source classes. It changes no
active-v4 atom, entire J interval, unrestricted endpoint or Prize verdict.
No large computation or contributor compute request is required.
