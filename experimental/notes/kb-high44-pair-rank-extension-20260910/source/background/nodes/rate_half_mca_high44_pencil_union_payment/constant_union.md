# Constant Direction: Refund Every Original Record

Let h=0,e<=567500. The complete P43 core union is nonempty and has
|U|>=m because e<R-d=981104. Any represented pair gives either
u+gamma0*v=c on U, c in h_*+V, or v=c on U, c in V. Charge the
possible finite kernel label once. The compatible-domain theorem excludes
EVERY tuple entirely inside U from the retained original resource, giving

    sum_retained min(raw,44)
       <=Q(J)*(1-(n-e)_falling_12/(n)_falling_12).             (REFUND)

This applies also to higher raw and off-pencil records. It is not a new
bound for a punctured source. The factor Q(J) must remain unrounded.

## Scalar On-Pencil Count

Coverage on U and the empty ORIGINAL universal core imply that evaluation
on V is nonzero at every x in U. Indeed a common V-zero in a joint core
would have u=h_*,v=0, contradicting that original hypothesis.

In a constant primitive direction the scalar parameter belongs to an affine
translate of a subspace of V. Each represented H has at least m-43 joint
agreements on U. Anchoring at one such agreement gives dimension<=10;
subtract one solution and divide by X-x to get degree<J-1 on |U|-1
points, with at least m-44 agreements. Thus

    M_on <= |U|/(m-43)*V10(e),
    V10(e)=LIST(r=R-e,w=d-43,K_max=E-1,dimension10).          (ON)

For an e-box[a,b], use V10(a), justified by auxiliary padding. The field
contains more than enough distinct padding coordinates. Agreement and
degree differences are exact, and 1<=w<=r throughout these boxes.

## Shared-Carrier Off-Pencil Count

Every off-pencil P43 pair has at least Aoff=d-42=67430 joint agreements
on the e-point complement: the nonzero row remainder has degree<J.
If b<Aoff there are none. Otherwise auxiliary padding to b and degree
relaxation to E give a shared-dimension-eleven joint LIST instance

    M_off(b)=LIST(r=b-E,w=67430-E,K_max=E,dimension11).       (OFF)

This counts joint pairs directly, not a product of scalar lists. The
original labels are paid separately using(OFF) of proof.md.

## Exact Whole-Source Envelope

Let nL=R+L,mL=d+L,Q0=Q(L). For e in[a,b], the entire source is at most

    E(a,b)=1+134944+Q0/44*(1-(nL-b)_12/(nL)_12)
           +(43/44)*b*(nL-a)*V10(a)/(mL-43)
           +(43/44)*981147*M_off(b).                         (BOX)

For fixed e, the refund factor decreases with J: each factor
(n-e-i)/(n-i) increases with n. Q(J) also decreases. The prefactor
(R+J-e)/(d+J-43) decreases with J since e<=567500<R-d+43.
For e in[a,b], bound its numerator using a and its deficit factor using b.
These positive monotonicities justify using L without a degree scan.

The23 boxes a=25000*i,b=min(a+24999,567500),i=0..22 exhaust every
integer e in the stated range. Their exact certificates give

    max ceil E(a,b)=261996525491320703,

at[550000,567500]. The21 nonzero off-counts and23 on-counts use461
LIST transitions. Both primary and independent legality checks retain
the rational Q0 and all original high labels. Near and the possible
kernel label are included once, not once per counted pair.
