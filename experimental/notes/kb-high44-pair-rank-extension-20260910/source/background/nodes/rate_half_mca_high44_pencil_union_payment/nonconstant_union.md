# Nonconstant Direction: Pay Preferred Labels And Every Off-Pair

Let1<=h<J. On the complete P43 core union U, primitive parameterization
gives a scalar receiver w with H=w at every joint agreement. The parameter
carrier has dimension<=10. Its stronger degree bound J-h can be relaxed
to J for the present ON count. Scalar affine incidence gives

    M_on <= ((R+1-e)/(d-42))^10.                            (SCALAR)

If |U|<m-43 there are no on-pencil pairs. Otherwise the ratio is at least
one. For a nonempty P43 union, |U|>=m-43>=J, so R+1-e>0. The same
estimate bounds singleton families; no dimension-equality assumption is made.

There are at most n_max=R+E preferred finite labels. Their TOTAL deficit
is at most n_max, not n_max per pair. Every nonpreferred on-pencil pair
contributes at most(43/44)*e to the deficit by the same-pair argument.
Writing Z=R+1, the derivative of e*(Z-e)^10 is(Z-e)^9*(Z-11e), so

    e*(Z-e)^10 <=Z^11*10^10/11^11.                         (PEAK)

The exact truncated44 identity, with all original higher raw values
retained, therefore gives the on-pencil part plus the original resource
and near a uniform upper envelope

    BASE=W0/44+n_max+134944
                +(43/44)*Z^11*10^10/[11^11*(d-42)^10],
    ceil BASE=43891123512334720.                           (BASE)

This rederives the groupwise calculation for cutoff43/truncation44;
it does not substitute these cutoffs into an earlier cutoff-two conclusion.

For an off-pencil pair, the row remainder has degree<J+h. At most J+h-1
of its >=m-43 joint agreements lie inside U, so at least

    Aoff(h)=d-42-h=67430-h

lie outside. For each band ell=1,2001,...,20001, set

    u=min(ell+1999,21498), ebar=441382-8*ell.

When ell<=h<=u and e<=ebar, pad the auxiliary complement to ebar,
relax the degree to E and the agreement threshold to Aoff(u). The shared
carrier remains dimension<=11. Its LIST data are

    (r,w,K_max,s)=(ebar-E,67430-u-E,E,11).                   (OFF)

All agreement, corridor, dimension and field-size gates are positive.
An empty on-pencil union cannot meet e<=ebar; singleton unions are
handled by the same scalar and preferred-label bounds. All relevant
primitive heights1..J-1 lie in the eleven declared bands.

Let M_off be the certified shared-carrier joint LIST cap. Applying the
original per-pair deficit bound from proof.md gives

    |Z_bad|<=BASE+(43/44)*981147*M_off.                     (PRICE)

The11 traces (121 transitions) certify a largest ceiling
110665369786278512, in band18001..20000 with ebar297374.
The constant-direction finite-kernel refund is NOT used here. Off-pair
labels are charged with n-|H_f|, not the on-pencil complement e, and
all original labels remain in the single unrefunded HIGH44 resource.
