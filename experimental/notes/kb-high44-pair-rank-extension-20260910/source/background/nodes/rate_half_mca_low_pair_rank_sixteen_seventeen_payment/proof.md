# Pay Both Terminal Child Types Before Counting Original Slope Deficits

Write R=1048576,d=67472,E=21499,W0=624373932788019251.
If a large complete P43 pencil union from the required supplier occurs,
the WHOLE original source already pays by261996525491320703. We work
in its complement, retaining the original source and assignment unchanged.

For t=7 or23 put A=m-t and S=P_t. If S is empty, the completed-HIGH
resource gives |Gamma|<=floor(W0/(t+1)), which pays below both claimed
bounds after adding near. Suppose S is nonempty. Let q=16 when t=7
and q=17 when t=23. By hypothesis its affine hull has dimension<=q.
Enlarge only this AUXILIARY direction space inside V x V to dimension q.
Keep an actual pair as its offset. This contains every counted pair and
changes neither the original carrier nor the source degree, core or labels.

## 1. Hereditary Pencil Bounds And The Joint Incidence Gate

The preceding source-class exclusions give, on EVERY affine pencil
subspace of dimension v, pair count at most C_t^v for a constant
direction, or P_t^v for either type, where

    C_t=481076/(67473-t), P_t=max(9,607203/(67474-t)).

The denominator A-2J+2=d-J+2-t is positive throughout the interval.
The determinant quotient increases with J, giving

    H_t(J)=(R-J+2)/(d-J+2-t)<=H_t(E)=1027079/(45975-t).

Exact rational checks give1<=C_t<=P_t and H_t(E)<=P_t^2 for both
cutoffs. Hence the hereditary pencil-incidence theorem bounds every
function-field-rank-two affine r-space by H_t(E)*P_t^(r-2). No claim
that its children stay rank two is needed: the theorem includes pencils.

## 2. Guarded Shared-Carrier Anchor Chains

Use the shared-carrier anchor at pair/shared ranks

    t7:  16/11,14/10,12/9,10/8,8/7 -> terminal6/6,
    t23: 17/11,15/10,13/9,11/8,9/7,7/6 -> terminal5/5.

At every listed stage s<r<=2s. Bad joint-evaluation coordinates number
at most J+s-r, INCLUDING the earlier anchors now common to the carrier.
Good sections drop pair dimension by2 and shared dimension by1; the
original degree J and agreement A do not change. Each incidence factor is

    (n-(J+s-r))/(A-(J+s-r))=(R+c)/(d-t+c), c=r-s.

Thus c runs5..1 or6..1, with positive denominators. The counted subset
need not span the enlarged space or any subsequent section. Empty sections
contribute zero and do not affect the uniform upper cap.

At the terminal pair/shared equality v/v, a function-field-rank-one
direction is CONSTANT by the full-carrier rigidity part of the shared
anchor theorem. It costs C_t^v. A rank-two terminal costs H_t(E)*P_t^(v-2).
Both possibilities must be kept. Consequently

    M_t=prod_(c=1)^(q-11) (R+c)/(d-t+c)
                      *max(C_t^(22-q),H_t(E)*P_t^(20-q)).     (PAIR)

For t7, the RANK-TWO terminal dominates and floor M7=132933633906.
For t23, the CONSTANT terminal dominates and floor M23=260482505691.
The final source prices below retain the rational M_t, not separate floors.

## 3. The Same Original HIGH44 Funds Every Other Label

For one actual pair f, selected defect sets of distinct assigned labels
are disjoint outside its COMPLETE core H_f. Since every f in P_t has
|H_f|>=m-t, the TOTAL original LOW raw mass obeys

    sum_(raw<=t) raw <=(R-d+t)*|P_t|<=(R-d+t)*M_t.

The exact truncated identity and termwise inequality
1-raw/(t+1)<=t*raw/(t+1) for raw>=1 give

    |Gamma|<=W0/(t+1)+t/(t+1)*(R-d+t)*M_t.                  (SLOPE)

This uses t+1<=44 and the same original all-record resource. Floor once
and add near134944 once. The two no-large-pencil branch prices are
192166560782635999 for t7 and270933399088173359 for t23. Taking the
MAXIMUM with the alternative large-pencil cap proves the stated totals.

## 4. Necessary Surviving Structure

Contraposition gives dim aff(P7)>=17 and dim aff(P23)>=18 for EVERY
valid assignment on any over-budget source. The preceding projection
frontier supplies full shared carrier and full generic projection for P7.
Since P7 is a subset of P23, its affine-hull direction is contained in
that of P23, so the latter projection is also full. The algebraic
projection dichotomy's dual-pencil minor bounds exceptional finite slopes
by22-r for either family, giving at most5 and4 respectively.

No hull point is counted as an actual explanation, and no exceptional
label is removed from the original resource. Pair-rank cases beyond these
two hypotheses, whole degrees and original higher source ranks remain open.
