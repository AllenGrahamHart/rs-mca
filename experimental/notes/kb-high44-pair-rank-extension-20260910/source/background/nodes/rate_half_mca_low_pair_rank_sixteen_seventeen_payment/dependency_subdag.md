# Required Sub-DAG

    HIGH44 pencil-union payment -> hereditary scalar section bounds --+
    hereditary pencil incidence -> rank-two terminal bound -----------+
    shared-carrier anchor -> guarded 5/6-stage chains, constant child -+--> pair caps
    original HIGH44 + same-pair raw ownership ------------------------+--> source caps
    earlier deficient-projection frontier ---------------------------+--> remaining regular ranks

The earlier frontier also supplies the algebraic dual-pencil exceptional
slope theorem through its required closure. It does NOT depend back on
this stronger finite-rank consumer. This direction prevents a dependency
cycle. Only a supplier-owned evidence edge points to the open original
rank/support router; no red node is promoted by a conditional cover.
