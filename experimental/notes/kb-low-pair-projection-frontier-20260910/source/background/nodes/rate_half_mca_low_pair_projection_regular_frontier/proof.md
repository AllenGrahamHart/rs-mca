# Exhaust The Deficient Projection Branch In The Original Source

Retain the original carrier, assignment and every raw value. If P7 is
empty, all records have raw>=8. The completed-HIGH resource gives
|Gamma|<=floor(W0/8), so adding near pays below the stated bound.

Suppose P7 is nonempty and choose the fixed original pair f0. All pair
differences lie in V x V and have actual affine dimension r<=22.
The represented P2 subset has affine dimension<=r.

If r<=15, the required low-hyperplane supplier's rank-fifteen dependency
pays the entire source by274138707278280353. If the SUM S of W's
component images is proper in V, then dim S<=10; the P2 component-image
sum is contained in S, so the required proper-carrier theorem pays by
256541462574529459. Neither case changes the original source rank.

It remains to consider r>=16 with S=V. Put c=22-r<=6 and suppose
the generic projection is proper. The required algebraic dichotomy gives
a minimum polynomial normal whose d+1 coefficients are independent,
with d>=1 and d+2<=c. Therefore its parameter degree e=d+1 satisfies

    2<=e<=c-1<=5.

This also shows directly why c<=2 cannot occur in this deficient branch.
EVERY represented pair belongs to f0+W, so the normal annihilates its
ENTIRE affine parameter line, as a polynomial identity. Its fixed offsets
are a0 in h_*+V,b0 in V. Thus the required contracted-basis payment
applies to the actual G7 family with NO noncontained-line exceptions.
It pays the whole original source by272889015800964850, including
all original higher raw records, moving-zero labels and near once.

The alternatives combine by MAXIMUM, namely274138707278280353,
not by adding independent full-source budgets. This proves payment of
every deficient-projection source, with no unproved cover or genericity
assumption. The standard empty/singleton low-pair cases are included.

Conversely, any over-budget source has P2 affine rank>=16 and full
shared low-pair carrier by the earlier payments. Its P7 family therefore
also has r>=16 and full shared carrier. The deficient alternative was
just paid, so the generic projection must be full. The algebraic
dichotomy's dual-pencil minor then bounds its finite rank-drop set by
c=22-r<=6. It counts actual distinct gamma, not pair multiplicity.

This is a necessary surviving structure, NOT a payment of the regular
case. Full projection of the affine hull does not assert that all such
projected explanations are realized by the original low-pair family.
No exceptional label is removed from the original all-record resource.
