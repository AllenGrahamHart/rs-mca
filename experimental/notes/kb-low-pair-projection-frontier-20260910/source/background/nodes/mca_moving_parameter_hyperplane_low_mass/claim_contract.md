# Claim Contract

-2<=e<s<=K and independent ell_i; dim intersection kernel is positive.
- One fixed domain, field, offsets and original finite labels throughout.
- Actual chosen defects, not auxiliary re-minimization or source rank.
- Entire pair-line polynomial identity is required; a single point is not enough.
- Nonzero original V evaluation is required only on selected joint cores.
- At most K-s+e exceptional LABELS; at most e owners per common tuple.
- No arbitrary-hypersurface theorem, noncontained-line payment or HIGH claim.
- A stronger separately proved LOCAL basis bound may replace the greedy one;
  the actual moving kernels, common tuple resource and exceptions stay fixed.
