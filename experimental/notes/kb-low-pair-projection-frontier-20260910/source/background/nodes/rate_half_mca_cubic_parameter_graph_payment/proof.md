# Combine Three-Owner LOW Incidence With Completed Original HIGH

Use the fixed original source in the statement. Its new required
completed-HIGH supplier gives sum min(raw,44)<=W0. Consequently
sum min(raw,T+1)<=W0 for EVERY1<=T<=43. This is one original
all-record resource; the auxiliary low counts below do not reapply it.

## 1. Pay Every Noncontained Pair Line

Let P_T be the represented minimizing pairs of raw<=T labels. Every
pair has at least m-T complete joint agreements. Enclose them in one
affine V x V carrier and use the required shared-carrier anchor at
pair/shared dimensions22/11,20/10,...,2/1, ending at a point.
At shared dimension j its bad-coordinate bound is K-j, so the anchor
factor is(R+j)/(d-T+j). All denominators and strict rank guards are
positive, giving

    |P_T|<=floor product_(j=1)^11(R+j)/(d-T+j).                (PAIR)

For a fixed chosen pair write a'=a-a0,b'=b-b0. Its restriction of
(CUBIC) has coefficients

    ell0(a'), ell1(a')+ell0(b'),
    ell2(a')+ell1(b'), ell2(b').                               (COEFF)

If this degree-at-most-three polynomial is nonzero, it has at most three
finite roots. Thus at most3*P_T actual inside-Q labels belong to
noncontained lines. This is a label count after paying the actual pair
census, not an assumed pencil or curve cover. Each original label owns
only its chosen pair. A polynomial vanishing as a function on a small
field is not automatically identified with the zero polynomial.

## 2. One Fixed Cubic Tuple Resource Pays Contained Lines

If all four coefficients(COEFF) are zero, the required moving-hyperplane
lemma applies with e=3,s=11. Its nonzero evaluation hypothesis follows
from the ORIGINAL empty universal core: at V(x)=0 all a in h_*+V
equal h_*(x) and all b in V vanish, so a joint agreement would make
x a forbidden universal-core point.

At most J-8 original labels have a zero moving-kernel evaluation on
their selected joint core. The other contained-line labels satisfy

    sum raw <= floor F_T(J),
    F_T(J)=3*(R+J)_falling_11 /
                     [11*(d+J-T)*product_(j=1)^9(d-T+j)].     (LOW)

Every common tuple has at most three owners in the SAME cubic incidence
space. There is no per-label core cancellation or sum of independently
normalized carrier budgets. The pairs need not span their auxiliary space.

The exact ratio

    F_T(J+1)/F_T(J)
        =(R+J+1)/(R+J-10)*(d+J-T)/(d+J+1-T)

is at most1 precisely when11*(d+J-T)<=R+J-10. The right-minus-left
margin decreases with J and increases with T; even at J21499,T1 it
is91395>0. Thus the printed M_T at J9965 bounds(LOW) throughout
the ENTIRE integer corridor. Conservatively use J-8<=21491 for
moving-zero exceptions, separately from the low-mass endpoint.

## 3. Original-Slope Composition And Exact Specializations

For retained contained-line labels, T+1-raw<=T*raw. Each excepted
inside label and each of the e_T outside labels has deficit at most T.
The exact original-source identity is

    (T+1)*|Gamma|=sum min(raw,T+1)+sum_(raw<=T)(T+1-raw)
                 <=W0+T*(M_T+21491+3*P_T+e_T).

Flooring and adding the original near allowance ONCE proves(PRICE).
The two exact specializations are

    T    M_T                  P_T             E_T
    8    227325639596777196    12776402205892    38329206639167
    43   228493023165539663    12849534849046    38548604568629.

At e_T=0 these give(G8),(G43). If the original source is over budget,
|Gamma|>=274980728111260144. Rearranging the last inequality gives
the two stated outside-label minima. At one fewer outside label the
sufficient envelope is B*, and at the printed minimum it is B*+1.
This is arithmetic adjacency of an envelope, not its attainability.

The contained-line pair-direction space has dimension18, with full
component-image sum V and a parameter hypersurface of full affine span12.
It is therefore not silently a proper affine-graph case. Noncontained
exceptions can increase actual pair rank and remain paid. What is NOT
proved is that arbitrary low graphs lie on one such cubic, or on any
bounded, priced union of them. Original higher ranks and both Prizes
remain open.
